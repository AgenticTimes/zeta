# 债务清零登记表（backlog.md）

> 2026-09-22 建立（**债务清理期**，用户决定：先清理债务、暂缓主线批次 301）。
> 汇总自 roadmap.md 的 26 个 OPEN 段与全部编号任务提及（**汇总动作为只读，roadmap 未改一字**）。
>
> ## 登记规则（让这张表能清完的四条）
> 1. **唯一登记点**：新问题只写进本表；roadmap.md 回归纯执行日志，批次记录内不再"登记为任务 #N"；
> 2. **有界**：OPEN 总数 ≤ 30。新增一项必须同时关闭一项旧任务，或经用户批准扩容；
> 3. **七字段**：编号 / 一句话 / 来源 / 粒度（S≤半天，M≤1 天，L>1 天）/ 状态 / 最后提及；
> 4. **状态三值**：⬜ 待办 · 🟡 进行（部分实现）· ✅ 关闭（附证据批次）；`❓` = v1 汇总未逐项核对，处理到该项时先核状态再动手。

---

## 0. 清理期边界

- **目标**：§2 表内 ⬜/🟡 全部转为 ✅ 或降级为"显式不做"（写明理由留在表内，不算未清）；
- **退出条件（满足任一即回主线）**：① S/M 粒度项清零；② 清理期时长 ≥ 5 个工作日；
- **主线冻结例外**：#7 里的 universe/parquet 数据正确性部分不与清理工作冲突，可顺手先修。

## 1. 主线预占（冻结中）

| 编号 | 一句话 | 来源 | 粒度 | 状态 |
|---|---|---|---|---|
| #7 | 批次 301（预占）：0 成交根因 + f-string 可观测性 + universe 分歧（119→107 vs 115→103，parquet 回退）。**⇒ 批次 405 划掉一条嫌疑**：301 笔记（roadmap `:8833-8836`）记的"`getattr` 未实现的 error 出现十几次 ⇒ 0 笔成交头号嫌疑"是**误报**——告警块排在实现它的两条臂前面，喊完仍被接住（编译 rc=0、值正确、`nm -u` 无 `_getattr`）；搬家后仓内 5 行误报清零，t225 那条真幽灵保留。剩下两条约束：acceptance 夹具不在仓里（`run_backtest` 在 `*.z` 中零命中，`/tmp/wl/drv_accept.py` 引的 `strategies/code/jq_wufu_local.py` 全盘 find 无），短 Vec 几何判形在永不触手的 `runtime/py_additions.c:3475`。**⇒ 批次 406 两处更正 + 一处收口**：① 上一条的"夹具不在仓里"**判错**——`/Users/meetai/source/quant/REasyQuant/strategies/code/jq_wufu_local.py`、`backend/strategy/wufu_constants.py` 都在，端到端能跑：CPython `1000000 -> 994576 (-0.54%)` vs Zeta `1000000 -> 0 (-100.00%)`（那次 `find` 作用域取错；负断言要用"搜索本身覆盖得到目标"的正证据来作，见 roadmap 批次 406 §七）；② 本批在 acceptance 路上收掉一条**硬链接失败**（`gen.rs:5670` 后加第五条守卫：根文件 `_x` 的定义是裸名键，288 的改名分支把调用点改成 `__main____x` 幽灵 ⇒ `nm` `U ___main_____fmt` vs `T __fmt`、rc=1；t442 锁，改前 rc=1 正证据在案）；③ **但 acceptance 读数没动**（仍 `-100.00%`、`[PARITY]` 37 行全是指针形），因为夹具里的 `_fmt` 住在被导入模块、没走到这条路径 ⇒ 301 的下一格押在**打印族**（#117/#38/#45：动态 str 槽无类型标记 ⇒ 插值打成地址），另登记两条实拍：acceptance **跨运行非确定**（同一次调用旧二进制 rc=139 / 新二进制 rc=0 ⇒ 这条路 A/B 单次跑不足以判回归）、同一夹具**调用形态改变符号发射**（相对路径 + `cd` 到源目录 ⇒ `U _run_backtest`；绝对路径 + 根 cwd ⇒ rc=0，两个变量同时变，归因未做）**⇒ 批次 407 收掉打印族的第一格、但夹具那一格没动**：`resolver.rs:1553`（`collect_calls`）补 `FString` 下钻臂 —— 同文件 `:1794`/`:2937`/`:3187` 三处都把 `FString` 当叶子答 `Str`，唯独取证据这一处不下钻，于是"唯一调用点写在插值里"的未注解形参停在 `dyn` → 返回 `PyDynamic` → `gen.rs:11196` 兜底 I64 → `to_string_i64` 打堆地址（六探针矩阵：p1 插值 sole→地址、p8 加一条非插值使用即正确、p10 顶层 print 位正确、p11 两形并存正确、p9 两条都插值→两列地址；t443 锁，第四条 `len(f"{...}")` 证明地址**参与了算术**：20 vs 期望 4）。**acceptance 零位移**：`jq_wufu.py --dump-mir` 改前/改后 85,066 行**逐字节相同**、`to_string_i64` 26 处不变，因为夹具的 `_fmt` 是**嵌套 def**（提升名 `__closure_0__parity_snapshot_cc4d40894`）⇒ 同病因的另一格，已由 p12 探针（8 行，`t=4303314848` vs CPython `t=a;b`）在套件级复现，成因为**候选**：证据消费端 `resolver.rs:1558-1570` 只试裸名/`from X import f`/模块前缀三形，闭包提升名不在其中。定价：套件 0→1（只 t443 自身）；语料 5 处"插值内带调用"，用户函数 2 处（`jq_wufu.py:366`、`jq_wufu_daily.py:366`）**全是闭包**、内置 3 处 **⇒ 批次 408 收掉这条"另一格"的返回侧，但 407 给它猜的成因判死**：错不在取证据（p14 与 p13 只差打印形、拼接形改前就对；改前 `outer` 块里那条 `Call` 已经解析到提升名 `__closure_0_outer_c8f5e1bd4`），而在**登记**：`gen.rs:14380` 的 `AstNode::Block` 臂把 `body_val` 自造成哑 `IntLit(0)`，于是 `last_closure_ret_ty` 对任何语句列表体都记 I64；`fn_depth > 1` 的提升臂（`gen.rs:2089`）从来没有写 `closure_ret_tys`（lambda 那条臂有）。两条改在 `gen.rs:2096`/`:14398`，t444 锁（改前 `t=4312309664`/`e=4309068594` vs 改后 `t=a;b`/`e=-`，返回 i64 的 `num(3)` 两态都对＝"没顺手扩大"的哨兵）。**夹具第一次有真实移动量**：85,066 → 85,042 行、`to_string_i64` 26 → 24、`_parity_snapshot` 内 `args=[88]`/`[92]`（正是 407 §十一 预写的判据）两处消失。**但 `[PARITY]` 那一行还剩第三格、且这格在 C 侧**：残留的 `args=[81]` 上游是 `Call zeta_dt_date args:[82]`（源码 `jq_wufu.py:366` 的 `{context.current_dt.date()}`），而 `runtime/py_additions.c:3422` 是 `int64_t zeta_dt_date(int64_t h) { return h; }`、`codegen.rs:1078` 也声明成 `i64(i64)` ⇒ 类型侧现在全对也读不到日期，要 C 侧点头（与 406 §十(b) 同一扇门）。定价（AST 尺：剥 `//` 行后 `ast.parse`，334 文件 / 261 可解析＝覆盖 78%）：套件嵌套 `def` 成员 5 个文件（t180/t288/t302/t30 + 本批 t444）、语料 4 个文件；存量 4 例改前改后**输出逐字相同、rc 相同** ⇒ 这一格在套件里从来没有成员，成员只在语料侧。**⇒ 批次 409 收掉"跨模块具名实参"那一格，但 acceptance 三格读数未动**：`from 模块 import 函数` 的调用点由 `gen.rs` 的 `py_member_aliases` 臂下沉（本批插入点 `gen.rs:5259-5275`），该臂按**位置**贴实参并按位置补默认值、**从不读** `name=value` 包上的 `__kwarg__` 名字（修前 `gen.rs` 内该标记被读 16 处，这条臂 0 处） ⇒ 凡跳过中间带默认值的形参，后面每个实参早落一格；乱序具名实参按书写序落槽。`--dump-mir` 实拍 `args=[9,18,27,36,63]`（位置追加签名）。修法：新增 `Self::bind_kwarg_markers`（`gen.rs:988-1042`）按被调方声明序铺开，五种"静态绑不了"（无标记 / `**` 展开 / 名字不在表里 / 同名给两次 / 某形参既无实参也无默认值）返回 `None`、调用方保留原位置列表不猜。t445+kwmod_b409 三翼锁：A 全位置（修前修后逐字相同＝没越界）、B 跳中间默认参、C 乱序（两翼改前错改后对，18 条 `// expect:`）。**语料命中形状**：`strategies/code/jq_shim.py:46-52` 的 `inject_local_data` 与 `strategies/code/_zeta_local_drv.py:26`、`jq_wufu_local.py:84` 两个调用点，两处都跳中间 `nav_df`；触达上界（非损害量）＝语料 `f(kw=…)` 形状 605 行（grep，未按 lowering 路径甄别）。**acceptance 仍 `13401`/`13402` + `0 0`、37 行 `[PARITY]` 三格全 `-`、`1000000 -> 0 (-100.00%)`——且这个"未动"不构成反证**：错位贴进去的 `codes` 与正位的 `trading_days` 长度都是 0，`len()` 读数分不出对错。同批判死两条假线索（负断言配正证据）：① "根文件 print 全丢"不成立——编译的根是三行 print 的 `_drv_accept_409.py`，不是带 13 条 `R…` 的 `_zeta_local_drv.py`，stdout 实拍 1 行且每跑必变＝`print(json.dumps(...))` 打了地址（#117 那格）；② "`_local_cache` 模块全局 dict 写不进/读不出"不成立——裸 `{}` 与带 `dict[str, Any]` 注解两种形均与 CPython 逐字相同。**本批新取到两条确定性、两文件、零抖动的静默错值（下一批第一候选）**：(a) `store(c, [1,2,3])` 里 `len(cache["k"])` 打 **0**（CPython 3）——值进了 dict、读回来长度是 0；(b) 同一形参拿到冲突证据（两 list 一 str）保持动态后 `len([1,2,3])` 打 **1**（同一次运行 `len("ab")` 打 2 正确）——即 #117 的**列表侧**，同一行 `type(x).__name__` 打地址；(a)(b) 互为因果还是同一条链未隔离。这正是 301 原始假设"值到了、读成空"的形状（另立一档：门禁整体退出码本批未实拍，包装器回 0 而 `run_all.sh:576` 在 `py_fail=2` 下按代码应为 1，直跑补 `echo $?`）。（OPEN 净增 0：409 折进本行与 roadmap 批次 409 §十一）**⇒ 批次 410 收掉 (a) 那条 dict 往返读空，但它是另一条病、不在 0 成交路径上**：`store(c,[1,2,3])` 里 `len(cache["k"])` 打 0 的成因是**下标赋值的兜底分支把 key 原样送进 `DictInsert`**，而同一条兜底的读侧早已走 `lower_map_key`（字符串按内容 FNV 哈希，`gen.rs:3195` 立的规则）⇒ 写指针、读哈希，每次取值都 miss ⇒ `len(d)` 涨、值读回 0 且一声不出（写点 `gen.rs:1869-1878`，key 行 `:1877`；对照：dict 字面量 `:5159` 与接收者已知为 `map` 的 `:1797` 两条写点本来就哈希 ⇒ 模块全局 dict 一直对，只有穿过未标注形参才红）。值形不是判据（列表/标量/字符串一起红）。t446 五条 `// expect:` 锁（改前 `inside 0 / ovr 0 / scalar 0 / strs 0 / root 0 2`，改后与 CPython 逐字同），隔离探针 v1/v2/v6/v7/v9/v10/same1 转绿；v3（模块全局接收者）与 v5（保留动态的列表槽＝(b)）改前后读数逐字未动 ⇒ (b) 仍属 #117/#32，本批没收。**acceptance 四跑**：2 次 rc=139 / 2 次 rc=0；两次成功的日志归一化（6 位以上数字→ADDR）后彼此 0 行差异、与 409 的日志也 0 行差异 ⇒ `13401`/`13402`、`0 0`、37 行 `[PARITY]` 三格全 `-`、`0 笔成交` 一格未动（roadmap 批次 410 §七）。**本批另登记一条独立静默错值（#130，未修）**：词首 `var` 前缀标识符被特殊对待 —— `varkey(u,"q")` 的调用语句在 lowering 里整条被吞（被调函数 MIR 完整、`nm` 有 `T _varkey`、main 的 stmts 无 Call；改名对照 `varkey/varx/varkey2/var` 丢、`vkey/xvar/storevar/VARKEY` 正常），同族 `variable = 5` 读回 1、`var_x = 7` 读回 1；语料 `var` 前缀 token 13 处（jq_wufu.py:521、jq_wufu_daily.py:583、蛇皮走位小市值.py:38 的 `var_x = np.sum(...)` 一类），其中以 var* 开头的调用语句 0 处 ⇒ 不在 0 成交路上，但那两处方差是实测错值。**门禁**：`gate rc=1`（常驻红＝`run_all.sh:576` 的 `py_fail != 0`，本批已直跑实拍整体退出码）；python_style 329/2/5（glob 336，`PASS t446_dyn_dict_param_store`）、official 194/194+191、语料 39/39、jit ok=176 trap=355 total=531、diff 120/130 92.3% bad_case=0、clean_checkout rc=0（rev 53f2f5bf）。**ABI 锚点**：fb019702 内读数仍是 漂移 66 / 新 11 / 消失 3（基线 251）—— 那次 `--rebind` 跑在 gen.rs 最后一次挪行之前；记录批重跑 `--rebind`（改写 33 行 / 57 个数字，基线 251→250）回到常驻 30 / 11 / 3，3 条消失无唯一配对 + 2 条定位失败保持悬空待收。**⇒ 批次 411 收掉"闭包/推导式对跨模块导入全局名的捕获"那格，acceptance 第一次有真实移动量**：`from <mod> import <VAR>` 的名字在**定义模块**里以 `<mod>__<VAR>` 写进进程级环境表（`runtime/py_additions.c:2656 zeta_env_get` 按内容哈希查 map），而 `lower_closure` 的自由变量预绑定直接发 `StringLit(裸别名)` ⇒ 读一个没人写过的槽 ⇒ 恒回 0 ⇒ `[c for c in codes if c not in BS]` 一位都不滤、且 rc=0 一声不出。普通 Var 读点（`gen.rs:3738-3771`）**早就**做了这次改名，闭包这条路径一次都没查那张表。`--emit-llvm` 字符串表实拍两侧不同源：读侧 `@str_lit`=`c"BS\00"` 用在 `zeta_env_get`（n5.ll:1113），写侧 `@str_lit.49`=`c"lib_c__BS\00"` 用在 `zeta_env_set`（n5.ll:1326）。修法＝新增 `Self::capture_env_key`（`gen.rs:14268-14291`）逐段镜像那三条判据、任一不成立则退裸名不猜，另把捕获槽类型的硬 `I64` 回退换成查 `global_ty_of`（列表/字典捕获此前会被截成整数）；`+33 -1`。**改前读数取自 HEAD~1 编译出的二进制实拍**（临时 worktree，`grep -c capture_env_key` = 0 确认源码是改前那份）：t447 五翼 `notin 4 / in 0 / dict-in 0 / local-alias 2 / dict-keys 0` → 改后 `2 / 2 / 1 / 2 / 1`＝CPython 逐字同，`local-alias` 两态相同＝没越界的哨位；四翼探针 n4 `G 2 / H 3 / I 5 / J 0` → `I 2 / J 3`（G、H 逐字未动）。**acceptance**：`上市日过滤` 由 `119 → 107 只` 变 **`115 → 103 只`**，与 CPython 参照 `115 → 103 只` 逐字相等 —— 301 主线自 405 起第一次有对齐的读数。**另三格一格未动**：`codes=0 / trading_days=0`（#131 —— 本批假设对它**判死**，`fix_run1.out:130` 实拍两态都是 `... 12856 0 0`，最小复现仍未取到）、37 行 `[PARITY]` 全 `-`、`1000000 -> 0 (-100.00%)` vs CPython `994576 (-0.54%)`。**ABI 锚点本批不需 `--rebind`，且这是量出来的**：`gen.rs` 被引用的 46 个锚点中行号 > 插入点 14263 的有 **0 条** ⇒ 读数仍 漂移 30 / 新 11 / 消失 3（基线 250）。**门禁**：`gate rc=1`（唯一红仍是 `run_all.sh:576` 的 `py_fail != 0`）、official 194/194+191、语料 40/40、jit ok=176 trap=357 total=533 GREEN、断言族全 FAIL 0；**口径注明**：那次 `run_all.sh` 起在 t447 落盘**之前**，其日志内 `python_style 329/2/5` 不含本批用例，补跑 `tests/python_style/run.sh` 得 **330/2/5** 且 `PASS t447_closure_capture_imported_global`。**建一条夹具口径**：acceptance 驱动必须从 `REasyQuant` 仓根构建，在 `strategies/code` 里建只出 `_main`（`.o` 2,624 字节）、链接期缺 `_run_backtest`（响的、非静默，未立案）。**工具陷阱一条（已入用户记忆）**：`git worktree` + **共享** `CARGO_TARGET_DIR` 会把主树 `target/release/zetac` 覆盖成旧二进制，而 `cargo build` 因 mtime 未变答 "Finished" 不重编 ⇒ 必须 `touch` 源文件并比对 `md5` 才算恢复。（OPEN 净增 0：411 折进本行与 roadmap 批次 411 §十一） | 批次 300 文末 → 405 清掉一条误报嫌疑 → 406 收一条硬链接失败 → 407 收打印族第一格 → 408 收闭包**返回**侧那格 → 409 收跨模块具名实参那格 → 410 收 dict 写侧不哈希 key 那格 → 411 收闭包捕获跨模块导入全局名那格（acceptance 另三格读数未动，堵点＝#131 的 `codes=0` 来源链与动态槽列表侧 #117/#32） | L | 🟡 2026-09-24 裁定恢复；夹具在盘、端到端已复现，408 已收闭包返回侧（407 §七 的候选成因判死，见 roadmap 批次 408 §三），堵点转为闭包**参数**侧与 C 侧 `zeta_dt_date` 恒等桩；409 已收跨模块 kwargs（roadmap 批次 409 §四/§七），acceptance 三格读数未动，410 已收 dict 往返读空（roadmap 批次 410 §三/§七），acceptance 三格读数未动，411 已收闭包对跨模块导入全局名的捕获（roadmap 批次 411 §二/§三/§七），acceptance 的"上市日过滤"两态与 CPython 逐字相等（`115 → 103 只`）、另三格读数未动，下一候选＝#131（`codes=0` 来源链，本批假设对它判死、需换假设）与 #130（var 前缀吞语句）下一候选＝#131（`codes=0` 来源链，本批假设对它判死、需换假设）与 #130（var 前缀吞语句）；412 已收 kwargs 绑定的空槽左移（roadmap 批次 412 §一/§二/§三）：`f(b=3)` 实测改前 `F 3 0`、`f(zz=7, b=3)` 改前 `F 3 7`，语料成员 `market_data_fetcher.py:252` `groupby(level=0)` 的 `by` 收到 `sort` 的默认 True；两个既有依赖方同批归位（`pandas.z:134` `drop` 显式声明 `columns` 按名字绑、`gen.rs:9124` 的 str 包装按声明槽定位，t203/t206 的 expect 一字未改回绿）。**acceptance 三格读数一格未动**（pre/post 各两跑逐字相同：`115 → 103 只`、`12858 0 0`、`1000000 -> 0 (-100.00%)`），MIR 位移 180 个 hunk 全在远端取数路径`_fetch_remote_rq`（同侧噪声底 3 hunk）⇒ 412 对 #131 同样无效，下一步只问"真实 `market_df` 身上有没有 `stock_code` 这一列"，不再猜形状；另登记 `GroupBy.__len__` 读 0 与"门禁语料步 30 s 硬超时在并行负载下把 40/40 读成 0/0"两条新残口；413 已收 `dict[...]` 注解在解析期被丢（roadmap 批次 413 §一/§二/§三）：根因是 map 的值类型只能由第一次写入钉死（批次 299 精化的反面），保留注解的代价是四处"只认裸 Var 的绑定目标"扫描失明（`collect_module_global` / `module_global_types` / 体内心智推断 / `module_level_bindings`，四处各有独立实测症状：`_keys` 链接失败、`map_insert` SIGSEGV、`return m` 把整函数否决成 UNIT、静默 len 0）。**acceptance 从崩到跑完**（本批后 5 跑 4 次 rc=0、走完 37 交易日；残余 1 次 rc=139 与 HEAD 同点位、HEAD 自己 5 跑 2 次 ⇒ 非本批回归），但**三格读数一格未动**（`12858 0 0`、`[PARITY]` 37 行指针形、`1000000 -> 0 (-100.00%)`）；主线 301 第 1 步的答案是 **`market_df` 身上有 `stock_code` 列**（12,855 行 × 10 列，HEAD 上探针实拍）⇒ `codes=0` 的嫌疑从"缺列"改判到 `jq_shim.py:60-62` 刚写进 `_local_cache` 三行后又读回的那两格（#131 换假设，两翼＝dyn-map 往返 vs `_build_stock_arrays` 自身返回）。套件 333/2/6/0（+t449 +t451 通过、+t450 known-fail），ABI `--rebind` 后 漂移 32 / 新 11 / 消失 3（批前 HEAD 同一把尺子 32/10/6）。另登记同类漏点族候选 2 处（**只有位置证据、无实测损害**，#136）与 `--dump-mir` 跨模块同名类别名序掷硬币的机制归因（#9：acceptance 的"非确定性"读数有一部分就是它）；414 已收 `with` 体抛异常时 `__exit__` 从不跑（roadmap 批次 414 §二/§三）：异常 `longjmp` 跳出块边界 ⇒ release 从不执行 ⇒ 语料 slim 探针 **rc=124 死锁**在 `py_threading_lock_acquire`（同一驱动 `.venv/bin/python` 1.571 s 跑完，S1–S7 全非零）；修法＝把 with 体下进 try 帧、两条分支都跑 exit、handler 分支 `zeta_raise` 原码，而 `return`/`break`/`continue` 三条边各补一次 `zeta_try_end`（实测 `finally` 对它们不跑 ⇒ 不补就是僵尸帧，与 `load_metadata` SEGV 同类）。t452 锁四条出口（改前 HEAD 实拍 rc=124 零输出 → 改后 `t452 [1, 4, 5]`）。套件 **334/2/6/0**、门禁 rc=0、ABI 不需 `--rebind`（基线 250 条里 `stmt.rs` 出现 0 次；读数 32/11/3 与 413 逐字相同）。**主线位移**：S1 119 / S2 115 / S3 2023-08-05 三格第一次出声且与 CPython **逐字相等**，卡点移到 S4 的出声异常（rc=1；改前臂从未到得了这一格 ⇒ 归因未做，#137④）——**但这条走的是联网批量拉取路径，acceptance 走缓存命中（`待拉取 0 只`）⇒ 它不在 0 成交链上**，`codes=0 / trading_days=0` 三格本批未动，下一批要换**缓存命中形**驱动复测。另登记 5 条（#137）：`def loop():` 让解析器静默截断整个文件（两臂同形 ⇒ 作废本批 w3/w4/w5 三条"零输出"读数）、用户自定义类的 `with` 不调 `__enter__/__exit__`、`try/finally` 无 `except` 时异常被吞、S4 归因、本批把**任何用 `with` 的程序**的 JIT 依赖面变宽 5 个 try 帧符号（t452 落 trap 桶：total 538→539、trap 362→363、ok 176 未回退）。OPEN 净增 0：414 折进本行与 roadmap 批次 414 §十。**⇒ 批次 415 收掉 `__file__` 的"全程序一份"，主线 301 第 2 步（选股数量不一致）就此结案**（roadmap 批次 415 §二/§三/§六）：模块体读到的是**入口**路径 ⇒ `market_data_sources.py:146` 的三级 `parent` 落到 `/Users/meetai/source` ⇒ `stock_cache_dir` 指错 ⇒ 每一只 cache miss ⇒ 走联网拉取 ⇒ 抛异常退出；此前 acceptance 没红在这条上，是因为它的入口住在 `strategies/code/`、恰好两级深把算术**凑**对了（＝路径深度巧合，不是行为）。修法＝`resolver.rs:71`（表）+ `:2491`/`:2707`（两处写点，`path` 已在作用域）+ `:3407`（传入），读点 `gen.rs:3799-3813` 先查 `py_module_paths[current_module]`、查不到才兜入口 ⇒ `__main__` 逐字未动。t453 + 新夹具 `pyfilepath_fixture.py` 锁（**改前实拍**＝只把 `gen.rs`/`resolver.rs` 两文件退回 HEAD 重编：`FIXTURE t453_file_per_module.z python_style`，模块读到入口的 `.z` 名）。**语料位移**：探针五格与 CPython **逐字对齐**（X1/X2 回到 `…/REasyQuant/data/stocks`、X4 由"读不到"变"读到"）、acceptance **第一次 rc=0 跑完 37 个 `[PARITY]` 日**、`缓存命中 103 只；待拉取 0 只`、`上市日过滤 115 → 103 只` 保持，且 **`codes=0` 那格收掉但改成抖**（同一个二进制三跑 `107 / 232 / 107`；同源 `合计 12858 行，103 只标的` ↔ `228 只标的`，行数与 136 日恒稳）⇒ 链上第一格转为第 3 步（`0 笔成交`、`1000000 -> 0 (-100.00%)` vs CPython `994575.8405772317 / -0.542%`）。套件 **335/2/6/0**（+t453）、diagnostics 105 文件/220 行（+1＝t453 自己那声 `imported module`）、jit ok=176 trap=364 total=540（+1 trap＝新用例进 sweep，与 414 的 t452 同机制）、语料 40/40、diff 120/130 92.3% bad_case=0、clean_checkout rc=0；**门禁 rc=1**，唯一红仍是 `run_all.sh:576` 的存量 `py_fail=2` —— **与 414 §五 记的 rc=0 口径不一致，本批不解释、不回改旧文**。ABI 锚点 `--rebind`（改写 27 行 / 59 个数字）后回到常驻 **32/11/3**（不带 rebind 重跑是 74/11/3 ⇒ 本批 +37 行确实动了锚）。**#131 判为已闭**；另登记 4 条（**#139**）：① `def f(): return __file__` 打地址（函数体 MIR 里是 `StringLit`+`Str`、同形 `return "abc"` 正常 ⇒ 编译期常量 str 作返回值时调用点拿不到类型证据，#33/#114/#117 同族）、② 导入模块的**函数**内读 `__file__` ⇒ rc=133 SIGTRAP 零输出（lldb 停 `main+176` 的 `brk`）、③ 0 成交 + final 归 0、④ `codes` 唯一标的数跨跑非确定（#134 邻族，下一批第一候选）。OPEN 净增 0：415 折进本行与 roadmap 批次 415 §六/§七。**批次 416 接 ③（0 成交 / final 归 0）：病因已证、修法未上门** —— 模块的普通 `def` 从不进环境表（只有模块级绑定会镜像），而两处"当值读"仍走 `zeta_env_get`（裸名 `src/middle/mir/gen.rs:3890`、模块限定 `:12278`）⇒ 别名持有 0，`zeta_call1(0,ctx)` 按合同静默返回 0（`runtime/py_additions.c:3414`、docs/ABI.md C9）⇒ 五个每日例程一次都没发生。改下型成 `FuncAddr` 后 `[晨间] 计算流动性阈值` 0→1、新用例 `t454_imported_fn_as_value.z` 四条形状全出（改前二进制 md5 `43f32ec718f60f14a277c67a469ab874`），但同一次 acceptance 跑的三格初始化读数被抹掉：`codes` 107→0、`trading_days` 136→0、`交易成本` 万1/0.10%/true→万0/0.00%/false，且注入行 logger 名打空 —— 交错对照里改前二进制两次仍 107/136 ⇒ 退化来自这次下型。**二分收窄**：只开 `:12278`（模块限定值读）就复现全部三格、此时 `[晨间]` 仍是 0 ⇒ 退化与修到的那格不同源；只开 `:3890` 在注入行之前就 rc=139（崩点**撤回**为本批新增：acceptance 的 rc=0/139 跨跑翻是批次 410 起的存量非确定）。三次归因落空：类构造器守卫（加 `!type_decls` 后 `0 0` 依旧，但守卫本身是真发现 ⇒ 另登）、上市日路径（12 行逐字一致）、语料状态（交错对照排除）。MIR 定量：`env_get` 790→768、`FuncAddr` 97→119、`env_set` 185 恒、键集只 7 名/22 处翻形 ⇒ 不对称在**语句数**（环境读 2 条/处，`FuncAddr` 0 条）。**下一批判据**：site2 处保留两条哑语句再叠 `FuncAddr`，看 107/136 是否回来。编译器改动全量回退，只上 known-fail 登记（套件 335/2/**7**/0、门禁 rc 未落盘、ABI 常驻 32/11/3 不带 `--rebind`）；详见 roadmap 批次 416 §一~§七。OPEN 净增 0：416 折进本行 **⇒ 批次 417 上门 416 的下型、并把它的退化归因判死**：两处当值读点位（裸名 `src/middle/mir/gen.rs:3890` 臂 / 模块限定 `:12290` 臂）在 `func_ret_types − global_consts − type_decls` 命中时把槽值覆盖成 `FuncAddr`（`+23 -0`，环境读那两条语句照旧发 ⇒ 语句数与槽号分配与修前逐字一致），t454 摘 `known-fail` ⇒ 套件 **336/2/6/0**（known-fail 7→6）。**可归因位移**：`[晨间] 计算流动性阈值` 只在改动版二进制里出现（11 跑 9 次；改前两侧二进制 7 跑全为 0）＝每日例程第一次真被派发，**但走到该行的 9/9 随后 rc=139** ⇒ 301 第 3 步的堵点从"例程没跑"换成"例程体一跑就崩"。**416 §五 那条"省两句 ⇒ 三格读数塌"判死**：`git show HEAD:src/middle/mir/gen.rs` 重编（md5 与 416 的 prefix 逐字相同 `43f32ec…`）出码 4 版，3 版跑完的都是 `codes 0 / trading_days 0 / 保守模式 false`，而 14:24 那份旧改前二进制 3 跑仍 `12858 107 136 / true` ⇒ 塌不塌由**出码轮次**决定。尺子：同编译器同输入连编两次 `.o` 756,432 / 756,344 B 不等；`--dump-mir` 两次 md5 相同（T0 的规范化仍成立）；`--emit-llvm` 两次各 113,079 行而 diff 15,825 hunk（`store i64 %0, ptr %15` ↔ `ptr %5` 整体错位 + warning 顺序同时变）⇒ 抖动在 MIR **之后**的发射阶段，具体容器未定位 ＝ **批次 418 第一候选**（它同时是那条崩点能被 A/B 的前置）；另注 `--emit-llvm` 的 IR 走 stderr、且"注释不改编译器 md5"是量出来的（改注释前后同为 `552e335…`）⇒ 探针版 `507880…` 与本批之间有一笔未落账的真实代码差，acc8 那 4/5 的复原读数不可复现、本批不引用它作因果。门禁 **rc=1** 已落盘（`/tmp/b417/gate417b.log:142`，唯一红仍是 `run_all.sh:576` 存量 `py_fail=2`）、official 194/194+191、语料 40/40、jit 176/365/541 segv=0、diff 120/130 92.3% bad_case=0、clean_checkout rc=0 rev=`5ce9503e`、ABI `--rebind`（改写 48/64 行）后回常驻 **32/11/3**；`#142`（类构造器过同判据、`gen.rs:3987` 读点无守卫）保持未定价。OPEN 净增 0：417 折进本行 **⇒ 批次 418 划掉一条观测障碍**：出码非确定性两处落地（`codegen.rs:1606` alloca 序排序、:66 `struct_defs` 改 `BTreeMap`），代码批 `0f5bec05`。决定性对照＝提交前驱动 `.o` 3 连编 3 种（756328/756296/756336）vs 提交后 756384×3；夹具 3 种 vs 1 种；**只落源 A 时驱动 IR 仍 3 种** ⇒ 源 B 独立必要。门禁化＝第 16 步 `tools/emit_stable.sh`（2 夹具、违规 0）。三条边界入 418 §四：两个夹具盯不住"按名字扫 struct 表"那条（把 BTreeMap 退回 HashMap 照样全过，夹具只证明源 A）、跨窗口 IR 体积异常未解释（结论只用同窗口对照）、先前口误"ABI rc=0"实为 **rc=2**（`ABI.md:277/:438` 两处裸行号指向空行/注释＝#52 的账，本批之前已在）。崩点 5/5 稳定（134 行后 rc=139）⇒ 419 头名候选具备 A/B 前置；三格钉在多数支 `0 0 + false`（判据"消失"那半未达成）；`rows` 同二进制仍跨跑抖 12854–12858（运行期侧，非 codegen）。门禁 gate rc=1（`/tmp/b418/gate418.log`，唯一红移到 `run_all.sh:605` 存量 `py_fail=2`：t231/t233）、official 194/194+191、python_style 336/2/6/0、语料 40/40、jit 176/541 segv=0、diff 120/130 92.3% bad 0、clean_checkout rc=0 rev=`738618b8`、ABI `--rebind` 77/77×2 后常驻 32/11/3。OPEN 净增 0：418 折进本行。**⇒ 批次 419 收掉 `[dynamic]` 幽灵名绑类别名那一格、并把它换成可诊断的异常**：`resolver.rs:1014-1026` 把每个 `Class::method` 既按限定名注册（:1021）又按**裸成员名**再注册（:1026），`get_or_declare_function` 的两处裸名兜底于是能把 struct 体绑到 vec/str 句柄上（`df["col"].to_dict()` ⇒ `[dynamic]str::to_dict` ⇒ rc=138/139）。判据必须问 **MIR 清单**（装配前就完整），不能问模块：实拍 `DBG419 name=[dynamic]str::to_dict argc=1 bare_blocks=0` ⇒ "有体才绑"这一档在查幽灵名那一刻别名还是 0 基本块声明；只守 `type_args.is_empty()` 那一趟也**没用**（带实参类型时走 extern 声明趟，`v_e_p2.ll` 与改前逐字节相同）。落地＝裸成员名若被某个 `Class::method` 定义则改绑 `zeta_raise(1)` 跳板 + 编译末尾逐名出声；`_[dynamic]str__nth`（`runtime/py_additions.c:994`）这类真运行时符号不受影响（t411 `42/0/False/0` 未回归），成员名真不存在的两例仍到链接期才 `Undefined symbols`（h1/h2，边界没动）。**主线位移（同 cwd＝`REasyQuant` 根、两侧二进制同目录）：语料 134→135 行、rc=139→139**，新增行 `[WARNING] jq_shim: 动态池更新失败: 1`＝语料自己的 except 接住了异常码 1 ⇒ 崩点越过那处 `try/except`；**新崩点 `str_trim + 24`（`ldrb w0, [x0]`，`x0 = 0x3532`）＝小整数当 `char*`，与 #117 打印族同族**。同族未收：语料里 `[dynamic]` 幽灵还有 `copy`×2 / `dropna` / `ffill` / `isin`（现都走抛异常，真对齐要 Series + `DataFrame.index`，未定价）；`get_function:2323-2327` 有同形状裸名兜底但**可达性未证**。门禁 **rc=1**（`/tmp/b419g/gate419.log`，唯一红源仍是 `run_all.sh:605 py_fail=2`）、python_style 337/2/6/0＝`ls t*.z` 345 逐字对上、jit 176/**542** segv=0、diff 120/130、clean_checkout rc=0 rev=`969f8ec5`、ABI 定位失败 4→0 / 漂移 32→23（`--rebind` + `--bless-only` 点名 9 条；终态 23/11/10 rc=1，消失 10 里 8 条＝我手改后基线留下的旧号，按设计不自动删）。OPEN 净增 0：419 折进本行；批次 420（代码 `24514e84`，4 文件 +77 −24）：动态接收者的 `.get(<str 键>)` 不再下到 `array_get`（那条 load 是 `base + key*8`，字符串键＝拿键地址当元素下标 ⇒ 确定性内存不安全；改前 `def g(dd): return dd.get("x")` 一次调用 rc=139 实拍，崩溃指令 `ldr x19, [x19, x8, lsl #3]`），改为复用静态类型路径的 `MirStmt::DictGet`（`gen.rs:10423-10446` +24 行；负对照 `arr.get(1)` 整型键逐字节未变）。新用例 t456 改前 rc=139 空 stdout / 改后 `A 25 B 2 C 8` rc=0。**主线位移 0 且配正证据**：改前二进制仍死在 `MarketDataFetcher::fetch_stocks`（`str_trim+24` 读 `0x3532`，lldb `-k` 帧链），在本批改到的 6 个 IR 函数上游；语料 IR 里 `@map_get` 225→239（+14 调用点，口径＝IR 调用点不是源码行）。门禁 rc=1（唯一红源仍是 `run_all.sh:605 py_fail=2`）、python_style 338/2/6/0＝`ls t*.z` 346 逐字对上（对 419 净增 1＝t456 无回归）、jit 176/543 segv=0、diff 120/130、clean_checkout rc=0 rev `26504a95`、ABI `--rebind` 13 条后终态 23/新 11/消失 10 rc=1 定位失败 0（与 419 终态逐字相同，`docs/ABI.md` 1032 行不变）。三条负结果入册（#145 改判 OPEN）：parquet 读侧三条路都产出真 NUL 结尾 `char*`、崩点位置同二进制四跑 135/135/119/135 非确定、"误下型 `.get` 读出 packed ASCII"假设被自己的探针否掉（只 print 不消费也崩）。新增 OPEN 折本行：①`.get(k, default)` argc=3 名表零臂 ⇒ 幽灵名，语料 17 处已逐行号实测（含驱动直接 import 的 `jq_wufu_local.py:198/284/288/309`）＝421 头名；②动态接收者 str 值直接 print 打堆句柄（`DictGet` dest 仍打 I64，正解是动态槽自带标记 ⇒ #117 近邻；仓内无运行时动态打印机，要动 `runtime/py_additions.c`，未授权）；③`("set", 3) => array_set` 同形状写侧未守（语料 `.set(` 站点实测 0 ⇒ 只作加固）；④`_[dynamic]str__copy` / `__dropna` 在 `runtime/*.c` 里逐条 grep 确认不存在，`copy` 4 处全在 acceptance 那个模块内、`dropna` 15 处全在另外 13 个策略文件（`jq_wufu_local.py` 内 0），`ffill`/`isin` 归零 ⇒ 419 队列里"还剩 5 个成员"订正成 2 个。⇒ 批次 421 把同一族的 `argc=3` 臂补上（`gen.rs:10470-10509`，两笔提交 `04a6aeeb`+`1bea38b9`）：`.get(k, default)` 从"剥 arity 后缀 → bare `@get` weak 桩必 abort rc=134"改为 `map_get_default(ptr, map_str_key(k), default)`，与静态类型路径（`:10804`）同符号同 ABI。语料 IR `get_3` **32→0**、`map_get_default` **27→61**，语料 LLVM bare `@get(` **34→0**；**主线位移 0 有正证据**（pre/post 各 4 跑都是 135/135/120/135 行 rc=139、末条日志同行 ⇒ 仍卡 `str_trim` #145）。两条判据负结果（都是本批自己踩的、当场实拍）：① 键必须静态可知是 str —— 放开后 `dd.get(k,"fb")` 在 `k` 真在表里时**静默打 `fb`**（`lower_map_key` 对动态键按裸句柄哈希 ⇒ 恒缺键），宁留 abort；② 有静态路由的接收者不得抢 —— 第一版抢在 siteA/siteB 前，抢掉 batch 288 的 **2 个 PyJson 活点**（`py_json_get_default` 4→2），运行期形状守卫 `runtime/tokio_runtime_stub.c:220` 把它喊成 rc=134，加 `receiver_has_typed_route`（问 `qualified_method_candidate` ∨ pylib `handle_tag`+`method_symbol`）后回绿，t458 锁三进制读数。**另订正 420 §五 第 3 条的另一半**：那 2 个 `DataFrame::get` 站点改前也不是能跑的路由（`pylib/pandas.z` 无 `def get`、最终二进制无 `_DataFrame::get` 符号、MIR 名同样落 bare `@get`），所以是"必 abort → map 查表"而非回归，但对 pandas 的列标签语义对不对**未判**⇒ 记边界。421 之后 #7 的本族剩余成员：①动态键 **7 个调用形状**仍留 `_get` abort（`jq_shim.py:272`、`jq_wufu_local.py:198/309`、`jq_wufu.py:465/492`、`jq_wufu_daily.py:473/515`，正解＝#117 的动态槽标记）②`DataFrame.get` 路由语义未判 ③f64 default 返回打位模式（g1 `4612811918334230528`，折进 #117/#33）④`("set", 3) => array_set`（现 `:10525`）同形兄弟仍 0 站点未守；⑤`str_trim` packed 生产者需动 `runtime/py_additions.c`，未授权。**⇒ 批次 422 把幽灵名族从头名摘掉（位移 0，正证据＝点名行 0）**：419 的桩按 argc 共享（`zeta_dyn_member_missing_1/_2`）＋全仓抛掷恒 code 1 ⇒ 一次跑答不出"这个调用点抛没抛"；422 改逐名生成（`zeta_dyn_missing_<名>_<argc>` → `zt_dyn_member_missing`，`runtime/unavailable_stubs.c:81`；**点名一次、每次调用都抛**，419 契约不变）后，语料 6 个桩点自报身份 —— `data_cleaning.py:302`（`[dynamic]str::isin` argc2 + `::copy`）/`:304`（`[dynamic]i64::copy`）在 `clean_market_ohlcv` 内，该函数全 IR **calls=0**（唯一调用方 `market_panel.py` 进闭包 define 数＝0）⇒ 死代码；`bt_helpers.py:62`（`::dropna`）/`:77`（`::ffill`）唯一调用点来自 `__run_backtrader`，而 `backtrader_engine.py` define＝0；`jq_wufu.py:455`（`::to_dict`）2 个调用点全在 `midday_routine`＝午间、崩点在晨间 ⇒ 之后。改后二进制两次语料跑 `run=139 errlines=135`、stderr 点名行 **0**。三条旧账入册订正：①419/420 的"头名＝`copy`×4（`jq_wufu_local.py:166/190/248/301`）"作废 —— 那 4 个绑的是 pylib 真定义（`call @"DataFrame::copy"` 语料 **37** 处，裸名 `@copy` 的 define 反而 0 个调用点），真 `copy` 幽灵只有 data_cleaning 那 2 个；②worktree §1"`ffill`/`isin` 归零"不准（各 1 个幽灵，`dropna`×15 是静态绑定数不是幽灵数）；③"bt_helpers.py:67 的 copy 是幽灵"订正为真绑定。**方法坑一条**：带 `::` 的 LLVM 符号在 IR 里被引号包住（`@"DataFrame::copy"`），`@[A-Za-z0-9_:]*copy` 会零命中 ⇒ 假负。真修＝掩码族（`zt_vec_cmp` `runtime/py_additions.c:1013` ＋ `DataFrame.__getitem__` 形参分派 ＋ 行过滤运行时）⇒ 记拦路不在语法面补。**422 之后本族剩余成员**：6 个具名幽灵（全在崩点后/死代码）＋ §五那条结构拦路。**新登记的下一格**：`market_panel.py`／`backtrader_engine.py` 两模块语料 define＝0（晨间链路少哪些模块，未定价）。**⇒ 批次 423：主线 301 二十批来第一次位移**（`runtime/tokio_runtime_stub.c:264` `map_resolve` 出口加粗判形 `zt_map_cap_ok`：首字非 [16,2^30] 的 2 的幂 ⇒ `zt_map_not_a_map` 点名 + `zeta_raise(1)`，不再解引用）。语料两跑一致 `run=139 → 0`、stderr 135 → 357 行、38 条点名（37 个 `py_map_items` 的接收者＝2024-01-02~02-29 的**交易日日期整数**：句柄 19724…19782、跳过周末 19728/19729 ＋ 1 个 `map_get` 收浮点文本串），74 次抛掷／38 行点名 ⇒ 422 的"去重只吞消息、不吞抛掷"在语料尺度复现。`tests/python_style/t460_map_handle_shape_guard.z` 锁（改前 run=139／stdout 0 行，改后 rc=0／stdout 四行逐字如 expect）。**但 rc=0 ≠ 修好**：仍 `1000000 -> 0 (-100.00%)`、74 条 `[ERROR] … 失败: 1`、`target=- \| holdings=- \| ranked=-`。**订正两条**：① 421 那条"位移 0 的正证据"只比了 `errlines=135` 相同 —— 行数相同崩点完全可以搬家，位移判据换成 `符号+偏移` 与 rc；② 本批上一轮自记的"崩点从 `map_resolve+8` 搬到 `map_get+256`"作废（lldb 实拍打在同名二进制的前一代上），新坑：**实拍文件必须与二进制同批产出并带 md5**。**上一条"下一格"的归因作废**：define=0 成立，但"函数体内 import 不进闭包"这条原因是错的 —— `bt_helpers` 在语料里的入口 `jq_wufu_local.py:158` 正写在 `_run_backtrader`（:154）体内，它 IR define＝**8** ⇒ 函数体内 import 会进闭包；剩下的问题是"同形的两条 import 为什么一条进一条不进"（`jq_shim.py:1025` 是相对 `from .backtrader_engine import`），未证。**新头名**：`jq_wufu.py:702` `for _name, code in indexes.items()` 的接收者为何是当天日期（`indexes`＝:695-699 的字典字面量）⇒ 疑槽位复用/串号（#130/#131/#142 同族），IR 判据现成（`py_map_items` 25 站点／23 宿主，`check_a_share_weak_period` 内恰好 1 个）。门禁 `GATE_RC=1`，唯一红源仍是存量 `t231/t233`；python_style 342/2/6/0＝350（+1＝t460，零回归）；jit ok=176 trap=371 total=547；`md5 target/release/zetac` 与 422 逐字节相同 ⇒ 位移纯在运行时侧。OPEN 净增 0：423 折进本行 ⇒ 批次 424 把上一条"新头名"的**归因换对了、但语料那一格没修到**：`refine_param_types`（`src/main.rs:80`）取被调项只认"精确名"与"`模块__名`"两形，而 `gen.rs:11461` 的 `suffixed` 给**每一条裸名调用**都追加实参个数后缀（`daily_stop_loss`→`daily_stop_loss_1`）、条目名没有 ⇒ 一条候选都找不着 ⇒ 未注解形参保持 `dyn` ⇒ codegen 的 FieldAccess 落 `("", 2)` 兜底、`codegen.rs:6700-6708` 又把越界索引**夹回 0** ⇒ 三字段结构体读成 word0（`ZETA_DBG_FA` 实拍 `field=portfolio variant="" field_count=2 base_ty=Some(PyDynamic)` + `idx 2 >= count 2 -> fallback 0`，第二跳 `field=positions base_ty=Some(I64)`）⇒ 从整数身上按指针读字段＝SIGSEGV。**423 §八.1 的"槽位复用/串号"判死**（全程没有槽位复用参与，改的只有类型证据通道）。代码批 `06c67cda`（4 文件 +74/−9：`src/main.rs:183-207` +19 词干兜底 —— 只在无精确条目时走、只认 `name == stem`，重载集 `show_1`/`show_2` 的绑定一字未变）+ 新用例 `t461_arity_suffix_binding_chain.z`（46 行，`total 3/tag 7/dt 19724/done`；改前 compile=0 run=**139** stdout **0 行** → 改后 run=0，stdout 与 `python3` 当场对数逐字相同）。**家族定价（两把尺子）**：`jq_wufu.py` 单编成根模块 ⇒ 301 条目/2,518 条调用里 44 个后缀目标/82 站点无条目，其中词干==裸条目 **32 个目标/46 站点/24 宿主**；语料整体（628 条目）⇒ 后缀目标 **87 个、其中有精确同名条目的 0 个**、词干==裸条目 **5 个**（＝本批新绑：`execute_trade_4`/`final_value_2`/`iterrows_1`/`on_trading_day_2`/`to_dict_2`）、词干落在限定条目尾的 6 个按边界**不绑**（跨模块同名怎么消歧＝与 #9 同一扇门）、76 个查无条目（内置/外部库）。**语料位移 0，三层都是正证据**：`--emit-llvm` 两侧 md5 逐字节相同 `2997d4f5212568acccf6dcb72fb4abfd`（113,722 行、diff 0）；MIR 唯一 hunk 是 `BaoStockSource` 的模块前缀别名，而**同侧两跑之间也翻、且差 2 个 hunk** ⇒ #9 的噪声底，可归因位移 0；运行期改前/改后各两跑读数逐字相同（stderr 357 行、38 条点名＝37 `py_map_items`＋1 `map_get`、WARNING 107、`[晨间]` 37 逐日），数字归一后只差 `map_get` 那一行的 ASLR 句柄与首字。**为什么够不着**：语料里 `jq_wufu` 不是根模块 ⇒ 它的调用点全是限定名、从不带后缀；而 `check_a_share_weak_period` 的 `context` 要的证据在唯一调用方 `check_weak_period_daily` 身上，**它本身没有调用点**。**这一族在语料的规模当场量出**：`ZETA_DBG_FA=1` 全量编译，529 条 FieldAccess 里 **28 条走了"夹回 0"**（`portfolio`×8 的 base 是 `PyDynamic` ＝ 动态 context 那一形；`avg_cost`×5、`paused`×3、`context`/`routines`/`trading_dates`/`low_limit`/`closeable_amount`/`bar_types` 各 2；`positions` 另 7 条 base＝`I64`）。37 条点名的句柄是 37 个互不相同的连续整数 `0x4d0c…0x4d46`、中间跳过 `0x4d10/11` 等 ⇒ 逐日 +1 的交易日序数（`0x4d0c`＝19724＝夹具里那个 `dt` 的来源）；**但"28 处里哪一处 emit 了这 37 次点名"未证**（点名句仍只有原语名＋`dladdr` 返回位置，423 §八.2 那条"带用户函数名"的前置工作本批未做）。**新头名**＝`("", 2)` 兜底与索引夹回这两处静默本身（28 处成员已定价；方向 (a) 缺证据即出声、沿用 423 那一招 —— 它直接换来 rc 139→0＋38 个名字，方向 (b) 真修＝"名字→字段"扫描补 variant 判据，t461 是这条路的第一个夹具成员）；**第 2 格＝回调／函数值实参的参数证据**（＝1 的上游根因；census：628 条目/625 个不同调用目标名，**55 个宿主发 `FuncAddr`**、被取地址的目标 **106 个**、其中 **103 个从不出现在任何调用点** ＝ 97 个 `__closure_N_*` ＋ **6 个具名日程例程** `morning_routine`/`afternoon_routine`/`buy_routine`/`sell_routine`/`reset_daily_flags`/`check_weak_period_daily`，正是 `run_daily(<fn>, time=…)` 注册的那批 ⇒ 被调度执行的函数形参永远拿不到调用点证据）。门禁 `GATE_RC=1`（唯一红源仍是存量 `t231/t233`）、python_style **343/2/6/0 ＝ `ls t*.z` 351** 逐字对上（净增 1＝t461，零回归）、official 194/194+191、语料 40/40、jit 176/372/548 segv=0、diff 120/130 92.3% bad 0、clean_checkout rc=0 rev=`ff5514e4`、诊断面 official 2/5、python_style 231 行/108 文件与 422/423 逐字相同；ABI `--rebind` 改写 3 行/5 数（`main.rs` 五锚集体 +19）＋ **1 条表格内裸引用手工成对搬**（`main.rs:568`→`587`，`--rebind` 按设计不收无唯一配对的引用），终态 28/11/10 rc=1 定位失败 0、ABI.md 仍 1032 行。**尺子级负结果两条**：① `--dump-mir` 的打印在 `main.rs:897`、`refine_param_types` 在 `:905` ⇒ 这条路根本照不到（410~413 期间"形参类型 0 行变化"那类读数一律作废）；② emit-llvm 的 md5 基线必须同 cwd 同环境（换到 `/tmp` 漏 `REPLAYQUANT_LOCAL` 会得到另一颗 md5＝数据分支不同，不是出码非确定）。OPEN 净增 0：424 折进本行。**⇒ 批次 425 收掉跳 3/4 的读侧掷硬币**（代码批 `7dcae28f` + 注释等行笔 `0d5b52e0`）：`("", 2)` 兜底前多一条按**字段名**反查布局的判据（`codegen.rs:5918`，采纳条件＝所有声明该字段名的类别在 (variant, 宽度, 索引) 上完全一致，分歧退回旧兜底）。**语料第一次真位移（423 之后第二次）**：`ZETA_DBG_FA` 全量编译夹回 **28→11**（收回 `portfolio`×8→`_LocalContext`、`avg_cost`×5 与 `closeable_amount`×2→`Position`、`context`×2→`NautilusBackend`），PY-A 点名 **38→2**（`py_map_items` 37→1）、stderr 357→321、改前 4 跑里 1 次段错误在改后 4/4 跑消失；新用例 t462 改前 run=**139**/stdout 0 行 → 改后三行、与 CPython 逐字相同。**主线第 1 步（0 笔成交）未闭**：`[PARITY] target=- \| holdings=- \| ranked=-` 74 条与 `1000000 -> 0 (-100.00%)` 一字未动，stdout 仍是地址样整数（#117/#145）；残 11 处＝≥2 类别声明同名且分歧（结构可证、逐对 variant 未证），其上游＝424 §八.2 回调/`FuncAddr` 形参证据缺失 ⇒ **头名换成那一格**。类型层仍允许"从整数身上读字段"（改后 `base_ty=Some(I64)` 那一形还在），424 §八.1 的"缺证据时出声"方向未做。OPEN 净增 0：425 折进本行 |

## 2. v1 问题汇总（按处理顺序分组）

### A. 门禁可信度类（第一优先——"绿"本身不可信时，其他一切验证都押不住）

| 编号 | 一句话 | 来源 | 粒度 | 状态 |
|---|---|---|---|---|
| #26 | `--emit-llvm` 退出路径空指针 | 批次 313 OPEN | S | ✅ 315（未绑符号就地填自报名桩） |
| #28 | `--emit-llvm` 不带 `-o` 会**执行**被编译的程序（CLI 语义缺陷） | 批次 314 OPEN | S | ✅ 344（`main.rs:536-539` `probe_only` + `:924` 收窄 else；门禁第 12 步 `cli_semantics` 18 断言，负对照喂旧二进制只红"收窄翼"6 条。顺带纠正两处旧认知：缺陷**不止** `--emit-llvm`（`--dump-mir`/`--report-*`/`ZETA_DUMP_IR` 同病），且 `ir` 口径含执行程序 ⇒ 3 文件实测 597.5 → 388.6 ms（−35%），批次 313 基线由 `ir_executes_program` 标签自动拦下不可直比） |
| #29 | ci.yml:61 JIT 冒烟引用 3 个不存在的文件 + `if [ -f ]` 假绿 | 批次 314 OPEN | S | ✅ 347（两处说谎的步骤都换成真判据：① `Run known-good tests` 引用 `tests/test_hello.z`/`test_values.z`/`test_basic.z`——**三个文件都不存在**，命令还用 `--jit`，而 `main.rs` 里根本没有这个标志；外套 `if [ -f ]` ⇒ 循环体一次不执行、静默跳过，末行却无条件打印 "Self-host compilation verified"。已删除该步骤（JIT 覆盖在 baselines job 的 `run_all.sh` 步骤 4 `jit_sweep.sh`）。② 同 job 的 `Compile all zeta_src/ files` 用 `zeta_src/*.z`，只覆盖顶层 **5/51**——改名 + 改判据（见 #79）。同批清 benchmarks.yml（见下行）） |
| — | benchmarks.yml 5 处幽灵引用 | 批次 313 OPEN | S | ✅ 347（实测是 **8 处**不是 5 处：`--bench compiler_bench` ×2 / `--bench runtime_bench` ×2 / `--bin regression_test` ×4。根：`benches/` 自 **v0.10.0 housekeeping**（175f4cd3）就被删了，该工作流从此是死的，而 `Cargo.toml:94` 的 criterion 依赖还在为它守着。处置按轴 A：删工作流 + 清理 4 条零引用 dev-dependency（proptest/libfuzzer-sys/criterion/tempfile，grep 实证 `proptest!`/`prop_assert`/`TempDir`/`tempdir`/`fuzz_target`/`libfuzzer`/`criterion_*` 在 src/ 与 tests/ 下命中均为 0）⇒ `Cargo.lock` **−550 行 / 56 个 crate（0 新增）**；`cargo build --release --all-targets` 通过。真实性能基线由 `tools/perf_baseline.py` 承担（其文档串早已记录这个幽灵）） |
| #71 | 干净检出 + `cargo check` 进门禁（配方已在手：worktree + 符号链接 zetac，4 秒量完） | 340 OPEN 4 | S | ✅ 342（门禁第 10 步 `clean_checkout`；`rev`/`secs`/`rc` 进 JSON。汇总时漏更新，本表 343 补记） |
| — | `.gitignore` 7 个 NUL 字节 + CRLF（改动对 review 全不可见的根） | 340 OPEN 3 | S | ✅ 345（NUL 7→0、CR 行 154→0；"根"实证：`git show --numstat 801fc78a`/`6fe84c69` 对它都是 `- -` ⇒ 314/340 两批的规则改动在常规 diff 里就是一行 `Binary files differ`。净账用全集比对：被忽略路径 71,572 行逐行 diff 输出 0 行。`zeta/` **带证据不复活**——`tests/zeta/` 有 10 个已跟踪手写 `.z`，且该规则位置晚于 `!tests/**/*.z`。防回潮断言按登记规则并入 #72 的 ①，未新开登记行）|
| #40 | 裸 `*.z` 吞新建用例——340 只在 tests/、pylib/ 开负例，src/tools/docs/examples 仍吞 | 批次 322 | M | ✅ 346（量完才发现 `*.z` 只是同根因的第一个受害者：**10 条不带斜杠的规则**长期压住 **290 个已跟踪文件**（`tests/` 136 · `zeta_src/` 51 · `src/` 39 · `zetas/` 17 · `.github/` 13 · `examples/` 5 · `tools/` 1；`.rs` 就有 92 个，其中 `tests/unit/` 52）。按 gitignore 语义收窄成根锚定 ⇒ 290→28，残差 28 逐条有归属（`/build/**` 24 · `*.o` 2 · `*.backup` 1 · `.codegraph` 1）；`zeta_*` **例外**：它吞的 `zeta_src/` 本身在仓库根 ⇒ 根锚定无效，只能删，最小仓三条路径对照为证。反方向读数 0 条（无物变被忽略），可见未跟踪只多 1 个用户文件 `zeta_src/qwenadvice.md`。防回潮 = 门禁第 13 步 `ignore_rules` 19 断言四翼，4 个反证里 2 个是门禁级（N3 证明它进退出码、N4 证明 `--skip-ignore` 如实登记）。OPEN 折进本行不新开：残差 2 个根级已提交 `.o` 的正解是 `git rm --cached`（改规则只是换个地方继续）、`zeta_src/` 51 个 `.z` 是否进门禁语料（与 #42 同族，先测"喂进去会怎样"） |

### B. 解析器截断类（#36 = 官方 12 用例 1,805 行被静默丢弃的余量）

| 编号 | 一句话 | 来源 | 粒度 | 状态 |
|---|---|---|---|---|
| #36 | ✅ 批次 366 实测结果：11 文件总丢行 1,019+ → **712**；已清零 5 文件（minimal_compiler 230、test_suite 127、bootstrap_validation 58、integration_all_features 58、integration_test_program 16——后两个由批次 366 的无参闭包修复清掉）。剩余 8 文件 / 712 行，逐文件卡点已探针定位（批次 366 记录有全表）：benchmark 357（static mut）、selfhost 158（impl Trait for + concept）、quantum_basic 85（use 深路径）、advanced_patterns_test 62（@绑定 or 模式）、primezeta 36（预处理冒号误判）、test_const_expression 14（数组类型注解）。按性价比逐文件清。**⇒ 批次 368 附注（同一类、不同层：静默丢代码不止解析器一层）**：handoff §3 报的"for 循环体不执行"实测是另一回事——循环体照跑（`for i in 0..3 { n = n + 1 }` 退出码 3），丢的是**嵌在语句块里的宏调用**：`expand_macros_in_node` 只递归 `Program` 与函数体，`For`/`While`/`If`/`Loop`/`Block` 的语句列表走兜底臂原样返回，块里的 `println!` 保持未展开 MacroCall，MIR 降低对它静默跳过（`src/middle/mir/gen.rs:2888`）。同批第二个独立根因：`for i: usize in …` 的模式外包一层 `TypeAnnotatedPattern`，`src/middle/mir/gen.rs:2476` 取循环变量的匹配不认它 ⇒ 掉进集合分支、Range 当空集合迭代，循环体一次不跑（这就是 t413 锁的 0）。两处已修（roadmap 批次 368），**仍未修的实拍在册**：闭包体里的宏（`/tmp/b368t/h.z`，什么都不打）、match 臂里的宏（`/tmp/b368t/i.z`，`2 => println!("two")` 不打）——同一个兜底臂 `src/middle/resolver/resolver.rs:3544` 没覆盖到。连带新出现的 link-only：`integration_all_features` 缺 `_predict`、`_train`，引用点是 `tests/unit-tests/integration_all_features.z:71`、`:74`（`ml::neural::Network` 全仓无定义 ⇒ declare 无 define，与 #17 同形），归因是"批次 366 丢行清零后这两条调用第一次参与编译 + 本批静态排除（顶层非宏语句）"，**同尺子重建对照未做**。另登记一条尺子问题：按 handoff §7"一律 `ZETA_NO_OPT=1`"跑门禁，python_style 从 2 失败变 4 失败（多 t06 链接失败、t31 段错误，两条都是 #17/#18 存量）⇒ "一律带 NO_OPT"与"只有 t231/t233 红"两条规矩现在不能同时成立，待裁决，本批仍按既有基线尺子（不带）交付。OPEN 净增 0（全部折进本行，不新开登记项）。**⇒ 批次 369 更正（368 的归因写错了，本批改对）**：本行末句与 roadmap 批次 368"边界"节都写"match 臂里的宏卡在同一个兜底臂 `resolver.rs:3544`，补臂即可"，批次 369 照这句话加了 `Match` 展开臂并加宽了 `gen.rs:2892` 的委托臂，实测三处输出一字未变 ⇒ 那句归因作废（原文不回改，旧结论→新结论：兜底臂=真卡点 → 语句位置 match 整条不进 MIR（`gen.rs:1206` 的语句分发无 `AstNode::Match` 臂，落到 `:2895` 的 `_` 空臂），另两条同时曝光：臂体在 if-else 链之外被提前下推（`gen.rs:10687`，实测三个臂的副作用全跑、选值正确），`println!` 展开是两个节点而臂体槽只有一个表达式（`macro_expand.rs:112`-`:126` 对 `ast.rs:18`-`:25`）所以永不替换）。两处试改已撤回，本批源码与 368 提交一致。**§4 复测（369 实测）**：5 文件 / 676 行（benchmark 357、selfhost 158、quantum_basic 85、advanced_patterns 62、test_const_expression 14），`primezeta_usize_test` 不再报 W1002 且文件仍在、tracked ⇒ 清零由 367+368 完成；工具报的 237 = 本仓 198 个 `.z` + 外部语料 39 个 `.py`（分母随那个目录变动，命中表不受影响）。**⇒ 批次 370（§4 第一块实现）**：`test_const_expression` 的 14 行清零（数组类型注解的长度位改成收表达式，`src/frontend/parser/parser.rs` 的 `parse_zeta_array`），同尺子复测 **4 文件 / 662 行**（benchmark 357、selfhost 158、quantum_basic 85、advanced_patterns 62）；三组读数未动（official compile 194/194、python_style 只红 t231/t233、语料 39/39），official 诊断 7 文件/31 行 → **6 文件/30 行**；jit ok 171→170 已归因到本批（该文件修前连 `fn main` 都被丢，"ok"是空程序换来的；修后函数体真编译，JIT 模式缺 `zeta_dyn_getitem`/`zeta_map_set_tag` 绑定 ⇒ 出声 trap，AOT `-o` 退出码正确；同二进制连跑两次读数一字相同，排除抖动）。本批同时把上面那张逐文件卡点表改对两处、确认两处：benchmark=函数体里的 `static mut`（成立，`unsafe {}` 块本身不背锅）、quantum_basic=函数体里的 `use` 深路径（成立）、advanced_patterns=or 模式的分支只要不是数字字面量就丢（**原记"(@绑定 or 模式)"作废**，详见 #43）、selfhost **原记"impl Trait for + concept"证不成立**（三份最小夹具都无 W1002），本批未定位到最小形状，下一步用现成探针 `ZETA_PARSE_TRACE` 量，不再手工截文件（截坏的夹具判据无效）。**⇒ 批次 371（§4 第二块实现）**：`advanced_patterns_test` 的 62 行清零（or 模式在 `parse_pattern` 里统一收成一条链，`src/frontend/parser/pattern.rs`），同尺子复测 **3 文件 / 600 行**（benchmark 357、selfhost 158、quantum_basic 85）；三组读数一字未动（compile 194/194、python_style 只红 t231/t233、语料 39/39），official 诊断 6 文件/30 行 → **5 文件/29 行**（明细里 `advanced_patterns` grep -c = 0），jit ok=170 未变；`pattern.rs` 插 14 行 ⇒ ABI.md 两处承重行号重绑（`:854` 121→135、`:913` 201→215，等长换号 + tsv 同步，没跑 `--rebind`，避免把别的会话那 58 处漂移一起改写）。**⇒ 批次 372 定位（只量，未动源码）：quantum_basic 的 85 行是两层问题，解析层单独收不掉**。触发点确认是函数体里的 `use`（`ZETA_STRICT_PARSE=1` 报 `tests/unit-tests/quantum_basic.z:73` 起 85 行未解析；`parse_stmt` 没有 `use` 臂，且 `src/frontend/parser/stmt.rs:1679`-`:1708` 已是 21 臂，顶到 nom 的元数上限，加臂必须走嵌套 alt）。把那两行 `use` 删掉、让 85 行回来的副本实测（`/tmp/b372/qb_nouse.z`，154 行）：AOT `-o` **链接失败**，5 个未定义符号（`_factor`、`_optimal_iterations`、`_success_probability`、`_test_fn`、`_[dynamic](i64, i64)__iter`）；JIT 无 `-o` 出声 E4016（13 个无绑定符号，含上面那几个）而**不是段错误**。根因侧证据：`ShorsAlgorithm`/`GroversAlgorithm` 只在 Rust 侧 std（`src/std/quantum/mod.rs:638`、`:756`，extern "C" 出口 `:1225`、`:1246`），仓内没有可被 `use` 载入的 `.z` 定义（`build/stubs/std/` 无 quantum 一条），而顶层 `use std::quantum::algorithms::ShorsAlgorithm;` + `ShorsAlgorithm::new(15)` 的 `/tmp/b372/t1.z` 编译 rc=0、**零诊断** ⇒ 幽灵调用不出声（#41 那一族）。所以单实现解析修复，读数会从"静默丢 85 行"变成"compile 过、link 出声"（compile 194/194 不动、compile+link 193→192、jit ok 170→169 且是 trap 不是 segv），要跑通还得补 quantum 一族绑定，与 #42 同族；另有一条未实测的疑点先记着：语句位置的 `Use` 走不走得到 `resolver.rs:782`（`register` 只见到往 impl 体里递归），本批没装修复所以没量。**⇒ 批次 373（selfhost 158 行的最小形状闭掉了）**：不是 `impl Trait for`、也不是字符臂/字符串臂/臂体里调方法（`/tmp/b373/m1`-`m4`、`m7`、`m8` 全无截断），而是**match 臂体写裸赋值**——`m5.z` 的 `match ch { ':' => 1usize, _ => i += 1 }` 出 W1002 且整条 fn 被丢；同一句包成块（`m9.z`）或挪到 match 之后当普通语句（`m12.z`）都不截断 ⇒ 臂体用的是表达式规则、赋值不在其中（`tests/unit-tests/selfhost.z:80` 的 `_ => i += 1,` 即此形）。OPEN 净增 0（全部折进本行与 #43，不新开登记项）。**⇒ 批次 374（selfhost 的 158 行收成 91 行）**：#38 的 ②③⑥ 实现（臂体收裸赋值 + 语句位 match 进 MIR + 臂体语句留在自己分支），同尺子复测 **3 文件 / 600 → 3 文件 / 533 行**（`benchmark_simd_vs_scalar 357` 未动、`selfhost 158 → 91`、`quantum_basic 85` 未动）；selfhost 剩的 91 行换了一形，最小形状已闭：`let x = if let … { } else { }`（`if let` 当表达式用，`/tmp/b374/d1.z` 报 NOT parsed、对照组 `d2.z` 同位置换普通 `if` 不报）⇒ 批次 375 候选。三组读数里 compile 194/194、official 诊断 5 文件/29 行、语料 39/39 一字未动；`compile+link 193 → 192`（selfhost 第一次进编译的 4 个 declare 无 define 符号 `_as_str`/`_build_ast`/`_is_alphabetic`/`_push`，与 #42 同族，静默丢代码换成出声报错）；jit `ok 170 → 171`、`total 499` 多出的一条是本批新用例 `t415`，JIT 下实拍通过。OPEN 净增 0（全部折进本行与 #38，不新开登记项）。**⇒ 批次 375（本行未收一格，只把两处卡点量到能照着做）**：同尺子复测仍是 **3 文件 / 533 行**，逐文件一字未动（`benchmark_simd_vs_scalar 357`、`selfhost 91`、`quantum_basic 85`）——那批的改动在值表示层，不碰解析，本行没有可收的格。**上一行末句的"批次 375 候选 = `let x = if let …`（if let 当表达式用）"改判**：实测 `if let` 卡得比解析更深（#38 新增的 ⑦：构造子模式没有语义、`else_` 整个丢掉、枚举载荷是不透明句柄），只把解析修通会把"静默丢 91 行"换成"静默错值"，所以本批不动它（明细在 #38 ⑦）。本行两处卡点各补一手实测：① `selfhost` 剩的 91 行下游 = #38 ⑦；② `benchmark_simd_vs_scalar` 的 357 行压在 `tests/unit-tests/benchmark_simd_vs_scalar.z:11` 那一行 `static mut counter: u64 = 0` 上，最小复现 `/tmp/b375/sm1.z` 实测 W1004（`static` 被当独立语句、后面的词被吞）+ W1002（整条 fn 被丢），文法里没有 `static`、AST 里没有 `AstNode::Static`，"只初始化一次"要么加运行时助手（模块全局走 env 表，`src/backend/codegen/runtime_decls_core.rs:51-52` 只有 `zeta_env_get`/`zeta_env_set`，**没有** `zeta_env_has`）要么把初始化提到模块初始化里 ⇒ 跨解析+AST+MIR+运行时四层，不是一个解析批。门禁三组读数未动（compile 194/194、python_style 只红 t231/t233、语料 39/39），official 诊断 5 文件/29 行、jit `ok 171 → 172`（`total 499 → 500`，多的就是新用例 `t416`，JIT 下实拍通过）。OPEN 净增 0（三条新登记全部折进 #36/#38/#45，不新开行）**⇒ 批次 381（只有定位，零代码改动）：本行"quantum_basic 85（use 深路径）"改判。** 卡的不是深路径，是"函数体／模块块里的 `use` 这一种拼法没进语句规则"：`/tmp/b381/g1.z`（体里一条 `use std::quantum::algorithms::ShorsAlgorithm;`，后面再跟一条 `fn`）→ W1002 落在 `:1`、整文件 5 行全丢；同一位置换成 `import std.quantum` 或 `from std.quantum.algorithms import ShorsAlgorithm` 一声不出 ⇒ `src/frontend/parser/stmt.rs:896` 的语句规则里有 Python 两种拼法，`use` 只挂在顶层（`src/frontend/parser/top_level.rs:132`），深路径 `a::b::c` 本身没问题。同族第二拼法是 `mod { }` 块里的 `pub use`（`tests/zeta/test_pub_use.z:3` 丢 15 行），`tests/stdlib-foundation/fmt_time_env_test.z:113` 的 47 行也归它。修法在手边（`parse_use_targets` 已是 `pub(crate)`、`stmt.rs:935` 就在复用，MIR 语句位对 `AstNode::Use` 已是无操作 `gen.rs:2796`），**但单独收它会让 official 的 compile+link 从 192/194 掉到 191/194**：把 `quantum_basic.z:75` 那一行删掉再编译（等价于"`use` 已过"之后的下游），shors 那条 `fn` 进了程序，`shor.factor()` 在接收者类型未知时下发成裸名 ⇒ `Undefined symbols: "_factor"`、`Linking failed`（该文件现在是能链接的）；同一趟还显形出下一个卡点 `test_grovers_algorithm`（`:95` 起 62 行，首条语句还是体里 `use`，在 `:98`）⇒ 这一族要和 quantum 方法的真符号一起收（Rust 侧有 `shors_algorithm_new`/`shors_algorithm_factor` 在 `src/std/quantum/mod.rs:1225`、`:1231`，`.z` 链接的 C 运行时侧要动 `runtime/py_additions.c`，本会话不动该文件）。本批无源码改动 ⇒ 门禁与丢行尺子沿用批次 380 的在册值；明细在 roadmap 批次 381。OPEN 净增 0（只在 #36 行内改判＋记卡点）。**⇒ 批次 382 附注（selfhost 那 91 行的卡点从"解析"改判到"表示"）**：值位写法 `let x = if let 5 = t {…} else {…}` 本批补上解析规则（`src/frontend/parser/expr.rs:806`，展开成两臂 match），字面量、绑定、or、区间四种模式实测取到正确值；但 `tests/unit-tests/selfhost.z:97`、`:106`、`:111` 三条用的是构造子模式（`if let Token::Ident(n) = …`），被本批新加的守卫（`expr.rs:787`）主动退回原路径 ⇒ 91 行未收回、W1002 照旧（丢行尺子仍是 3 文件 / 533 行）。改判依据：`src/backend/codegen/codegen.rs:6210-6211` 下发 `MirExpr::Struct` 时把 `variant` 丢掉，带数据的 enum 变体是没有 tag 的堆格子、单元变体是裸整数，模式侧没有"取 tag / 取载荷"的入口，所以这一族不是解析层半修得了的；明细在 roadmap 批次 382。**⇒ 批次 384 附注（benchmark 那 357 行归零，尺子到 2 文件 / 176 行）**：`benchmark_simd_vs_scalar.z:11` 的 `static mut` 补了 `parse_static`（`src/frontend/parser/stmt.rs:118-144`，接在 `:1735` 与 `let` 同槽），并新增一次"把函数体里的 `static` 抬到模块作用域"的编译期搬运（`src/frontend/parser/top_level.rs:1784`，`synthesize_implicit_main` 首行调用），撞名出声拒搬（W1009）、抬不到的出声按局部（W1008）—— 两条都不许静默共享。实测：该文件 W1002 归零、AOT 编译能链接、全语料唯一剩下的 W1004（1 处）也归零；丢行尺子 `3 文件 / 533 行 → 2 文件 / 176 行`，剩余两格与 §4 一致（`selfhost` 91 = 表示层，`quantum_basic` 85 = 函数体 `use`，都卡在不动 `runtime/py_additions.c` 就过不去的那一层）。JIT 侧该文件从 ok 变 trap：原来那个 ok 是空程序的 ok，现在真编译了而 JIT 缺 `zeta_module_decl/env_get/env_set` 绑定 ⇒ 响亮 E4016，归 #26 类。**批次 385 收掉同一批实测出的另一条**（模块全局走 `+=` 没有环境表镜像，`= 1` 能持久、`+= 1` 不能）：`AssignOp`/`Var` 支路补上 env 镜像（`src/middle/mir/gen.rs:1822`），三处模块全局写路径的镜像实参从"传原表达式"改成"传刚写过的槽"（:1668/:1741/:1822）—— 同一个函数里第二次写时，那个表达式带着这个槽自己、codegen 在使用点重新求值 ⇒ 加了两遍（中间态实拍 `twice=20` 而 `peek2=22`；`=` 同形 `r=7 / p=11`，本批一起改）。同批再登记两格未修（各钉一条 known-fail）：主程序读自己声明的模块全局走那条 `=` 留下的旧槽（`t425_module_body_stale_read.z`，`top=0`）、f64 的模块全局过不了 env（写侧 `fptosi` 丢小数 + 读侧按声明的 F64 把整数当位模式 ⇒ `2.5` 在别的函数里读回 `0.000000`，`t426_f64_env_roundtrip.z`；整数与容器过 env 实测都对）。合同在 `docs/ABI.md` R8。**⇒ 批次 386（只有定位，零代码改动）：f64 过 env 这一格的判据定下来了。** 同一份存储上实测两格：`/tmp/b386/q1.z`（`t426` 同形）`outer=2.500000`/`inner=0.000000`；`/tmp/b386/q2.z`（函数里 `global ratio` 之后 `ratio = ratio + 1.0`）`outer=3`/`peek=0.000000` ⇒ 小数和声明类型一起丢。IR 实拍：写侧 `/tmp/b386/f1.ll:1122` 是 `fptosi`（`zeta_env_set` 两个实参都声明成裸 i64，`src/backend/codegen/runtime_decls_core.rs:52`），读侧有声明类型时是 `store i64` → `alloca double` → `load double`（取位模式，`f1.ll:1075`），q2 那一形下改走硬写 I64 的臂（`q2.ll:1163` → `print_i64`）⇒ `gen.rs` 七个 env 读点里三处按声明类型给槽（`:3472`、`:11525` 段、`:13671` 段，各自注释都写明过硬写 I64 的坏处）、四处硬写 `Type::I64`（`:1260`/`:1707`/`:3449`/`:3490`），规则只做了一半。修法：写侧对"声明类型是 f64"的名字存位模式，**判据留在 gen.rs**（`tests/python_style` 316 个文件实测 22 条 `ABI coerce` 里 5 条是用户函数把 f64 传给 i64 形参，那 5 条要的就是数值截断，所以不能下沉成 codegen 按被调符号名改），读侧那四处同批改用 `global_ty_of`；闭包 `nonlocal` 那一族没有声明类型（`/tmp/b386/pA.z` 实测 `read= 2`），保持现状不改，留给"让 env 带上类型"（轴 B/F）。明细在 roadmap 批次 384→386。**⇒ 批次 387（本行 f64 那一格闭合，规则做成对称的）**：写侧读侧收敛到两个同源入口 —— `env_store`（`src/middle/mir/gen.rs:428`）与 `env_slot_ty`（`:485`）都问同一句 `global_ty_of(name)`，八条 `zeta_env_set` 写点（:1317/:1718/:1738/:1742/:1793/:1824/:1857/:13108）与四条硬写 `Type::I64` 的读点（:1326/:1753/:3476/:3518）全部改走它们；声明是 `F32`/`F64` 且值也是浮点时，值先落槽再 `AddrOf` + `Deref{pointee_width: 8}` 取裸词入格（与容器写侧 `codegen.rs:4782`/`:5503`/`:6792` 同形，不动 C 运行时）。实测改前→改后：`q1` `2.500000`/`0.000000` → `2.500000`/`2.500000`；`q2` `3`/`0.000000` → `3.500000`/`3.500000`；`q3` 的 `peek` → `3.500000`（`outer` 停在 `t425` 那一格）；批次 385 边界 2 的连带 `acc += 2.5` 两次 → `peek=5.000000`；`pA`（闭包 `nonlocal`，无声明类型）逐字未变 `read= 2`、两条 fptosi 仍在 —— 386 记的那个 `build_int_z_extend(i64→i64)` 风险实测未发生。`python_style` 里 `zeta_env_set` 的 fptosi 告警 17 → 3（余 3 条全属无声明类型那一族），用户函数 f64→i64 实参那 2 条未波及；`t426` 由 known-fail 转通过（摘标记），新增 `t427_f64_env_write_paths.z` 钉 `global` 写与 `+=` 写两形（`3.500000`/`3.750000`/`3.750000`，与 `python3` 逐字相同）。本行仍 OPEN 的两格：主程序/模块体自己读回旧值（`t425`）、`x: float` 全局被整数字面量赋值这一形（本批未改未测，已写进 `docs/ABI.md` R8）。**⇒ 批次 390（只有定位，零代码改动）：34 例回归归因完成，而且结论不是"写点没镜像够"**：三条机制各自的 2–3 行最小夹具都复现了——M1 副本落后于槽（`xs.remove(3)` 不走那 8 处 `env_store`，`/tmp/b390/m1.z` 打成 `[3, 1, 2]`）、M2 一格只有裸 64 位字所以类型/元素信息传不过来（`/tmp/b390/m2.z` 的 `parts[1]` 打成地址 `4370309088`；`t155` 前四行 bool 打成 `1/0/1/0`、末行 `3.000000` 打成 `3`；`t183` rc=134 分派落到 `_get` 桩）、M3 格按裸名字索引所以跨作用域同名并格（`t423` 只差 `top=0`→`top=10` 一行）。批次 389 留下的"`append` 重绑旧指针"解释被 `/tmp/b390/g1.z` 反证（A/B 两版都给 `3`/`[1, 2, 3]`；同份 MIR `env_get` 5 处对 `env_set` 1 处，读侧确实打了格）。⇒ 这条规则要从"写读对称"改成"模块全局只有一个存放点"（槽落在环境表里 / 格内存槽地址且类型随行），补写点追不完也补不齐 M2/M3。同批 2.2 读数：W1002 全扫 237 文件只剩 2 处（91+85），`quantum_basic` 85 行唯因是**函数体里的 `use`**（`/tmp/b390/f2.z` 一条即炸，删两行后 155/155 解析），但解析放开仍缺 5 符号 ⇒ 还要 `std::quantum` 与元组解绑值的调用；且官方基线 compile-only（`run_all.sh:79`、`:93`），该文件今天是**假绿**（`/tmp/b390/qb_orig.bin` 零输出 rc=0，85 行一行没跑）。`parse_bisect.py` 对 selfhost 报的病因行 `:96` 被 `/tmp/b390/f1.z` 证伪。回退后门禁 310/2/5/0（`/tmp/b390/ps_revert.log`），红线仍只有 t231/t233。**⇒ 批次 391 交付（4.3.b 写侧）**：M1 收掉一处 —— 模块全局的环境表镜像从"按语句种类补"改成一个降低后置遍 `mirror_module_global_writes`（`gen.rs:523`，装配点在 `:1339`/`:13891`），体内每条 `Assign{lhs}` 命中模块全局槽就在其后补 `zeta_env_set`，if/for/while 嵌套块递归同做；A/B（A=`60eb2caa^` 隔离 worktree 构建，`/tmp/b391/p3.z`+`p4.z`）顶层与块内的 `xs.remove(3)` 都是改前 `peek()=3`、改后 `2`。口径更正：批次 390 说的"8 处镜像"里只有 **3 处**是模块全局副本的镜像（另 5 处是 nonlocal/闭包，那里环境格就是存放点），本批真实收敛是 3→1，旧号→新号见 roadmap 批次 391。M2/M3 未动：`/tmp/b391/p2.z`（函数里同名局部写穿模块全局那格）A/B 逐字相同 ⇒ 未放大也未修。门禁 official 194/194 + link 192/194、python_style **311**/2/5/0（+1 是新用例 t428）、语料 39/39。OPEN 净增 0（全部折进本行，不新开登记项）。**⇒ 批次 392 定位（4.3.b / M3，代码零留存）**："按项过滤镜像"这条路判死。登记的 `fn_depth` 探针开跑前就是死的（加减只裹 `lower_ast` 体内，后置遍在 `:1284` 之后装配 ⇒ 顶层函数项与模块体项同样是 0，照原样跑会拿到 delta=0 并被误读成"M3 无依赖"）。换 `py_entry` 后机制生效：`p2.z` 三行 `6/6/100 → 6/100/100`，MIR 差 = `f` 体内两条 `zeta_env_set("total", 局部槽)` 被删。套件 delta 恰好 4 例，逐个 A/B + MIR 归因后 **0 例支持 M3**：`t46`/`t273` 是"被导入模块的体项叫 `X__init`、parser 不给它打 PY_ENTRY_ATTR"（探针自己的漏；`t273` 的 MIR 只少 `pyvarfixture__init` 里那条 `zeta_env_set("pyvarfixture__D")` ⇒ `D` 从没进 env），`t424`/`t423` 的 `a2` 是括号式 `fn` 的共享格合同（批次 385、R8），其 `fn main` 同样没 attr ⇒ 整份程序的镜像被关掉。M3 至今实测损害样本量仍 = 1（`p2.z`），套件无锁定用例。真修法在 **2.3 名称绑定**（py 模式 `def` 体内裸赋值绑局部；`D[k]=v`/`D.append(x)` 是变异不是改名，仍读 env；`fn` 与 `global`/`static` 保留共享格），开格前先取该形在语料里的计数。回退后重编二进制与改前 `cmp` 逐字节相同、门禁复跑 311/2/5/0 + 194/194 + 192/194 + 39/39。M3 候选降级至第 3。**⇒ 批次 393 交付（2.2，同一族的第 9 个成员清零）**：函数体里的 `use 路径;` 在 `parse_stmt` 没有臂 ⇒ `parse_block_body` 失败 ⇒ **整个顶层项连同其后每一项**被丢掉。接线（`stmt.rs:1759` 的 `parse_use_stmt`，复用顶层已有的 `parse_path`+`parse_use_targets`）+ 提升（`top_level.rs:1855` 把体内 `AstNode::Use` 提到模块级、原地留 `Ignore`；必须提，`Resolver::register` 在 `resolver.rs:782` 只遍历顶层项、从不进函数体）+ 收窄（`in_body`，`top_level.rs:1882`：顶层 `import a::b;` 也 lowers 成 `Block`，对它动手实测撞批次 337 的 `import` ≡ `use` MIR 等值红线 ⇒ 门禁 import_form 4 条 FAIL、`Return val` 4 → 5；三段分开实测矩阵在 roadmap）。同尺子读数：`truncation_inventory` 237 文件 **2 处/176 行 → 1 处/91 行**，本行旧表里的 `quantum_basic 85（use 深路径）` **清零**（`fmt_time_env_test` 另 47 行同因同修，合计 132 行回来；`--dump-mir` 759→1675、2001→2876）；提升真到达解析器有正证据（`fmt_time_env` 链接期符号从裸名 `_as_secs` 变类型限定 `_duration_as_secs`，唯一变量就是那条被提升的 `use`）。门禁 official compile **194/194** 不动、compile+link **192→191**：`quantum_basic` 的尾巴第一次参与编译，缺 `_factor`/`_optimal_iterations`/`_success_probability`（`std::quantum::algorithms` 全仓只有 `build/stubs/std/quantum/algorithms.z` 那个 `pub struct Stub;` ⇒ #42 族）与 `_test_fn`（`quantum_basic.z:136` 的元组一等函数值被下成裸符号调用 ⇒ 新成员，本批不收），改前二进制同目录 link OK、改后 link FAIL 实拍成对；python_style **312**/2/5/0（+1 是新用例 `t429_body_use_not_truncating`）、语料 39/39、jit ok 174、import_form 22 条 FAIL 0、clean_checkout rc=0；**整体 rc=1 的来源是 `run_all.sh:576` 的 `py_fail=2`（t231/t233 存量，批次 392 同为 2），不是本批**。顺手实测到、另登记的六条边界：顶层 `mod NAME { … }` 解析不了（探针整份 30 行、`test_pub_use.z` 15 行）、`use 路径::名字;` 裸用不绑定（#64 第二成员）、`mod::方法()` 返回地址（`u=4379267008`，#41 第二成员）、std::time 缺 7 个绑定（#42 族）、不存在的模块空桩**一声不出**（`t429` 四条 `nonexistent::*` 全无 W1007，#86 成员）、`if…else` 当块值取回 0；另有三处截断点未定位（`practical_programming_test.z` 245、`test_high_assurance.z` 411、`examples/quantum.z` 280）。结构证据：`parse_use_stmt` 在 codegraph **图内无边**（函数项进 `alt` 元组），以文本证据 `stmt.rs:1759` 单点接线为准；ABI 锚点读数 68/7/4、待归属 100 条 91 种，与批次 392 逐字相同 ⇒ 零新增漂移（存量归 #52）。OPEN 净增 0（全部折进本行，不新开登记项）。明细在 roadmap 批次 390、391、392、393 **⇒ 批次 396（同一类、卡点在下一层）**：selfhost 那 91 行收回（W1002 从 1 条→0 条，`selfhost.z:89` 之后 91 行回到程序），**但归因要更正**：本行原记的「`impl Trait for` + concept 两格」不是这 91 行的病因，实拍首段未解析文本是 `fn build_ast(tokens: Vec<Token>) -> Ast`，卡点是**值位置的构造子模式 `if let Token::Ident(n) = …`**（3.2 的枚举变体没有标签字 ⇒ 解析器把该模式判「不可测」而拒绝整个顶层项）。另一条口径教训：**丢行数不是门禁收益** —— selfhost 改前改后都停在 compile+link 那一档（缺 std 绑定），official 194/194 与 191/194 一字未动。 | 批次 319→396 | L | 🟡
| #36 附注 | 2026-09-23 晚实测：minimal_compiler 丢行已从 757 降到 **230**（停在 ：572 的 `fn main` 内部）。15 个可疑构造逐个测试**全部能解析**（原始字符串 r#".."#、多行原始字符串、`\|_\|` 闭包、std::fs 路径调用、点号续行、vec! 宏、for 元组解构、if 作参数、emoji 字符串、字符串内花括号、引用切片、区间内调方法）⇒ 剩余丢弃是**上下文组合**触发，单个构造不背锅。下一步：给 parse_program 的顶层循环加位置探针（报告最远解析到哪一行），用仪器定位而不是继续猜 | 第二会话 | M | ✅ 探针已完成（ZETA_PARSE_TRACE，批次 362），本次剩余卡点全部由它采集 |
| #39 | `s[i..]` 切片——**已验证当前解析器能处理 `..` 区间**（expr.rs:2293 分隔符同时接受 `:` 和 `..`；`&s[0..5]`、区间边界调方法均通过），757 行的真因已随文法修复消解，此条按已关闭处理，如有残留用 W1002 实测说话 | 批次 321 | M | ✅ 本会话核实 |
| #42 | ✅ 14 个 std 方法绑定全部实现（批次 363-365）：to_string、is_empty、is_whitespace、clone、parse、unwrap、unwrap_or、unwrap_or_else、chars、nth、iter、push_str、is_digit、is_alphanumeric。三个官方文件（bootstrap_validation_test / minimal_compiler / test_suite）从"编译通过、链接失败"恢复为全链接。**契约修订**：Result 用运行时既有的 host_result 三字段单元（364 登记的 0 哨兵被推翻——nth 元素可能是合法 0）；闭包用 FuncAddr + zeta_call1 回调。遗留观察（非本任务）：① minimal_compiler 运行 rc=0 但无输出（内嵌逻辑的运行期行为，下一层）；② unwrap 结果无静态类型，其后 .len() 派发错（归轴 F）。**⇒ 批次 395 追加（同一族的两个新登记 + 一个收尾）**：③ 本行"闭包用 FuncAddr + zeta_call1 回调"那句只覆盖 1 arity ⇒ 派发判据 `arg_ids.len() == 1` 让 0/2/3/4 arity 的"值里取函数再调"全部落回符号路径、把**局部变量名**当函数发出去（`let f = add2; f(3,4)` → `Undefined symbols "_f"`）；四档改前实拍 + 正证据（同一行只差实参个数）+ 修法（`zeta_call0/2/3/4` 逐 arity 跳板、`codegen.rs:1074-1077` 四条 External、`gen.rs:10576-10600` 判据 `<= 4`）在 roadmap 批次 395。**语料定价：237 文件尺子上这一族只有 1 行 1 文件**（`quantum_basic.z:136` 的 `test_fn()`），修后该文件 Undefined 5→4；④ 新登记：`iter` 在**静态已知类型**接收者上有绑定（本行 363-365 那批），在**动态数组**上没有 —— 最小复现 `let a = [1,2,3]; for x in a.iter()` → `nm -u` 出 `_[dynamic]i64__iter`，元组数组 → `_[dynamic](i64, i64)__iter`，`pylib/registry.txt` 无此号；`.z` 语料只有 2 文件写 `.iter()`（minimal_compiler 2 处能链接、quantum_basic 1 处），它是 quantum_basic 现在唯一还能动的那条 Undefined，也是 ③ 那条 0-arity 调用的上游；⑤ 边界：arity ≥ 5 仍落回符号路径（跳板只配到 4），且整族跳板在 JIT 无绑定（`error[E4016]: 'zeta_call0' has no binding in JIT mode`，`pylib/jit_mappings.txt` grep `call` 0 命中 ⇒ 归 #26 那条"JIT 只映射 jit_mappings"）。OPEN 净增 0（三条全部折进本行，不新开登记项）。**行状态从 ✅ 改 🟡**：✅ 说的是那 14 个绑定实现，④⑤ 是本批新登记、未修 **⇒ 批次 396 复测 selfhost 这一行的成员表**：Undefined 明细仍 4 条，但**成员换了一个** —— `_build_ast` 消失（那 91 行回到程序了）、`_into_iter` 出现（新解析出来的函数体引用的）⇒ 本行 selfhost 缺的绑定改为 `as_str / into_iter / is_alphabetic / push`；门禁计数不变（194/194、191/194），明细在 roadmap 批次 396 §一、§五。**⇒ 批次 402 追加 selfhost 在 JIT 侧的第 17 条绑定缺口**：`str_get` 在 `pylib/jit_mappings.txt`（149 行）grep **0 命中**，而 C 侧实现一直都在（`runtime/tokio_runtime_stub.c:2634`，LLVM 声明 `codegen.rs:957`）⇒ 批次 402 把 Str 下标改道 `str_get` 后，`selfhost` 的 JIT 从 rc=139 零输出变成 rc=1 ＋一条具名 `E4016`，未绑定符号集**逐字 diff＝只多 `str_get` 这一条**（16→17），程序仍不打印任何一行。**更正一处口径**：改道 `str_get` 只修 AOT，不修 JIT；`selfhost` 现在 JIT 侧唯一新增的挡路石就是这一条，补它比再猜一条方言便宜（roadmap 批次 402 §五①、§八）**⇒ 批次 403 追加同族的第 18 条：`runtime_malloc` 在 `pylib/jit_mappings.txt` grep 也是 0 命中**，而它是 `MirExpr::Struct` 构造（`codegen.rs:6214` 那条臂、malloc 查表在 `:6222`）在 JIT 侧唯一的石头 ⇒ 任何"结构化值进堆块"的写法在 JIT 都过不了：新用例 `t439_named_field_variant_tag` 在 JIT rc=1、诊断具名 `E4016: 'runtime_malloc' has no binding in JIT mode`、零输出，同一份二进制 AOT 七条期望值全对。jit sweep 计数随之 `total 522 → 523`、`trap 348 → 349`、`ok=174` 未动 ⇒ 这条不是本批引入的缺陷（`runtime_malloc` 早在批次 396 就进了 struct 构造路径），是本批的第一条把它**量出来**的用例；它和 `str_get` 一起构成 #42 现在最便宜的两格绑定表补丁。**⇒ 批次 406 把 #42 整族的定价一次量完，并据此改判（不再是"加表行"的活）**：351 个 trap 文件聚合出 **403 个不同缺失符号 / 2,742 次出现**（取数坑已记 roadmap 批次 406 §六：`error[E4016]` 行带 ANSI 色 ⇒ `grep -F` 永不命中，符号清单要从**纯文本的 `warning[E4016]` 括号列表**取）。首命中榜 `zeta_module_decl 93` / `runtime_malloc 86` / `zeta_dynarray_new 44` / `println_f64 19`；按文件并集 `zeta_env_set 192` / `zeta_module_decl 173` / `zeta_dynarray_new 142`。**可绑性是决定性的**：403 个里只有 `runtime_malloc`（`src/runtime/host.rs:17`、`src/runtime/memory.rs:40`）与 `to_string_str`（`host.rs:386`）有 Rust 侧 `fn` 可指进 `pylib/jit_mappings.txt`；三个最大族全在 C 侧（`zeta_dynarray_new` `runtime/py_additions.c:36`、`zeta_env_get` `:2656`、`zeta_env_set` `:2659`、`zeta_module_decl` `:2668`、`py_math_sqrt` `runtime/tokio_runtime_stub.c:854`）⇒ 加行最多翻 1-2 个文件，大头要的是"JIT 模式下把 C 运行时 build/dlopen 进来"这条**新机制**（批次 315 根因的正面解），而它要动 `py_additions.c` 一侧的构建 ⇒ 转为**待用户裁决**，本行不再作为"下一批默认候选" | 批次 323→396→402→403→406 | M | 🟡
| #38 | match 一类（七条实拍，同一段代码 `gen.rs:10676` 起重构一起收；第 ⑦ 条由批次 375 增）：① 结果槽恒 I64（臂内 return 字符串打印地址）；② 语句位置的 `match` 整条不进 MIR——`gen.rs:1206` 语句分发没有 `AstNode::Match` 臂，落到 `:2895` 空臂，实测 `/tmp/b368t/i.z` 只打 `done`，同一输入 `ZETA_STRICT_PARSE=1` 下无 E1002（不是解析层丢的）；③ 臂体在 if-else 链之外被提前下推（`gen.rs:10687` 的 `arms.iter().rev()` 里调 `lower_expr`）⇒ 每个臂的副作用都跑，实测 `/tmp/b368t/p.z` 打 `f1`、`f2`、`f1` 而返回值 2 正确，表达式位置的 `match` 一直在受影响；④ `println!` 展开成两个节点（`macro_expand.rs:112`-`:126`）而臂体槽只有一个表达式（`ast.rs:18`-`:25`）⇒ 宏进不了臂，未展开的 `MacroCall` 在表达式位置换成 `IntLit(0)`（`gen.rs:3236`），写成块体的臂也不出声（`/tmp/b368t/n.z` 的 MIR 里 `"two"` 根本没出现）；⑤ 臂头 `q @ _`（绑定套通配）匹配判过了但绑定值丢成 0，且不出声——实测 `/tmp/b370ap/r1.z`（`pick(4)`，臂体 `q + 1`）退出码 1、期望 5，同文件只改这一行成 `q => q + 1`（`r2.z`）退出码 5，差在 `@ _` 那一步，jit 与 AOT 同值（`Result: 1`），批次 371 的 or 链收集不碰这条；⑤ 的下推点未定位，本条只有读数；⑥ 臂体写**裸赋值**时整条 `fn` 在解析层就被丢掉——实测 `/tmp/b373/m5.z`（`match ch { ':' => 1usize, _ => i += 1 }`）出 W1002 且丢弃点落在整条 fn 上，同一句包成块（`m9.z`）或挪到 `match` 之后当普通语句（`m12.z`）都不丢，字符臂/字符串臂/臂体里调方法单独测都不丢（`m1`-`m4`、`m7`、`m8`）⇒ 臂体用的是表达式规则、赋值不在其中，与 ④ 的"臂体槽只有一个表达式"是同一处地形的两侧（`tests/unit-tests/selfhost.z:80` 的 `_ => i += 1,` 即此形，该文件因此丢 158 行）。修法共同点：臂体按语句列表下进各自分支（分支本就是语句列表，②③④一起收，顺带才有地方谈①）。批次 369 的试改已撤回，现状源码未动。**⇒ 批次 374：②③⑥ 已完成，①④⑤ 未动**。② 语句位 match：`gen.rs` 委托组加 `AstNode::Match` 臂；③ 臂体下推前后夹 `self.stmts.len()`，这段 `split_off` 接到该臂 `then` 前面；⑥ `expr.rs` 臂体 alt 增 `parse_assign`（`stmt.rs` 的 `parse_assign` 提 `pub(crate)`）＋ `lower_expr` 的 `Assign` 补已绑定槽位的 store、新增 `AssignOp` 表达式臂。三条改前/改后对照（改前用仓内 02:13 之前的 debug 构建）：`/tmp/b374/c4.z`（语句位 match，无 W1002）退出码 0→1、`c2.z`（三臂各 `n+=…`）**111→10**（改前三条臂的副作用全跑，一声不出）、`c1.z`/`c3.z`（臂体裸赋值）W1002+0 → 0 条+1/7、`b3.z`（`_ => (i := i + 1)`）0→1、`/tmp/b368t/p.z` 打 `f1/f2/f1` → 只打 `f2`。`selfhost.z` 丢行 158→91，剩的 91 行换形：`let x = if let … { } else { }`（`/tmp/b374/d1.z` 截断 / `d2.z` 普通 `if` 不截断），不在本条地形内。本批另暴露一条机制并已在本批内回避：赋值表达式的值若取 rhs 的 id，该 id 被消费两次会让求值重跑（`SemiringFold` 每个用处重发一次）——`d = (m := m + 3)` 打 10 而非 7，`t33_starred_walrus` 当场红；改取值槽位后 t33 恢复 `6|3|7`。**发射层"每用一次重发一次"没有动，仍是一条待归属的机制**（折进本行，不新开登记项）。**⇒ 批次 375：① 已完成**：`src/middle/mir/gen.rs` 的 match 降型在建链前开 `arm_value_tys`（`:10721`）、两条臂体下推路径各记一次类型（`:11158` 的 `Return` 臂记返回值、`:11164` 的求值臂记结果 id），链末（`:11200-11209`）改成"每条臂都有类型且彼此相同"才统一用那个类型，否则保留改前的 I64（有 `return` 臂的槽没人写、混型臂没有单一答案，两种都落回旧行为，不猜）。改前/改后（改前＝批次 374 实现后的 release 构建，另用仓内 00:42 的 debug 交叉验过一次）：`/tmp/b375/m1.z`（`let s = match 1 { 1 => "hello", _ => "world" }` 再 `print(s)`）打 `4370672064` → 打 `hello`；`f1.z`（浮点臂 `1 => 1.5, _ => 2.25`）打 `4609434218613702656`（double 位模式被当 i64 读回来）→ 打 `1.500000`；`m3.z` 整数臂退出码 22、`m4.z` 臂内 `return 5` 退出码 5，改前改后一字未动；`m2.z` 混型臂仍打地址（两次跑 `4308838848`/`4329925056` ⇒ 是地址不是值，本批判为无统一答案所以留着）。锁在 `tests/python_style/t416_match_result_slot_type.z`（PASS）。**⑦ 本批新增（构造子模式匹配整条没有语义，是 ①/⑥ 之外的另一处地形的第一手读数）**：`/tmp/b375/k1.z`（`enum T { A(i64), B }`、`t = T::A(7)`、`match t { T::A(v) => v + 1, _ => 100 }`，期望 8）退出码 **1**，`k2.z`（`t = T::B`，期望 100）退出码 **1** ⇒ 第一条臂恒被选中且 `v` 从没绑上；语句位 `if let` 同形（`s1.z` 期望 100 → 退出码 1，`s2.z` 期望 8 → 退出码 **192**），`--dump-mir` 的正证据是 `s1` 只有 3 条 `Assign` + 1 条 `Return`、一个 `If` 都没有（`grep -c` = 0）⇒ then 无条件跑、`else_` 整个丢掉。卡点 `gen.rs:2735` 的 `AstNode::IfLet` 在 `:2770` 走"复杂模式：只求值 expr，总是跑 then"兜底臂；更下面是表示层缺件——带载荷的枚举构造出来是不透明句柄 `zeta_platform_obj`（`runtime/py_additions.c:2790`，存 `[name|a|b|c]`），没有取 tag/取载荷的入口。所以只修解析会把"静默丢代码"换成"静默错值"，本批按 handoff 的排序只登记不半修；批次 374 记的 selfhost 剩 91 行（`let x = if let … { } else { }`）真正的下游就是这一条，**"批次 375 收掉 if let 表达式位"这个候选作废，改判 ⑦ 在前**。**⇒ 批次 378：④ 收三形（单宏臂、块臂、`match` 放在取值位），残余一形未收**。`src/middle/resolver/resolver.rs` 的宏递归补三处：`ExprStmt`（外壳里是宏就展开，`:3550`）、`Match`（逐条臂递归，`:3561`）、`Let`/`Assign`（初值/右值，`:3581`、`:3587`），加 helper `expand_expr_node`（`:3609`：0 节点原样、1 节点拆壳、多节点包成 `AstNode::Block`）；纯插入 61 行（4791 → 4852）。改前/改后六形实拍（全表在 roadmap 批次 378"现象"节）：`/tmp/b377/i.z` 只 `done` → `two`+`done`；`i2.z` 只 `hits=5` → 多打 `two with prefix=2`；`i3.z` 只 `after` → `in-block`+`second`+`after`；`i4.z`（`let` 位）只 `x=7` → 多打 `side`；`/tmp/b378/e2.z`、`i5.z`（`=` 位）同形。**与批次 369 那句"加了 `Match` 展开臂、实测三处输出一字未变"不冲突**：369 的实现只在"展开结果恰好 1 个节点"时替换臂体，而 `println!` 展开是 2 个节点 ⇒ 条件永不成立；本批多节点时包成块，槽的单节点限制由块自己满足。定位手段：临时 `eprintln!` 探针拍到块臂里的语句形是 `ExprStmt(MacroCall(println))`（探针量完已撤），对照组 `/tmp/b378/e1.z`（函数体里的普通块，宏是裸 `MacroCall`）一直能打。④ 的残余一形（未收，卡点在另一层）：`2 => { println!("arm-two") }` 这种"块里只有一条表达式"的臂被读成**集合字面量**——`/tmp/b378/i6.z` AOT 只打 `end`、退出码 0 一声不出，`--dump-mir` 里该臂 `then` 只有 `zeta_dynarray_new` + `vec_push`、整份 MIR 没有打印调用，逗号分隔版 `i6b.z` 读数一字相同，JIT 下出声（E4016 缺 `zeta_dynarray_new`）⇒ 与 369 定位 3 末句（`/tmp/b368t/n.z`）同形，归解析/表达式规则，不在宏递归这一层。读数：门禁 `rc=1`（只由 t231/t233 解释），official compile 194/194、诊断 5 文件/29 行未动，python_style `302 → 303` 通过（多的就是新用例 `t419_match_arm_macros.z`：7 条期望、JIT 与 AOT 同读、含"未命中臂不出声"的正证据），诊断 193 行/82 文件逐字未动，语料 39/39，jit `ok 172 → 173`、`total 502 → 503`（同一条新用例），diff `120/130=92.3%`、`bad_case=0`，锚点 `漂移 58/新 0/消失 1` 与 377 逐字相同且 `resolver.rs` 不在锚点集内（两份日志 `grep -c` 都是 0）⇒ 无需 `--rebind`；丢行尺子 3 文件/533 行一字未动（本批不碰解析，#36 没有可收的格）。裸行号承重：旧 `resolver.rs:3552`（`expand_stmts`，批次 377 记录里的活引用）→ 现 `3598`，旧 `:3544`（兜底臂，368/369 三段历史记录）→ 现 `3590`，两个新号实读核对。**⇒ 批次 379：④ 的残余一形收掉，④ 四条实测形状全部实现**。`2 => { println!("arm-two") }` 这种"块里只有一条宏"的臂（以及同形的 `let x = { println!("…") }`）改前一声不出，卡点不在宏递归而在解析：`src/frontend/parser/expr.rs` 的 `parse_dict_lit` 集合分支（`:975`-`:994`）先按表达式拿 `{` 后面第一项，而 `parse_expr` 能把宏当原子吃下 ⇒ 花括号被读成"一个元素的数组"，排在它后面的 `parse_block` 没机会跑，宏带着 `ArrayLit` 外壳进不了批次 378 那条递归，MIR 在表达式位置留下 `IntLit(0)`（`gen.rs:3242`）。修法是在返回 `ArrayLit` 前否掉"元素里有宏调用"（`:1001`-`:1006`），让 `alt` 落回 `parse_block`；纯插入 13 行（`expr.rs` 3236 → 3249），没动展开器、没动 MIR、没动 C 侧。改前用临时回退到 HEAD 的构建实测（14 个夹具全表在 roadmap 批次 379"现象"节）：`/tmp/b378/i6.z`、`i6b.z`、`/tmp/b379/f1.z`、`f7.z` 只 `end` → `arm-two`／`one` ＋ `end`；`/tmp/b379/h1.z`（`let` 位、不在臂里）只 `done` → `in-block`＋`done`；四种字面量读法逐字未动（臂里字典 `dict-len=2`、臂里集合 `set-len=3`、取值位 `{7}` 仍打 `[7]`、`f8.z` 合跑同读），语句位臂体 `{ total += 10 }` 仍 `total=10`。一条已知代价：`let z = {1, println!("mixed")}` 从静默错值 `z=[1, 0]` 改成整文件丢弃＋`W1002`／`W1004` 出声（`f9.z` 两读都在册；全仓 `--include="*.z"` 的 `{宏` 只有 3 处且全是注释文字，门禁三组读数逐字未动是同一件事的第二证据）。读数：门禁 `rc=1`（仍只由 t231/t233 解释），official `194/194`、`192/194`、诊断 `5 文件/29 行` 未动，python_style `303 → 304`（新用例 `t420_macro_in_single_stmt_brace.z`：6 条期望，AOT 编译干净无警告，JIT 下 `arm-two`／`in-let` 两行同读后停在 `map_str_key` 的 E4016），诊断 `193 行/82 文件` 逐字未动，语料 `39/39`，jit `ok=173`、`total 503 → 504`、`trap 330 → 331`（多的就是 t420 那一条），diff `120/130=92.3%`、`bad_case=0`，锚点 `漂移 58/新 0/消失 1` 与 378 的汇总行逐字相同（两份日志 `diff` 只差 2 行，都是 378 就已漂移的 `expr.rs:2292`/`:2310` 的"现在指向内容"）⇒ 本批未新增漂移；`--rebind --dry` 在 HEAD 上列 53 处搬家（绝大多数是 `gen.rs` 的存量账），不在本批一次抹平，留给锚点专项；丢行尺子 3 文件/533 行一字未动。裸行号承重：`parse_dict_lit`/`parse_block` 旧 `:1592`/`:1593` → 现 `:1605`/`:1606`，`fn slice_sep` 旧 `:2311` → 现 `:2324`（`docs/ABI.md:885`-`:886` 与本行 #39 那句 `expr.rs:2293` 在本批之前就已错指，真实位置 `:2324`/`:2342`）**⇒ 批次 380：⑤ 收尾，并且成因改判**。原句"匹配判过了但绑定值丢成 0"是反的：`q @ _` 这条臂**从没判过匹配**。解析侧没错（`pattern.rs:28`-`:36` 给 `AstNode::Ignore`，`:269`-`:281` 装进 `BindPattern`），丢在 MIR 下型——`gen.rs` 的 `BindPattern` 分支只给"内层是区间"开了特例，其余内层走"把内层当一个值下型、再与被匹配值比 `==`"的回落（现 `:11093`-`:11095`），而 `lower_expr` 对 `Ignore` 给 `IntLit(0)`（现 `:12926`）⇒ 臂条件成 `被匹配值 == 0`；`--emit-llvm` 正证据是 `%eq = icmp eq i64 %8, 0`。看到的 0 是被下型成值的 `_`，不是丢了载荷的绑定。修法：在 `:11055`-`:11071` 加一条带守卫的臂（内层是 `Ignore` ⇒ 名字照样绑到被匹配值的槽，条件直接 `IntLit(1)`，与裸 `_` 臂 `:10778`-`:10782` 同一读法）；纯插入 17 行（`gen.rs` 13881 → 13898），没动解析器、没动 C 侧、没动 `src/lib.rs`。改前用批次 379 的构建实测（`/tmp/b380/pre.log`、`post.log`，14 个夹具全表在 roadmap 批次 380"现象"节）：`pick(4)` 打 `1`→`5`，四个输入 `1 1 1 1`→`1 5 8 -2`，`q @ _ if q > 0` 恒假（两输入都走 `_` 臂）→ 只对正数成立，字符串载荷 `n=<null>`→`n=hello`，登记的 `/tmp/b370ap/r1.z` 退出码 1→5；对照组裸绑定 `q => q + 1`、`q @ 5`、字面量臂抢序改前后一字未变。第二条定位（不在本批修）：**一个臂都不匹配时返回的是没写过的结果槽**——`/tmp/b380/c1.z`（`match x { 1 => 5 }`，`x=2`）连跑 10 次都打 `5`，值稳定但不由源码规定，登记读数的"退出码 1"就是它；判据（编译期要求穷尽 vs 运行期出声）待裁决。读数：门禁 `rc=1`（仍只 t231/t233），official `194/194`、`192/194`、诊断 `5/29` 逐字未动，python_style `304 → 305`（新用例 `t421_bind_wildcard_arm.z`：9 条期望，JIT 与 AOT 同读，`plain=7` 那行是裸绑定对照），ps 诊断 `193/82` 未动，语料 `39/39`，jit `ok 173 → 174`、`total 504 → 505`、`trap 331` 不变（t421 在 JIT 下是过、不是卡），diff `120/130=92.3%`、`bad_case=0`，丢行尺子 `3 文件/533 行` 一字未动；锚点 `漂移 58 → 59/新 0/消失 1`，多出的那一条是本批把 `gen.rs:11973`（`docs/ABI.md:323` 的重载后缀引用）推到 `:11990`，插入点之下另外 6 条 `gen.rs` 锚点在 379 的日志里就已算漂移 ⇒ 未重复计数，不跑 `--rebind`（同 379 的处置）。顺带量到一条不属于 #38 的：`Array<i64>` 参数里 `xs[0]` 在读侧取 0（同一数组在 `main` 里 `a[0]` 是 3），换裸绑定、先 `let` 再 `match` 读数不变 ⇒ 责任不在匹配这一族，读数留在批次 380"边界"第 4 条等并族，本行不另立 | 批次 322 OPEN → 369 增三条实测成员 → 371 并入 #44 为 ⑤ → 373 增 ⑥（解析层：臂体裸赋值）→ 374 收 ②③⑥ → 375 收 ①、增 ⑦ → 378 收 ④ 三形、记残余一形 → 379 收 ④ 残余一形 → 380 收 ⑤（成因改判）→ 382 值位 `if let` 解析层收（字面量/绑定/or/区间实测通；构造子、tuple 由守卫退回，⑦ 未动） | M＋L | 🟡 ①②③⑥ 已修并实拍；④ 四条实测形状全部实现（单宏臂、块臂、取值位 `match`、块里只有一条宏）；⑤ 已修并实拍（臂恒不匹配这一成因已改判）；只剩 ⑦ 未动，且 ⑦ 的卡点已由批次 382 定位到表示层：`codegen.rs:6210-6211` 丢掉 `variant` ⇒ 带数据的 enum 变体没有 tag，模式侧无法判"是哪个变体 / 怎么取载荷"，实拍见 roadmap 批次 382（值位构造子 `match` 静默打 0、语句位 if let 不匹配也进 then、`Option::Some` 臂 rc=139） **⇒ 批次 396 收 ⑦**：带载荷 enum 变体现在有 tag（MIR 侧 `MirExpr::Struct{variant, fields:["__tag", …]}`，`src/middle/mir/gen.rs:3313` 判整个枚举是否块形），**codegen 一行没改**；382 登记的三形里两形（值位构造子 `match` 静默打 0、载荷臂恒匹配 + 绑 0）已 A/B 实拍收掉（`d=1 → d=6`、`some=100 none=100 → some=107 none=500`），剩下两形复测更新：跨类型 `match 5 { Option::Some(n) => … }` 仍 **rc=139**（未变），语句位 `if let Option::Some(n) = 裸 i64` 从「不匹配也进 then」变成**两分支都不进**（rc=0、stdout 空）。未收的那半：**具名字段变体构造**（`Shape::Rect { w: 3, h: 4 }`，语料 20 处 / 2 文件）写侧仍不带标签 ⇒ 恒不匹配（fail closed，不再静默绑 0）。**⇒ 批次 403 收掉这"未收的那半"**：`AstNode::StructLit` 支（现 `gen.rs:12319`）在写 `MirExpr::Struct` 前过一遍新增成员 `with_enum_tag`（定义 `:3320`、调用点 `:12355`），判据与元组形同源（`enum_variant_of` ＋ 载荷数与字段数相符）⇒ 具名字段形现在也写 `__tag`；载荷数为 0、字段数不符、裸名类字面量（`enum_variant_of` 要 `::`）三条都原样过去。实拍（`target/release/` 同目录双值 A/B）：`/tmp/b403/v3.z` `c=99 → c=1`；`v4.z` 逐槽绑定 `edges=-2 → 304`（slot1=`w`/slot2=`h` 的正证据，不是"臂靠常量碰对"）、`area=-1 → 12`、`direct=-1 → 42`，对照组 `pair=30 / pairfield=1020`（裸名类字面量）改前改后一字未动；`/tmp/b403/t5.z`（`Token::Ident(Str)` 元组形）两档都 `k=abc` ⇒ 不属本批。语料侧 `"__tag"` 行 `minimal_compiler 15 → 37`、`selfhost` 两份 `58 → 61`，808 文件 MIR A/B 只有这 3 个文件＋本批用例变（`_ran`=808 为正证据）。**行为面零推进，不冒充**：`minimal_compiler` PRE/POST 都 rc=134、stdout 只差两行堆地址数字，`selfhost` 仍 link-only ⇒ 这条支的写侧现在到位，读侧/运行期仍压在 #45/#47 打印族上。新残口见本行末与任务 #122：乱序具名字段 `Rect { h: 4, w: 3 }` 从"恒不匹配"变成"**落到这一臂但载荷互换**"（`outoforder=-2 → 403`，应 304），卡点是块布局取书写序而读侧取声明序，而 `TypeDecl::Enum` 只存载荷类型串（`gen.rs:90-94`）、字段名在登记前就丢了。锁在 `tests/python_style/t439_named_field_variant_tag.z`（PASS，7 条期望逐行实测）。 |
| #41 | `T::static_method` + codegen.rs:6176 缺表达式条目应报错而非崩 | 批次 323 OPEN | S | 🟡 401（**第一半闭**：`gen.rs:12460-12491` 新臂——限定名在 `func_ret_types`(`:217`，`resolver.rs:3361` 注入) 里就是调用，位置在批次 396 枚举变体臂(`:12434-12458`)之后、平台类兜底臂(`:12497`)之前，`is_upper`(`:12345`) 从此不再决定"是不是调用"。实拍：`minimal_compiler` 官方真运行 rc 139/0 行 → 134/21 行（main 跑完，停在 `_min` 桩的出声 abort）；全语料 806 文件 `--dump-mir` A/B＝**17 个变**（含本批用例），其中 15 个 stdout 逐字相同（两侧都是零输出）、20 处 `zeta_platform_obj` 改成真调用（`selfhost` 13×2 等），compile rc 两侧逐个相同 ⇒ 零新增链接失败；语料面 264 处 `Type::小写方法(` / 50 文件，本批只收 7 文件（其余限定名不在表里：`Box::new` 59、`Vec::new` 32…）。用例 `t437_path_static_call.z`(6 expect 实拍)。**第二半未交**：`gen_expr_safe`(`codegen.rs:5689`) 查不到 id 时出声——用"只含告警"的二进制量到正证（`MIR expression id 1 … never lowered`）与水位（**3,000,044 行 / 19 文件**，三个筛法基准各 ~1,000,00x；带不带第一半计数相同 ⇒ 洪水是存量），直接进门禁会把 official 诊断 2 文件/5 行打成七位数，故降级为已定价登记、`codegen.rs` 逐字节还原。门禁注：官方套件从不运行二进制（`run_all.sh:93-105`），本批新建尺子 `tools/official_run_sweep.sh` 才看见这条崩；jit 步 `segv 0→1`（`selfhost`）归因＝第一半让它真进 `tokenize`，撞上**与本批无关的存量** Str 下标缺陷（1 行探针在 HEAD 二进制同样 rc=139），详见 roadmap 批次 401 §六）**⇒ 批次 402 收 §七③（Str 下标）**：根因不在下标臂序，在**拼写表**——`Str`（Zeta 自己的字符串类型名，`tests/unit-tests/selfhost.z:29` 的 `fn tokenize(input: Str)` 用的就是它）从来没写进 `typecheck_new.rs:98-133` 那张"拼写→Type"表、也没写进 `gen.rs:1142` 的参数臂 ⇒ 形参槽落回 `Named("Str")`（**假类**）：AOT 侧下标一路掉到 `gen.rs:13308` 的 DictGet 兜底、对 `char*` 走哈希表打回 0（`/tmp/b402/p1.z` 实拍 `c=0`、rc=0 一声不出），JIT 侧 rc=139 零输出。两处一行臂**各自承重**（不是冗余）：专构"只补 gen.rs 那侧"的二进制量到"下标已对、三个 str 返回值仍全打地址"（`a=4302360560 b=4302360544 e=4302360432`），两处都补才是 `a=a b=c e=x`。`selfhost` MIR：`DictGet 16→4`、`str_get 0→12`、type_map `1: Named("Str", [])`→`1: Str`；806 文件三值 A/B 只有它两份拷贝变（另含一处未提交的第三表改动，量到贡献 0 行 ⇒ 已还原）。用例 `tests/python_style/t438_str_param_subscript.z`（5 条 expect 逐字实拍；同一份源改前**整文件零输出 rc=139**）。门禁：jit 步 **segv 1→0 转绿**（`run_all.sh:580`），GATE_RC 回到只剩 `:576` 那条存量红；python_style 诊断 95→96 文件 / 200→201 行**是本批新用例自己**那条 `print.value` 保持动态告警（#117 家族，值仍全对）。`String` 拼写故意不收——`build/stubs/std/string.z:4` 真 `pub struct String`，是实名不是别名（`p3.z` 改前改后都 `c=0`，在册）。OPEN 净增 0（更正与残口全部折进 #41/#42/#22/#33 与本行；详见 roadmap 批次 402 §二、§四、§五） |
| #37 | （v1 汇总未见定义体——燃到时回批次 322 记录核对） | 批次 322 OPEN | S | ❓ |

### C. 语义与打印小项批（S 粒度集中清）

| 编号 | 一句话 | 来源 | 粒度 | 状态 |
|---|---|---|---|---|
| #45 | 比较结果该是 Bool 却打 1/0（5 条）——**第一成员已由批次 327 闭**（整数比较折成 Int 而非 Bool 的根因在 CTFE，不在 gen.rs 下型；残留的 min/max 恒 I64 另见 #50）。**⇒ 批次 368 新增同族成员（打印族，比 #36 那条更靠上层）：带占位符的格式化串把字面量整个丢掉**——`println!("A={}", 1)` 打 `1`、`println!("{}B", 2)` 打 `2`、`println!("C={}D={}", 3, 4)` 只打 `3`（前缀、后缀、第二个参数全丢），而 `println!("plain")`、`println!("E=1+1")` 正常。编译成功、退出码 0、无声无息 ⇒ 正是本项目定为最恶劣的那一类（静默错值/错输出）。输入是函数体顶层语句、不含嵌套块，所以与本批改的宏展开递归无关（静态排除）。**门禁零覆盖**：`tests/python_style` 里带占位符的 `println!` 只有一种形状 `println!("{}", …)`（15 处；取证＝把两个套件里所有 `println!("…")` 的串抽出来再看含 `{` 的那些），`tests/unit-tests` 里带字面前缀的 0 处 ⇒ 谁都看不见它。为什么排前：主线批次 301 要靠打印读 `final_value=…`，这条不修，打印出来的每个数都没有标签、多参数的行只剩第一个数。**⇒ 批次 375 增第三成员（同一类、另一侧：格式串的重载选错）**：`println!("{}", <字符串变量>)` 打的是字符串句柄的数值。实测四份（release，本批改动实现后）：`/tmp/b375/v2.z` 打 `4343720384`；`v4.z` 一次给三档——`println!("{}", "lit")` 正常打 `lit`、`let a: str = "annot"` 打 `4301908468`、`fn f(x: str)` 里的形参打 `4301908474`；`v5.z` 同一函数里 `println!("{}", msg)` 打 `4364921296` 而 `println!("{}", n)` 打 `3` 正常；同一个 `msg` 走 `print(msg)` 打 `hi`（`v6.z`）⇒ 只有格式串那条路过载选错：MIR 里调用名是 `println_i64_1`，而单参直调的分派（`src/middle/mir/gen.rs:8156` 一带）会按静态类型选 `println_str`。与第二成员（占位符把字面量丢掉）同路不同病灶。批次 376 候选，夹具已在手（`/tmp/b375/v1`-`v6.z`）。**这条是批次 375 写 t416 时撞出来的**：期望行第一版按 `println!("{}", s)` 下，实测打地址 ⇒ 按实测改成 `print`，没把期望写成我想要的答案。**⇒ 批次 376：第三成员已修**（roadmap 批次 376）。根因不在分派表，在展开期：`src/frontend/macro_expand.rs` 的 `expand_println` 把"单值参且形状是 `Var`/`Lit`/`Call`"的 `println!` 直接展开成对 `println_i64` 的调用（`git log -S` 追到 v0.7.0 的 `13697ac8`，同段注释自陈是占位实现），展开期没有类型信息 ⇒ 变量与调用结果无论什么类型都进整数打印器；有类型信息的那处分派本来就在 `src/middle/mir/gen.rs:8156-8171`（字面量能正常打正是因为它的形状不写死，落到了这里；`AstNode::Lit` 只装整数，`src/frontend/ast.rs:149`，所以整数面量碰巧对）。改法：删掉那条按形状写死名字的叉，单值参统一发 `println`（净 -9 行）。改前/改后（`/tmp/b376/`）：`let t = "direct"` 打 `4331907520` → `direct`；`let a: str = "annot"` `4303383060` → `annot`；str 形参 `4303383066` → `param`；返回 str 的调用 `4307216848` → `from-call`；**`let g = 1.75` 打 `1` → 打 `1.750000`**（这条比地址更像合理值，是本成员最坏的一档）。整数一档（变量 42、面量 7、返回整数的调用 9、`3 * 4` 的 12）、`println!("{}", "lit")`、`println!("{}", 2.5)`、`println!("plain")` 改前改后一字未动；锁在 `tests/python_style/t417_println_format_arg_type.z`（六条 expect 逐字实拍）。**⇒ 批次 376 增第四成员（同族，本批未动，实拍在案）**：`println!("{}", <数组>)`、`println!("{}", <字典>)` 改前改后都打句柄（`/tmp/b376/d1.z`：改前 `4299902832`/`4299906560`，改后 `4311977840`/`4311981568`，值随分配变）——容器 repr 那条分派（`gen.rs:8020-8070` 的 `py_json_dumps_vec_typed`/`py_json_dumps_map`）在格式串参数循环里，而 `Var` 现在落到 `:8156` 的单参分派，那里没有容器档 ⇒ 仍选整数打印器。**本批同时把第一、二成员定位到一句**：`expand_println` 在 `:131` 把格式串整个剥掉、只传值参 ⇒ 字面量前后缀必丢；改后复测 `/tmp/b376/m1.z` 三条读数与批次 368 记的一字相同（`A={}`→`1`、`{}B`→`2`、`C={}D={}` 两值→只打 `1`），要修得让展开期产出"字面量段 + 值段"序列或换成带格式串的运行时打印函数，比一格大，仍未动。**⇒ 批次 376 增第五成员（本批把浮点变量送到 `println_f64`，暴露一条已登记的 JIT 缺口，非新病灶）**：`println!("{}", <浮点变量>)` 在 JIT 模式（无 `-o`）出声 E4016 退出 1，AOT 正常打 `1.750000`。实拍 `/tmp/b376/t417.jit.{out,err}`：stdout 只有 `text`，stderr `error[E4016]: println_f64 has no binding in JIT mode`；门禁读数 jit `ok=172`（未回退）、`trap 328 → 329`、`total 500 → 501`——多的那一条就是新用例 `t417` 自身，既有用例一条 trap 都没多。缺口本身早就写在 `pylib/jit_mappings.txt:66-68`："println_f64 / print_f64 故意不在此列：它们只有 C 侧实现，而仓里没有 build.rs（批次 315 根因），Rust 侧无符号可指"。改前的表现更坏：同一个浮点变量在 JIT 里被打成整数 `1`（静默错值），本批改后变成出声 trap，方向与批次 321 记的"被暴露的缺口"一致；根在批次 315 那层（C 侧符号进不了 JIT），不在本行这一类。**批次 376 收尾：OPEN 净增 0**（第四、第五成员全部折进本行，不新开登记项）。**⇒ 批次 377：第一、第二、第四成员一并收掉**（roadmap 批次 377）。修法：`expand_println` 首参是字符串面量时按 `{}` 切段，字面量段 → `print_str(段)`、值段 → `print(值, end="")`、末尾补 `print_str("\n")`（`macro_expand.rs:106-146`），没新增运行时函数也没改契约——`print` 那条分派早就带"按静态类型选名字 + 容器 repr + bool 打 True/False"，`end=""` 让中间不换行（`gen.rs:8004` 定 `has_end`、`:8013` 的 `is_last = !has_end && …`）。实拍（`/tmp/b377/m1.z` 与 `/tmp/b376/m1.z` 同一份源）：`A={}`→`1` 变成 `A=1`、`{}B`→`2` 变成 `2B`、`C={}D={}` 只打 `1` 变成 `C=1D=2`；`println!("plain")`、`println!("E=1+1")` 一字未动。**第四成员顺带收**：`println!("{}", <数组>)` 打句柄 `4311977840` → 打 `[1, 2, 3]`，`<字典>` `4311981568` → `{"k": 1}`。**另有一条本批实测撞出来的行为改动，按契约改档不是回归**：bool 走格式串此前打 `1`/`0`，现在打 `True`/`False`——这是批次 291 写在 `gen.rs` 注释里的契约（"bool must print Python-style (True/False), not as the i64 fallback"，当时只修了 `print`），套件里 `t410_string_predicates`（`0 5 1 1 0` → `False 5 True True False`）、`t411_parse_unwrap_chars`（`42 0 0 0` → `42 0 False 0`）两条期望行据此改档，旧读数写在文件头里不回改。锁在 `tests/python_style/t418_println_format_segments.z`（七条 expect 逐字实拍）。门禁：official 三项一字相同（194/194、192/194、诊断 5/29）、python_style `302/2/4/0`（多的就是通过数 `t418`，红的仍只有 t231/t233）、语料 `39/39`、diff test `match=120 judged=130 rate=92.3% bad_case=0` 一字未动（bool/容器改档没拉低与 CPython 的一致率）、jit `ok=172` 未回退而 `trap 329 → 330`/`total 501 → 502` 全是新用例 `t418`（串里一条浮点值 → `print_f64`，就是第五成员那条已登记的缺口）、丢行 3 文件/533 行未动。**⇒ 批次 377 增第六成员（同类、另一个宏，本批未动，实拍在册）**：`format!` 是桩——`expand_format` 不看参数、整个宏返回一个写死的字符串面量（源码里是 `"formatted string"`），实测 `let s = format!("value={}", 42); println!("got=[{}]", s)` 打 `got=[0]`（`/tmp/b377/f5.z`），既不是 `"value=42"` 也不是那个写死的串，编译成功、退出码 0、一声不出；修它要"拼出一个新串"的表示（运行时助手或展开成串接链），与本批"发一串打印语句"不是同一件事，另计。**批次 377 收尾：OPEN 净增 0**（三个成员收掉、第六成员折进本行，不新开登记项） **⇒ 批次 396 新成员（第七）**：**非宏 `print` 不做占位符替换** —— `print("a={}\n", a)` 打 `a={}` 然后把值另起一行，`print("b=%d\n", a)` 打 `b=0` 再另起一行打值。A/B 实拍**改前改后逐字同形**（`/tmp/b396/pf.z`、`nf3.z` 在 HEAD 二进制与本批二进制下输出一致）⇒ 与枚举族无关，是 `println!` 宏形（批次 376/377 修的）之外没人管的那条路。**⇒ 批次 398 新成员（第八，本批已收）**：**掩码元素打 `0`/`1` 而不是 `True`/`False`** —— `m = df["code"] >= "c"` 之后 `m[0]` 打 `0`、`m[]={}|{}|{}` 打 `0|1|1`（同目录双二进制实拍，`/tmp/b398/ab`：改前 `0`/`1`/`0|1|1` → 改后 `False`/`True`/`False|True|True`；pandas 3.0.5 参照 `[False True True]`）。病灶不在打印器的重载选择，在**值身上**：元素级比较的目的槽写成 `DynamicArray(I64)`，而这枚标记在本仓兼任"元素类型未知"⇒ 从掩码里取出一个元素只能按 i64 走。批次 398 给掩码补上 Bool 元素（roadmap 批次 398 §二①②③）⇒ 批次 291 那条契约（"bool must print Python-style (True/False), not as the i64 fallback"）第一次在**容器元素**这一档成立。**⇒ 批次 404：第六成员（`format!` 桩）已修**（roadmap 批次 404）。改前实拍（HEAD 二进制跑最终用例）：八行是 `[0]`×5 ＋ `[<null>]` ＋ `[formatted string][formatted string]`，语料里 **29 文件 / 121 处** `format!(`（`grep -rno 'format!(' --include='*.z'`）全打在同一个写死串上、编译成功、退出码 0、一声不出。修法三处同批、**缺一处就有成员落不回**：①`expand_format`（`macro_expand.rs:191-227`）按 `{}` 切段、值段发 `__fmtspec__(v, "")` 复用 f-string 的逐静态类型派发（`gen.rs:7994` ⇒ `py_fmt_{i64,f64,str}`，空 spec 即 `{}`），没新增运行时函数也没改契约；②`resolver.rs` 的宏递归补下进 `Return`(`:3817`)/`UnaryOp`(`:3818`)/`BinaryOp`(`:3822`)/`Call`(`:3827`)，并让宏调用点的**实参先展开**（新成员 `expand_macro_site` `:3852` ＋ `expand_expr_list` `:3858`，两个站点 `:3617`/`:3775`）——`println!("{}", format!(…))` 的内层宏是作为 MacroCall 实参送进去的，只补①时 8 行里 5 行仍落空；③`ImplBlock`(`:3701`) 从「和 StructDef/EnumDef/ConceptDef 一起只 clone ＋ 处理属性」那支拆出，方法体走 `expand_stmts`(`:3721`)、属性语义保持(`:3727`)。锁在 `tests/python_style/t440_format_bang_expansion.z`（十条 expect 逐字实拍）。**同批实测到的三件**：(a) impl 方法体里的宏过去**整块不展开**而方法照跑——`/tmp/b404/ip2.z` PRE `get=5`/`shoutret=7`（返回 7 说明体在跑，`println!` 一声不出）→ POST 多 `shout=5`；`ip3.z` PRE 零输出 rc=0 → POST `[shown=Counter(7)]`/`[t=k=7]`；(b) **语料行为面零推进**：15 个含 `format!` 的文件双二进制 A/B——12 SAME（两侧 stdout 都 0 行）、4 两侧同点位编译失败、`minimal_compiler` 唯一 DIFFER 只差两行堆地址（rc=134 两侧同），根因量到：它那 5 处全是 `push_str(&format!(…))`，而 **`&expr` 作实参本身 rc=139（改前改后同一份崩）、`push_str` 是空操作** ⇒ 卡在 #47/#42 那一族，不在展开期；(c) 840 文件 `--dump-mir` 双值 A/B 变 **7** 个（`examples/selfhost.z`＝`tests/unit-tests/selfhost.z` 8095→8255、`collections_test` 3028→3190、`traits-advanced` 四个 372→676 / 674→978 / 129→205 / 168→244，打点位 `py_fmt*/print_str/print_i64` 计数 0→10/10/2/2），其中唯一有二进制可跑的 `test_trait_object_simple` 退出码 **0→192**（`main() -> String` 的最后一条求值表达式把句柄当退出码 ⇒ 归 #55，不归本行）。**⇒ 批次 404 增第九、第十、第十一成员（同族，本批未动，实拍在册）**：第九＝**带 spec 的占位符不被切段**（`format!("z={:04}", 7)` 打 `z={:04}7`、`{:#x}` 打 `h={:#x}255`、`{:.2}` 打 `p={:.2}1.750000`——整串按字面量段照打、值照转；运行时 `py_fmt_i64` 的 zero/x/o 分支其实支持，缺的是宏侧切段）；第十＝**bool 档缺失**（`format!("{}", true)` 打 `b=1` 而不是 `True`，`__fmtspec__` 只派 f64/str/i64 三档 ⇒ 批次 291 那条 "bool must print Python-style" 契约在这一格仍未成立）；第十一＝三条同族落点：impl 方法**返回** Str 交给打印占位符打句柄（`[impl=4374191776]`，每次跑不同；方法体内自打自印是对的）＋ 非字面量模板仍是那个写死串（`format!(t, 1)` → `formatted string`；语料 0 成员这样写）＋ `x.to_string()` 在非 str 接收者上 rc=139 并伴 `warning: ABI coerce in call to host_str_to_string arg[0]: fptosi → i64`（本批因此没走 `to_string` 派发）。门禁：official 三项一字相同（194/194、191/194、诊断 2/5）、python_style `322 → 323 passed`（多的就是 t440，红的仍只有 t231/t233）、语料 `39/39`、diff `match=120 judged=130 rate=92.3% bad_case=0` 未动、jit `ok=174` 未回退而 `trap 349 → 350`/`total 523 → 524` 全是新用例自己（`Counter { n: n }` → `runtime_malloc`，还是 #42 那张绑定表）、诊断 python_style `96/201 → 97/202`（多的一条是 t440 自己的 `print.value` 动态槽告警，批次 400 的既有口径）。**批次 404 收尾：OPEN 净增 0**（第六成员收掉、第九～第十一折进本行，另两处归属 #55/#47，不新开登记项）。 | 批次 326 OPEN → 368 增第二成员 → 375 增第三成员 → 376 收第三成员、增第四、第五成员 → 377 收第一、第二、第四成员、增第六成员 → 396 增第七成员 → 398 收第八成员（掩码元素） → 404 收第六成员、增第九～第十一成员 | S＋M | 🟡 第一、第二、第三、第四、第六、第八成员已修并实拍；第五成员根在 #26（批次 315）那层；第七成员（非宏 `print` 不做占位符替换，批次 396 A/B 实测与本批无关）未动；第九～第十一成员（spec 占位符不切段、bool 档、impl 返回 Str 打句柄／非字面量模板／`to_string` rc=139）未动
| #65 | LAST_PP 串号 | 340 承接 | S | ⬜ |
| #63 | `--repl` 的 `_dump_mir` | 340 承接 | S | ✅ 349（量完发现不止"参数收下即弃"，而是 **REPL 四条静默**：① `--repl --dump-mir` 一个字节 MIR 都不打（修复前后同输入对照，输出前 60 字节逐字相同）；② `ZETA_DUMP_IR=1 --repl` 同样无声；③ `--repl --emit-llvm` / `--report-stubs` / `--report-untyped` 全部静默忽略；④ **EOF 之后无限打 `> `**（`read_line` 返 0 被 `is_empty` 折成"空行 continue"）——实测 2 分钟写 **173 MB** 才由 kill 停止，即 `--repl` 根本无法被脚本驱动。修法：①②接线（canonical MIR → stdout，与文件模式同口径；IR → stderr），③**出声拒绝** `--repl does not honour …`（标志要么生效要么说话），④ `read_line` 返 0 即 `return Ok(())`；另补 `--repl` 不在 argv[1] 时的诊断（此前它被当成输入文件名）。判据 = `cli_semantics_check.sh` 第六翼 15 条断言（33 → 48），每条 repl 调用都过 `head -c 4000` 保险丝；反证 = 修复前二进制跑新脚本 rc=1 / 8 FAIL 全在该翼。**残留（不在本行）**：REPL 仍没有退出命令，EOF 是唯一出口（Ctrl-D / 管道读完）——`exit` 与 `quit` 都被当**表达式**求值成 `1` 并继续，属于下面 #80 的"未知没有一档"族，不在本行修。 |
| #80 | **"未知"这一档在 CLI/解析层不存在**（三条实测成员，同族不同病灶）：① 参数循环 `src/main.rs:640` 的 `_ => input = Some(args[i].clone())` 把**任何**不认识的字串当输入文件——`zetac --dump-mir2 x.z`、`zetac -x`、`zetac x.z --nopt` 四种写法全部只回一行 `Error: Os { code: 2, kind: NotFound }`（不打**是哪个名字**找不着），且拼错的标志被**静默丢弃**；② `--bootstrap` 在参数循环**之前**就 `return bootstrap_zeta(...)`（`:609-610`），所以 `zetac <某个文件> --bootstrap` **无视**给定的那个文件——实测 `zetac /tmp/tiny.z --bootstrap` 打的仍是 `Lowered 285 functions` 然后堆溢出 rc=134，与不带文件时逐字相同；③ REPL 里 `exit` / `quit` 这类未声明名静默求值成 `1`（同形复现：跨行 `let x = 5` 之后另起一行敲 `x` 也打 `1`，而 `x` 那行本该报"未声明"）。修法方向：参数解析要有"未知标志"分支（带名字出声），`--bootstrap` 要么吃输入要么拒绝，未声明名要落到诊断而不是假值 | 批次 349 盘据（修 #63 时量出来的三条旁支） | ① S / ② S / ③ M | 🟡 ① ✅ 350，②③ 未闭 | §2-A `#63` 转 ✅ ⇒ OPEN 净增 0（本行 +1 / #63 −1）；`--repl` 已经按这个方向做了（不接受的标志出声拒绝），可作为 ① 的形状参考。**① 已闭（350），读数**：真相比登记时更糟——`zetac --dump-mir2 tests/unit-tests/test_minimal.z` 里拼错的标志被**后一个位置参数**盖掉，实测 **rc=0、程序照跑、一声不出**（登记时只量到"无文件 ⇒ 一行 `Os { code: 2 }` 不点名"那一档）。用同一把尺子量"唯一那份 `--help`"宣传的 16 个选项：`-O2 -g -v --verbose --emit-asm --incremental -I -D --lto --pgo --no-color -h --help -W --sanitizer -Wno-unused` ⇒ **16/16 全部静默吞掉**（rc=0，被编译的程序执行了）。修法：解析器加"未知选项"分支（`main.rs:690` 点名、`:695` 多输入点名两个、`:659/:674/:681` 掉尾缺值），`-h/--help` 接到新的 `usage_text()`（`:506`，只列解析器真认的选项），且早于所有提前 return，故 `x.z --help`、`--repl --help` 都只打帮助。判据 = `cli_semantics_check.sh` **第七翼 25 条**（门禁 `cli_semantics.checked` 48 → 73），含一条**漂移闸**（从 `--help` 抓出的每个长选项必须以字面量出现在 `src/main.rs` ⇒ 帮助不能说谎），闸自带前提断言"至少宣传 10 个"；反证 = 修复前二进制跑新脚本 rc=1 / **24 FAIL 全在该翼**，该翼唯一在修复前也绿的正是漂移闸——它的前提断言同时红了，说明"闸空转"这件事被自己抓住了。回归：10 个真标志（`--dump-mir`/`--emit-llvm`/`--report-*`/`--list-stubs`/`--strict-abi`/`--no-link -o`/`--target native`/`--features foo`/`-o`）修复前后输出**逐字节相同**。**新增 ④（记在本行内，不另计 OPEN）**：那份说谎的帮助连同它的解析器 `compiler_config::from_args`（`:107`）+ `validate`（`:300`）是**图内零调用者的死簇**（`codegraph callers from_args` = No callers found），本批只在 `main.rs` 一侧立起真帮助，死簇未删 ⇒ 轴 A 下一块。②③ 未闭。 |
| #67 | `;` 家族三残留（`pass;` / `del x;` / `fn…;`，收敛点已指名）。**⇒ 批次 383 附注（这一族另有两处在诊断侧）**：闭包尾巴的 `});`（16 声）与 `);`（4 声）一直被 W1004 当成"吞词"报，加上单行 `match` 的 `}, _ => 0 }`（3 声）与值位 `if let` 的 `} else { 0 };`（2 声），全语料 26 声里 25 声是这种"括号内尾值 + 闭括号"。本批给判据加了一条排除（头以 `}`/`)`/`]` 开头 ⇒ 不出声，`src/frontend/parser/stmt.rs:724`），实拍 A/B：同一份最小复现改前响 19 声、改后响 0 声而程序输出两版都是 8 ⇒ 排除的是噪声不是值；全语料 `26 → 1`，剩下那 1 声正是 `benchmark_simd_vs_scalar.z:11` 的 `static`（批次 337 真阳性）。**解析器侧一字未动，本行仍 OPEN**；明细在 roadmap 批次 383。**⇒ 批次 384 附注（那一处真阳性收了，判据侧归零）**：`benchmark_simd_vs_scalar.z:11` 的 `static` 由批次 384 补 `parse_static` 闭掉 ⇒ 全语料 W1004 读数 `1 → 0`（549 文件）。本行三个解析器侧成员（`pass;` / `del x;` / `fn…;`）本批一字未动，仍 OPEN；明细在 roadmap 批次 384 | 批次 337 | S | 🟡 |
| #61 | 占位/签名声明 `fn proto();`（可验收盘据已在 339 OPEN 1 量好） | 批次 339 | M | ⬜ |

### D. ABI / G.5d 收尾

| 编号 | 一句话 | 来源 | 粒度 | 状态 |
|---|---|---|---|---|
| G.5d | 六子项：① 强转 6 档补诊断 ② #33(b) 单一真源（⚠️ 改 M7 行为）③ #32 挂死 ④ 假旋钮 ⑤ M1-M7 探针固化 t4xx ⑥ registry 核签名。**⇒ 批次 397 落子项⑥ 的一档**：浮点接收者的方法调用改按 `pylib/registry.txt` 的签名路由（判据六条：接收者 `F64|F32` ∧ 表内命中 ∧ 元数相符 ∧ 参数全 `f64` ∧ 非桩 ∧ `ret∈{f64,i64}`，`gen.rs:10826-10844`），49 个 math 成员中认领 33 个；15 行同族探针 9 行由错转对、语料拼法 `(100 as f64).sqrt()` 100→10.000000。**余项按本批实拍分三档**：整数接收者不路由（判据有意收窄）、仓内 i64 形参仍走 `fptosi`（`zeta_qc_new` 2 行诊断留在 official 集内）、表里无 `abs`/`round` 成员名（`(-4.5).abs()` 仍 4） | 批次 319 OPEN | M×6 | 🟡 |
| #33 | 返回侧混型两步修复（附 B#8）。**⇒ 批次 397 读数（本行状态不变）**：浮点接收者方法的返回槽改取注册表 `ret`（`gen.rs:11050-11058`），未命中时仍走 `func_ret_types`、默认 I64 那一档一字未动 ⇒ 既没收敛两源也没加重，只是多了一档"命中表时短路"；正解仍在类型基础②（跨函数可查的参数/返回类型表。**⇒ 批次 399：类型基础② 已交付（roadmap 批次 399），本行收窄到两形**：`codegen.rs:1400-1409` 的 `infer_fn_return_type` 改为**委托** `Mir::signature_ret_ty`（`mir.rs:45-58`），resolver 侧 `unannotated_return_ty` 补上算术形（`resolver.rs:2832-2845`）与签名表参数覆盖层（`:2985-3008`）⇒ "无声明的那一半"两源合一（全语料 R7 逐文件实拍 5 行 → 1 行，探针合计 16 行由位垃圾转对、0 行由对转错，p4 那条只有整数证据的控制组一字未动）。**本行剩两形**：① **声明与实现不一致**仍按位重读（p1 的 `f`（`-> f64` 却 `return 1`）打 `0.000000`、`g`（`-> i64` 却 `return 2.7`）打 `4613262278296967578`，PRE/POST 同值 ⇒ 覆盖层里真声明赢、本批不碰），这是本行的主体重；② **一元负号**两源仍不同源（MIR `type_map` 有 `4: F64`，而 `infer` 没有 `UnaryOp` 臂），且更深一层：`def neg(x): return -x` 的 `Return{val:4}` 那条 id **没有任何产生语句**（`exprs` 里 `4: Var(4)` 自指、`5: FloatLit(0.0)` 无人引用），实拍 `neg(2.5)`/`neg(4)` 两侧都打 `8`，语料成员 0 ⇒ 新开任务 #115，登记折进本行、不新开 backlog 行。**⇒ 批次 400 读数（本行两形一字未动）**：③ 交付的是**参数侧**的"调用点证据不得单方面钉死"（`resolver.rs:1649-1716`），本行形①（声明与实现不一致）实拍探针 `e1.z` PRE/POST **同崩 rc=139** ⇒ ③ 的判据不覆盖有声明的函数，既未收敛本行也未加重；形②（#115）本批未触碰。批次 400 新登记的两条形都在参数侧 —— **#117**（动态接收槽没有类型标记，`gen.rs:8601-8608` 无 `PyDynamic` 臂 ⇒ 保持动态后 str 侧仍打指针地址；语料 17 处全在内置 `print.value`，`resolver.rs:3936`）与 **#118**（用户写过的 `x: i64` 先被 str 证据覆盖、再被 ③ 退成 `PyDynamic` ⇒ 注解权威丢失，`ann1.z` 值未坏但报告多一行），按 399 的先例折进本行、不新开 backlog 行。**⇒ 批次 407 对本行的两格读数（本行状态不变）**：① 上面 #117 那格（动态接收槽没有标记）没动 —— 全仓仍只有带类型的 `to_string_f64`（`runtime/py_additions.c:46`），`to_string_str`/`to_string_i64`（`src/runtime/host.rs:386`/`:429`）是单向转换，所以"证据齐了但值仍是动态"照旧打地址；② 但本批量出它前面还有**独立的一格**：证据**根本没取到**（`collect_calls` 缺 `FString` 臂），修在 `resolver.rs:1553`、t443 锁，四探针差分见 roadmap 批次 407 §一 的矩阵 —— 两格判据不同，别并成一条；③ 新登记一条 OPEN 残留（p2 族，**另一族**、本批未动）：`def rd(d): return d["k"]` + 非插值调用点 `rd({'k':'v'})`，407 改后仍 `d=4377586386`（地址）—— 它的值类型来自**容器元素**而不是参数证据，落在本行 #33 与批次 299 那条线上④ **⇒ 批次 408 对本行的读数（本行状态不变，折进本行）**：本批修的是**闭包返回侧的登记**（`gen.rs:14380` 的语句列表体拿哑 `IntLit(0)` 的型、提升臂 `gen.rs:2089` 缺 `closure_ret_tys.insert`），与本行"两源并行无检查"不同源、也不收敛本行 —— 它给"返回型从哪来"多加了一张**闭包专有**表，正解仍在 #117 那格（槽上没有标记）。新读到的一格留在参数侧：p14.mir 的闭包块 `type_map: 1: I64` 是形参 `codes`，且嵌套 `def` 根本不在 `resolver.rs:941-971` 注册的 `funcs` 表里（那里只登记顶层项）⇒ "实参证明形参"这条链对嵌套 `def` 整体不通；另读到一条与类型登记无关的 C 侧恒等桩 `runtime/py_additions.c:3422 zeta_dt_date(h){return h;}`（主线 301 的 `[PARITY]` 日期字段）（OPEN 净增 0：四格全部折进本行，不新开登记项） | 批次 317 → 407 → 408 | M | 🟡 |
| #52 | 锚点裸行号归属——335 实现第 4 层后余 93 条待归属。**批次 392 复扫（手动，核对器仍不在门禁里）**：待归属 100 条 / 91 种、漂移 68 / 新 7 / 消失 4；对照 `60eb2caa^` 的隔离 worktree 读 68/7/消失 6 ⇒ 这 68 条漂移是批次 391 之前的存量，不是上两批留下的（我最初归因给批次 391，被这个对照推翻）。另：`docs/ABI.md` 的引用行号要不要跟着 `--rebind` 是独立一步，本批未动该文件。**⇒ 批次 397 复扫**：`gen.rs` +27 行造成漂移 **28 → 40**，`--rebind` 接受 `ABI.md` 10 行 / 17 个数字（全是整段位移）+ `abi_anchors.tsv` 12 行 ⇒ 复核回到 **28**；基线 251 条 / 落单新 11 / 消失 5 / 待归属 100 条 91 种 / 仓外 14 条 / `[拒改]` 33 条 **五项与批次 396 记录逐字相同** ⇒ 本批零新增引用、存量一条未清。**⇒ 批次 398 复扫**：`gen.rs` +138 行造成漂移 **28 → 59**，`--rebind` 接受 `ABI.md` 23 行 / 46 个数字（全是整段位移，如 `:13769-13774→:13907-13912`、`10738-10762→10834-10858`、`8652-8658→8714-8720`）+ `abi_anchors.tsv` 31 行 ⇒ 复核回到 **28**、核对器 rc=0；**上述五项与批次 397 记录再次逐字相同** ⇒ 连续两批零新增引用、存量一条未清 ⇒ 清理仍是本行的活。**⇒ 批次 399 复扫**：`codegen.rs` 净 **−4 行**（`:1400` 那段 11 行换成 7 行委托）把核对器读数从 HEAD 实拍 **28 漂移 / 11 新 / 5 消失** 推到 **99 / 11 / 3**；`--rebind` 接受 `ABI.md` **67 行 / 133 个数字** + `abi_anchors.tsv` **69/69 行（总行数 342 不变）** ⇒ 复核 **30 / 11 / 3**。基线 251 条 / 落单新 11 / 待归属 100 条 91 种 / 仓外 14 条 / `[拒改]` 33 条 **五项与批次 397、398 记录第三次逐字相同** ⇒ 连续三批零新增引用、存量一条未清；漂移 28→30 与消失 5→3 之间那 2 条是同一批锚点换了栏目还是两回事，**本轮没留 HEAD 的消失清单、不做归因**（HEAD 读数靠`git checkout d909abeb -- <三文件>` 后复跑取得，跑完即还原）**⇒ 批次 400 复扫**：`resolver.rs` +115 / `main.rs` +5 把读数从 **30 漂移 / 11 新 / 3 消失** 推到 **37 / 11 / 3**（本批 7 条 = `resolver.rs` 4 + `main.rs` 3）；`--rebind` 接受 `ABI.md` **5 行 / 10 个数字** + `abi_anchors.tsv` **14 行（总行数 342 不变）** ⇒ 复扫回到 **30 / 11 / 3**，且这 30 条按文件分组实拍 `codegen.rs` 22 / `mir/gen.rs` 4 / `runtime/` 4 ⇒ **本批碰过的两个文件一条不剩**（"37 = 存量 30 + 本批 7"的归因由此坐实）。基线 **251** / 落单新 **11** / 待归属 **100 条 91 种** / 仓外 **14** / `[拒改]` **33** **五项与批次 397、398、399 记录第四次逐字相同** ⇒ 连续四批零新增引用、存量一条未清 ⇒ 清理仍是本行的活 | 批次 335 | M | 🟡 |
| #69 | 锚点核对器 `--rebind`（"搬家型漂移"可判定）+ 修 rc=1 被洗成 rc=0 | 批次 341 实现 | S | ✅ 341 |

### E. 已确认关闭（汇总时见明确关闭证据）

✅ #15（313/314）· #26（315，见 A）· #51（334 `//` 判据）· #53（331 负下标）· #54（332 for/range）· #55（333 入口返回）· #64（338 import `::`）· 附 B#4（318 裁决层=不存在，已定位）· 附 B#9（319）· 附 B#6（336，27 假旋钮收进 env_flag）

### F. 状态待核（❓——2026-09-23 已由第二会话完成核查，结论如下）

| 编号 | 核查结论 | 证据 |
|---|---|---|
| #9 | ✅ 合并至 #22 双轨收敛（同根不另立），双轨实现已随批次 309 删除 | roadmap:9783 |
| #22 | ✅ 双轨收敛 = 轴A 批次 309 删 new_resolver 3,115 行，已完成 | roadmap:9476 + 批次 309 |
| #23 | ✅ 转 known-fail 常驻（存量红已在 run.sh known-fail 段立住） | roadmap:11206 |
| #24 | ✅ 弃号（全 roadmap 无定义体，v1 汇总即无来源） | grep 零命中 |
| #27 | ✅ 成对同负载口径 = 批次 313 性能基线工具，后继批次已按口径使用 | roadmap:11015 |
| #32 | ✅ 短 vec 容器头盲区 = BATCH-301 修复（`1..7` 元素列表不再误拒），09-23 已提交 | 提交 6ea8f063 |
| #43 | ✅ 批次 371 闭（or 模式：分支不是数字字面量就整条臂被丢，advanced_patterns_test 62 行清零）。**批次 370 更正本行原句"构造子模式"**：实测 `Some(x @ 1) =>` 单独作臂头能解析、`Color::RGB(r,g,b)` 能解析、`1 \| 2 \| 3` 能解析，而裸标识符 `A \| B` 也丢 ⇒ 与构造子、与 `@` 绑定都无关。卡点一处：`src/frontend/parser/pattern.rs:48` 的 `parse_struct_pattern` 排在 `:52` 的 `parse_or_pattern` 之前，且它在路径后无 `(`/`{` 时兜底返回 `Var`（`:121`）⇒ 首选项吃掉、`\| B =>` 剩在输入里，or 那一臂对非字面量开头的模式不可达。粒度 S。**批次 371 交付**：`parse_pattern` 在 alt 之后就地收 `\|` 链（+14 行、0 删），语义不止解析——`match 3 { 2 \| 3 => 7 }` 与 `match 3 { ONE \| TWO => 7 }`（常量标识符开头）实跑都选中第二个分支（退出码 7）；丢行 4 文件/662 行 → **3 文件/600 行**，official 诊断 6 文件/30 行 → 5 文件/29 行，三组读数未动。本批 +14 行使 `pattern.rs:121` 变为 `:135`、`:201` 变为 `:215`，`docs/ABI.md` 的两处引用与基线 tsv 一起改号（未跑 `--rebind`，那会把别的会话留下的 58 条漂移一起卷进本批） | roadmap 批次 371 + 11422 |
| #49 | 🟡 前半闭（批次 329 差分库采样），后半"一致率进 CI 读数"并入 G.3 | roadmap:10802 |
| 附 B#7 | ✅ 即任务 #32，随之关闭 | roadmap:9795 |
| 附 B#11 | ⬜ 归并入 #42 缺绑定类 | roadmap:10311 |

---

## 3. 处理顺序（2026-09-23 晚调整：B 类提到第一位，用户裁定）

> 调整理由：解析器截断类（#36 一族）是唯一**直接影响编译结果正确性**的债——
> 它在丢用户的代码；其余各族只影响日志好不好看、工具顺不顺手。剩余清理时间全部投给它。

**新的顺序：B 类 → C 类（并批）→ D 类收尾 → 诊断码一行收尾**

1. **第一优先：B 类解析器截断**（#36 余 9 文件 / 1,019 行 + #39/#42/#38/#41/#43）。
   每接一个语法构造就少丢一批用户代码，价值最直接。执行纪律：
   - **#36 缩小到"match 表达式"一族先做**（一次修复即可找回 757 行，性价比批次 322 已算过）；
     其余文件若两天内清不完，按登记规则降级为 known-fail + 显式不做，**不让一个长杆拖住整个清理期**；
   - `static mut`（357 行那块）需要先跑最小语义探针，确认解析之外没有更深的坑再动手。
2. **第二：C 类 S 项并批**——#45/#65/#67 余项/#61 允许**一次提交全部闭合**，只跑快速门禁
   （python_style + 语料，约 1-2 分钟），全量 run_all 留到 B 类每个构造实现时跑。
3. **第三：D 类收尾**（G.5d 六子项 + #33 + #52 余 93 条）。
4. **诊断码一族设停损**：`WW####` 双前缀的一行修法（359 已给出改法）做完即**整族关闭**；
   不再开"取实拍"支线——一个警告编号的实拍验证不值得再花批次。
5. 每族完成跑一次三基线；**表内 OPEN 计数只减不增**。
6. **S/M 项清零即触发退出条件，回主线批次 301**——不等到 5 个工作日上限。

## 4. 新任务登记（只在此追加，遵守登记规则第 2 条）

> 规则 2 的账务：每条新行入表时，下面"配对关闭"一栏必须写明它用哪一行换的。
> 表内 OPEN 计数只减不增（§3 末句）。

| 编号 | 一句话 | 来源 | 粒度 | 状态 | 配对关闭 |
|---|---|---|---|---|---|
| #70/#77 | PY-A 模块搜索的**位置敏感性**一类：① 祖先 6 层基 ⇒ 家目录一个同名 `.z` 压过 `pylib`，判据收紧（裸名不跨祖先、点号名保留）未做；② 相对 `pylib` 基 ⇒ 换 CWD 时库面**消失且一声不出**（343 新量：仓库根 `import pandas` 有 `PY-A: imported module`，`/tmp` 下 rc=0 零输出）**✅ 351 已闭** | 批次 340 OPEN 2（汇总时漏了 ①）＋ 批次 343 新量 ② | M | 🟡 ① 已落"越界出声 W1005"读数半步（343）、判据收紧未做；② ✅ 351 | §2-A 的"干净检出"行转 ✅ 342（342 已交付、表未跟上）⇒ 本行是**补记 lost item**，OPEN 净增 0。② 读数：修前同一个二进制、同一个绝对路径源文件，CWD=仓库根 rc=0 / MIR **1045 行**，CWD=`/tmp` rc=**1** / MIR **158 行** → 链接 `Undefined symbols: _arange`（`pylib/numpy.z` 头部写明 arange/asarray/where/zeros 已从 registry `F` 撤下、只由该库提供）⇒ 不是"少一条告警"而是"少一份实现"。修法没有发明东西：搬仓内先例 `find_runtime_obj`（src/main.rs:440）的形状（cwd 基仍然第一 ⇒ 仓库根 pre/post 逐字相同 **12/12**，其后加"从 `current_exe()` 往上 4 级"的库面基），`OnceLock` 记忆化 ⇒ 每进程至多一条 **W1006**（两个基打架喊一条、一个都没有也喊一条），`resolver.rs:2276-2278` 展开该列表。判据 = `tools/py_module_search_inventory.sh` 新增 **G 翼 9 条**（门禁 `pysrc.checked` **29 → 38**），承重墙是 G3：把编译器拷进一棵没有 `pylib` 的裸树 ⇒ **必须**退回修前的样子（P0 断言要求告警里出现裸树路径）；反证 `ZETAC=/tmp/zetac_pre351` rc=1 / FAIL **6** 且 6 条全在新翼（A/A2/B/C/D/F/E 一条没动）。**本行内登记三条余项**：`build/stubs` 那一档仍是裸相对路径（实测全语料经它解析 **0 次**，故不顺手改）、`[W2002]` 一码两义（resolver.rs:927"多余类型转换" vs main.rs:458"运行时对象两侧都有"，error_codes 只登记前者）、E 段"全语料"口径不实 ⇒ **✅ 352 已闭**（那句 glob 里的 `tests/official/*.z` 在仓内不存在、被 `2>/dev/null` 静默吃掉 ⇒ 三批以来的"全语料"117 个全来自 python_style；352 改成数组逐目录自证非空 + 两个下限，语料 119 个 / 真解析到模块 55 个，反证三档分别喂 stale glob、接错语料、清单全空 ⇒ 红 1/1/3 条且其余 41 条不动，门禁级反证 rc=1） |
| #78 | CLI **只读标志的覆盖面**一类（344 收主路，剩三条旁路）：① `--bootstrap` 连标志都收不到（`main.rs:589-590` 只传 `output`/`target`），其 `else`（`:1127-1130`）无条件 JIT + `main.call()`；② 无输入 fallback 编译并执行 CWD 相对的 `examples/selfhost.z`；③ `--emit-llvm <f> -o g` **忽略** `--emit-llvm`，`g` 是链接好的可执行文件 | 批次 344 盘据 | ① M（挡在自举编译）/ ②③ S | 🟡 ②③ ✅ 348，① 未闭 | ②③ 读数：③ 同夹具 `test_minimal.z` 修复前 `g`=Mach-O **238,440 B** + `g.o` 368 B + IR 只在 stderr ⇒ 修复后 **ASCII text 20,691 B**、无 `.o`、stderr 0 字节；② 修复前"无输入 + 四个只读标志"**五个入口 W1002 签名全 = 1**（都把演示编译了一遍，且 `Zeta self-hosted result:` 那行**从未打出来**——它在 `FunctionNotFound` 之后）⇒ 修复后只读入口签名 = 0、rc=1 + `no input file` 诊断，裸跑签名仍 = 1（阳性对照）。判据 = `tools/cli_semantics_check.sh` 三翼扩到**五翼**（门禁 `cli_semantics.checked` 18 → 33），反证拿修复前二进制跑新脚本 = rc=1 / 7 FAIL 全在新翼。**① 的新事实（本批量）**：`--bootstrap` 与 `--bootstrap -o` 都在 `Lowered 285 functions to MIR` 之后堆溢出 rc=**134**，从未到达那段 `else` ⇒ 既不能证实也不能证伪"它执行"；`ulimit -s unlimited` 在本机不生效（查询仍 8,176 KB），没有绕过栈的测量手段 ⇒ ① 的前置是 285 函数全量 codegen 不炸栈，与 #79 同族，**本行不转 ✅**（OPEN 计数不变，未新增行）。 |
| #79 | **zeta_src/ 全量编译的真面貌**：51 个 `.z` 里 **4 个编译不过**，而原 CI 的 `zeta_src/*.z` glob 只盖住顶层 5 个、步骤名却写 "all" ⇒ 这 4 个从没进过任何门禁。失败形态逐条：`runtime/array.z` = W1002 解析截断（末尾 18 行丢弃，截断类 #36）；`runtime/xai.z` = W1004 字面量成为独立语句（`;` 家类 #67）；`runtime/actor/map.z` 与 `runtime/actor/result.z` = LLVM verifier 返回类型不匹配（`ret i64` vs `ptr`，另加 `host_result_free_1` 调用参数不匹配）——后两者是**新形态**（返回侧混型类 #33 / 附 B#8 的表亲）。判据已完成：`tools/selfhost_compile.sh` 覆盖全 51 个，4 个逐条钉住原因，**新失败即红、钉住项变通过也红**（清单只能缩）；ci.yml 同名步骤改为调它。剩余活 = 把 4 条从钉住清单里逐条打掉 | 批次 347 盘据（全量实测） | M | 🟡 判据已完成，4 条未修 | §2-A `#29` 转 ✅ 347 ⇒ OPEN 净增 0（本行 +1 / #29 −1） |

### 4.1 已登记项的实现进度（只追加，不重开）

- **#40 / §2-A "`.gitignore` 7 个 NUL"**：批次 340 只修了 `tests/`、`pylib/` 的负例，
  `src/`、`tools/`、`docs/examples/` 下的手写源仍被裸 `*.z` 吞（harness 侧记为 #72）。
  这一行原来把两件事混在一起记：**NUL 那半已由批次 345 闭**（见 §2-A 的 ✅），剩下的只是
  #40/① 的收窄，与本表 §2-A "根"无关了 —— 下次燃 #72 时按"裸规则判据"单件事做。
  345 同时给出该活的两个前置读数：规则表现在可 review（`2 0` + 正文可读，实测于提交后），
  以及"防回潮断言"该跟它一起做（345 边界 2 说明了为什么单独加一步不划算）。
- **#40 已闭（批次 346）**：上面那条"剩下的只是 #40/①"由 346 交付，且**剩下的比预想的多**——
  10 条裸规则一起收窄、290→28，防回潮断言按 345 的承诺搭同一批的门禁行号成本一起付掉
  （第 13 步 19 断言 + 4 个反证，锚点 `--rebind` 搬家 3 条）。本表 §2-A 的 #40 行转 ✅，
  OPEN 计数 −1；本批新量出的两个残差（根级已提交 `.o` 的 `git rm --cached`、`zeta_src/` 是否
  进语料）折在该行 ✅ 说明里，不新开登记行（登记规则第 2 条）。
- **#52 / §2-D "锚点裸行号归属"**：批次 343 又撞出**同族的新形态**——`roadmap.md` 正文里
  的"行号 + 同行反引号片段"引用完全不进核对器（它只扫 `docs/ABI.md`）。实测该形态在
  roadmap 里有 **16 处**：8 处引用带文件名、8 处是裸 `:NNN`（无文件名 ⇒ 核对器连"比哪个
  文件"都不知道）。带文件名的 8 处逐条对片段：**1 处命中**（`src/frontend/parser/expr.rs:364`）、
  **1 处片段唯一但行号错**（`runtime/tokio_runtime_stub.c:241` vs 实际 374）、
  **1 处片段在目标文件里一个字都找不到**（`src/middle/mir/gen.rs:8514`）、
  余 5 处片段是符号名且在文件内多次出现（`MirStmt::Call` / `emit_norm_index` /
  `PY_ENTRY_ATTR` / `parse_expr_stmt` / `parse_block_body`）⇒ 只能判"邻近"，不能判对错。
  ⇒ 归到 #52 名下做，不另立行。
- **#52 / 同族的第三种形态（批次 353 已闭一半）**：锚点是"引用指向行"，本条是"引用复述
  被引用者的内容"——`tools/run_all.sh` 的注释里写着各判据"有几翼"，而权威清单在判据脚本
  自己的文件头。实测 **10 处**（`:253 :277 :301 :391 :414 :440 :557 :559-560 :562 :564`），
  其中**最坏的一处（`:414` 对 `cli_semantics`）至今无从核对**：注释写"七翼"，脚本自己只标
  A~D 四条加四段无标号小节，两边数法不同 ⇒ 复述本身就是缺陷，不是"复述错了才修"。
  修法 = 10 处全删（改成"清单见其文件头"）+ 门禁新增第 15
  步 `comment_drift` 自查（本文件出现"<中文数字>翼"即红，**没有** `--skip` 开关：它扫的是
  本文件的注释，跳过了就等于没有）。判据自己先撞一个坑：第一版 `grep -E` 在 `LC_ALL=C`
  下对**已经干净**的文件报 1 条假阳（:415，全角分号 `；`= `EF BC 9B`，尾字节 `0x9B` 落在
  `四`/`九` 的字节集合里），换 UTF-8 locale 又报 0 ⇒ 改用内联 python3（"翼"字在正则里以
  码点转义给出，规则的源码不会自匹配），两个 locale 同测均 0——这条是判据能进门禁的前提。
  读数：`run_all.sh` **574 → 607 行**（`git diff --numstat` +44/-11；净 +33 全来自第 15
  步，10 处改写本身净 0 行）⇒ 锚点漂移 1 条，`--rebind` 搬家 1
  （`run_all.sh:543 → :574`）/ 拒改 0，复核 rc=0（243 可解析 / 0 定位失败 / 漂移 0 / 新 0 /
  消失 0）；门禁重跑 rc=1（唯一红源仍是 `py_fail != 0` 那 2 项）且**其余 14 步与 352 日志
  逐字节相同**；N1 门禁级反证 = 在 :485 插一条故意复述、其余 14 步全 `--skip-*` ⇒ rc=**1**
  + `FAIL :485`，恢复后 `cmp` 逐字节一致、阳性对照同参数 rc=0；`mbvar_lint` 19/0。
  **本行余项**（不新开登记行，登记规则第 2 条）：第 15 步只钉"复述条数"这一种形状，同族还有
  "各判据自报断言数 vs 门禁实测 `checked`"两处各说各话（S），留作下一批候选。
  **⇒ 该项已由批次 355 判负并关闭**（前提不成立）：`tools/*.sh` 注释里 46 处含数字的行，绝大多数是
  "批次 NNN 实测"型冻结读数或翼标签；只有 2 处复述活物 —— `dc_audit.sh:25`"101 条命中 / 44 个文件"
  实测 `tools/baselines/dc_default.txt` 非空行 = 101、去重文件数 = 44 ⇒ 今天为真；`knob_probe.sh:5`
  "27 处旋钮读法"是 Rust 侧**读法站点**数（`run_all.sh:231` 同写"27 个"），与门禁 knob `checked=23`
  （断言条数）不同量纲 ⇒ **没有一条自报数此刻在说谎**，为它立判据是拿两个量纲硬凑一个等号。
  唯一残留是一处 S 档改写（把 `dc_audit.sh:25` 的复述改成指向基线自身），已挂进 355 的下一批候选 2。
- **#52 / 同族的第四种形态（批次 354 已闭）**：锚点**指向**行，可第 3 层只认一种"对不上"——
  内容变了（搬家）。另一种是人已把文档改对、基线还是旧号 ⇒ 报 `[消失]` + `[新锚点]` 两条互不
  相干项，而 `--rebind` 对"消失"一律拒改。历史代价实测：批次 337 新 3/消失 3、338 新 4/消失 4
  （收尾动作 = `--bless`，即重采**全部 243 条**：为一条对上把另外 242 条的核对结果一并抹掉）、
  344 新 1/消失 1 ⇒"只能手工换 tsv 那一行"（roadmap:12859）。修法 = 新增 `pair_renumber()`
  （**同文件 + 内容逐字相同 + 两边都唯一**才配）+ `--rebind` 对配对项**只刷基线不动文档** +
  **落单的新锚点不写进基线**（新增 `[不纳新]`，rc 条件扩为 `refused or unmatched_added or
  problems2`）。读数：`tools/check_abi_anchors.py` **+105/-24、546 → 627 行**；文档与基线一行未动，
  真仓库核对 rc=0（243 条 / 漂移 0 / 新 0 / 消失 0 / 改号配对 0 对，待归属 93 条 84 种）。
  反证台 `/tmp/b354_exp2.sh`、`/tmp/b354_exp3.sh`（夹具一律打在 /tmp 副本上，仓库文件只读；每个夹具
  先自证"片段在文档里恰好命中 1 次"）：P0 现状 rc=0；E1 真改号 `pattern.rs:46 → :291` ⇒ `[改号]` +
  rc=1 → `--rebind` rc=0、**文档 md5 未变**、基线恰好只换那一行 → 复核 rc=0；E2 `:46 → :47`（内容不同）
  ⇒ 配对 0 对、落单新 1/消失 1、rebind rc=1、基线**一字节未写**、复核仍 rc=1；E3 一对多（`minimal_compiler.z:129`
  换成 `:156`+`:215`，三处内容逐字相同）⇒ 配对 0 对、落单新 2/消失 1；E4 真删除 ⇒ 落单消失 1、rebind 拒收；
  E5 搬家零回归（**只改基线副本**的存量内容）⇒ 判漂移 1 → 搬家 1 → 文档被改成 `:47` → 复核 rc=0。
  门禁重跑 `/tmp/b354_gate.log` rc=1（唯一红源仍是 `py_fail=2`：t231/t233），与 353 日志 diff **只有 4 行**
  （`clean_checkout` 的 rev/secs、`ts`、JSON 同那两项），15 个步骤读数一字未变；`mbvar_lint` 19/0。
  **本行余项**（不新开登记行，登记规则第 2 条）：① `--bless` 仍是全量重采，"只刷这一条"的档位没做（S，与本批
  同源同判据）；② 353 候选 1 的后半（roadmap 正文 16 处裸行号进尺子，其中 8 处连文件名都没有）未动 ——
  那是第 4 层"可归属"的射程，与本批第 3 层的配对互不依赖，另开一批。
  自伤 2 处：第一版台子 heredoc 少传一个参数 ⇒ 夹具**根本没建成**，E1/E2/E4 那三个 rc=0 全是空跑假绿
  （整批作废、重写台子、每个夹具加一行自证）；夹具最早挂 `codegen.rs:6211`，该数字在文档里有**两处**引用
  （`:295` 全路径 + `:136` 裸续引），改掉一处并不等于删掉那个键 ⇒ "改文档"类夹具的通用前提：
  **一个键只能有一处引用**。
  **⇒ 本行余项① 已由批次 355 闭**：`--bless` 在 HEAD 里位于"算出问题"**之前**
  （bless 分支 `:549-560` 直接 `open("w")` 重写整份 327 行基线，基线装载与 `pair_renumber` 在 `:569`
  之后）⇒ 全量重采时"此刻有哪些引用被判为假"根本没算过。实测洗白可复现：副本上装一条改号 + 一条真
  漂移 → 复核 rc=1 报 2 项 → `--bless` rc=0 → **再复核 rc=0 且打印"锚点全部对上"**，那条假引用从此带
  着工具自己盖章的证据。修法 = ① 装载/配对计算上移 + `washed = 漂移 + 落单消失` 非空且无 `--force`
  逐条 `[会被抹平] …` + `[E1005]` rc=2 **一字节不写**（`--force` 仍覆盖但打印覆盖清单；纯新增不在射程内，
  它不覆盖任何东西）；② 新增 `--bless-only 路径:行号[,…]` 只刷点名键、其余逐字不动，格式错 `[E1003]`／
  点名不中 `[E1004] 整次拒收（不部分生效）`／无基线 `[E1001] 不能凭空建表`，新键**按 (路径,行号) 插进
  应有位置**；③ 抽 `load_base()`/`write_base()`（后者先在内存拼完整份再落盘：要么全写要么不写）；
  ④ 三条开关组合守卫复用 `[E1002]`（`--force` 单挂、`--bless-only` 与 `--bless`/`--rebind` 同用、
  `--dry` 单挂）——"参数收下即弃"是 #63 那一类缺陷。读数：`tools/check_abi_anchors.py` **+146/-24、
  627 → 749 行**；文档与基线一行未动（`git status --short -- docs/ABI.md tools/baselines/` 为空），
  真仓库复核对 rc=0（243 可解析 / 漂移 0 / 新 0 / 消失 0 / 待归属 93 条 84 种）；`grep -c
  check_abi_anchors tools/run_all.sh` = **0** ⇒ 门禁不引用本批改的文件，"门禁读数不变"是无从改变而非
  碰巧。反证台 `/tmp/b355_exp2.sh`、`/tmp/b355_exp3.sh`（夹具一律打在 /tmp 副本，崩过一次后**整台
  `rm -rf` 重跑**而非补跑）：P0 现状 rc=0；H4 老流程零伤害（干净副本全量 `--bless` 产物与入库基线
  **逐字节相同**，`cmp` ✅）；H1 装两问题 → `--bless` rc=2 点名 `[会被抹平] expr.rs:364`、基线一字节
  未写、复核仍 rc=1 两项原样在；H2 `--bless-only …expr.rs:364` rc=0、基线 `diff` 只 `151c151` 一处、
  复核 **rc=1 仍报那条改号**（点一条不抹另一条）；H3 `--bless --force` rc=0 + 覆盖清单 1 条 → 复核
  rc=0（覆盖真发生的正证据）；H5/H8 点名不中（1 中 1 不中、点旧号）各 rc=2 + `[E1004]`、基线未写；
  H6 三档开关组合各 rc=2；H7 纳新改号后的新号 rc=0、基线 327→328 行、**升序性保持 True**、复核改报
  `[消失] :46`。门禁重跑 `/tmp/b355_gate.log` rc=1（唯一红源仍是 `py_fail=2`：t231/t233），与 354 日志
  diff **14 行 = 3 项**（`ts`、`clean_checkout` 的 secs/rev），15 步读数一字未变；`mbvar_lint` 19/0、
  `py_compile` 通过。自伤 2 处：`bless_only()` 把 `dict` 当 `items` 传 ⇒ `ValueError` 落在 `open("w")`
  之后，基线被截成 **0 行**（空基线会让 243 条**全被判成没人核过的新锚点**，比写坏一条糟得多）；
  `--bless-only` 第一版把新键 append 到文件尾 ⇒ 锚点行升序在第 **158** 行断掉，下次全量重采又排回去
  （一行真改动穿一身假 diff）—— 两处都是"防各说各话"的新代码自己开始各说各话，由 H7 这档**为本批
  补加的**判据抓到。**本行余项**（不新开登记行，登记规则第 2 条）：① `--bless-only` 不碰待归属（93 条 / 84 种）
  ⇒ 基线里"锚点行"与"待归属行"由两条路径维护；② 点名纳新旧号会留一条落单消失，"一条改号"的完整机械
  收尾仍是 `--rebind`，`--bless-only` 的定位是"只刷存量"；③ 354 余项②（roadmap 正文 16 处裸行号进尺子）
  —— **该项已于 2026-09-23 实测重形（读数见下，判据仍未建）**：把现有核对器直接打在 `roadmap.md` 上
  （`--doc roadmap.md --bless` 到 /tmp 临时基线，仓库基线未动）得 **383 个可解析 + 611 条待归属（428 种）
  + 41 个定位失败、rc=2**。41 里三类噪声：**无解 18**（`market_data.py`/`jq_wufu_local.py` 等聚宽侧仓外
  文件，从来不在本仓 ⇒ 不是腐烂是射程外）、**歧义 6**（裸名 `resolver.rs`/`mod.rs`/`README.md` 多解）、
  **"该行内容为空" 15**（`tools/run_all.sh:484`、`:387`、`src/main.rs:535` 等 —— 写下时指真行，文件
  搬家后落到空行 ⇒ 这就是历史引用腐烂的实测比率 15/383 = **3.9%**）。最要紧的一条：**越界 2 处全是假阳，
  而且是我自己那段"讲裸行号"的文字造成的** —— `roadmap:14024` 一句里同时出现 `pattern.rs:46`、`:295`、
  `:136`、`:855`、`:579`，核对器把 `:855`/`:579` 归给 `pattern.rs`（300 行）判越界 ⇒ **用裸行号讨论裸行号
  的文本会让这把尺子把自己读成缺陷**。⇒ 候选从"M：把 16 处裸行号纳入尺子"改写成"M：先给 roadmap 侧建
  **只出读数、不出门禁红**的核对（须先有三类噪声的豁免判法：仓外清单 / 裸名歧义 / 自匹配），因为历史
  引用按登记规则不回改，任何"红"都永远关不掉。

- **诊断码一类（`[W2002]` 一码两义）盘据已于 2026-09-23 做完，留给下一批直接实现**
  （不新开登记项：这是 #70/#77 行内余项"`[W2002]` 一码两义"的实测展开，OPEN 净 0）。
  扫 `src/**/*.rs` 的**非注释行** ⇒ **发射 15 种码 / 登记 19 种**，三份账：
  ① **空号 9 个**（`error_codes.rs` 登记了、仓内无发射点：`W1001 UNUSED_IMPORT`、`W3002 UNUSED_PARAMETER`、
  `W3003 DEAD_CODE`、`W3004 UNREACHABLE_PATTERN`、`W4001 INEFFICIENT_CODE_PATTERN`、`W8001`、`W8002`、
  `W9001`、`W9002`）⇒ 注册表 **47% 是空头**；② **未登记 5 个**（`W0001`/`W0002`/`W0003`/`W2003`/`W6001`）；
  ③ **名实错位 3 个**：`W2001` 登记 `TYPE_COULD_BE_INFERRED`，唯一发射点 `src/middle/mir/gen.rs:13390`
  打的是"closure symbol minted by two scopes"；`W5001`/`W5002` 登记"除零 / 下标越界"，两处发射点
  （`src/frontend/macro_expand.rs:697`、`src/frontend/macro_expand_advanced.rs:364`）都是 `Unknown attribute`。
  真撞名 2 处：**`W2002`** = `resolver.rs:927`（登记的那个）+ `main.rs:458`（运行时对象 cwd 与
  `ZETA_RUNTIME_DIR` 两侧都有）；**`W2003`** = `resolver.rs:1148`"Failed to parse macro" + `tools/run_all.sh:65`
  的"`.o` 比 `runtime/*.c` 陈旧"，而 `refactor.md:450`（用户文档、禁改）把 `W2003` 记为**后者** ⇒ 权威文档
  与两侧实现三方各说各话。`W1006` 的两站点（`pylib.rs:421/431`）是**同一判据的两个分支**，是我那条
  "消息前缀不同即多义"启发式的**假阳**，不立项；`W0003` 有 3 站点但换行写法让消息抽成空串 ⇒ **未判**。
  断言面（决定动不动得）：`W1002` **56 处**（含 `tests/unit-tests/*.z` 的期望输出与 `docs/ABI.md`）、
  `W1006` 18、`W1005` 16、`W1004` 13 ⇒ 动不得；`W2001`/`W2002`/`W5002`/`W6001`/`W0001`/`W0002` **0 处**、
  `W2003` 仅 `run_all.sh` 自己 1 处、`W5001` 仅 `docs/ABI.md` 1 处 ⇒ 可动。
  由此定下的最小安全改法：`main.rs:458` 让号 **`W1007`**（与 `W1006` 同类 —— 都是"从 `current_exe()`
  往上找资源"）+ `resolver.rs:1148` 让号 **`W0004`**（紧邻 `main.rs:758` 那条外层包装 `W0002`）；两处都是
  **等长替换** ⇒ 行号不漂、锚点基线不动（改后仍要跑复核对自证）。**判据不同批上**：门禁若查"registry
  无空号 + 每个发射码都登记"，光 9 个空号就立刻红 9 条，而删空号要先量 `error_codes::常量名` 的引用面
  （本轮只按**码字面量**扫，未扫常量名 ⇒ 现在删常量可能砍掉活引用）⇒ 拆成第二批：常量引用面清点 +
  删死常量 + 一码一义判据进门禁。
  **⇒ 让号方向已于同轮更正（这一条把上面两处计划里的"哪一侧迁号"改掉）**：不该让 `main.rs:458` 迁号 ——
  `refactor.md:447`（用户文档、禁改、且是本目标的规格来源）写的正是"`W2002` = 运行时对象 cwd 副本压过
  `ZETA_RUNTIME_DIR` 时告警"，而 `error_codes.rs:2181` 写的是 `REDUNDANT_TYPE_CAST` ⇒ **两个权威对同一个号
  的解释相反，这本身就是缺陷**，再挑一侧迁号会让另一侧的权威变谎。正解 = 让**注册表那一侧**迁到空号
  `W2004`（`resolver.rs:927` + 该常量），`W2002` 留给 `main.rs` 并把 `refactor.md` 的解释登记进去；
  `W2003` 一侧同理保留 shell 的 `run_all.sh:65`（`refactor.md:450` 记的就是它），只让 `resolver.rs:1148` →
  `W0004`。复扫断言面：`grep -rn "W2002\|W2003" tests/ tools/baselines/` = **0 命中**；锚点面
  `src/main.rs` 5 条 / `resolver.rs` 4 条 / `error_codes.rs` 0 条 ⇒ 迁移处一律**等长替换**，新增常量只在
  文件尾部（改后仍跑复核对自证 0 漂移）。
  **⇒ 本行两处迁号已由批次 356 实现（方向同上，但注册表那一侧换了治法，理由如下）**：
  `resolver.rs:927` `W2002` → **`W1007`**（不是 `W2004`：`W1xxx` 这一族本来就是"resolver / pylib 里以字面量
  发射的读取性告警"，`W1005`/`W1006` 都住在里面，而 `W2xxx` 是类型告警类 ⇒ 同族优先于"随便找个空号"），
  `resolver.rs:1148` `W2003` → **`W0004`**（`W000x` = 管线阶段非致命告警，`main.rs:745/758/784` 同住）。
  `REDUNDANT_TYPE_CAST` **不迁号、直接退役**：引用面实测 = 全仓 `grep -rn REDUNDANT_TYPE_CAST` 只有它自己的
  定义行 1 处（`tests/ tools/ docs/` 零命中）⇒ 给它一个新号等于往"登记 21 / 发射 16"的差额里再塞一个**永远
  没有发射点的号**，与文件里既立的规矩（`error_codes.rs:2164-2166` "this list follows the emitter"）相反；
  正解是拿它腾出的 `W2002` 槽登记 `main.rs:458` 那条的真实含义（`RUNTIME_OBJECT_CWD_SHADOW`），并把
  `refactor.md:447` 的解释写进注释。⇒ 同一个号的两个说法从此只剩一个，且那个说法两份权威（代码 / 用户文档）
  一致。实测（同一把尺子、同 3 文件范围）：登记 W 常量 **19 → 21**，发射种类 **7 → 8**（`W2003` 出列，
  `W0004`/`W1007` 入列），**一码多义 1 处（`W2002@2`）→ 0 处**；`resolver.rs` 的 `git diff --numstat`
  = 2/2 ⇒ 行数不变，锚点复核对 **rc=0 / 243 可解析 / 漂移 0 / 新 0 / 消失 0**，且改到的两行（927/1148）
  与 `main.rs:458` 都**不在 243 条锚点之内**（`grep -c -E "resolver\.rs:(92[0-9]|114[0-9])"` = 0、
  `main.rs:45[0-9]` = 0）。**余项（本行没做完的，另立批次）**：① `W0001`~`W0003`、`W2001`、`W3001`~`W3004`
  等仍有"登记未发射 / 发射未登记"的差额 21 vs 16；② `W0003` 自己现在有 **3 个发射点、3 种消息**
  （`main.rs:784` typecheck 失败 / `typecheck.rs:57` fallback / `typecheck.rs:528`）⇒ 下一个一码多义就是它；
  ③ 一码一义 + 无空号判据进门禁仍未做，做之前要先把 `error_codes::常量名` 的引用面扫一遍（本轮只扫了
  这一个常量名）。
  **⇒ 余项④（批次 359 量到，属本行同一族，不新开登记项）**：`diag_warning!` 宏在 `src/lib.rs:807` 自己补一个 `W`（`format!("W{}", $code)`），而 8 个站点**全部**传 `"W####"` 字面量（`main.rs:745/758`、`ctfe/evaluator.rs:115`、`new_resolver.rs:880`、`resolver.rs:926/1148`、`macro_expand.rs:697`、`macro_expand_advanced.rs:364`），`diagnostics.rs:246-253` 确实把 code 印成 `[{}]` ⇒ 这 8 条告警真发出来挂的是 `WW####`，与注册表 21 条登记号**在字符串层面永不相交**；对照组 `main.rs:784` 走 `diagnostic_from_code("W0003",…)` 不补前缀 ⇒ 同一次编译两套拼码规则。`grep -c 'WW[0-9]\{4\}' /tmp/b356_gate.log` = 0 ⇒ 门禁看不见它（这 8 臂在 194 用例语料里一条都没触发；实拍连续 356/357/360 三批落空，原因＝8 条臂全要上游 callee 先返回 `Err`，见 roadmap 批次 360）。**最小修法一行**：`lib.rs:807` 不补前缀。**卡在归属**：`src/lib.rs` 在共享工作树的不进提交清单上（#80 ④ 同卡 `:52`）⇒ 待用户裁决，不自行改。另：同批复现第 5 个名实错位 —— `main.rs:780-787` 把写着 `non-fatal`、也真的继续编译的告警按 **`error`** 严重度打印（`c1.z`/`a1.z` 两条输入各复现一次）。判据批上门禁时要一并加"码字面量必须与注册表逐字相等"（今天上必红 8 处 ⇒ 先修再上）。
  **⇒ 余项④ 已由批次 361 闭（2026-09-23 用户授权动 `src/lib.rs`）**：一行改法实现为**两行** —— `lib.rs:789`/`:807` 都改成原样透传 `Some(($code).to_string())`，因为普查发现 `diag_error!` **也是同款缺陷**：11 站点 11/11 首参已带 `E` ⇒ 合计 **19 个发射点无一例外**在发 `EE####`/`WW####`；19 个调用点一行未改即全部与注册表同形（未绕行改站点）。旧写法与新写法的**机制对照已 `rustc` 实跑**：`old=WW0001 / new=W0001`、`oldE=EE4001 / newE=E4001`（`/tmp/b361/ctrl.rs`）。验收：`cargo build` 通过、锚点复核对 rc=0/漂移 0（`lib.rs` 基线内 0 条）、门禁 15 步读数逐字未变、mbvar 19/0。**仍欠的另立一档，不算本行闭环**：这 19 站点在 194 用例语料里一条都没触发 ⇒ 实拍 `WW####` 连续四批落空，本批改的是"一触发必错"的性质而非当前可见行为（见 roadmap 批次 361 反证翼）。**新增余项⑤（同一行，不新开登记项）**：`src/frontend/borrow_enhanced.rs` **从未接线** —— `codegraph callers "EnhancedBorrowChecker::check"` = No callers found，全仓 `grep borrow_enhanced` 除自身外只有 `src/frontend/mod.rs:5` 的 `pub mod` ⇒ E 侧 11 站点里 10 个住在不调用的模块里（轴 A 家族，与 #22 同族）；连带 E 侧 2 组名实错位（`error_codes.rs:2147 MISSING_FUNCTION="E4001"` vs `borrow_enhanced.rs:250` 借用错误消息、`:2148 E4002` 同理）**待"删还是接"裁决**，先裁决再谈登记。判据批（逐字相等 + 一码一义 + 无空号进门禁）仍排在其后。**新增余项⑥（同一行，不新开登记项；批次 365 结构复核根因）**：上面「实拍连续落空」有一个比"上游拦截"更底层的解释 —— **`emit()` 的读取端在整个仓里不存在**。`grep -rn TL_REPORTER src/` 只有 2 个命中：`diagnostics.rs:501` 声明、`:507` 唯一写点 `r.borrow_mut().report(...)`，**没有任何 `TL_REPORTER.with(|r| r.borrow()…)` 的读方**；`format_all()`/`take_diagnostics()`（定义在 `:408`/`:446`）的 9 个调用点全在 `lib.rs:837/864/875/888/901/912/937/953`，且作用域是 `lib.rs:830` 自建的**局部** `let mut reporter = DiagnosticReporter::new()`，与 thread-local 不是同一个对象。⇒ 37 个发射点（19 宏 + 18 直接 `emit()`，含 typecheck.rs 的 16 处）流入结构性黑洞；`report()`（`:358`）只做 `push` 到 `self.diagnostics`，不落 stderr。修法是一行级：在 `main.rs` 退出前读一次 thread-local 并 `format_all()` 输出（或让 `emit()` 直接出声），**先接读取端再谈判据批**，否则判据批无可读的回。余下未接线族的整体清点与陪跑占比见 `docs/module-class-analysis.md` §6（59 文件 / 23,041 行 = 25.4% 行 / 34.3% 文件，逐行相加可复算）。
- **§2-A 门禁可信度族的续（批次 369 量到，不新开登记项）**：批次 368 的"验证"段写"全量门禁…整条
  rc=0"，可它自己那趟日志 `/tmp/b368_gate3.log` 第 5 行就是 `python_style: 298 passed, 2 failed`——
  `tools/run_all.sh:575` 是无条件的 `py_fail -ne 0 → rc=1`，且 `grep -rn "t231" tools/` = 0（脚本里没给
  存量红留豁免）⇒ 那句 rc=0 作废，368 原文不回改，旧结论→新结论在这一条里对。369 的实测：整趟复跑
  rc=**1**、三组读数与 368 逐字相同（两份 JSON 的 diff 只有 `ts` 和 `clean_checkout` 的 `rev`/`secs`），
  对照跑 `--skip-python` rc=**0**（`/tmp/b369_gate_skip_py.rc`）⇒ rc=1 单由 t231/t233 造成，其余各步全绿。
  **往后怎么写**：批次的验收看三组读数（official compile 全过 / python_style 只红 t231、t233 / 语料 39
  全解析），`run_all.sh` 的 rc 在这两条存量红被处理之前恒为 1（批次 353、355、356、361 记的都是 rc=1）；
  谁写 rc=0，必须先答"python_style 那步是不是被跳过了"。要把 rc 变成判据，那是另一件事：得先裁决
  t231/t233 是转 `// known-fail:` 还是修掉，本批不代答。
