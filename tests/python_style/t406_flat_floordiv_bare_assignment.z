// §5.2 known-fail 包（批次 334 登记）：无缩进文件里 `//` 的行内判据只剩
//   「该列上 `(`/`[` 未闭合」，所以**括号平衡**的赋值 `t = 7 // 2` 仍被当行
//   注释吃掉。失败形态比 t403 原来的截断更危险：这里是**静默错值**——`t` 拿到
//   7，程序照常编译照常跑，打印出来的是 7 而不是 3。
//   判据推不动是因为方言证据在这里用完了：official 语料里 `return 1  // Success`
//   与它逐字同形（实测 209 处行内 `//`，全部在深度 0）。按 `//` 右侧词形区分已
//   被实测否决：其中 36 处是单个词（`// Success`、`// SIMD`、`// Percentage`），
//   按「右边像一个操作数」判会把它们全改写成 `floordiv Success`。
//   带缩进的 python 文件不受影响（缩进本身就是方言证据），见 t138_floordiv。
// expect: 3
// known-fail: 平铺文件里括号平衡的 `//` 仍按注释处理，t 静默拿到 7
t = 7 // 2
print(t)
