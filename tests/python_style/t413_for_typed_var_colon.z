// 批次 367：缩进预处理的行内冒号规则曾把 Rust 风格的 for 变量类型注解
// `for i: usize in 0..10 {` 当成 Python 单行块，改写成
// `for i { usize in 0..10 { }`，其后 36 行被静默丢弃。
// 修复：冒号之后的代码里已有本语句自己的 `{` 时，这个冒号是类型注解，
// 不做单行块改写。
// 批次 368 补上第二个缺陷：带类型的循环变量 `for i: usize in …` 在 MIR
// 下游路时被当成整体丢弃，循环体一次都不跑，sum 算出 0。
// 现在两个缺陷都修好了，期望值回到 10（0+1+2+3+4）。
// expect: 10
// expect: done

fn main() -> i64 {
    let mut sum = 0
    for i: usize in 0..5 {
        sum = sum + i
    }
    println!("{}", sum)
    println!("done")
    return 0
}
