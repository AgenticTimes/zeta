// PY-A: `f(**mapping)` 实参解包。
// 此前 `**d` 被表达式解析器当成 `*(*d)`（两次一元解引用），被调用者**静默收到 0**
// —— 错值且零诊断，正是红线点名的「静默错值」，比解析失败更危险。
// 现在：在解析期就把 `**m` 标成解包标记，调用点按**被调用者的参数名**展开成
// `f(m["x"], m["y"])`（mapping 的键运行期才有，但参数名是静态已知的）；
// 签名未知（外部 shim / 方法调用）时改为显式警告，不再静默丢参数。
def f(x, y):
    return x * 10 + y

d = {"x": 1, "y": 2}
print(f(**d))
print(f(3, **{"y": 7}))

// expect: 12
// expect: 37
