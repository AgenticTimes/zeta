// PY-A: 库注册表数据驱动 —— math 仅靠 pylib/registry.txt + C shim 接入（未改 Rust）
// expect: 3.000000
// expect: 1024.000000
// expect: 3.000000
// expect: 2.500000
from math import sqrt, pow, floor, fabs

print(sqrt(9.0))
print(pow(2.0, 10.0))
print(floor(3.7))
print(fabs(0.0 - 2.5))
