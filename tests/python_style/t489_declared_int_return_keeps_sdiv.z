// 批次 454 第五格（backlog #201/#202）：**声明为整型返回的函数，`return a / b` 的商不能被抬成 f64**。
//
// 病因：`/` 的真除法抬型写在 MIR 的算术定型里（`gen.rs` 的 `op_type`），它只看两个操作数、
// 不看这条表达式的**消费者**。函数的 LLVM 签名由 `Mir::signature_ret_ty` 按**函数体**决定
// （第一个带类型信息的顶层 `return`），而调用点的目的槽按**声明的 `-> T`** 决定（resolver 的
// `func_ret_types`）。于是「声明 int、体是 `return int/int`」的函数拿到 `define double` 签名，
// 调用方拿 i64 去读同一个字 ⇒ docs/ABI.md §2 R7 的**按位重解读**（不是换算）。
//
// 本批抬型引入的两条实拍成员（都在 official 语料，读数＝进程退出码）：
//   `tests/unit-tests/test_arithmetic.z:16` `divide(100, 4)`：改前 rc=100，抬型后 rc=75；
//     最小复现 `/tmp/b454/ret_pin.py` 打 `4627730092099895296` ＝ 25.0 的位模式按 i64 读。
//   `tests/unit-tests/murphy_working_final.z:101` `return limit / 10`：改前 rc=5、抬型后 rc=2，
//     5 个调用点各报一条 `ABI return ... callee returns float, caller's dest slot is int`。
// 收口（`gen.rs` 的 `pin_declared_int_return`）：声明把返回字钉成整型、**且**决定签名的那个槽
// 恰好就是「两个整型操作数的 `/`」时，商保持 `sdiv`。浮点若是别处来的（`-> i64` 里
// `return 2.5`）一概不动 —— 那一格的漂移早于本批，归批次 399／任务 #33。
//
// 已登记的余量（本批不收，三格当场实拍）：
//   ① **间接形状**仍然漂移且出声 —— `fn divq(...) -> i64 { let q = a / b; return q; }` 打在
//     `/tmp/b454/ret_indirect.py` 上，改后二进制 `print` 出 4627730092099895296（改前 25），
//     并报 1 条 `ABI return` 警告（⇒ 不是静默错值，但值仍是错的）；决定签名的是 `q` 那条槽、
//     不是 `return` 的表达式，上面的判据打不到它。
//   ② **`-> float` 声明 + 只有整型实参的调用点**在改后仍打 `0.000000`（i64 的 3 被当 double
//     读成次正规数）；改前同读数 ⇒ 这一格是 #33 的旧账，不是本批引入。抬型不带槽门时它反而打
//     对（3.5）—— 本批把这条收益让了回去，是刻意的（见 ABI.md §2 R7）。
//     顺带一条对诊断的信任读数：`/tmp/b454/chk/f1.py`（同一函数、两处整型调用点）在三颗上
//     都报同样 3 行（2 条 `callee returns int, caller's dest slot is float` + 1 条汇总），
//     而 ungated 的运行期值是对的 ⇒ **出声条数不辨真假，值才是判据**；callee 侧那条读数
//     来自的表与 emit 出的签名不同源。
//   ③ 同一条 `-> float` 若**第一个调用点给的是浮点实参**，②的 0.000000 就变成 3.5
//     （`/tmp/b454/chk/f2.py`：`print(divf(7.0, 2.0))` 后 `print(divf(7, 2))` 打 3.5）
//     ⇒ 调用点次序能改判结果，属 #33／批次 399 的「一张表」面。本用例的 `div_float` 只喂浮点
//     实参，读数因此与次序无关。
//
// 正向半（防把钉子写成「所有 `-> int` 的 return 都整除」）：`-> int` 里返回的不是 `/`（`pick`）
// 不受影响；操作数本身是浮点的 `/`（`div_float(7.0, 2.0)`）仍是真除法；**裸表达式语句** `7 / 2`
// 也仍是真除法 —— 钉子只落在「声明钉整型 ∧ 决定签名的那个槽正好是两条整型操作数的 `/`」这一格。
//
// 期望值来源与**一处刻意分歧**（如实写明，不冒充真值）：本机 CPython 3.14.5 跑同一个程序打
// `25.0 / 3.5 / 3.5 / 7 / 3.5`（`-> int` 注解在 CPython 里不改变值）。本批的钉子选的是
// **声明赢**（`-> int` ⇒ 商保持 `sdiv`）⇒ 第 1、2 行在这里是 `25` 与 `3`，不是 CPython 读数；
// 它们锚的是两件事：① pre-454 的官方夹具期望（`test_arithmetic.z` 的 `divide(100, 4)` 要 25、
// `murphy_working_final.z` 的 `limit / 10` 要整商）；② 钉子确实收回了整除而不是碰巧对齐。
// 真正的 Python 语义（注解不改值）要么靠 #33 把签名两源合一、要么靠去掉注解，都不在本批。
// 三颗二进制的当场读数（程序＝本用例正文去掉 `// expect` 行，`/tmp/b454/chk/f4.py`；行序同本用例）：
//   改后（抬型 + 槽门 + 钉子）＝ `25 / 3 / 3.500000 / 7 / 3.500000`  ← 本用例的期望
//   pre-454（无抬型）          ＝ `25 / 3 / 3.500000 / 7 / 3`        ← 第 5 行是抬型半的读数
//   抬型·无槽门·无钉子          ＝ `4627730092099895296 / 4615063718147915776 / 3.500000 / 7 / 3.500000`
//   ⇒ 红证落在第三颗（第 1、2 行按位重解读）；第 5 行只在 pre-454 上会红（那条归抬型，不归钉子）。
def div_int(a, b) -> int:
    return a / b

def div_float(a, b) -> float:
    return a / b

def pick(a, b) -> int:
    return a

print(div_int(100, 4))
print(div_int(7, 2))
print(div_float(7.0, 2.0))
print(pick(7, 2))
print(7 / 2)
// expect: 25
// expect: 3
// expect: 3.500000
// expect: 7
// expect: 3.500000
