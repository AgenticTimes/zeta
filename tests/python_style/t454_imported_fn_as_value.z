// PY-A: 被导入模块的 `def` 当**值**读 —— 修前读到 0，调用静默不发生。
// 语料形：`jq_wufu_local.py:41-45` 把 `_strategy.morning_routine` 绑成模块级别名，
// `:111-112` 再 `for routine in [morning_routine, …]: routine(context)`。
// 修前这些别名持有 0（模块的 `def` 从不写进环境表，只有模块级**绑定**会被镜像；
// 读侧却走 `zeta_env_get`），而 `zeta_call1(0, x)` 按合同返回 0 而不跳（docs/ABI.md C9）
// —— 于是 37 个交易日一次调用都没发生：acceptance 0 笔成交、`1000000 -> 0 (-100.00%)`
// （CPython 同夹具 `-0.54%`）。具名直调（MARK-A）修前就是对的，留在这里做对照。
// 修法（批次 417）：两处当值读点位（裸名 `gen.rs:3890` 臂、模块限定 `gen.rs:12290` 臂）在
// `func_ret_types − global_consts − type_decls` 命中时把值下型成 `FuncAddr`。
// 两处都**只覆盖那个槽的值**，原来的 `StringLit` + `zeta_env_get` 照旧发 —— 这样语句数与槽号
// 分配和修前逐字一致，本批改动的可观察面只剩"槽里放地址还是放环境读数"这一维。
// 更正（批次 417 对照实测）：批次 416 记的"省掉这两条语句会把三格模块初始化读数抹成 0"是**误归因**。
// 未改动的编译器重新出四版，三版完整跑通的那三版读数就是 `codes 0 / trading_days 0 / 保守模式 false`，
// 而同一版二进制自己也会在 119 行处提前 SIGSEGV —— 同源码同编译器的两次出码 IR 行数相同、
// 寄存器编号不同。这三格读数的塌与不塌是**出码轮次**决定的，与本处两条语句无关；详见 roadmap 批次 417。
// expect: MARK-A
// expect: FNREF morning
// expect: MARK-B
// expect: FNREF morning
// expect: MARK-C
// expect: FNREF morning
// expect: FNREF buy
import fnref_fixture as fx

ROUTES = [fx.morning, fx.buy]

print("MARK-A")
fx.morning("ctx")

print("MARK-B")
mr = fx.morning
mr("ctx")

print("MARK-C")
for r in ROUTES:
    r("ctx")
