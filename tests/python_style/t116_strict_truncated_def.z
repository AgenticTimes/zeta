// PY-A: 定义关键字不能悄悄退化成语句。
// 此前 `def X(...)` 只要有一个构造让定义解析失败，alt 就会退到 parse_stmt：
// `def ` 被当成一个裸标识符语句（只吃 4 字节）、`X(...) ` 再被当成一条语句 ——
// 定义被**静默拆成碎片**，many0 继续往后啃，整份文件被截断而编译器一言不发。
// 现在这类输入必须报错（严格模式下退出码非零），诊断指向真正的起点 `def`。
// env: ZETA_STRICT_PARSE=1
// expect-error
def only_a_comment():
    # 体内没有任何语句 —— 头部的冒号不会被改写成 `{`，定义解析必然失败
