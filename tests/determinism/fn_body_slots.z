// 出码稳定性夹具（批次 418）—— 只测"同一份源码连编两次字节相同"，不测运行结果。
// 这一条盯的是函数序言：每个局部槽一个 alloca，发射序跟着 `collect_all_local_ids`
// 的集合序走；集合序不固定 ⇒ 整个函数的寄存器编号跟着出码轮次翻。

fn main() {
    let a = 1;
    let b = 2;
    let c = a + b;
    let d = c * 3;
    let e = d - a;
    println!("{}", e);
}
