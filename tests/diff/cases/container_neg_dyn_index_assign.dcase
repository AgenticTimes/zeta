# @cat: container
# @note: #53 的写侧一档：`l[k] = v` 的 k 在运行时为负。读侧判据（批次 331）与写侧判据共用
#   `normalize_subscript_index`，因为两侧原来都只按 AST 形状认负号。
#   修复前实测（AOT，前两条 print）：`l[0 - 1] = 9` 没写进末槽，而是把**长度**改成了 9 ——
#   得到 `7` 与 `[3, 5, 7, 0, 0, 0, 0, 0, 0]`（期望 `9` / `[3, 5, 9]`）。值没落在数据区、
#   元数据却变了（写侧比读侧的"读到垃圾"更坏），所以单独钉一条。第三条是同一判据的
#   第二个负形状（`0 - i`）。
#@@ python
l = [3, 5, 7]
k = 0 - 1
l[k] = 9
print(l[2])
print(l)
i = 2
l[0 - i] = 42
print(l)
#@@ zeta
l = [3, 5, 7]
k = 0 - 1
l[k] = 9
print(l[2])
print(l)
i = 2
l[0 - i] = 42
print(l)
