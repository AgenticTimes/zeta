# @cat: container
# @note: 与 container_neg_dyn_index 同族的崩溃档：嵌套 list 上用动态负下标链式取下标直接段错误
#   （rc=139/SIGSEGV），而字面量负下标 `m[-1][-1]` 正常。族由生成器 seed=20260922 约出，
#   嵌套形态是约族后加探的（生成语法里没有嵌套 list），所以它不在那 60 条采样里。
#   已知红：#53（崩溃档比错值档更重，单独一条用例，避免被绿档淹掉）。
#@@ python
m = [[1, 2], [3, 4]]
print(m[0 - 1][0 - 1])
#@@ zeta
m = [[1, 2], [3, 4]]
print(m[0 - 1][0 - 1])
