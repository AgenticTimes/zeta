# @cat: container
# @note: 与 container_neg_dyn_index 同族的崩溃档：**元素本身是容器句柄**时，负下标一次就段错误
#   （rc=139/SIGSEGV），不需要链式下标 —— 批次 330 实测把复现从 `m[0-1][0-1]` 缩到 `m[0-1]`，
#   而字面量档 `m[-1]` 正常。族由生成器 seed=20260922 约出（8 条命中都在扁平 list 上），
#   嵌套档是归族后加探的（生成语法里没有嵌套 list）。
#   已知红：#53（批次 331 与错值档**同批**转绿 —— 单条转绿不算修好，这条判据留给后续同族修复）。
#@@ python
m = [[1, 2], [3, 4]]
print(m[0 - 1])
print(m[-1])
#@@ zeta
m = [[1, 2], [3, 4]]
print(m[0 - 1])
print(m[-1])
