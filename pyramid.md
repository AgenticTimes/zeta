```text
编译器金字塔——从第一性原理推导编译器必须做的每件事
│  每行一句话；叶子按「是什么/为什么/怎么做」展开；「🔬 前沿」子节点为 2026-09 SOTA 调研
│  注解（前沿是什么 → 对本项目的设计映射 → 任务锚点，W 系任务登记于 refactor.md §9）。
│
├── 0 公理层：编译器是什么、为什么存在
│   ├── 是什么：编译器是语义保持的程序翻译器，把人写的源程序变换成机器可执行的程序。
│   ├── 为什么：人擅长抽象逻辑、机器只执行二进制指令，这道语义鸿沟只能由程序化翻译填补。
│   ├── 怎么做：全程服从唯一公理——源语义 ≡ 目标语义，任何变换、优化、捷径都不得违反。
│   │
│   ├── 职责一「读得准」：源语义若被理解错，后续一切变换都在精确地翻译一个错误的理解。
│   ├── 职责二「翻得出」：目标语义若生成错或丢失，前端的全部努力归零。
│   └── 职责三「证得对」：不能证明或检验等价，就永远不知道错没错——编译器的信任级别是软件中最高的。
│
├── 1 语言规范层（先于一切代码存在）
│   ├── 是什么：语法规范（什么文本合法）+ 静态语义规范（编译期规则）+ 动态语义规范（运行期含义）。
│   ├── 为什么：没有规范，"正确"无法定义——测试没有 oracle，优化没有边界，实现没有裁判。
│   ├── 怎么做：语法用文法定义；语义按"静态可判/动态才知"两分；动态语义是运行时设计的直接输入。
│   │
│   ├── 1.1 语法规范
│   │   ├── 是什么：定义合法程序的形式——词如何成句、句如何嵌套。
│   │   ├── 为什么：前端每个阶段都要对照它工作，文法有歧义则同一文本可有多种含义。
│   │   └── 怎么做：EBNF/CFG 写文法，优先级与结合性显式写入文法而非口头约定。
│   │   └── 🔬 前沿：docs/python-syntax.md（PY-1~5 定稿）是决策文档而非文法；工业界规范形态 =
│   │           PEP（提案+参考实现+一致性测试）——升级路径是把决策文档补成可测的文法。
│   │
│   └── 1.2 语义规范
│       ├── 是什么：静态语义 = 类型与作用域规则；动态语义 = 求值顺序、对象模型、错误行为。
│       ├── 为什么：类型检查器是静态语义的实现，运行时是动态语义的实现——规范是两者的合同。
│       └── 怎么做：语义继承型语言（如 Python 子集）以参考实现行为为准绳，写不清的语义先不要实现。
│       └── 🔬 前沿：Nuitka 把"兼容"推到连错误信息都对齐——语义继承型编译器的行规是
│               以 CPython 可观察行为为规范本体；oracle 即规范（G.3 差分测试的行为性代偿）。
│
├── 2 前端层（职责一「读得准」）
│   ├── 是什么：把文本逐层结构化为带语义信息的内部表示。
│   ├── 为什么：机器不认识文本，理解必须分级——字符→词→句→义，每一级只消灭一类歧义。
│   ├── 怎么做：五个子阶段串联，各阶段产物是下一阶段的唯一输入，阶段之间禁止越层判断。
│   │
│   ├── 2.1 词法分析
│   │   ├── 是什么：字符流 → 带类别的 token 流（标识符/关键字/字面量/操作符）。
│   │   ├── 为什么：词是语法的最小单元，切词不准则后续结构全是歧义。
│   │   ├── 怎么做：每类词用正则定义，有限状态机扫描，最长匹配加关键字表优先。
│   │   └── 🔬 前沿：手写 DFA/正则仍是 AOT 编译器主流（tree-sitter 类工具面向 IDE 增量解析，
│   │           不改变 AOT 选择）——此项无欠账。
│   │
│   ├── 2.2 语法分析
│   │   ├── 是什么：token 流 → AST，一棵只含语法形式、不含语义判断的树。
│   │   ├── 为什么：嵌套结构必须显式成树，否则优先级、结合性、作用域无从谈起。
│   │   ├── 怎么做：文法驱动的递归下降或 LR；解析错误要恢复并继续，一次报一批。
│   │   └── 🔬 前沿：rust-analyzer 的弹性解析是当代标准答案——错误产生式与同步 token 显式
│   │           编码进文法、永远产出完整语法树；Ruby 弃手写 parser 重写为 YARP 是工业先例。
│   │           映射与任务：G.7a（报告化）→ G.7b（顶层同步恢复 + span）。
│   │
│   ├── 2.3 名称解析
│   │   ├── 是什么：把每个名字的使用绑定到唯一的声明（作用域规则在此生效）。
│   │   ├── 为什么：同名异义是语言常态，绑定错了语义全错且毫无声响。
│   │   ├── 怎么做：符号表加作用域栈，按语言查找规则（如 Python 的 LEGB）逐层解析。
│   │   └── 🔬 前沿：查询式（salsa/rust-analyzer）按需解析是 IDE 级标准，AOT 编译器一次性
│   │           解析即可——增量留到 W9（远期）。
│   │
│   ├── 2.4 类型检查
│   │   ├── 是什么：验证静态语义并推导每个表达式的类型，产出全程序统一的类型环境。
│   │   ├── 为什么：编译期拦住一大类错误，且类型是内存布局与分派的依据——不查类型就只能运行期对内存取证。
│   │   ├── 怎么做：独立 pass、双向检查；未知类型标成 gradual 并在边界插检查点，绝不静默猜成具体类型。
│   │   │
│   │   ├── 2.4.a synth（自下而上综合）
│   │   │   ├── 是什么：从子表达式类型推父表达式类型。
│   │   │   ├── 为什么：字面量与运算的类型天然可知，不需要上下文。
│   │   │   └── 怎么做：字面量定类型、运算按操作数规则、调用查签名表。
│   │   │
│   │   └── 2.4.b check（自上而下校验）
│   │       ├── 是什么：在期望类型已知的上下文中验证表达式。
│   │       ├── 为什么：赋值、实参、返回的期望类型是免费信息，放弃它推断就变成瞎猜。
│   │       └── 怎么做：期望类型向下传递，不匹配报编译错误或显式 gradual 转换，绝不静默改类型。
│   │   └── 🔬 前沿（2026 调研，Python 子集两条已验证路线）：① mypyc 式渐进类型——标注喂
│   │           推断、未标注回退动态语义、严格保 CPython 语义（已用其编译 mypy 自身，~20x）；
│   │           ② Codon 式全静态推断——2025 实证研究平均提速 94%（10-100x）但明确弃动态兼容。
│   │           映射：zeta 走 ①（F 轴既定：未标注 = PyDynamic + 边界检查），Codon 的教训 =
│   │           "前端类型关漏了才被迫后端猜"（ARCHITECTURE-REVIEW 同结论）。任务：轴 F 全部。
│   │
│   └── 2.5 降糖（desugaring）
│       ├── 是什么：把语法糖按语义等价规则改写为核心语言形式。
│       ├── 为什么：规范只需精确定义小核心，糖在核心上组合——实现面最小、语义只有一份。
│       ├── 怎么做：AST→AST 改写，展开时点全语言统一，禁止逐例特判。
│       └── 🔬 前沿：rustc 的 lowering 把糖展开固定在 HIR 一级、每条糖有唯一展开规则——
│               "时点统一"比"展开本身"更容易欠债。任务：F.1 审计覆盖。
│
├── 3 中端层（职责二的规范形态）
│   ├── 是什么：把带语法的表示变换为与源、目标都解耦的中间表示（IR），并在其上做分析与优化。
│   ├── 为什么：M 种语言 × N 种目标直接互译要写 M×N 遍，引入 IR 降为 M+N；优化只在规范形上可行。
│   ├── 怎么做：三步——设计 IR、逐级 lowering、分析加变换成对推进。
│   │
│   ├── 3.1 IR 设计
│   │   ├── 是什么：编译器内部语言的"宪法"——程序在阶段之间的唯一规范表示。
│   │   ├── 为什么：前后端的一切通信、优化的全部对象都发生在 IR 上，IR 里没有的信息后端只能猜。
│   │   ├── 怎么做：分层 IR；显式控制流（CFG）加显式数据流（SSA）；类型随 IR 携带或随值携带，不许靠旁路哈希表。
│   │   └── 🔬 前沿：MLIR 已是 AI/异构编译器的事实标准（dialect 多层 IR，NVIDIA/Mojo/IREE 在用）；
│   │           2025 年连学界都在补它的测试学（DESil，检测 MLIR 静默 bug，arXiv:2502.05216）。
│   │           映射：zeta 显式豁免分层 IR（refactor 显式不做），但 MIR 的 `op: String` 与
│   │           业界 typed-op 完全相反——W1（低成本先做）：op 字符串 → typed enum，还债③决断前完成。
│   │
│   ├── 3.2 Lowering（降级）
│   │   ├── 是什么：高层表示到低层表示的逐级语义等价变换。
│   │   ├── 为什么：一次跨太大台阶会让语义判定散落到不该在的层，逐级下降让每步可验证。
│   │   ├── 怎么做：每级定义结构不变量，变换前后用 verifier 与差分保等价；语义分派必须在 lowering 前定型。
│   │   └── 🔬 前沿：MLIR 每个 dialect 自带 verifier 且 pass 框架强制 pass 后校验——"变换后
│   │           即验证"是框架级纪律。任务：G.6（MIR verifier）承接此思想。
│   │
│   └── 3.3 优化
│       ├── 是什么：在源语义 ≡ 目标语义的约束下改进速度、体积、能耗等非功能属性。
│       ├── 为什么：这是编译器存在的第二理由——不但要翻对，还要翻得更好。
│       ├── 怎么做：分析与变换严格成对——没有分析支撑的变换就是在赌 UB 不发生。
│       └── 🔬 前沿：等价饱和（e-graph）已进生产编译器——Cranelift aegraph（PLDI 2023）用
│               "重写即证明"消灭重写序死锁；LLVM 侧 Alive2 自动验证 InstCombine 重写的语义
│               正确性。映射：zeta 规模不配自建 e-graph，还债③判据不变（两周无收益即删
│               optimization.rs）；若保留，每条重写必须能陈述其依赖的分析（Alive2 思想）。
│
├── 4 后端层（职责二「翻得出」的落地）
│   ├── 是什么：把 IR 规范形映射到具体目标（CPU 指令集或字节码）。
│   ├── 为什么：目标资源有限——寄存器有限、指令集固定、调用约定既定，生成就是规范形到受限资源的映射。
│   ├── 怎么做：指令选择 → 寄存器分配 → 发射；数据布局与 ABI 全程一致。
│   │
│   ├── 4.1 指令选择
│   │   ├── 是什么：把 IR 操作映射为目标指令序列。
│   │   ├── 为什么：同一 IR 操作有多种指令实现，选择决定性能下限。
│   │   ├── 怎么做：树覆盖或模式匹配，合并寻址模式。
│   │   └── 🔬 前沿：LLVM（21/22）仍是重优化的黄金标准；Cranelift 证明"快编译 + 可验证"
│   │           是另一个正交极（regalloc 自带 checker 做差分验证）。映射：保留 LLVM 单后端，
│   │           Cranelift 双后端列入不采纳（维护两套后端不合算）。
│   │
│   ├── 4.2 寄存器分配
│   │   ├── 是什么：无限虚拟寄存器到有限物理寄存器的映射加溢出。
│   │   ├── 为什么：物理寄存器只有十几个，分配质量直接决定生成代码的速度。
│   │   ├── 怎么做：图着色或线性扫描，以活跃性分析为底座。
│   │   └── 🔬 前沿：Cranelift 的 regalloc checker（给分配结果跑解释器差分）证明"分配器
│   │           正确性可机检"——LLVM 侧等价物是 MachineVerifier。任务：交给 LLVM，无欠账。
│   │
│   └── 4.3 ABI 与数据布局
│       ├── 是什么：调用约定、参数传递、栈帧布局、每个类型的字节级布局。
│       ├── 为什么：ABI 是生成代码与运行时、外部世界之间的法律，错一个字段偏移就是静默错值。
│       ├── 怎么做：一份书面 ABI 文档加单一实现点；调用点与定义必须从同一份声明生成。
│       │
│       ├── 4.3.a 调用约定与返回语义
│       │   ├── 是什么：参数与返回值在寄存器/栈上的传递规则、栈清理责任、闭包环境如何随行。
│       │   ├── 为什么：调用点与定义若各自理解约定，实参错位、返回值是垃圾——struct/元组返回是重灾区。
│       │   └── 怎么做：调用点与定义从同一份声明生成；约定未稳定的语义标 OPEN 当场选型写死，不许逐批漂移。
│       │
│       ├── 4.3.b 值表示（槽位编码）
│       │   ├── 是什么：一个槽位的全部合法内容及其编码——裸整数、位模式、指针、句柄、tag 单元。
│       │   ├── 为什么：写读两端对同一编码理解不一致，就是"值对、类型错"的静默 bug（整数裸存进浮点槽读回 4.9e-322 一类）。
│       │   └── 怎么做：表示表穷尽——用运行期一切判形代码反查校验；写/读对称规则（何时转换、何时位回读）逐条锚定实现。
│       │
│       ├── 4.3.c 类型与容器布局
│       │   ├── 是什么：每个类型的字段偏移与对齐，容器头结构（长度/容量字段的位置与含义）。
│       │   ├── 为什么：运行期一切按偏移取值的代码（判形、序列化、FFI）都押在布局约定上，偏移错一位全是静默错值。
│       │   └── 怎么做：布局假设升格为明文合同；改布局 = 改合同 = 过评审，不许静默挪字段。
│       │
│       ├── 4.3.d 名字修饰
│       │   ├── 是什么：编译产物符号的命名规则——模块前缀、重载/特化编码、运行期魔法符号。
│       │   ├── 为什么：修饰规则即链接与动态分派机制，双规则并存会让调用点解析到错误符号。
│       │   └── 怎么做：单一修饰函数生成全部符号；新增规则先写进合同再实现，禁止拿符号命名约定当分派机制用。
│       │
│       ├── 4.3.e 合同治理
│       │   ├── 是什么：让 ABI 文档保持权威而不腐烂的流程——单一实现点、变更评审、bug 回写。
│       │   ├── 为什么：无治理的文档会退化成另一份没人看的 wiki，合同失效即退回补丁文化。
│       │   └── 怎么做：每条决策唯一所有者；"值对类型错"类 bug 先修合同再修实现；每个批次记录声明"ABI 影响：无/§x"。
│       │
│       └── 🔬 前沿：链后优化（BOLT/Propeller）与 PGO 是自举语言拿免费性能的标准动作——
│               但前提是有可用的性能基线与正确门禁。任务：W3（自举二进制 PGO，轴 C 后期）。
│
├── 5 运行时层（语言语义的另一半）
│   ├── 是什么：语言承诺但无法或不该编译为静态代码的部分——分配、回收、动态分派、内建。
│   ├── 为什么：动态语言的语义本质上是运行期语义，生成代码与运行时的边界划错，类型就只能靠猜。
│   ├── 怎么做：四件套——值模型、内存管理、动态分派、标准库；每个值自带类型标签是第一块砖。
│   │
│   ├── 5.1 值模型 / 对象模型
│   │   ├── 是什么：每个运行期值的位级表示——类型标签加载荷（tagged cell、NaN-boxing、指针标记）。
│   │   ├── 为什么：值不带类型信息，一切动态语义（真值、分派、len）就只能对内存做几何猜测。
│   │   ├── 怎么做：定义唯一的 boxed 表示与 tag 集；静态可知处用裸值优化，边界处装箱拆箱。
│   │   │
│   │   ├── 5.1.a 边界规则
│   │   │   ├── 是什么：什么位置必须装箱、什么位置允许裸值。
│   │   │   ├── 为什么：全量装箱性能不可接受，零装箱退回猜测，边界划在"静态类型断点"上。
│   │   │   └── 怎么做：类型检查器输出 dynamic 流动的位置 = 装箱点清单。
│   │   │
│   │   ├── 5.1.b tag 集设计
│   │   │   ├── 是什么：值的种类枚举（int/float/str/bool/vec/map/handle…）。
│   │   │   ├── 为什么：tag 集是运行期分派的 dispatch 表，缺一类值就多一套启发式。
│   │   │   └── 怎么做：数据驱动定义（注册表），加一种值等于改一行数据而非改代码。
│   │   │
│   │   └── 🔬 前沿：三大流派——boxed cell（CPython PyObject，JSON 域的 ZJ 单元即此）、
│   │           NaN-boxing（JS 引擎）、tagged pointer（LuaJIT/ObjC）；CPython 3.14 落地 JIT
│   │           也未换值模型，证明 boxed cell 可行但**必须配 inline cache**（见 5.3）。
│   │           映射与任务：B/T3 的 zt_box 选型不做拍脑袋——W5（microbenchmark：ZJ-cell vs
│   │           NaN-box vs tagged-ptr，aarch64 实测装箱/拆箱/分派三成本）。
│   │
│   ├── 5.2 内存管理
│   │   ├── 是什么：对象的分配与回收策略（手动/引用计数/追踪 GC）。
│   │   ├── 为什么：回收正确性与值模型耦合最深，中途换方案等于重写运行时。
│   │   ├── 怎么做：动态语言默认追踪式 GC；保守 GC 与"指针整数混槽"互斥——先定值模型，再选 GC。
│   │   └── 🔬 前沿：低停顿并发已是标配（Go、ZGC 分代模式 2023+）；CPython 3.13 落地
│   │           free-threading（PEP 703）但 AOT 单线程阶段与之无关。映射：zeta 用 Boehm 是
│   │           "值模型未定前的权宜"（ARCHITECTURE-REVIEW 同判）；值带 tag 后可换精确
│   │           mark-sweep。任务：维持显式不做，触发条件 = B/T5 完成。
│   │
│   ├── 5.3 动态分派
│   │   ├── 是什么：运行期按值的实际类型选择行为（方法表/vtable/内联缓存）。
│   │   ├── 为什么：鸭子类型的调用点编译期不可知，分派机制就是动态语义的执行引擎。
│   │   ├── 怎么做：数据驱动的方法表为正道；靠符号命名约定或内存几何猜测做分派是歧路。
│   │   └── 🔬 前沿：inline cache 是动态分派的标准答案——PEP 659（3.11 提速的主力）本质就是
│   │           解释器级 IC，V8 的 IC 已演化四十年；CPython JIT（PEP 744/836）也建立在
│   │           特化槽之上。映射：B/T4 方法表落地后，热调用点加单态 IC（tag 比对→直达→失链
│   │           回慢路径）。任务：W6（IC，B/T5 后性能专项）。
│   │
│   └── 5.4 标准库与原语
│       ├── 是什么：语言承诺的内置能力——内建类型、内建函数、I/O。
│       ├── 为什么：动态语言的"类型系统的一半"实际长在标准库的类上。
│       ├── 怎么做：与值模型同源实现；API 声明与运行时实现分离，用契约测试锁死两边一致。
│       └── 🔬 前沿：单一 schema 生成多端绑定（bindgen/cbindgen 模式）已是 FFI 标准实践——
│               registry.txt 正是未完成的该模式。任务：E.3（契约测试）+ G.5e（工业化）。
│
├── 6 正确性层（职责三「证得对」，横切所有层）
│   ├── 是什么：证明与检验"源语义 ≡ 目标语义"的全部机制。
│   ├── 为什么：编译器的信任级别最高——它错，下游所有软件一起错，而且错得无声。
│   ├── 怎么做：验证器、测试金字塔、差分测试、全优化级别验证，四位一体。
│   │
│   ├── 6.1 内部验证
│   │   ├── 是什么：每级 IR 的结构不变量检查（verifier）与类型系统可靠性陈述。
│   │   ├── 为什么：变换错误要在产生它的那一级被抓住，而不是下游崩溃时倒查三层。
│   │   ├── 怎么做：每个 pass 之后跑 verifier；类型系统给出 soundness 承诺（保证拦住哪些错误）。
│   │   └── 🔬 前沿：MLIR dialect 级 verifier + pass 框架强制校验；学界 2025 的 DESil 专门
│   │           检测 MLIR 类"静默错译"——证明"verifier 抓不到的语义 bug"也要专门测试学。
│   │           任务：G.6（结构）+ G.3（语义）两级。
│   │
│   ├── 6.2 测试金字塔
│   │   ├── 是什么：单元（pass 级）→ 快照（IR/输出 golden）→ 端到端（编译并运行断言输出）逐层放大。
│   │   ├── 为什么：只测编译通过不测运行结果，等于只验证了一半的语义等价。
│   │   └── 怎么做：端到端用例必须断言输出值——compile-only 的测试是安慰剂。
│   │   └── 🔬 前沿：rustc 的 ui 测试体系 + crater（对全生态重跑）是"回归面"的工业上限；
│   │           zeta 的对应物 = 语料 39 + python_style 282（mini-crater）。任务：轴 E。
│   │
│   ├── 6.3 差分测试
│   │   ├── 是什么：同一程序在参考实现与本编译器上各跑一遍，对比可观察行为。
│   │   ├── 为什么：对语义继承型语言，参考实现就是最权威的 oracle，还能自动生成无限用例。
│   │   ├── 怎么做：语义片段库起步，进阶到随机程序生成器；一致性率作为核心指标进 CI。
│   │   └── 🔬 前沿：Csmith（C 随机程序生成器）证明了"生成器+oracle"可以工业化——二十年
│   │           抓出上百个编译器 bug。任务：G.3（进阶 = Python 版生成器）。
│   │
│   └── 6.4 全优化级别验证
│       ├── 是什么：正确性断言在 -O0 到 -O3 每个级别下分别成立。
│       ├── 为什么：优化器只在 IR 违反其规则时才"误编译"——某级别红就说明存在潜伏 UB，关掉优化只是冻结 bug。
│       ├── 怎么做：门禁矩阵覆盖全部优化级别；ASan/MSan/UBSan 常态化运行抓未定义行为的现行。
│       └── 🔬 前沿：Alive2 是 LLVM IR 语义验证的事实标准（自动证明优化重写前后等价）——
│               zeta 查 -O3 根因可借其思路：提取可疑函数 IR、对 UB 属性（noundef/TBAA）做
│               机器核对。任务：并入 G.1 工具清单。
│
├── 7 工程层（让一切可持续，横切）
│   ├── 是什么：架构分层、编译器自身性能、工具生态——决定它三年后还能不能改。
│   ├── 为什么：编译器是长寿命基础设施，语义隐式、层次纠缠的代码会在每个新特性上收复利税。
│   ├── 怎么做：阶段间只经 IR 与符号表通信；语义全部显式化；编译速度与工具链一并经营。
│   │
│   ├── 7.1 架构分层
│   │   ├── 是什么：阶段管道加层间唯一接口，依赖单向。
│   │   ├── 为什么：跨层判断（解析器做类型检查、后端猜真值）会让同一语义存在多处不一致的实现。
│   │   ├── 怎么做：每阶段一个模块、禁止反向依赖；任何一条信息全局只有一个所有者。
│   │   └── 🔬 前沿：rustc 的 query 系统（按需计算+跨阶段缓存）是"阶段解耦"的工程终态，
│   │           但引入成本高。任务：轴 D/F 先做到"信息唯一所有者"，query 化 = W9（远期）。
│   │
│   ├── 7.2 编译器自身性能
│   │   ├── 是什么：编译速度、内存占用、增量编译能力。
│   │   ├── 为什么：编译器是开发者每天跑几十次的工具，它慢则全团队慢。
│   │   ├── 怎么做：名字 intern 化、消灭无谓克隆、增量重编译按模块失效。
│   │   └── 🔬 前沿：Cranelift 的存在证明"编译速度本身是产品特性"；salsa 式按需查询是增量
│   │           编译的成熟实现。任务：轴 C（intern/FxHashMap）→ W9（增量，远期）。
│   │
│   └── 7.3 生态与工具
│       ├── 是什么：诊断质量、LSP、调试信息（DWARF）、构建系统集成。
│       ├── 为什么：语言之争常常赢在工具，而所有工具都压在编译器暴露的接口上。
│       ├── 怎么做：结构化诊断（错误码/位置/修复建议）替代裸打印；调试信息从 IR 一路携带到目标文件。
│       └── 🔬 前沿：LSP 已是语言工具链的默认入口（zeta 有 719 行 lsp 模块未接线）；DWARF5
│               支持矢量调试。任务：低优先级——主线与正确性轴全部落地后评估。
│
└── 8 塔基回顾（自检清单）
    ├── 一句话：规范定对错 → 前端读得准 → 中端立规范 → 后端翻得出 → 运行时补齐另一半语义 → 正确性贯穿证明 → 工程层保证十年后还改得动。
    │
    ├── 问 1：动态语义有没有书面规范？——没有则运行时设计靠猜。
    ├── 问 2：每个值的运行期表示带不带类型标签？——不带则一切动态语义靠内存取证。
    ├── 问 3：类型检查是不是独立 pass？——不是则同一表达式在三层可能得到三个答案。
    ├── 问 4：语义等价在哪一级被验证？——说不出就是从没验证过。
    ├── 问 5：每个优化是否有对应分析支撑？——否则是在赌 UB 不发生。
    ├── 问 6：阶段之间有没有唯一接口？——否则语义散落各处、改一处崩三处。
    └── 问 7：测试断言的是输出值，还是只断言编译通过？——后者什么也没验证。
```
