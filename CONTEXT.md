# Zeta 项目词汇表（本项目语境）

**这个项目是干什么的**：一个 AOT 编译器，把 Python 的一个静态子集（带兼容层）编译成原生可执行文件。它的真实客户是量化交易策略代码（「语料」）。远期目标是让 Zeta 语言自己编译自己（自举）。

**这份文件是干什么的**：项目里有一套自己的行话。为了让测试、文档、bug 报告彼此说得清，大家必须用同样的词。这里是唯一权威定义。每个词条给两部分：**人话解释** + `_Avoid_`（不要用哪些词替代，以及为什么不能用）。

## 使命与红线

**静默错值**：
程序编译成功、跑出错误结果、而且全程没有任何报错提示。你拿到了错答案，却没有任何线索告诉你它是错的——所以这是本项目最危险的一类 bug，也是主要防治对象。
_Avoid_: 错译（太泛）、bug（区分不出危险度）

**响亮失败**：
遇到处理不了的情况，必须当场终止并大声报错（编译期报错，或运行期自己 abort）。宁可崩溃，也绝不悄悄改用假值或默认值继续跑。
_Avoid_: fail-fast、报错（都少了"绝不悄悄降级"这层意思）

**值对、类型丢**：
本项目最常见缺陷模式的名字：运行期拿到的数值本身是对的，但它的「槽编码/类型」在静态世界与动态世界的交界处丢了。典型症状：本该打印 3.14，结果打印出内存地址或 4.6e18。
_Avoid_: 类型问题（太泛）、转型 bug

## 值与类型

**槽**：
运行期一个值住的地方：一个 64 位的机器字。关键：同一个槽可以有几种合法解读——当整数读、当浮点位模式读、当字符串指针读、当 GC 引用读、当 JSON 单元读。写入方和读取方必须约定同一种解读，否则就出「值对类型丢」。
_Avoid_: 变量、寄存器、word

**声明类型**：
程序员在源码里手写的类型标注（如 `x: f64`）。它说了算：编译器推断出的类型**不许**悄悄覆盖它（权威性有专门裁定规则）。
_Avoid_: 跟「推断类型」混用（光说"类型"时必须讲清是哪一种）

**推断类型**：
编译器根据上下文证据自己推出的类型。声明类型、推断类型、槽里的实际编码是三个不同的东西——混着说是「声明类型赢」这类误判的根源。
_Avoid_: 光说"类型"

**位型**：
把槽里的 64 个二进制位换一种方式解读（比如把浮点数的位模式当整数读，就得到 4.6e18 这种数字）。位型重解读永远意味着某次类型诊断缺席了、出事故了。它和「强转」（把 3.14 转成 3 那种值转换）是两回事。
_Avoid_: 强转（那是值转换）

**PyDynamic**：
编译期表示「这个值的类型还不知道」的占位标记。落到机器上它就是一个普通的 64 位槽（ABI 层面走 i64），运行期没有对应的实体。没写类型标注的参数，规范上归它（永远不会归 I64）。
_Avoid_: 动态类型（那指运行期语义，见「装箱值」）、Any

**装箱值**（规划中，属于值模型这条轴）：
运行期带类型标签的通用值表示（16 字节的 tag 单元）。用在 PyDynamic 值流动的边界上，让运行期也能知道值的类型。
_Avoid_: PyObject（没用那个模型）

**探针**：
运行期靠试探内存几何/可读性来猜「这个值是什么形状」的判型函数。这是历史遗留的幸存者，不是正经设计；装箱值落地后逐个领域退役。
_Avoid_: 判形器（听起来像正当机制）

## 兼容层

**PY-A**：
写在源码注释里的语义对齐标记：「此处行为是故意和 CPython 的可观察行为保持一致的」。哪里的行为和 Python 不一样，必须显式标注为分歧，不许悄悄偏离。
_Avoid_: Python 兼容（太泛，没有定位作用）

**方言**：
源码的两种书写风格——Python 缩进风和花括号风。同一个解析器两种都吃。注意：不能用「这段源码有没有被做过缩进改写」来判断方言（没缩进可改的平坦 Python 和花括号源码可能长得一样）。
_Avoid_: Python 模式 / Zeta 模式（暗示有两套实现，其实只有一套）

**句柄**：
注册表分配给库对象（DataFrame、Path、Date……）的引用，**带类型标签**。方法调用按标签精确分派，不靠猜。
_Avoid_: 指针、引用（那是不带标签的东西，见「GC 引用」）

**GC 引用**：
指向 Boehm GC 堆内对象的裸 64 位指针（vec/map/struct 底层用它表示）。没有标签——和「句柄」是两套机制，搞混会导致分派事故。

**桩**：
已经在注册表里挂了名、但没有真实实现的库函数绑定。按「出问题时怎么表现」分三种，报告和清单必须分清：
- **硬桩**：一调用就自报并终止；
- **软桩**：返回一个明摆着是假的值，不终止（registry 里有注释明示的也叫响亮桩）；
- **noop 桩**：导入时被接受、但行为为零（`__future__` 那一族）。
_Avoid_: 假实现、placeholder（区分不出失败方式）

## 正确性与验证

**三套基线**：
门禁里的回归底线组合，三条各有各的口径：
1. 官方单测 194 个——只检查「编译能成功退出」；
2. python_style——真实运行程序，对比输出值和预期（golden 断言）；
3. 语料——检查真实策略代码能否被完整解析。
（另有 jit sweep 作为第四步。）
_Avoid_: 测试套件（分不出口径）

**语料**：
REasyQuant 的真实量化策略代码。它首先是编译器要交付服务的客户，其次才是行为基准（oracle）。
_Avoid_: 测试集（它首先是客户）

**回归底线 194/194**：
只能读作「194 个文件编译成功」；**不能**读成「194 个程序完整编译通过」——截断债已于批次 437 前清零（truncation inventory 实测 0 hit / 238 文件，2026-09-26；历史口径见 docs/ABI.md 附 B#10）。

**门禁**：
`tools/run_all.sh`：把全部检查一次跑完并给出总判定的脚本，任何改动要落地它必须过。
_Avoid_: CI（ci.yml 只是它的一半，而且曾整段静默失效过）

## 过程

**批次**：
工作和记账的最小单位。一个批次 = 一次独立可回滚的提交 + 一条证据记录（实拍数据 / 差分 / 门禁读数）。
_Avoid_: 迭代、sprint

**ABI 合同**：
编译器生成的代码和运行时之间的成文约定（`docs/ABI.md`）。治理规矩：出了「值对类型丢」类的 bug，先修合同、再修实现。
_Avoid_: 文档（听起来可改可不改，实际是强制合同）
