# worktree.md — 双 worker 状态板与提交台账

> 本文件是两个并行 agent 的**消息板 + 台账**。设计要点：worktree 侧的更新在合并前不出现在主线工作副本里，
> 但**分支内容立即可见**——所以主线侧用 `git show cleanup:worktree.md` 读最新状态板，旁路侧直接看自己副本。
> 两个台账 section 按块分离，两边各自只写自己的 section，同文件编辑可自动合并。

## 0. 角色与工作区

| | Agent-1 主线（qwen） | Agent-2 旁路（ZCode） |
|---|---|---|
| 工作区 | 主树 `/Users/meetai/source/zeta-src` | `../zeta-bz`（分支 `cleanup`） |
| 方向 | 正向：roadmap2.md 主链向前（当前 = 主线 301 根因链，损害量序） | 反向：债务尾巴向回收（见 §1 队列） |
| 分支 / 号段 | `bootstrap` / 批次 438+ 顺序、t45x–t49x | `cleanup` / 批次 460–489（440 已用作度量批）、t50x 起 |

## 1. 两条工作队列（一个正着修，一个倒着清）

**正向队列（Agent-1 负责）**：按 roadmap2.md §2 主链的「下一批候选」，实测损害量大的优先。当前（批次 429 后）：

- **425 结案：头名（`("", 2)` 兜底 + 越界索引夹回 0）已在读侧收掉一半**。`codegen.rs:5918` 新增 `resolve_struct_layout_by_field`（调用点 `:6701-6704`）：declared-type 路线落空时按**字段名**反查布局，只有所有声明该名字的类别在 (variant, 宽度, 索引) 上完全一致才采纳，分歧退回旧兜底。语料实测：`ZETA_DBG_FA` 全量编译 **夹回 28→11**（收回 `portfolio`×8→`_LocalContext`、`avg_cost`×5/`closeable_amount`×2→`Position`、`context`×2→`NautilusBackend`），**PY-A 点名 38→2**（`py_map_items` 37→1）、stderr 357→321、改前 4 跑里 1 次 `Segmentation fault: 11` 在改后 4/4 跑消失；新用例 t462 改前 run=**139**/stdout 0 行 → 改后三行且与 CPython 逐字相同。**三条残留**：① 主线第 1 步（0 笔成交）未闭——`[PARITY] target=- \| holdings=- \| ranked=-` 74 条与 `1000000 -> 0 (-100.00%)` 一字未动；② 类型层仍允许"从整数身上读字段"（改后 `base_ty=Some(I64)` 那一形还在，424 §八.1 的"缺证据时出声"没做）；③ 残 11 处是 ≥2 类别声明同名且分歧（结构可证、逐对 variant 未证），其上游＝下面第 1 格。（【426 订正】句中"下面第 1 格"当时指回调那格，本批定价为语料位移 0 ⇒ 队列换到本节头名那条，见下。）
- **426 结案：425 的采纳判据从"三样全一致"放宽到"索引一致"**（代码批 `8ab17efb`，`codegen.rs` **+30/−30 等行**、新用例 `t463` 66 行）：读哪个 word 只由索引决定，`variant`/宽度只参与加载类型与 `:6754-6757` 的夹回上界 ⇒ `resolve_struct_layout_by_field`（`:5918`）改成"所有声明者给出同一索引即采纳、宽度取**最小声明者**"（索引 < 每个声明者的字段数 ⇒ 夹回进不去）。**语料夹回 11→6**：收回 `paused`×3、`low_limit`×2 ⇒ 现按 `Bar` count=4 读 idx 3/2（529 条 FieldAccess 读两侧同数，28 里累计已收 22）；残 6 处＝`bar_types` idx 9／`routines` idx 7／`trading_dates` idx 8 各 2，索引在声明者之间不一致。**运行期位移 0（本批的负结果）**：两侧各 5 跑，改前 **2/5**、改后 **1/5** 次同一形态早期崩（stderr 停 120 行、rc=139/138）⇒ n=5 无统计意义，**先前 2 跑得出的"改前必崩"作废**；`TRADE=0`、`1000000 -> 0 (-100.00%)`、`[PARITY] target=- \| holdings=- \| ranked=-` 74 条一字未动。**收回≠读对**：`paused`/`low_limit` 在 strategies\|backend 里没有 `self.X =` 声明点 ⇒ 若真实对象不是 `Bar`，值仍错、只是不再被夹回 0 掩盖。门禁 345/2/6/0＝`ls tests/python_style/t*.z` 353、`GATE_RC=1`（唯一红 `t231/t233`）、ABI `--rebind` **真·无操作**（＝被引行未位移的正证据）。
- **427 结案：424 §八.1 的 (a)「缺证据时出声」落地，判据一字未动、位移 0（代码批 `7c3ffa23`，`codegen.rs` **+61/−20**、新用例 `t464` + 两份 `pyswap_*.py`、`docs/ABI.md` 与 `abi_anchors.tsv` 随行号重绑）**。两支 `ZETA_DBG_FA` 探针：读侧 `FA decls`（两路反查都落空时点名该字段名的全体声明者与各自 variant/宽度/索引）、收集侧 `struct_defs conflict`（同键异序即将被首写者丢掉时打印 kept/dropped）。**主读数**：语料 529 次 FieldAccess 读里 **152 次走进 `("", 2)` 占位版式，152 条的 `final idx` 全为 0**；其中 **146 条 `declarers=0`**（45 个字段名，`index`/`columns`/`metadata`/`__dict__`… 多为外部对象属性）⇒ 索引 0 永不过范围检查 ⇒ **425/426 的 clamp 计数（28→11→6）把这一族低估 25 倍**，"残口只剩 6 处"在落空族口径下作废。另 6 条首次拿到分歧对：`NautilusBackend`(宽 11)↔`NautilusJqStrategy`(宽 9)，`trading_dates` 8↔3、`routines` 7↔2、`bar_types` 9↔4 ⇒ 三个名字成对出现在同两个类别之间＝**同一接收者的两条候选版式在争**，判据再放宽＝猜布局，这条路到头。位移 0 的正证据＝语料 `--emit-llvm` 文本改前改后 **113,722 行逐字节相同**（机器码不能当尺：同一颗编译器两跑产物 `cmp` 在 char 1609 就 differ）；`FA read` 529=529、夹回 6=6；门禁 python_style 345/2/7/0＝`ls t*.z` 354、`GATE_RC=1`（红仍是 `t231/t233`）。**顺手抓出一个已实拍的静默错值**：`struct_defs` 键用**裸类别名** ⇒ 跨模块同名同宽类别共用一版布局、首写者胜，`t464` 出 `y 3 4` 而 CPython 真值 `y 4 3`（改前改后两颗二进制同错，rc=0 无诊断）；该探针在语料 **0 命中**（负断言），其"不是死代码"由 `t464` 正面供证（新二进制 1 行／pre427 0 行）。
- **428 结案：走到"连声明都没有"那一步的 `[dynamic]` 幽灵不再拖没整份二进制**（代码批 `01edfb53`，6 文件 **+293/−82**，`codegen.rs` +47、新用例 `t465` 50 行、门禁 +1 步 26 行、`tools/dyn_binding_lint.sh` 88 行、ABI.md/锚点随行号重绑）。判据落在 `get_or_declare_function`（`codegen.rs:2683`）的最后一条 `add_function(.., External)` 之前：**名字以 `[dynamic]` 开头 × 不在例外表 × 模块里没有定义 ⇒ 绑到 419/422 那条按名抛异常的桩**（`dyn_member_missing_thunk`，`:3122`）。改前实拍＝t465 **rc=1 且不出二进制**（`"_\[dynamic]str\_\_nonesuch"` 一路到链接期），改后 rc=0 且 stdout `caught`＝异常真被外层 except 接住（负断言的正证据）。**例外表 4 条与 C 侧标签的集合相等交给门禁第 17 步当场核对**（`run_all.sh` 16→17 步、639→665 行），两条 FAIL 分支均实拍；表的活性＝删掉 `nth` 后 t411 立即 FAIL（`42`/`0`、缺 `False`、rc=1）。**语料位移 0（三层正证据）**：编译 rc 0/0、运行 rc 0/0、out 1/1、err 321/321、PY-A 幽灵告警 6/6 逐字相同；机理＝语料里这一格的幽灵都有 C 侧定义，改前不会在链接期出声。门禁 `gate_rc=1`（唯一红仍是存量 `t231/t233`）· official 194/194 编译、191/194 链接 · python_style **346/2/7/0**＝`ls t*.z` 355（净增 1＝t465、`xpass=0`）· corpus 40/40 · jit 176/376/552 segv=0 · dyn_binding 4 条不一致 0 · 锚点终态 258 可解析 / 定位失败 0、漂移 28/新 11/消失 10＝与 427 常驻读数逐项相同。**两条残留**：① 接收者完全无类型时成员调用走**裸名**的同族本批没收（`/tmp/b429/probe465.z` 实拍仍是 `"_nonesuch"` 链接失败）＝下面第 3 格；② 抛异常 ≠ 修语义，`copy`/`dropna`/`ffill`/`isin`/`to_dict` 的正解在掩码族与库侧（#112，动 `runtime/py_additions.c` ⇒ 等授权）。
- **429 结案：句柄身上没有类标签时的零参成员名不再读第 0 个字**（代码批 `0923c676`，4 文件 **+163/−60**：`gen.rs` +49、新用例 `t466` 54 行、`docs/ABI.md` 26/26 与 `abi_anchors.tsv` 34/34 随行号重绑）。判据＝`unique_zero_arg_property`（`gen.rs:898`，调用点 `:12610`，守 `!matches!(type_map[base], Some(Type::Named(..)))`）：名字在声明表里**唯一** × 除 `self` 外**零实参** ⇒ 发 `MirStmt::Call`；一名多主／参数表缺失／带参一律返回 `None`、旧路径一字未改（那条落空仍由 427 的尺子出声）。**改前实拍＝静默错值**：`ZETA_DBG_FA=1` 编 acceptance 语料，152 条两路反查都落空的读里 33 条是这三个名字（`index` 16／`columns` 12／`empty` 5），全在列门控与真值判断上；改后 `FA decls` **152→125**、`FA read` **529→502**、`columns` **12→0**、`empty` **5→0**、`index` **16→6**（那四个预跑读数本批当场复核＝逐项复现，见下面队列换位那条），IR 侧按符号数 call 站点 **+12/+5/+10＝27**（与 `FA read` −27 逐项对上；hunk 数当尺是假扩散——`@str_lit.N` 全局改名）、噪声底 0。t466 改前 `V missing a / M missing b / M missing zzz / done`（rc=0、stderr 0 行）→ 改后与 CPython 3.14.5 逐字相同，第三行反向对照 ⇒ 不是把这条读一律判成命中；边界行 `len(xs[i].columns)` 两侧同读 `N 2`（守卫挡在外面）。**主线 301 运行期位移 0**（两侧各 11 跑：compile 0/0、run 0/0、stderr 321/321、`[PARITY]` 74/74、`TRADE` 0/0、`1000000 -> 0 (-100.00%)` 各 1 行）；同侧方差入册不许当位移：119 行 rc=139 崩改前 1/11 vs 改后 2/11、改后另 1 次 rc=134 停 135 行（`numpy.vstack` 弱桩）、`rows` 12857/12858 同一颗二进制自己翻。门禁 `GATE_RC=1`（唯一红仍是存量 `t231/t233`，`run_all.sh:629`）· python_style **347/2/7/0**＝`ls t*.z` 356（净增 1 绿＝t466、`xpass=0`）· 诊断面 234 行/110 文件与 428 逐项相同（＝改前那条读静默的旁证）· official 194/194 编译、191/194 链接 · corpus 40/40 · jit 176/377/**553** segv=0（对 428 各 +1＝新用例进 sweep，ok 未回退）· diff 120/130 92.3% bad_case=0 · clean_checkout rc=0 rev=`27d3f8a0` · 锚点 `--rebind` 26 行/49 数 ⇒ 终态只读核对 rc=1：**漂移 28／新 11／消失 10／定位失败 0（258 可解析）＝与 426/427/428 常驻读数逐项相同**（对照在 `/tmp/b429/isohead` 隔离 worktree 取 HEAD 自基线：裸检出 28/11/12，补进未跟踪的 `runtime/aliases.inc.c` 后 28/11/10，第三次遇到同一差异）。**三条残留**：① "名字唯一"仍是按名猜接收者——语料那 27 个站点的真身没被证明是 `DataFrame`，比旧的"必读 0"接近真值但不是类型证据；② 跨语言句柄的属性读没动（`market_data_sources.py:316/317` 的 MultiIndex `raw.columns`）；③ 残 6 条 `index` 全在 `Named("Series")` 上（有类标签、类别里没这个字段）＝另一族，连 #156 那 146 条一起归到下面第 3 格。
- **430 结案：跨模块同名同宽的类别不再共用一条字段序**（代码批 `05fc9841`，4 文件 **+304/−167**：`codegen.rs` +140/−9、`t464` 夹具注释 +7/−1、`docs/ABI.md` 73/73 等行、`abi_anchors.tsv` 84/84 并按删掉的 10 条 358→348 行）。病因在**表本身**：`struct_defs` 的键是裸类别名（`struct_{variant}_{字段数}`，`codegen.rs:1467`）× `entry().or_insert()` 首写者胜 ⇒ 后到的那份字段序整条丢掉 ⇒ 读侧按错表解索引、写侧不受影响 ⇒ **字段互换、静默、rc=0**。修法三支：收集点按**构造方**（带模块的 MIR 条目名）多记一份布局 `struct_ctor_layouts`（`:74`/`:1495`）并登记撞车裸名 `struct_variant_collisions`（`:81`/`:1478`）；每函数 `collect_ctor_symbols`（`:1724`）把表达式 id 归到产生它的 `Call.func`；读侧 `:6940-6960` **只在撞车集合里**才改问"这个值是谁造的"，未撞车者一条指令没重排（取类型那一路刻意留裸名＝同宽两侧 LLVM 匿名型逐字节同型）。**实拍**：`t464` 改前 `y 3 4`（rc=0）→ 改后 `y 4 3`（CPython 一致、**expect 一字未改**）、4 行 `FA ctor-route` 点名两个构造者；**改前是稳定错**——同一补丁独立编译 5 次（`determinism_{1..5}.bin`，md5 五颗各异）首写者与输出 5/5 相同 ⇒ 429 结案里"随 `RandomState` 抖"那句猜测作废（从未实拍，本批推翻）。**语料位移 0**：`--emit-llvm` 两侧 md5 逐字节相同（`5f83d776ee40b9db0f4e371871f5fe14`、113,704 行）＋四次编译探针逐项相同（log 1407／`conflict` 0／`ctor-route` 0／`FA read` 502／`FA decls` 125，后两项复现 429）；**语料 stderr 逐字节比是假尺**（同侧方差 122–126 行 ≥ 跨侧 126/122 行，跨侧全落在噪声底内）。**泛化量申报：语料成员 0、夹具成员 1**（`conflict` 语料 0 次命中；负断言的正证据＝夹具那 4 行路由）。门禁 `GATE_RC=1`（唯一红存量 `t231/t233`）· python_style **348/2/6/0**＝`ls t*.z` 356（`t464` known-fail→passed 换格，用例数未增、`xpass=0`）· 诊断面 234 行/110 文件＋official 2/6 **与 429 逐项相同** · official 194/194 编译、191/194 链接 · corpus 40/40 · jit 176/377/**553** segv=0（与 429 逐项相同）· diff 120/130 92.3% bad_case=0 · truth/str/container/numeric/control 22/20/27/23/28 逐项同 · clean_checkout `rc=0 rev=05fc9841`（这一步这次跑在**含本批代码改动的 HEAD** 上，与 429 的口径不同，已点名）· 锚点 `--rebind` 搬家 74／拒改 44 ⇒ 手移 4 处（`:2325` 等五条同文本按设计拒改）＋只删被孤立的 10 条基线行（**不跑全量 `--bless`**，它会顺手抹平在场 24 条漂移）⇒ 终态 **258 可解析／定位失败 0／漂移 24／新 11／消失 10**，对 HEAD 的 28/11/12 **净修 4 条陈旧引用**（`codegen.rs:4001/4017/4552/4709`，越界顺带修、点名）新增 0。**四条残留**：① 取类型仍按裸名，只在"同宽"前提下无错，同名不同宽那一形未测；② 写侧书写序（#122）没收；③ 归因只到直接构造者，值经容器往返后回落旧路径、影响面未测；④ 三遍扫是经验上界（">3 跳"未实拍，机理上只会回落、不会读到更错）。**结构验收一条负结果**：`codegraph query struct_ctor_layouts -j` 返回 `[]` —— 图里没有字段级符号，接线与否得用 grep 计数（6/5/4 处），别把 `query` 空结果当"零引用"。
- **431 结案：降级成裸名的成员调用改为只读上报，不在出码处改绑**（代码批 `9f400338`，6 文件 **+199/−73**：`mir.rs` +16/0、`gen.rs` +31/0、`main.rs` +59/−1、`cli_semantics_check.sh` +21/0、ABI 两侧 30/30 与 42/42 等行重绑；锚点批 `cd4becab` 1/1+1/1）。病因（3.2＝`pyramid.md:102`，下游"修饰规则即链接与动态分派机制"`pyramid.md:160`）：`recv.member(...)` 两个降级臂（`gen.rs:11333-11337` 类型已知但候选集非单一方法、`:11339-11341` 接收者无类型）之后统一被追加 `_argc`（`:11492-11529`），出码侧最后一路 `add_function(…, External)`（`codegen.rs:3151`/`:3157`）⇒ **编译期零诊断、真正选定目标是链接器决定的**。守卫形状试过并**实测推翻**：在出码处按名改绑会把真调用打成异常 —— 反证 `clip`（68 名单之一 ∩ `zeta_runtime_c.o` 有 1 处定义 ∩ 链接后 `00000001000335a0 (__TEXT,__text) external _clip`）；守卫版 rc=1／stderr 903 行／stdout 0 B、改前版 rc=0／stderr 321 行／stdout 11 B，且三次 uncaught-raise 的 `code` 槽值互不相同（4383748960/4343144096/4374991712＝未初始化）⇒ 守卫回退，补丁只留档 `/tmp/b431/attempt431_rebind_guard.patch`。落地三支：`Mir.member_bare_calls`/`plain_call_names`（事实必须在 lowering 期搭车，出码期降级串与普通调用串逐字节同形）、`report_bare_member_calls`（`--report-stubs` 同族只读诊断，无链接无执行，空集整段沉默）、`cli_semantics` 7 条断言含沉默翼 2 条正证据（该步 73→**80**）。**归因表（68 名，可复现 `/tmp/b431/attribution.txt`）**＝5 类别方法体 + 3 运行时真实现 + 54 batch-156 注册表（**32 abort / 22 soft**，`login`/`logout` 手写强符号按源码读归入 soft、非机器判定）+ **6 链接宇宙无定义**（`abs alarm close mkstemp signal strftime`，各自 `(undefined) external _X (from libSystem)`）；Σ(名×体)=106、**0/68 同时以普通调用拼写**（⇒ `plain_call_names` 收窄语料成员 0＝负结果，字段保留：报告逐名点出、夹具证明开关）；与既有 16 条 `# stub:` 只重叠 2 名。**位移 0（双轴）**：`--emit-llvm` md5 与 430 尺逐字节相同（`5f83d776ee40b9db0f4e371871f5fe14`、113,704 行）＋主线 301 未碰出码判据。**锚点对 HEAD 净中性**（隔离 worktree 取 430 HEAD 自基线＝24/11/10 rc=1 ⇒ 终态 258 可解析／定位失败 0／漂移 24／新 11／消失 10 逐项相同，本批无新增漂移也无越界顺带修；现场抓到一条本批自己留下的假引用：`--rebind` 搬了兄弟条却把 `ABI.md:957` 的 `gen.rs:12958` 留在原位＝空行 ⇒ 手移 `:12987`＋`--bless-only` 源坐标点名＋按 tab 删孤立行；教训：**rebind 后逐条 `grep -n` 复核内容，别只看漂移数**）。**本批不新增 t4xx**（面上是 CLI 只读报告，号段 t45x–t49x 未动用，已申报）。门禁 `GATE_RC=1` 唯一红源＝常驻 `t231/t233`、python_style 348/2/6/0＝`ls t*.z` 356 未增、`md5 zetac`＝`cf8c2e88…` 收尾 touch 重编后逐字节不变（门禁那颗＝提交那颗）；clean_checkout `rc=0 rev=798f4ccf` **口径点名**＝跑在**本批代码之前**那条 430 记录 commit 上，与 430 那步（跑在含改动的 HEAD）不同。
- **432 结案：降级成裸符号之前先问 W 注册表（四条判据全过才改绑）**（代码批 `0cafbf52`：`pylib.rs` +71/−7、`gen.rs` +64/−50 净 +14、新用例 `t467` 43 行、`cli_semantics_check.sh` +33/−5；锚点批 `a7904b48` 搬 13 条引用/21 个数字）。判据＝`pylib::unique_method_for_bare_call`（`pylib.rs:375`，调用点 `gen.rs:11320`）：拼写在句柄间**唯一** × **非桩** × **arity（含接收者）与实参数相等** × 不在 `NAME_ROUTE_DENYLIST`（`:312`，与 429 的 B4 路线合并成同一份）；返回类型取表项（`registry_ret_type`，`gen.rs:14972`）而非猜 I64。缺一条就保持旧行为。**实测位移**：语料 bare-member 降级名 **68→66**（掉 `strftime`/`setdefault`）、**7 个调用点改绑真实现**（`_refresh_all_cached_stocks_impl` 2 ＋ `refresh_all_stocks_rqdatac_incremental` 2 ＋ `jq_wufu.check_a_share_weak_period` 1 ＋ `data_ops.log._detail_by_source` 2）、**2 处 `icmp slt`→`host_str_cmp`**（返回值定为 Str 之后的连带升级；diff 里另 6 对 icmp 只是 SSA 编号抖动，不算收益）。`t467_pre.bin` 编译 rc=0／运行 **rc=139、stdout 0B** → 改后 rc=0 打四行 expect（`e:` 那格是反向对照：列表接收者的 `strftime` 早在批次 145 就落 `zeta_vec_strftime`，本批没抢）。**9 对小夹具 pre/post IR 逐字节相同**＝这些夹具的接收者是 `[dynamic]`、被 429 的 B4 在上游接走，**打不到新臂**（不是改动没生效）；顺带暴露 **B4 不查 arity**（g4 两侧都发 `py_dt_strftime_3`），本批未动。**libSystem 桶 6→5 的归因当场查表**：`abs alarm mkstemp signal` W 表 **0 条**（缺 runtime 实现，非编译器 bug）、`close` W 表 **2 条**（`:56`/`:376`）⇒ 唯一性判据故意不猜。**对主线 301 位移 0**：语料两侧各 6 跑，stdout 都是 11B 堆地址；pre 6 次里 1 次崩（14,957B/119 行）post 0 次 ⇒ n=6 不足归因，431 那条被判"不可复现"的 139 读数本批自己出现 1 次 ⇒ 措辞改正为"**真实但偶发**"（历史正文不回改，见 roadmap 432 §七/§十.3）。门禁 `GATE_RC=1`（红源仍是存量：official link-only 3 ＋ 锚点非零）· python_style **349/2/6/0**（净增 1 绿＝t467、`xpass=0`）· **cli_semantics 80→87**（本批 +7 断言）· 锚点终态 258 可解析／定位失败 0／**24/11/10 rc=1＝对 HEAD 净中性**（未 `--bless`；隔离 worktree 读的 256/2/…/12 差异全是 `runtime/aliases.inc.c` 构建产物不在干净检出里）。**OPEN 净增 0**。
- → **队列换头（432 之后，只追加不改历史句）**：新头名＝**#161 `date` 那 2 个体仍降级**（registry `:285 W PyDate date py_dt_identity args=1` 四条判据看着全过 ⇒ 病因只可能是"不在 site A"（更可能走 `gen.rs:11335` 的无接收者类型那条，432 没给它加注册表臂）或实参数与 args=1 不合，**两种均未实测**）；第 2 格＝B4 路线补 arity 判据（g4 的 `py_dt_strftime_3` 为证）；第 3 格＝libSystem 余 5 名（其中 4 名要的是 **runtime 实现**、不是编译器改动，`close` 要的是接收者种类证据）；其后照旧 #156 → #142 → #134 → #117/#118 → #136；#145（`str_trim+24`，要动 `runtime/py_additions.c`）仍在等授权；**主线 301 第 1 步（0 笔成交）仍 OPEN**。
- → **430 头名行已兑现、正文由上面的结案条覆盖（只追加，不改历史句）**：把 `struct_defs` 的键从裸类别名换成带模块身份的名字，收掉 `t464` 那形字段互换。**第一格不是"键怎么写"而是"身份从哪来"** —— 读侧到达时接收者 `base_ty` 只有 `Named("Pair")` 一形（不含模块），要先把模块身份传到 `gen_mirs` 的收集点与读点。验收现成：`t464` 红着，修好即转绿（expect 一字不改）。损害量＝已实拍的静默错值（错字段、无诊断、rc=0），语料成员数当前为 0、夹具成员为 1。（实际落地的形状比"换键"更窄：键没动，另建一张带构造方的表 ＋ 撞车集合，只有撞车的裸名才路由——见上面结案条与 roadmap 430 §二。）
- → **431 头名行已兑现（＝上面那条裸名幽灵同族，正文由 431 结案条覆盖；只追加，不改历史句）**：431 收的是"可见性"这一格（68 名各自归宿查清了、守卫那条路判死），**降级本身没动** —— 这 68 个名字仍在链接期才定目标，但主线 301 三格（0 笔成交／选股数量／`final_value`）的病因都不在这一格（`pre` 侧 stdout 仍是 `1000000 -> 0 (-100.00%)`）。**队列换头**：新头名＝**那 6 个只在 libSystem 里找到满足的降级名**（`abs`/`alarm`/`close`/`mkstemp`/`signal`/`strftime`；四桶里唯一"链接对象宇宙无定义"的一格，也是 428 那条"绑一个按名抛异常的桩"的判据**没有"打死真实现"反例**的唯一一格 —— 其余三桶各有真定义／真方法体顶着，431 已实拍）；第 2 格＝#156（下面那格，内容未动），第 3 格起照旧 #142→#134→#117/#118→#136；#145（`str_trim+24`，要动 `runtime/py_additions.c`）仍在等授权；**主线 301 第 1 步（0 笔成交）仍 OPEN**。
- **头名＝裸名幽灵同族（428 §六.1 登记；430 结案后由第 2 格升头名，其余内容一字未改）**：接收者**完全无类型**（未标注形参）时成员调用下到**裸名**，428 的判据守不住（`[dynamic]` 前缀不在）—— `/tmp/b429/probe465.z` 实拍仍是 `"_nonesuch", referenced from: _probe` + `Error: "Linking failed"`，损害量与 428 同一格（整份二进制）。开工第一步不是写判据，是量"裸名幽灵在语料里真有几个"再定收口形状（428 的教训：语料位移可能是 0，但**改前会链接失败**这一条本身就限定了成员集）。
- **第 2 格＝`declarers=0` 那 146 条的按名生产者归因（任务 #156）**：尺子 427 已交付（一行 `FA decls` 给到 `declarers` 与逐条三元组），只差对 45 个名字逐个查 `self.X =` 声明点在仓内还是仓外。它决定这一族要不要第二条反查路径（属性表？外部对象的动态槽？）—— 若多数是外部对象属性，则继续放宽 `struct_defs` 判据是走错了方向。
- → **429 头名那条备料行已兑现、正文由上面的结案条覆盖（只追加，不改历史句）**：备料写的"上一段会话预跑读数未复核 ⇒ 不进台账"这一条，本批按 428 口径当场重测 —— FA decls 152→125、`columns` 12→0、`empty` 5→0、`index` 16→6 **四项逐项复现**；但同一段预跑里的**三处引用是错的**，已订正后入库：站点在 `backend/datasrc/derivations.py`（不是 `strategy/`）且实测 7 处 `61/65/71/75/81/84/91`（原写 4 处）、唯一性判据真身是 `crate::middle::pylib::method_by_unique_name`（调用点 `gen.rs:10299`，原写成 gen.rs 自己的方法、坐标 `:10255`/`:10267`）、Named 臂坐标 `:12551`→`:12552`。429 队列换位＝原第 2 格（430）升头名、裸名族上移第 2 格、#156 上移第 3 格（上面三条行首前缀已同步，正文未动）；备料里"另要登记外部句柄的限制"那格随 roadmap 429 §五.2 入册。
- → **426 头名「给取名反查落空这一路加出声」＝427 已交付（见本节 427 结案那条）**；**426 第 2 格「`struct_defs` 同键异序首写者胜」＝427 已判定并升级**：语料成员 0（探针实测），但夹具里存在同一形状的缺陷 ⇒ 换成 428 头名，"未证"改为"已证"。**428 队列换位（只追加，不改上面的历史句）**：428 实际收的是 `[dynamic]` 幽灵走到链接期的那一格（＝419/422 判据的补全，roadmap 428 §一），键冲突那一格**改号为 430**（roadmap 428 §一，任务 #155 同步）并退到第 2 格；头名＝429 动态接收者属性读。
- → **425 §七.1「回调／函数值实参的参数证据」＝本批定价判死（ROI 0）**：425 那组 census 读数（628 条目／55 宿主发 `FuncAddr`／106 个被取地址目标／103 个从不出现在调用点）**保留**，但真修在语料里够不着 —— 全语料只有 **6 个** `zeta_callN` 站点，喂那 5 个日程例程的调用点实参来自 `zeta_env_get` 别名而**不是** `FuncAddr` ⇒ "从注册点实参反推形参"没有可落的点。连带作废：425 §七.1 那句"这一格修好会同时消掉 425 残 11 处的未知接收者"。
- → **425 §七.2「declared-type 路线多候选取宽度最大者」（`codegen.rs:6679`）＝本批定价：语料成员 1**（同一份 `ZETA_DBG_FA` 日志里 `variant!=""` 且候选>1 的读点只有 1 个语料变体）⇒ 损害量太小，往后排。
- → **423 头名（`.items()` 的接收者＝当天日期）本批结案为"机制换对了、语料那一格没够着"**：423 写的"强烈指向**槽位复用/串号**（#130/#131/#142 同族）"**判死** —— 全程没有槽位复用参与，改的只有类型证据通道。读数保留并加细：37 条 `py_map_items` 点名的句柄是 37 个互不相同的连续整数 `0x4d0c…0x4d46`、跳过 `0x4d10/11` 等周末 ⇒ 逐日 +1 的交易日序数（`0x4d0c`＝19724＝t461 里 `dt` 的来源）；`py_map_items` 语料 IR 25 站点／23 宿主、`check_a_share_weak_period` 内恰好 1 个（423 读数当场复现）。**未证的一格**：28 处夹回里究竟哪一处 emit 了这 37 次点名 —— 点名句仍只有原语名＋`dladdr` 返回位置，423 §八.2 那条「让点名句带上用户函数名」的前置工作仍未做。
- → **424 的语料位移＝0（三层正证据，写在这里防下一批误读成回归）**：`--emit-llvm` 两侧 md5 逐字节相同、MIR 唯一 hunk 是 #9 的别名掷硬币（同侧两跑差 2 个 hunk），运行期四跑读数逐字相同。够不着的原因：语料里 `jq_wufu` 不是根模块 ⇒ 调用点全限定、从不带 `_N` 后缀。
- → **422 那格「下一格」的归因作废**（读数保留）：`market_panel.py`／`backtrader_engine.py` 语料 define＝0 成立，但「函数体内 import 不进闭包」是错的——`bt_helpers` 在语料里的入口 `jq_wufu_local.py:158` 正写在 `_run_backtrader`（:154）体内，它 IR define＝**8** ⇒ 体内 import 会进闭包。剩下的问题是「同形两条 import 为什么一条进一条不进」（`jq_shim.py:1025` 是相对 `from .backtrader_engine import`），未证。
- → **第 3 格＝`str_trim + 24` 读 `x0=0x3532`**（419 批的崩点；423 之后这条已不是当前死点——守卫版跑完不再崩，但它仍在 #145 的账上：packed str 的生产者还没定位）。生产者三条负结果已入册，真修大概率要动 `runtime/py_additions.c` ⇒ **等用户授权**，未获授权前不作为头名。
- → **第 4 格＝#134 `GroupBy.__len__` 恒为 0 / #117 动态键 `.get(k, default)` 语料 7 种形态仍落 `_get`**（两个都不等授权）。
- → **`[dynamic]` 幽灵名族（422 已定价完，从头名摘掉）**：6 个具名桩的调用点全在晨间崩点**之后**或死代码里，语料实际跑到的点名行数为 0 ⇒ 位移 0。逐名读数（`copy`×2 在 `data_cleaning.py:302/304`、`dropna`/`ffill` 在 `bt_helpers.py:62/77`、`isin` 在 `:302`、`to_dict` 在 `jq_wufu.py:455`）**订正**了 419/420 的「`copy`×4 头名」与 §1 旧句「`ffill`/`isin` 归零」；真修＝掩码族（`zt_vec_cmp` 在 `runtime/py_additions.c:1013`，任务 #112）⇒ 等授权，不在语法层打补丁。
- → **`rows` 同一个二进制跨多次运行结果抖动**（跑 5 次出 4 种值 12854–12858；420 又录到 119/135 位置的抖动；归因在运行期/库侧，不在 codegen）。
- → **原「名字→字段的全局扫描选错 struct」那格＝425 已收（见本节第一条）**：坐标曾三次移位（418 记 `:6535`、419 后记 `:6665`、424 记 `:6452`/`:6700-6708`），425 入库后是 **兜底点 `codegen.rs:6701-6704`、夹回点 `:6754-6757`、调试旋钮 `ZETA_DBG_FA` 在 `:6712`**；队列下一格从"`("", 2)`"换到它的上游（本节第 2 条）→ #142（`gen.rs:3987` 读点无守卫）→ #134（`GroupBy.__len__` 恒 0）→ #117/#118 打印族 → #136 漏点族定价。
- **433 结案：字段读的兜底不再是写的答案 —— `StructFieldStore` 改为复用 `resolve_field_slot`**（代码批 `d5b95c26`：`codegen.rs` +244/−213＝净 +31、新夹具 `t468` 47 行；锚点批 `86970a98`）。写侧改点 `:5826`、读侧 `:6992`、判定体 `:6126`（夹回在返回前做完，`:6344`）。**位移**：套件 IR **0**（360 份逐字节比，唯一差异 t23＝418 在册的发射序噪声；38 条字段 `store` 偏移改动 0 —— 这一形状套件原本无成员），**语料 3 条指令／2 条源语句**（`nautilus_backend.py:113` 写偏移 16→32、`jq_wufu_local.py:336` 72→0＝改前那笔是 56 字节越界堆写）⇒ 434 段以来第一条真改到语料落点的批；**主线 301 位移 0**（n=6 两侧主道逐字全同）。**边界入册**：构造期 `self.x=v` 走 `StructNew`（另一套序），只有构造后的赋值才发 `StructFieldStore`。**队列头名换成 §九 三条残口的第 ① 条**（写 `@property` 名字无 setter 路由——读侧 property 前奏在写侧没有对应物），第 2 格＝#156（152 处 stand-in，写侧现在与读侧一起错），第 3 格＝#142；OPEN 净增 0。
- **433 收尾当场把上一行的头名量掉了（更正随本行入库，上一行原文不回改）**：`@property` 名字上的赋值这一路，语料成员＝**0**。两把尺：① 粗筛（按文件配对）在 `~/source/quant/REasyQuant` 得 **2** 个唯一成员（原表 7 行，另 5 行是 `.worktrees` 里同一份源码的副本）（`tests/test_pi_agent.py:94 stdin`、`backend/engines/jq_shim.py:318 name`）—— 都不是编译器看到的形状：前者在 `tests/` 不在编译面，后者 `self.name = name` 住在 `_FundField.__init__`，而那个 `name` 的 `@property` 在**同类之外的另一个类体**里（`:195`）；② 按类别作用域重扫（排除 `.worktrees` 副本）＝**308 个 py 文件 / 28 个 `@property` 定义 / 命中 0**。扫描器自己也被反证过：喂一份"类内 property + 类内赋值"的合成文件 ⇒ 命中 **1**（`pos.py:7 val A`），所以 0 不是漏配的假负。**⇒ 队列头名换成 #156**（`("", 2)` stand-in：152 处读全落索引 0、其中 146 处的字段名无人声明；433 之后写侧继承同一条兜底 ⇒ 读写现在"对称地一起错"，这一格的损害量比 property 写那一格实在）。#163 留作低优先登记（成员 0）。
- **434 结案：构造之后的属性有了自己的槽 —— 病因是 `struct_defs` 的收集面，不是名→槽**（代码批 `a5418a77`：`codegen.rs` +299/−10＝8413 行、新夹具 `t469` 49 行；锚点批 `49a92075`）。追加表 `struct_ext_layouts`（`codegen.rs:114`、收集遍 `:1546-1597`、探针 `FA ext+ :1587`）＋两处分配点加宽（`:5952`、`:7097`）＋`resolve_field_slot` 夹回前先问追加槽（`:6361`→`ext_slot :6295`）。语料侧 15 站点／去重 11 名／4 条布局落地，7 条写从偏移 0 挪到 328~376；**下一批头名换成三条未收口**：① 126 行替身读为何离开探针路径（未定位，本批不记收益）② 写 `@property` 名字无 setter 路由（#163）③ 核对器两条缺陷（#52）。
- **435 结案（度量口径批，无代码改动）：434 那对"改前/改后"trace 其实差在二进制所在目录 —— 126 行替身读的位移是假的**（夹具批 `ab48e72f`：`t469` 头注 +13/−8 全在注释、expect 三行未改；记录批＝本提交）。**上一行的头名 ① 就此结案**：`pylib` 库面基从**二进制位置**向上找 ⇒ 434 的改前那趟跑在 `/tmp/b434/`（trace 有 W1006 出声）⇒ `struct_defs` 少 3 条布局（`DataFrame_1`/`Series_1`/`GroupBy_2`）⇒ pandas 访问器名退成替身读（`columns 60/empty 30/index 19/values 10/loc 4/iloc 2/dt 1` 合计 126，反向对上 502 = 598 − 126 + 30）。同目录重测（`target/release/zetac_pre434` md5 `d7d44a9a…` vs `zetac` md5 `e3267a47…`）：`FA read` 502／替身 125／`FA write` 126／替身 20／`FA decls` 145（`declarers=0` 138、去重 48 名）**两侧逐条相同**，按 `(role,variant,field,field_count,base_ty)` 多重集比较**差异集为空** ⇒ 434 既非收益也非丢读；污染侧同命令复现 271 替身行 ⇒ 位置决定、非偶发。434 §五 的 IR 读数**不受影响**（重跑两份与入册 md5 `e5470dd9…`/`fb201a62…` 逐字节相同）⇒ 只有 trace 一侧脏。**主线 301 位移 0**（本批无代码；434 的"未判定"只撤销读侧那一支，写侧影响仍未测）。门禁 `GATE_RC=1`（`/tmp/b435/gate435.log`）：python_style 351/2/6/0 与 434 逐字同、official 194/194 与 191/194、语料 40/40、jit 176/380/556 GREEN、诊断 2/194·6 行与 112/238 同、`comment_drift` 复述 0、`clean_checkout rc=0`（rev `cc0feda1`＝本批之前）⇒ **零新增红**；无 `.rs` 改动 ⇒ **无锚点重绑**。**#156 现口径入册**（同目录那组数），只剩"427 原数 152/146 的口径未复核"一格。**队列头名换成 ③ 核对器两条缺陷（#167/#52）**；新登记一条损害量实测的缺陷候选：库面随二进制位置变化＝#76/#351 没收的**位置维度**（缺库面时静默错值只出声一条 W1006）。OPEN 净增 0。
- **436 结案（证据工具批）：锚点快照的键里少一个区间终点 —— 撞键的 `--rebind` 改判拒收、"同键多义"进 rc**（代码批 `fb6030dc`：`tools/check_abi_anchors.py` **+98/−6**；记录批＝本提交）。上一行的头名 ③ **收一半**：根因是 `collect()` 写 `snapshot[(rel, start)]`（无终点）⇒ 306 条引用塌成 **259 个键、47 条从未进过比较、8 个键有两种内容**，每个这种键上至少一条引用带着**不是它自己**的核对结果，而打印那句是"锚点全部对上"；434 那条 `:679→:1392` 撞键正是它在改号路径上的表现（354 当时把"一个键只能有一处引用"当**夹具前提**手写守住，本批变成机器判据）。修法两支＝`rebind()` 落点撞键 `[拒改]`＋保留原样、`key_conflicts()/print_conflicts()` 三条路径逐组打印并把 rc 判红。**红→绿实拍**（`/tmp/b436/fixture.py`，行号当场算）：改前 E1 `搬家 1/拒改 0`→rc=**0**、键 2→**1**、文档被改；E2 rc=**0** 且打印"全部对上" ⇒ 改后 E1 `搬家 0/拒改 1`、rc=1、键 **2**、文档未改；E2 rc=1 并列 `:2` 与 `:2-4`。**真文档净中性**：漂移 23/新 11/消失 10/基线 258/rc=1 逐项未变，只多一类读数；`--rebind --dry` 拒改 33（10 多命中＋3 零命中＋10 同键不同长度＋10 消失）、**新护栏触发 0 次**；基线 md5 未变、`--bless` 仍 E1005（33 条会被抹平，rc=2）。门禁 `GATE_RC=1` 唯一红源仍是存量 `py_fail=2`（t231/t233，419–435 同格）⇒ **零新增红**；无 `.rs` 改动 ⇒ 无锚点重绑，核对器也不在门禁里（`grep -c check_abi_anchors tools/run_all.sh`＝0）。**主线位移 0**（未动编译器）。**#167 余项入册**：`find_snippet_lines` 宽容假行号那条未动（**"漂移 0"≠"引用行号对"**）、真修要把终点写进键（连锁改基线格式与 `--bless-only` 语法）、`--bless` 的冲突打印排在 E1005 之后、表头改口径 ⇒ 老 grep 会落空。**队列头名换成：434 写侧位移的主线运行 A/B**（两颗同目录、n≥6/侧，判 301 三格里有没有真位移——434 §六 那句"未判定"是主线唯一悬着的一格，且不需要新二进制）；第 2 格＝#156 只剩"427 原数 152/146 口径未复核"；第 3 格＝#167 余项①（假行号那半，S）；#168（库面随位置变化）要先裁"要不要跟着二进制走"，#145/`str_trim` 族仍等 `runtime/py_additions.c` 授权。OPEN 净增 0。
- **437 结案（度量批，零跟踪文件改动）：434 的写侧位移把函数在镜像里挪了 76 字节，把崩点留在原地 —— 301 三格 0 位移**（记录批＝本提交）。两颗同目录（`target/release/zetac` md5 `e3267a47…` / `zetac_pre434` md5 `d7d44a9a…`，都与 434/435 入册值逐字对上）各建一颗 acceptance 二进制：**都 586,768 B、`cmp` 在 char 217 differ** ⇒ 写侧位移真进了链接产物（434 §六 缺的那层证据到手）。n=6/侧交替序：主道 8 次（pre 5/post 3）读数八次全同＝`[PARITY]` 74／`TRADE` 0／`失败: 1` 74／`[晨间]` 37／`动态池更新失败` 37／stderr 321 行／末行 `1000000 -> 0 (-100.00%)`、stdout 1 行＝地址样整数（#117 未动）；崩 4 次（pre 1/post 3）都 rc=139／stdout 0 行／stderr 119 行。**崩点身份取 macOS `.ips`**（423/430 判据）：`str_trim+24 ← backend_datasrc_code_conv__normalize_to_jq+16 ← MarketDataFetcher::fetch_stocks+3080` —— pre 那 1 次与 post 那 3 次**符号/symbolLocation/帧序三项逐字相同**，只有镜像内偏移搬家（`str_trim` 250904→250980、`normalize_to_jq` 50624→50664）⇒ 位移改了函数**位置**没改**崩的指令**。1/6 vs 3/6 **算过**再说不是位移：Fisher 精确 单侧 p=0.9394／双侧 **p=0.5455** ⇒ n=6/侧分不出本侧方差，按 432 §十.3 措辞入册。**⇒ 434 §六 那句"未判定"就地闭掉：写侧位移不移动 301 三格**（"修好了"栏照旧空）。门禁未重跑并点名理由：`git status --short` 只有 ` M .ouroboros/work.md`（不是我的）＋既有 `??` ⇒ 零跟踪改动，生效读数仍是 436 那颗 `GATE_RC=1`（红源＝存量 t231/t233）、无 `.rs` 改动⇒无重绑。工具坑两条：① **lldb 打不出这个偶发崩**——`-b -o run -o "bt all"` 连跑 20 次（每侧 10）**20/20 `exited status=0`**、每份日志 329 行且 `回测完成` 在 ⇒ 走完全程，"调试器下未复现"≠"崩不可达"也不许当"改后不崩"；② `.ips` 里 `symbolOffset`/`address` 恒 0、基址随 ASLR 逐次变 ⇒ 取 `symbol`+`symbolLocation`+`imageOffset` 三件。**头名换格（顺手从 435 那份同目录 trace 里量到的，`/tmp/b435/post435.err`）**：145 条 `FA decls` 里 `declarers=0` **138 行／45 名**、`declarers>0` 只剩 **7 行**（`trading_dates`×2、`routines`×2、`bar_types`×3＝427 那对 `NautilusBackend`(11)↔`NautilusJqStrategy`(9) 分歧 ⇒ 判据到头）；**但配对的 `FA read` 行里有 10 条接收者 declared type 已经拿到了却仍回 `("", 2)`** —— `_backend` 6 行 `base_ty=Some(Named("backend_strategy_nautilus_backend__NautilusJqStrategy"))` + 1 行 `Named("Any")`、`history` 4 行 `Named("backend_engines_bt_helpers__ValueRecorder")`（另 `positions` 7 行 `base_ty=Some(I64)`＝425 残留 ②）。已核到的结构事实＝`struct_field_decls`（`codegen.rs:6188`）**只扫 `self.struct_defs`**，而 `resolve_field_slot` 另外问 434 的 `struct_ext_layouts`（`:6285`/`:6301`）⇒ 读侧按名反查与写侧追加表不对称；**"declared type 为什么没用上"的因果未证**，下一步先读接线点再动手（登记为任务 #170）。队列第 2 格＝#156（427 原数 152/146 口径仍未复核），第 3 格＝#167 余项①；#168 要先裁基、#145 等 `runtime/py_additions.c` 授权。OPEN 净增 0（#169 闭、#170 开）。
- **438 结案（代码批 `5e69d584`，5 文件 +408/−89）：函数体里的 `class` —— 兄弟方法调用绑回自己的合成符号，无定义成员改由调用点抛异常**。机制＝`lower_closure` 在 hoisting 臂发布别名**之前** clone 了父级 `closure_vars` ⇒ "先写后定义"的 `self._jq_bar_types()` 当趟永不绑，必须由 ImplBlock 收尾补一趟窗口改写（`gen.rs:3247` 记窗口、`:892`/`:935`/`:987` 三个新函数）。**两处根因分开撞出、各配各的实拍**：① 判据问了 `func_ret_types`＝**调用点证据表**（扫描器每见一个 `recv.member` 就建键，未定义成员也在里面）⇒ 幽灵臂永不触发，`ld: Undefined symbols: _Inner__notsdefined` 实拍在案 → 改问 `generated_mirs` 的**定义名**；② 第一版只扫顶层 ⇒ 漏 `If{then,else_}`/`For{body,else_body}`/`While{pre_cond,body,else_body}`（`Vec<MirStmt>` 只嵌这三处，`src/middle/mir/mir.rs:9,180,181,220,223,230,231,233`），`for x in self.bar_types(): self.notsdefined(x)` 一字未改 → 递归三变体。**没去 `runtime/unavailable_stubs.c` 加名**（428 已拒过那条创可贴，且该文件不在本侧所有权内）。**语料侧读数（同目录两颗：改后 `504e8e32…`／改前 `e3267a47…`）**：`backend/strategy/nautilus_backend.py` 单文件编译未定义符号 **4→2**（少的两个正是 `_NautilusJqStrategy___jq_bar_types`/`_NautilusJqStrategy__subscribe_bars`，剩的是 #155/430 那对顶层类别既有名——本批只改发布了别名的窗口，顶层 `impl` 一字未动）、点名 0→**1**（`[dynamic]_Impl::subscribe_bars` 无定义 ⇒ 该调用点改为抛异常）；驱动 `_drv_accept_409.py` n=6/侧：`无定义` 7→8、PY-A 246→247 行、cerr 34,423→34,528 B、**两侧各 6/6 一致**；**运行侧不判定**（两侧都 2/6 崩 rc=139，按 437 的 Fisher 结论这分不出方差）。快子集门禁＝353/2/6/0（＋1＝t471）、诊断 239 行/113 文件（＋1＝t471 那条点名）、其余与上轮逐字相同 ⇒ 零回归；锚点 `--rebind` rc=2＝HEAD 对照 rc=2 ⇒ 存量红非本批伤。**登记四条实测**：① **`try:` 包着的幽灵调用＝整个方法体静默丢**（改后 `run_rc=0`、stdout 只剩 `fwd 4`，`no-raise`/`caught` 都没打；改前是响的＝链接失败。对照：同一幽灵调用在 `for` 体里（t471）按名抛 ⇒ 只有 try 那一支吞；**语料成员 0**——`_Impl.on_start` 的幽灵调用在 `for` 里、`on_bar` 的 try 包里是 `routine(...)`）⇒ **换成了头名候选**；② `%` 格式化在嵌套类方法里丢插值（`print("inner-n %s" % n)` → `inner-n `，逗号形正常）⇒ 归 #45 族，**并作废本批早先那条"带基类的嵌套类丢 print"读数**（就是被 `%` 骗出来的）；③ 别名表按窗口 `split_off` 隔离但 `closure_vars` 键是裸成员名 ⇒ 两个同名嵌套类串线风险（形状未量，#155/430 同族）；④ `run_all.sh` 只在 `[[ ! -x "$ZETAC" ]]` 时建 zetac ⇒ 改完源码不重编就取旧二进制读数。**三条未量**：`MirExpr::FuncAddr` 上的 `{ty}::method` 拼写（本批只改 `Call`/`VoidCall`）、非嵌套路径的 `&mut self` 接收者类型、`unavailable_stubs.c:148-153` 那两个名是否已无人引用（要 `--report-stubs`＋授权）。**头名换格**：438 号原派 #170，被这格吃掉（硬链接失败 > 静默降级），**#170 原样留队头未动**；新队头＝上面 ①。OPEN 净增 0（无关闭项；①～④ 折进 §1 队列文字，不另开号）。
- **439 结案（度量口径批，代码批 `19ce411f`＝4 文件 +58/−48）：替身字段读的那把尺打在被解析状态的中途 —— 437 那条"10 条 declared type 已到手却仍回 ("",2)"作废，真口径 125→107**。`ZETA-DBG FA read|write` 在 `codegen.rs:6512`，追加槽那步在 `:6580` **之后** ⇒ 探针行的 `variant=""` 不是最终布局；把那 10 条逐条对到收尾：`_backend` 6 条经 `variant="_Impl" -> ext slot 0 of 2` 解出、`history` 4 条两颗二进制都解出 ⇒ **#170 的因果问题本身就是尺读错**。改法＝收尾处（`:6615`）另打 `FA {role} final field= variant= field_count= idx=`（与探针行一条对一条：502 读／126 写），旧行保留＋注释点名中间态；先查过引用面（四份夹具只在头注提到该探针、`tools/`/`docs/`/`src/middle` 唯一命中是 `gen.rs:1056` 一句注释）⇒ 无基线解析其格式。**补记 438 未报的一处位移**：`-> ext slot` 23→29，多出的 6 行全是 `_backend`。**真口径现量**：读 502＝替身 107＋非替身 395、写 126＝替身 9；107 按类型＝`I64` 72／`Named("Any")` 7／**具名类别 24**（`Series.index` 6、`FinanceModule.FUND_NET_VALUE` 5、`ValueRecorder.strategy` 4、`DataFrame.attrs` 4、其余 5）／`Str` 2、`PyDynamic` 2 ⇒ 下一格是"类别已知但名字不在声明格也不在追加表"那 24 行（与 #163 同形嫌疑，**成因未测**），不再是 437 说的那一类。**另登记一条度量完整性形态**：同颗二进制同份输入，链接产物 586,696 → 603,208 B（三趟尺寸稳定、md5 互异）⇒ "产物尺寸变了没"不能当位移判据。**主线位移 0，正证据**＝两侧 `--emit-llvm` 的 IR 逐字节相同（4,398,143 B、md5 `bdbbbe86…`），整体在 `env_flag` 臂内；运行侧未取 A/B。**头名换格**：#170 结案，队头＝438 登记的 ①（try 体幽灵调用＝整方法体静默丢）。OPEN 净增 0（#170 关，两条余项折进 backlog §4.1 同一行）。

**反向队列（Agent-2 负责）**：从历史欠账尾巴往回收，队列固定，两个子队列依次吃：

1. **解析器截断十族，按丢行量降序**（docs/ABI.md 附 B#10，批次 325 时点状态）：`static mut` 局部声明（丢 357 行）→ `s[i..]` 开区段（230，任务 #39）→ selfhost（158，未定位，先出定价批）→ 函数体内 `use a::b::C`（85）→ 块体闭包实参（58）→ or 模式构造子（62）→ `for i: usize in`（36）→ 单段 `import pkg;`（19）→ 常量表达式数组（14）；
2. **backlog 里最老的 OPEN 项**（按编号升序，S/M 级优先）。

两条队列一个吃「当前损害」、一个吃「历史欠账」，文件面不相交（见 §2），天然无冲突。


## 2. 所有权矩阵（禁手清单）

| 资源 | Agent-1 | Agent-2 |
|---|---|---|
| `src/middle/**`、`src/backend/**`、`src/main.rs` | ✅ 独占 | 🚫 |
| `src/frontend/parser/pattern.rs`、模式解析相关 `stmt.rs` 臂 | 🚫 | ✅ 独占 |
| `runtime/*.c` | ✅（#117 若需 C 侧） | 🚫 |
| `tests/python_style` 号段 | t45x–t49x | t50x 起 |
| `docs/ABI.md`、`tools/baselines/**` | ✅（含 `--rebind`） | 🔒 只读 |
| `roadmap.md` 尾部追加 | ✅ | 🚫（写自己台账，合并时誊入） |
| `backlog.md` 登记行 | 各自只写自己批次产生的行 | 同左 |
| 推送 `agentic bootstrap` | ✅ 唯一 | 🚫 |

## 3. 同步方式（简化版，2026-09-26）

- 旁路侧：只管往 `cleanup` 提交自己的批次（fix + docs 双提交照旧）。
- 主线侧：每批提交完（工作区干净时）`git merge cleanup` + 推送；顺带把旁路台账里的记录誊进 roadmap.md（批次号按时间归位）。
- 旁路侧在主线合并后 `git rebase bootstrap`，继续下一批。
- 共享文件（本文件、AGENTS.md）两边都可编辑，但**重排/恢复时必须保留另一班写的段落**——424 前后发生过一次按 HEAD 复原丢掉对侧行的事，别再犯。

## 4. Agent-1 主线台账（qwen 维护；bootstrap）

| 批次 | 分支 | 主题 | 门禁读数 | 状态 |
|---|---|---|---|---|
| 413 | bootstrap | `dict[...]` 注解保留 + 四处解包；主线 301 第 1 步有答案（codes=0 非缺列） | 194/194 · 333/2/6 · 40/40 · diff 92.3% | MERGED（已推送） |
| 416 | bootstrap | 主线 301 第 3 步定位：0 成交病因＝模块 `def` 不进环境表、当值读回 0（修法当时判死回退，登记 known-fail） | 194/194 · 335/2/**7** · 40/40 · diff 92.3% | MERGED（已推送 `72462832`+`66d61724`） |
| 417 | bootstrap | `FuncAddr` 值读上门（`gen.rs:3890`/`:12290`，+23）+ **更正 416 的退化归因**：三格塌＝出码轮次（HEAD 重编照样塌），抖动在 MIR 之后的发射阶段 | **rc=1**（`gate417b.log:142`）· 336/2/6 · 194/194+191 · 40/40 · jit 176/365/541 · diff 92.3% · ABI 32/11/3（`--rebind`） | MERGED（已推送 `9b7246c7`+`7c0f16cb`） |
| 418 | bootstrap | 出码非确定性两处落地：`codegen.rs:1606` alloca 序排序 + `:66` `struct_defs`→`BTreeMap`（代码批 `0f5bec05`，7 文件 +291/−161）；门禁第 16 步 `tools/emit_stable.sh` 上门；**两条负结果入册**：两个夹具盯不住源 B（把 BTreeMap 退回 HashMap 照样全过）、先前口误的 ABI 读数实为 **rc=2**（裸行号＝#52 的账） | **rc=1**（`/tmp/b418/gate418.log`，唯一红移到 `run_all.sh:605` 存量 `py_fail=2`）· emit_stable 2 夹具/违规 0 · 336/2/6/0 · 194/194+191 · 40/40 · jit 176/541 segv=0 · diff 120/130 92.3% bad 0 · clean_checkout rc=0 rev=`738618b8` · ABI 32/11/3（`--rebind` 77/77×2） | 代码批已提交 `0f5bec05`；记录批已推送 `969f8ec5` |
| 419 | bootstrap | 动态接收者的裸成员名兜底绑到类别名 ⇒ struct 体套在 vec 句柄上崩（`resolver.rs:1014-1026` 双注册 × `get_or_declare_function` 两处裸名兜底）。判据换成问 MIR 清单（`class_method_members`）：是别名就改绑 `zeta_raise(1)` 跳板；**两条负结果入册**（一律抛 ⇒ t411 回归；`count_basic_blocks()>0` ⇒ 查幽灵名那刻别名还是 0 块声明，且只守一趟输出逐字节不变）。代码批 `92e05c11`（4 文件 +321/−155） | **rc=1**（`/tmp/b419g/gate419.log`，唯一红源仍是 `run_all.sh:605 py_fail=2`）· 337/2/6/0 ＝ `ls t*.z` 345 逐字对上 · 194/194+191 · 40/40 · jit 176/**542** segv=0 · diff 120/130 92.3% · clean_checkout rc=0 rev=`969f8ec5` · ABI 定位失败 4→**0**、漂移 32→**23**（`--rebind` 74 行/147 数 + `--bless-only` 点名 9 条；终态 23/11/10 rc=1）· **主线位移：语料 134→135 行 rc=139→139，新增行＝越过 try/except 的 `[WARNING] jq_shim: 动态池更新失败: 1`；新崩点 `str_trim+24` / `x0=0x3532`** | 代码批已提交 `92e05c11`；记录批已推送 `92e05c11`+`26504a95` |
| 420 | bootstrap | 动态接收者的 `.get(<str 键>)` 下到 `array_get`（load＝`base + key*8`，字符串键＝拿键地址当元素下标 ⇒ 确定性内存不安全，改前 rc=139 实拍）；改为复用静态路径的 `MirStmt::DictGet`（`gen.rs:10423-10446` +24 行，负对照整型键逐字节未变）。新用例 t456。**一条弯路负结果**：直接换名表符号 ⇒ LLVM verifier 拒 `map_get` 的 `i64(ptr,i64)`（只有 DictGet 那条发射路插 `build_int_to_ptr`）。代码批 `24514e84`（4 文件 +77 −24） | **rc=1**（`/tmp/b420/gate.txt`，唯一红源仍是 `run_all.sh:605 py_fail=2`）· 338/2/6/0 ＝ `ls t*.z` 346 逐字对上（净增 1＝t456 无回归）· 194/194+191 · 40/40 · jit 176/**543** segv=0 · diff 120/130 92.3% · clean_checkout rc=0 rev=`26504a95` · ABI 终态 23/11/10 rc=1 定位失败 0（`--rebind` 各 13 条，与 419 终态逐字相同、ABI.md 1032 行不变）· **主线位移 0（有正证据）**：改前二进制仍死在 `fetch_stocks`（`str_trim+24`），在本批改到的 6 个 IR 函数上游；语料 IR `@map_get` 225→239＝14 个调用点换写法 | 代码批已提交并推送 `24514e84`；记录批随后 |
| 421 | bootstrap | 动态接收者的 `.get(k, default)`（argc=3，名表零臂）下成 `map_get_default`：语料 IR `get_3` 32→0、`map_get_default` 27→61、LLVM bare `@get(` 34→0。**两笔代码提交**：`04a6aeeb`（gen.rs +39 / t457 / ABI 两侧，4 文件 +111/−24）+ `1bea38b9`（`receiver_has_typed_route` 判据 +24 / t458 / ABI 两侧，4 文件 **+72/−24**）。**第二笔是自己抓自己的回归**：第一版抢掉 batch 288 的 2 个 PyJson 活点（`py_json_get_default` 4→2）⇒ 运行期形状守卫喊 rc=134，t458 三进制实拍锁住（pre 绿 / post2 红 / post4 绿）。两条判据负结果入册：① 动态键放开＝静默打 default（宁留 abort）②`DataFrame::get` 那 2 点改前也落 bare `@get`（`pylib/pandas.z` 无 `def get`、`nm` 无 `_DataFrame::get`）⇒ 非回归、语义未判 | **rc=1**（`/tmp/b421/gate_guard.txt:150`，逐条排查后唯一红源＝`run_all.sh:605 py_fail=2` 常驻两例）· **340/2/6/0 ＝ 348**，与 `ls tests/python_style/t*.z` 348 逐字对上（净增 2＝t457+t458，零回归）· 194/194 compile（191/194 link，3 条 link-only 点名）· 语料 40/40 · jit 176/**545** segv=0 · diff 120/130 92.3% bad 0 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 73/ignore_rules 19/mbvar 22 全 0 违规 · comment_drift 0 · emit_stable 2 夹具/0 · clean_checkout rc=0 rev=`04a6aeeb` · ABI 终态 23/11/10 rc=1 定位失败 0（两次 `--rebind` 各 11 行/13 行，ABI.md 仍 1032 行）· **主线位移 0（正证据）**：pre 135/135/120/135 rc=139 ↔ post4 同读数、末条日志同行 ⇒ 仍卡 `str_trim`（#145）· disclosure：门禁首行 W2003 喊 `tokio_runtime.o`（` M`，非本批产物）比 `.c` 新 ⇒ 运行期目标文件是改前的；注释订正后重编，`zetac` md5 逐字节不变（`b5f1abc6…`）⇒ 读数对应 `1bea38b9` 源码 | 代码批已提交 `04a6aeeb`+`1bea38b9`；记录批随后 |
| 422 | bootstrap | 幽灵桩**逐名点名**（2.3 可观测性）：419 的桩按 argc 共享 + 全仓抛掷恒 code 1 ⇒ 一次跑答不出"这个调用点抛没抛"。改为每个 ghost 一个 `zeta_dyn_missing_<名>_<argc>` → 新 `zt_dyn_member_missing`（`unavailable_stubs.c:81`，点名一次、**每次调用都抛**、不 abort ⇒ 419 契约一字未改；`codegen.rs:3064` + 两个调用点 `:2744`/`:2966`）。代码批 `8c5b9f79`（6 文件 +192/−85，含 `tokio_runtime.o` 重建与 ABI 两侧 36 行重绑）+ 新用例 t459（48 行，钉"去重只吞消息不吞抛掷"：5 抛/2 点名/6 行 stdout 逐字同改前）。**定价结论＝位移 0，该族从头名摘掉**：语料 6 桩点名后自报身份 —— `data_cleaning.py:302`(`isin`/`copy`)+`:304`(`copy`) 的宿主 `clean_market_ohlcv` 全 IR **calls=0**（`market_panel.py` define=0）、`bt_helpers.py:62`(`dropna`)/`:77`(`ffill`) 唯一调用方是 `__run_backtrader`（`backtrader_engine.py` define=0）、`jq_wufu.py:455`(`to_dict`) 2 个调用点全在**午间** routine；改后二进制两次跑 `rc=139 errlines=135` 且点名行 **0**。**三条订正**：①419/420"头名＝`copy`×4（`jq_wufu_local.py:166/190/248/301`）"作废（那 4 个绑 pylib 真定义，`call @"DataFrame::copy"` 语料 37 处，裸名 `@copy` 的 define 反而 0 调用点）②§1 旧句"`ffill`/`isin` 归零"不准（各 1 幽灵；`dropna`×15 是静态绑定数）③"bt_helpers.py:67 的 copy 是幽灵"订正为真绑定。方法坑入册：带 `::` 的 LLVM 符号在 IR 里带引号（`@"DataFrame::copy"`），`@[A-Za-z0-9_:]*` 搜族名＝假负。真修＝掩码族（`zt_vec_cmp` `py_additions.c:1013`，#112）⇒ 等授权 | **rc=1**（`/tmp/b422/gate_guard.txt:149`，唯一红源＝`run_all.sh:605 py_fail=2` 常驻两例）· **341/2/6/0 ＝ 349**，与 `ls tests/python_style/t*.z` 349 对上（净增 1＝t459，零回归）· 194/194 compile（191/194 link，3 条 link-only 同 421 逐字未动）· 语料 40/40 · jit **176 ok 未回退 / 546 total / trap 370** segv=0（+1/+1＝新用例；实拍归因：t459 无 `-o` rc=1，首个硬缺绑 `zeta_module_decl` E4016，未绑 36 符号清单与 t455 同一份 ⇒ #42 既有族，非本批新形态）· diff 120/130 92.3% bad 0 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 73/ignore_rules 19/mbvar 22 全 0 违规 · comment_drift 0 · emit_stable 2 夹具/0 · clean_checkout rc=0 rev=`610ad28d` · 诊断 official 2/5、python_style **231 行/108 文件**（421＝226/107，+1 文件＝t459）· ABI 终态 **28/11/10 rc=1 定位失败 0**（改前对照在隔离 worktree 取 HEAD 自基线＝23/11/12 rc=2，那 2 条是 `aliases.inc.c` 生成文件不在裸检出里的既有红；本批 rebind 判搬家 36/拒改 38、改写 ABI.md 36 行 68 数，`codegen.rs` 漂移 13→18＝同形多命中拒猜，ABI.md 仍 1032 行）· disclosure：本次门禁首行**没有** W2003 ⇒ 三套基线链的就是本批重建的 `tokio_runtime.o`；`md5 zetac`＝`1aa05f8f…` 与 A/B 所用 `zetac_b422` 逐字节相同 | 代码批已提交 `8c5b9f79`，记录批随后（本行随记录批入库） |
| 423 | bootstrap | dict 原语先把**句柄判形**再用（G.5d）：`map_resolve`（`runtime/tokio_runtime_stub.c:264`）出口加 `zt_map_cap_ok`（首字＝2 的幂 ∧ `cap∈[16,2^30]`；`map_new` 从 16 起、`map_insert` 只翻倍 ⇒ 真表首字必是这个形状），不合格走 `zt_map_not_a_map` 点名 + `zeta_raise(1)`，**不再解引用**。通用探针 `zt_c_str_readable` 改名 `zt_ptr_readable`（9 个调用点跟着搬）。去重按 (原语名, 句柄) 只吞消息、不吞抛掷（422 合同）。代码批 `9c192241`（5 文件 **+135/−30**，含 `tokio_runtime.o` 162,772→**163,748** B、ABI 两侧 9 行 16 数重绑 + C9 行手工补正）+ 新用例 t460（53 行，改前 run=**139**/stdout 0 行实拍 → 改后 rc=0/stdout 四行逐字如 expect）。**主线 301 二十批来第一次位移（有正证据）**：语料两跑一致 `run 139 → 0`、stderr 135 → **357** 行、**38 条点名**（37 个 `py_map_items` 的接收者＝2024-01-02~02-29 的**交易日日期整数**，句柄 19724…19782 跳过周末 19728/19729；1 个 `map_get` 收 8 字节 ASCII 浮点文本串）／74 次抛掷 ⇒ 抛掷没被吞。**但 rc=0 ≠ 修好**：仍 `1000000 -> 0 (-100.00%)`、74 条 `[ERROR] … 失败: 1`。两条订正入册：① 421 的"位移 0 正证据"只比了 `errlines` 相同 ⇒ 判据换 `符号+偏移`＋rc（行数相同崩点可搬家）；② 本批上轮自记的"崩点从 `map_resolve+8` 搬到 `map_get+256`"**作废**（lldb 实拍打在前一代同名二进制上；当场复测：422 那份 `dc1d2e90` 崩在 `map_get+256`，守卫版 `3fe2b270` lldb 下 status=0）⇒ 只存在"同一份代码里两处未判形的解引用"。新坑：实拍文件与二进制同批产出并带 md5 | **rc=1**（`/tmp/b423/gate.rc`、日志 `/tmp/b423/gate.txt` 148 行，唯一红源＝`run_all.sh:605 py_fail=2` 常驻 `t231/t233`）· **342/2/6/0 ＝ 350**，与 `ls tests/python_style/t*.z` 350 对上（净增 1＝t460，零回归）· 194/194 compile（191/194 link，3 条 link-only 同 422 逐字未动）· 语料 40/40 · jit **176 ok 未回退 / 547 total / trap 371** segv=0（+1/+1＝新用例，仍是 #42 那一族既有形态）· diff 120/130 92.3% bad 0 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 73/ignore_rules 19/mbvar 22 全 0 违规 · comment_drift 0 · emit_stable 2 夹具/0 · clean_checkout rc=0 rev=`300d9e2e` · 诊断 official 2/5、python_style **231 行/108 文件 ＝ 与 422 逐字相同**（t460 的点名是运行期 stderr，不产生编译期告警）· ABI 终态 **漂移 28 / 新 11 / 消失 10 / 定位失败 0，rc=1**（改前对照 37/11/10；`--rebind` 判搬家 38 项，`tokio_runtime_stub.c` 剩 1 条＝同行号不同长度区间引用被拒改）· disclosure：门禁首行无 W2003 ⇒ 三套基线链的是本批重建的 `.o`；`md5 target/release/zetac`＝`1aa05f8f…` **与 422 逐字节相同** ⇒ 位移纯在运行时侧，与 codegen 无关 | 代码批已提交 `9c192241`，记录批随后（本行随记录批入库） |
| 424 | bootstrap | **`.items()` 的接收者为什么是一个日期整数 —— 机制换对了、语料那一格没够着**（2.2 签名表＝调用点证据的绑定点）：`gen.rs:11461` 的 `suffixed` 给**每一条裸名调用**追加 `_实参个数`，而 `refine_param_types`（`src/main.rs:80`）只认「精确名」与「`模块__名`」两形 ⇒ 一条候选都找不着 ⇒ 未注解形参保持 `dyn` ⇒ codegen 的 FieldAccess 落 `("", 2)` 兜底、`codegen.rs:6700-6708` 又把越界索引**夹回 0** ⇒ 三字段结构体读成 word0、第二跳从整数身上按指针读＝SIGSEGV（`ZETA_DBG_FA` 两行实拍在案：`field=portfolio variant="" field_count=2 base_ty=Some(PyDynamic)` + `idx 2 >= count 2 -> fallback 0`）。修法＝`src/main.rs:183-207` +19 词干兜底（只在无精确条目时走、只认 `name == stem`、重载集 `show_1`/`show_2` 绑定一字未变）。代码批 `06c67cda`（4 文件 +74/−9）+ 新用例 t461（改前 compile=0 run=**139** stdout **0 行** → 改后 rc=0/四行，stdout 与 `python3` 当场对数逐字相同）。**家族尺子（两把）**：`jq_wufu.py` 单编成根模块 301 条目/2,518 条调用 → 44 个后缀目标/82 站点无条目，词干==裸条目 **32 个目标/46 站点/24 宿主**；语料整体 628 条目 → 87 个后缀目标（有精确同名条目的 **0 个**）、新绑 5、词干落限定条目尾的 6 个按边界**不绑**、76 个查无条目。**主线位移 0（三层正证据）**：`--emit-llvm` 两侧 md5 逐字节相同 `2997d4f5212568acccf6dcb72fb4abfd`（113,722 行、diff 0）；MIR 唯一 hunk 是 `BaoStockSource` 的模块前缀别名，而**同侧两跑差 2 个 hunk**＝#9 噪声底；运行期改前/改后各两跑读数逐字相同（357 行／PY-A 38＝37 `py_map_items`＋1 `map_get`／WARNING 107／`[晨间]` 37 逐日）。**语料侧这一族的规模第一次有数**：`ZETA_DBG_FA=1` 全量编译 529 条 FieldAccess 里 **28 条走了夹回 0**（`portfolio`×8 base＝`PyDynamic`＝动态 context 那一形、`positions` 另 7 条 base＝`I64`、`avg_cost`×5、`paused`×3…）；37 条点名的句柄是 37 个互不相同的连续整数 `0x4d0c…0x4d46`、跳过 `0x4d10/11` 等周末 ⇒ 逐日 +1 的交易日序数（`0x4d0c`＝19724＝夹具那个 `dt` 的来源），**但「28 处里哪一处 emit 了这 37 次点名」未证**（点名句仍无用户函数名）。**订正 423 §八.1**：「槽位复用/串号」判死。**头名换格**：① `("", 2)`＋索引夹回＝把「缺证据」放大成内存不安全的那一级（出声 vs 补 variant 判据两方向，423 的 precedent 是出声直接换来 rc 139→0）② 回调／函数值实参的参数证据（①的上游根因；census＝628 条目/625 个不同调用目标名、**55 个宿主发 `FuncAddr`**、被取地址的目标 **106 个**、其中 **103 个从不出现在任何调用点**＝97 闭包＋**6 个具名日程例程** `morning_routine`/`afternoon_routine`/`buy_routine`/`sell_routine`/`reset_daily_flags`/`check_weak_period_daily`，正是 `run_daily(<fn>, time=…)` 注册那批 ⇒ 被调度执行的函数形参恒 `dyn`）。尺子级负结果两条：`--dump-mir` 的打印（`main.rs:897`）在精化（`:905`）**之前**＝这条路照不到；emit-llvm 的 md5 基线必须同 cwd 同环境（漏 `REPLAYQUANT_LOCAL` 会得到另一颗 md5＝数据分支，不是非确定性） | **rc=1**（`/tmp/b424/gate.txt` + `gate.rc`，唯一红源仍是 `run_all.sh:605` 存量 `py_fail=2`＝t231/t233）· python_style **343/2/6/0 ＝ `ls t*.z` 351** 逐字对上（净增 1＝t461，零回归、known-fail 6 未动）· 194/194 compile（191/194 link，3 条 link-only 同 422/423 逐字未动）· 语料 40/40 · jit **ok=176 未回退 / total 548 / trap 372** segv=0（+1 total＝新用例进 sweep）· diff 120/130 92.3% bad 0 · comment_drift 0 · emit_stable 2 夹具/违规 0 · clean_checkout rc=0 rev=`ff5514e4`（＝门禁起跑时 HEAD，代码提交在其后）· 诊断 official 2 文件/5 行、python_style 231 行/108 文件＝与 422/423 逐字相同 · ABI 终态 **漂移 28 / 新 11 / 消失 10 rc=1 定位失败 0**（`--rebind` 改写 3 行/5 数：`main.rs` 五锚集体 +19，另 **1 条表格内裸引用手工成对搬** `main.rs:568`→`587`；ABI.md 仍 1032 行）· disclosure：本文件出现**同文件并发编辑**（§1 被整体重排、423 台账行被删、并新增 `## 5` 标题修 HEAD 里那张无名表格）⇒ 台账行按 `git checkout HEAD -- worktree.md` 复原后合并，`## 5` 标题保留 | 代码批已提交并推送 `06c67cda`；记录批随后（本行随记录批入库） |
| 425 | bootstrap | **读侧不再掷硬币：按字段名反查 struct 布局**（3.2／424 §八.1 头名）：接收者 declared-type 路线落空 ⇒ `("", 2)` 兜底把加载宽度写死 2 word，索引却来自 `resolve_struct_field_index`（`:5872`）的全量 `struct_defs` 扫描 ⇒ `:6754-6757` 把 `idx>=count` 夹回 `unwrap_or(0)`＝0，读到接收者第 0 个字段、下一跳把那个整数当句柄解引用。修法＝`resolve_struct_layout_by_field`（`:5918`，调用点 `:6701-6704`）：只在**所有声明该字段名的类别在 (variant, 宽度, 索引) 上完全一致**时采纳，分歧退回旧兜底（宁可不猜）。代码批 `7dcae28f`（4 文件 **+151/−49**：`codegen.rs` +55/−1 ＋ t462 48 行 ＋ ABI 两侧各 24 行）+ `0d5b52e0`（注释措辞**等行**改 3/3 ⇒ `md5 zetac` 逐字节不变 `d33dc1ec…`）。**语料夹回 28→11**（收回 `portfolio`×8→`_LocalContext`、`avg_cost`×5 与 `closeable_amount`×2→`Position`、`context`×2→`NautilusBackend`；残 11＝`paused`×3、`trading_dates`/`routines`/`low_limit`/`bar_types` 各 2，结构可证为 ≥2 类别声明同名且分歧）、**主线位移：PY-A 点名 38→2（`py_map_items` 37→1）、stderr 357→321、改前 4 跑 1 次 `Segmentation fault: 11` → 改后 4/4 完成**。t462 两跑逐字节相同、与 `python3` 当场对数零差异；`ZETA_DBG_FA` 原图（改前两跳都夹回 `idx 3`/`idx 2`，改后 `variant="Ctx" count=4 final idx=3`、`variant="Leaf" count=3 final idx=2`）。**未闭**：`[PARITY] target=- \| holdings=- \| ranked=-` 74 条与 `1000000 -> 0 (-100.00%)` 一字未动、stdout 仍地址样整数（#117/#145）、`base_ty=Some(I64)` 那一形类型层未禁止（424 §八.1 的"出声"没做）。**头名换格**：① 回调／`FuncAddr` 形参证据（424 §八.2，残 11 处的上游）② declared-type 路线"多候选取宽度最大者"（`:6679`，成员数未定价⇒先定价批）③ 起照旧。工具坑七条入册（含"改注释二次搅动锚点 ⇒ rebind 必须放在最后一次源码改动之后"、"印象级数字不进注释"、"A/B 之前先 cp 一颗改名"、"记录批自己吞掉两个文档标题＋一处表格转义竖线丢失，全部当场抓回并留正证据"） | **rc=1**（`/tmp/b425/gate.txt` 148 行 + `gate.rc`，唯一红源仍是 `run_all.sh:605` 存量 `py_fail=2`＝t231/t233）· python_style **344/2/6/0 ＝ `ls t*.z` 352** 逐字对上（净增 1＝t462，零回归、known-fail 6 未动）· 194/194 compile（191/194 link，3 条 link-only 同 422/423/424 逐字未动）· 语料 40/40 · jit **ok=176 未回退 / total 549 / trap 373** segv=0（+1 total＝新用例进 sweep）· diff 120/130 92.3% bad 0 · comment_drift 0 · emit_stable 2 夹具/违规 0 · knob/swallow/import/empty_stmt/pysrc/cli_semantics/ignore_rules/mbvar 全 0 违规 · clean_checkout rc=0 rev=`6f780352`（＝门禁起跑 HEAD，两笔代码批在其后）· 诊断 official 2 文件/5 行、python_style 231 行/108 文件＝与 422/423/424 逐字相同 · ABI 终态 **漂移 28 / 新 11 / 消失 10 rc=1 定位失败 0**（`--rebind` 两趟：首趟 23 行/42 数、末趟 16 行/33 数；**1 条拒改后手工成对搬** `codegen.rs:6647`→`6701`＋`--bless-only` 点名；`comm -13` 对 HEAD 漂移清单空差集＝本批无新漂移；ABI.md 仍 1032 行）· disclosure：门禁首行无 W2003；末趟注释改动后重建 `md5 zetac` 与门禁那颗逐字节相同 ⇒ 读数对应入库源码 | 代码批已提交 `7dcae28f`+`0d5b52e0`；记录批随后（本行随记录批入库） |
| 426 | bootstrap | **425 的采纳判据放宽到"索引一致"**（3.2／425 残口的下一形）：读哪个 word 只由索引决定，`variant` 与宽度只参与加载类型和 `:6754-6757` 的夹回上界 ⇒ 判据从 (variant, 宽度, 索引) 全一致换成**只要索引在全体声明者上一致**、宽度取**最小声明者**（索引 < 每个声明者的字段数 ⇒ 夹回分支进不去；`min_by_key` 在 BTreeMap 序上并列取先现者）。代码批 `8ab17efb`（2 文件 +96/−30：`codegen.rs` +30/−30 等行、新用例 `t463_field_index_agreement.z` 66 行；注释与判据同笔入库，无第二次改动）。夹具三档实拍：pre425/pre426 均 run=**139** stdout 0 行 → post426 rc=0 五行、两跑逐字节相同且与 CPython 逐字相同。语料＝**夹回 11→6**（收回 `paused`×3、`low_limit`×2 → 现按 `Bar` count=4 读 idx 3/2；529 条 FieldAccess 读两侧同数；残 6 处＝`bar_types` idx 9／`routines` 7／`trading_dates` 8 各 2，索引不一致），28 里累计已收 22。**运行期位移 0（负结果）**：两侧各 5 跑，改前 2/5、改后 1/5 次同形态早期崩（stderr 停 120 行、rc=139/138）⇒ n=5 无统计意义，先前 2 跑的"改前必崩"作废；`TRADE=0`、`1000000 -> 0 (-100.00%)`、`[PARITY] target=- \| holdings=- \| ranked=-` 74 条一字未动。 | 194/194 compile·191/194 link（3 条 link-only 同 422–425）· python_style **345/2/6/0** ＝ `ls tests/python_style/t*.z` **353** 逐字对上（净增 1＝t463，零回归）· known-fail 6 未动 · 语料 40/40 · jit ok=176/trap=374/total=550/segv=0（+1 total＝新用例进 sweep，ok 未回退）· diff 120/130 92.3% bad_case=0 · 真值面 truth 22/23、str 20/23、container 27/28、numeric 23/28、control 28/28 未动 · comment_drift 0 · emit_stable 2 夹具/违规 0 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 73/ignore_rules 19/mbvar 22 全 0 违规 · clean_checkout rc=0 rev=`a18a0aee` · 诊断面 official 2/5、python_style 231 行/108 文件与 422–425 逐字相同 · **`GATE_RC=1`（唯一红源仍是存量 `t231/t233`）** · ABI：**`--rebind` 真·无操作**（`diff` 判 ABI.md 逐字节未变、`git diff --stat` 对 ABI.md 与 `abi_anchors.tsv` 双双为空）＝"被引行未位移"的正证据；只读核对复现常驻 **漂移 28 / 新 11 / 消失 10 rc=1 定位失败 0**、ABI.md 仍 1032 行 · disclosure：门禁首行无 W2003；`md5 zetac`＝`3f2dd9f5…` 与注释定稿重建那颗相同。 | 代码批已提交 `8ab17efb`；记录批随后（本行随记录批入库） |
| 427 | bootstrap | **给"两路反查都落空"这一路加出声**（3.2／＝424 §八.1 的 (a)，欠三批；426 头名）：`ZETA_DBG_FA` 门后两支探针 —— 读侧 `FA decls`（`codegen.rs:6722-6743`，点名该字段名的全体声明者与各自 variant/宽度/索引）、收集侧 `struct_defs conflict`（`:1443` 段，同键异序即将被首写者丢掉时打印 kept/dropped）；`struct_field_decls`（`:5910`）从 resolver 抽出共用，**判据一字未动**、无新旋钮、无新诊断码。代码批 `7c3ffa23`（6 文件 +234/−159：`codegen.rs` +61/−20、`docs/ABI.md` 66/66、`abi_anchors.tsv` 72/73、新用例 `t464_field_order_key_collision_swap.z` 27 行 + `pyswap_a.py`/`pyswap_b.py` 各 4 行）。**主读数（尺子换成"落空族"，clamp 口径作废）**：语料 529 次 FieldAccess 读里 **152 次走进 `("", 2)` 占位版式、152 条 `final idx` 全为 0**，其中 **146 条 `declarers=0`**（45 个字段名，`index`/`columns`/`metadata`/`__dict__`… 多为外部对象属性）⇒ 索引 0 过不了范围检查 ⇒ **425/426 的 clamp 计数（28→11→6）低估 25 倍**；另 6 条的分歧对首次到手＝`NautilusBackend`(宽 11)↔`NautilusJqStrategy`(宽 9) 的 `trading_dates` 8↔3／`routines` 7↔2／`bar_types` 9↔4 ⇒ 三名成对争同一接收者 ⇒ 判据到头（再放宽＝猜布局）。**位移 0（正证据）**：语料 `--emit-llvm` 文本改前改后 **113,722 行逐字节相同**（并钉一条方法论：机器码不能当等价尺，同一颗编译器两跑产物 `cmp` 在 char 1609 就 differ）＋ `FA read` 529=529、夹回 6=6。**收集侧负断言＋正证据**：`conflict` 语料 **0 命中**（426 那条隐患当前无成员），同探针在 `t464` 上 1 行（pre427 无此探针＝0 行）⇒ 顺带抓出**已实拍的静默错值**：`struct_defs` 键用裸类别名 ⇒ 跨模块同名同宽类别共用一版布局、首写者胜，`t464` 两颗二进制都出 `y 3 4`、CPython 真值 `y 4 3`（rc=0 无诊断）⇒ 登记 428（#155，第一格＝读侧 `base_ty` 只带裸名，模块身份从哪来）。**头名换到 428**，第 2 格＝`declarers=0` 那 146 条的按名生产者归因（#156）。 | **`GATE_RC=1`**（`/tmp/b427/gate.txt` 148 行 + `gate.rc`，唯一红源仍是存量 `py_fail=2`＝t231/t233）· python_style **345/2/7/0** ＝ `ls tests/python_style/t*.z` **354** 逐字对上（净增 1＝t464 落 known-fail 桶 6→7，`xpass=0` 零回归）· 194/194 compile（191/194 link，3 条 link-only 同 422–426 逐字未动）· 语料 40/40 · jit **ok=176 未回退 / trap=375 / total=551 / segv=0**（对 426 各 +1＝新用例进 sweep）· diff 120/130 92.3% bad_case=0 · 真值面 truth 22/23、str 20/23、container 27/28、numeric 23/28、control 28/28 未动 · comment_drift 0 · emit_stable 2 夹具/违规 0 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 73/ignore_rules 19/mbvar 22 全 0 违规 · clean_checkout rc=0 rev=`d61008ef` · 诊断 official 2 文件/5 行未动、python_style **233 行/109 文件**（426 为 231/108，增量归因 t464 的两条 `PY-A: imported module` 与它自己那个文件）· **ABI：本批真动了行号**（`codegen.rs` 净 +41 ⇒ tsv 72 行改号，三段 +11/+21/+41），终态只读核对 **漂移 28 / 新 11 / 消失 10 / 定位失败 0 rc=1** 与 426 常驻读数逐项相同；人读两处（M4 兜底点 `6701-6704`→`6722-6743` 并补 426/427 真判据与分歧对；N7 五条同文本手移 `:2325/:2548/:2583/:2599/:2711`，`--rebind` 按设计拒改）＋ `--bless-only` 重采被真改写的那条 arm；**一处覆盖变窄点名**：基线 **258→257**、tsv 349→348 行，少的是 `codegen.rs:7113` 的 `fn coerce_call_args(` 定义行（§3.2 范围锚 `7113-7238` 位移成 `7154-7279` 后与同句 `abi_note` 的 `:7154` 撞成同一个「文件+行号」槽位）· disclosure：门禁首行无 W2003；`md5 zetac`＝`8a8e6448…` 与三套基线链那颗相同，改前那颗留盘 `zetac_pre427`（`3f2dd9f5…`，两颗同放 `target/release/` 满足 A/B 同目录） | 代码批已提交 `7c3ffa23`；记录批随后（本行随记录批入库） |
| 428 | bootstrap | **链接期不再被一个成员名拖没：`[dynamic]` 幽灵走到"连声明都没有"就绑按名抛异常的桩**（3.2 Lowering → 链接层／＝419/422 那条判据的补全，不是第四根桩）：419 只覆盖"成员名恰好是某类别方法的裸别名"（`dyn_member_is_class_alias`，`codegen.rs:3079`），其余 `[dynamic]` 幽灵一路落到 `get_or_declare_function`（`:2683`）末尾的 `add_function(.., External)` —— 名字是下型器凭空造的，链接器只可能 miss ⇒ `Error: "Linking failed"`；**损害量不是"这一格读错"而是"全程序出不来"**（acceptance 那份编译一次过 **28 个 `PY-A: imported module`** 的模块面）。修法按 2026-09-24 裁定走结构路：**不新增 C 桩、不逐成员补名字**，改兜路线的归向（`:2993-3014` ⇒ `dyn_member_missing_thunk`，`:3122`）。**新增门禁第 17 步 `dyn_binding`**（`tools/dyn_binding_lint.sh` 88 行、`run_all.sh` 16→17 步 / 639→665 行）当场核对例外表 4 条（`map`/`pct_change`/`abs`/`nth`）↔ `runtime/py_additions.c:967/986/988/994` 的集合相等，**两条 FAIL 分支均实拍**；表的活性正证据＝删 `nth` 后 t411 立即 FAIL（stdout 只剩 `42`/`0`、`False` 缺失、rc=1）。新用例 `t465` 50 行：改前 **rc=1 且不出二进制**（`"_[dynamic]str__nonesuch", referenced from:`），改后 rc=0 ＋ stderr 按名点名 ＋ stdout `caught`（＝异常真被外层 except 接住）。代码批 `01edfb53`（6 文件 **+293/−82**：`codegen.rs` +47/−0、`dyn_binding_lint.sh` 88 新增、`run_all.sh` +26/−0、t465 50 行、`docs/ABI.md` 与 `abi_anchors.tsv` 各 +41/−41） | **rc=1**（`/tmp/b428/gate.log` 154 行 + `gate.rc`，唯一红源仍是存量 `py_fail=2`＝t231/t233）· python_style **346/2/7/0 ＝ `ls t*.z` 355**（净增 1＝t465、known-fail 未增、`xpass=0`）· official 194/194 compile、191/194 link（3 条 link-only 明细未动）· 语料 40/40 · jit **ok=176 / trap=376 / total 552** segv=0（对 427 的 trap 375 / total 551 各 +1＝新用例进 sweep，ok 未回退）· diff 120/130 92.3% bad 0 · **dyn_binding 4 条／不一致 0／rc=0** · knob 23 / swallow 6 / import 22 / empty_stmt 68 / pysrc 42 / cli_semantics 73 / ignore_rules 19 / mbvar **23 脚本**（427 为 22）/ emit_stable 2 夹具 / comment_drift 0 —— 全 0 违规 · clean_checkout rc=0 rev=`e79fd478`（**代码批之前**那颗＝门禁起跑 HEAD，不含本批改动，roadmap 428 §八已交代）· 诊断 official 2 文件/6 行、python_style 234 行/110 文件（+1 行 +1 文件＝t465 自己那条）· ABI 锚点 258 可解析／定位失败 0，终态 **漂移 28 / 新 11 / 消失 10 rc=1**＝与 427 常驻读数逐项相同（`--rebind` 按 +47/+22 位移重绑 41 条；`docs/ABI.md:187` 位移后目标行为空 ⇒ `--rebind` 判定位失败且配不出唯一候选，手改 `:7687→:7734` 后按"改号配对"收编；隔离 HEAD worktree 读 **消失 12**，差的 2 条＝未跟踪生成物 `runtime/aliases.inc.c`＝427 §九.5 同型第二次遇到）· **主线位移 0（三层正证据）**：语料同目录两颗 A/B —— 编译 rc 0/0、运行 rc 0/0、out 1/1 行、err 321/321 行、PY-A 幽灵告警 **6/6 逐字相同**（`ghost_A/B/B2.txt` 各 157 行两两 `cmp` IDENTICAL；6 个名＝`i64::copy` 与 `str::{copy,dropna,ffill,isin,to_dict}`）；机理＝语料里走到这一格的幽灵都有 C 侧定义，改前不会在链接期出声。**两条残留**：裸名同族（接收者完全无类型，`/tmp/b429/probe465.z` 实拍仍 `Error: "Linking failed"`）与四条 C 绑定的语义 · disclosure：门禁首行无 W2003；收尾三次重编（两条负分支＋一次注释改动）后 `md5 zetac`＝`5ef81704…` 与跑门禁那颗逐字节相同，改前那颗留盘 `zetac_pre428`（`8a8e6448…`＝427 产物）· 工具坑 8 条入册（含"二进制拷到仓外／从非仓根 cwd 编译＝整套运行时符号消失的假链接失败"、"负分支实拍要看输出变了没"、"印象值进了已提交注释 ⇒ 本批复核时抓出来订正"） | 代码批已提交 `01edfb53`；记录批随后（本行随记录批入库） |
| 429 | bootstrap | **句柄身上没有类标签时的零参成员名不再读第 0 个字**（3.2／2.2 签名表的下游；＝427 那 152 条落空读里的 33 条）：`unique_zero_arg_property`（`gen.rs:898`，调用点 `:12610`）名字唯一 × 除 `self` 外零实参 ⇒ 发调用；一名多主／带参／参数表缺失保持旧行为。代码批 `0923c676`（4 文件 +163/−60，`gen.rs` +49、新用例 `t466` 54 行） | `GATE_RC=1`（唯一红＝存量 t231/t233）· python_style 347/2/7/0＝`ls t*.z` 356 · 诊断面 234 行/110 文件＝与 428 逐项同 · official 194/194·191/194 链接 · corpus 40/40 · jit 176/377/553 segv=0 · diff 120/130 92.3% bad 0 · clean_checkout rc=0(`27d3f8a0`) · 锚点终态 28/11/10/定位失败 0（净位移 0，隔离 HEAD worktree 复核）· **位移**：语料 `FA decls` 152→125、`FA read` 529→502、`columns` 12→0/`empty` 5→0/`index` 16→6、IR call 站点 +27（逐名对应）、噪声底 0；**主线 301 运行期位移 0**（两侧各 11 跑，同侧方差入册） | 已推送 |
| 430 | bootstrap | **跨模块同名同宽类别不再共用一条字段序：撞车时改问"这个值是哪个构造调用造的"**（3.2／收集点的键不带身份＝425/426/427/429 反查链的上游）：`struct_defs` 的键未动，另建 `struct_ctor_layouts`（带构造方的 MIR 条目名）＋`struct_variant_collisions`，读侧 `codegen.rs:6940-6960` 只在撞车集合里路由；代码批 `05fc9841`（4 文件 +304/−167）。**改前是稳定错、不是掷硬币**（同一补丁独立编译 5 次 ⇒ 首写者与输出 5/5 相同）—— 429 那句 `RandomState` 猜测作废（从未实拍） | **rc=1**（`gate.log:155`）· 348/2/**6**/0＝`ls t*.z` 356（`t464` known-fail→passed 换格、`xpass=0`）· 诊断面 234/110 与 official 2/6 **逐项同 429** · 194/194+191/194 · 40/40 · jit 176/377/553 segv=0 · diff 120/130 92.3% · clean_checkout rc=0 rev=`05fc9841`（这一步跑在含本批改动的 HEAD 上，与 429 口径不同）· 锚点 258 可解析/定位失败 0/漂移 24/新 11/消失 10（对 HEAD 的 28/11/12 **净修 4 条陈旧引用**、新增 0）· **语料位移 0**：`--emit-llvm` 两侧 md5 逐字节相同＋四次探针逐项相同（语料 stderr 逐字节比已实测为假尺：同侧方差 122–126 行）· **语料成员 0、夹具成员 1** | MERGED（代码 `05fc9841` ＋ 记录批同次推送） |
| 431 | bootstrap | **降级成裸名的成员调用改为只读上报，不在出码处改绑**（3.2 Lowering → 链接层／头名）：两个降级臂（`gen.rs:11333-11337`／`:11339-11341`）之后的串与普通函数调用**逐字节同形** ⇒ 出码期无从判定、`codegen.rs:3151`/`:3157` 的 `External` 兜底把选目标这件事交给链接器（编译期零诊断）。代码批 `9f400338`（6 文件 **+199/−73**）＋锚点批 `cd4becab`。**守卫已试且被推翻**：按名改绑打死 `clip`（真定义在 `zeta_runtime_c.o`、链接后有 `_clip` 符号；守卫版 rc=1/903 行 stderr/0 B stdout、改前 rc=0/321/11 B，raise 的 `code` 槽三次各异＝未初始化）⇒ 回退，仅留档。**68 名归因**＝5 类别方法体／3 运行时真实现／54 batch-156 注册表（32 abort＋22 soft）／**6 链接宇宙无定义**（`abs alarm close mkstemp signal strftime` → libSystem 静默误绑），Σ(名×体)=106，**0/68 同时以普通调用拼写**（负结果：`plain_call_names` 收窄在语料无成员）。交付三支：MIR 携带 `member_bare_calls`/`plain_call_names`、`--report-stubs` 下的 `report_bare_member_calls`（只读、不链、不执行、空集沉默）、`cli_semantics` +7 断言（含沉默翼 2 条正证据）。**本批不新增 t4xx**（已申报：CLI 只读报告面）。**"修好了"与"推进了主线"两栏分开**：前者＝可见性＋归因表＋一条被推翻的形状；后者＝**位移 0**（`--emit-llvm` md5 与 430 尺逐字节相同 113,704 行；主线 301 三格病因均不在此）。**头名换成那 6 个 libSystem 误绑名**，第 2 格＝#156；OPEN 净增 0 | **rc=1**（`/tmp/b431/gate431.log` 155 行，末行 `GATE_RC=1`；唯一红源＝存量 `py_fail=2`＝t231/t233，与 418–430 同一颗、**零新增红**）· python_style **348/2/6/0** ＝ `ls t*.z` **356** 逐项对上（用例总数未增、`xpass=0`）· 194/194 compile（191/194 link，3 条 link-only 同 427–430 逐字未动）· 语料 40/40 · jit **176 ok 未回退 / trap 377 / total 553 / segv=0** · diff 120/130 92.3% bad_case=0 · 真值面 truth 22/23、str 20/23、container 27/28、numeric 23/28、control 28/28 逐项同 430 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/**cli_semantics 80（对 430 的 73＝本批 +7）**/ignore_rules 19/mbvar 23/comment_drift 0/emit_stable 2 夹具/dyn_binding 不一致 0 · 诊断 python_style 234 行/110 文件＋official 2/6 **与 430 逐项相同** · clean_checkout **rc=0 rev=`798f4ccf`**（口径点名＝本批**代码提交之前**那条 430 记录 commit，与 430 那步跑在含改动 HEAD 上不同）· **锚点终态 258 可解析／定位失败 0／漂移 24／新 11／消失 10 rc=1，对 HEAD 净中性**（隔离 worktree 自基线＝24/11/10 逐项相同；`--rebind` 30/30＋42/42 后仍留一条本批自己的假引用，手移 `gen.rs:12958→:12987`＋`--bless-only` 源坐标点名＋按 tab 删孤立行；ABI.md 仍 1032 行、tsv 仍 348 行）· disclosure：`md5 zetac`＝`cf8c2e88f8144051aa04f6e0b47176c7`，收尾 `touch` 三份已提交源重编后 md5 **逐字节不变** ⇒ 门禁那颗＝提交那颗；两条不可复现读数已点名作废（129 站点探针随守卫删除、lldb 帧链日志未落盘），结论改靠 `clip` 定义集＋A/B 运行读数两条可复现证据 | 代码批 `9f400338`＋锚点批 `cd4becab` 已推送；记录批随后（本行随记录批入库） |
| 432 | bootstrap | **降级成裸符号之前先问 W 注册表：四条判据全过才改绑真实现**（3.2 Lowering → 链接层／431 归因表最后一桶）：`pylib::unique_method_for_bare_call`（`pylib.rs:375`，调用点 `gen.rs:11320`）＝拼写唯一 × 非桩 × arity 含接收者相等 × 不在 `NAME_ROUTE_DENYLIST`（`:312`，与 B4 合成一份）；返回类型取表项 `registry_ret_type`（`gen.rs:14972`）。代码批 `0cafbf52`（4 文件 +211/−62：`pylib.rs` +71/−7、`gen.rs` +64/−50、`t467` 新 43 行、门禁 +33/−5）＋锚点批 `a7904b48`（13 条引用/21 个数字，未 `--bless`） | **`GATE_RC=1`**（`/tmp/b432/gate432.log` 155 行；红源＝存量 official link-only 3 ＋ 锚点非零，**零新增红**）· python_style **349/2/6/0**（349+2+6=357；`ls *.z`=360 的 3 文件差额未追）· 194/194 compile ＋ 191/194 link · 语料 40/40 · jit 176/378/554 segv=0 · diff 120/130 92.3% bad 0（truth 22/str 20/container 27/numeric 23/control 28）· **cli_semantics 80→87** · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/ignore_rules 19/mbvar 23/emit_stable 2/dyn_binding 4/comment_drift 0 全 rc=0 · clean_checkout rc=0 rev=`2bdd01c0`（口径点名＝跑在本批代码之前）· 诊断 237 行/111 文件＋official 2/6 · `md5 zetac`=`7694c742af6598c3cbc63feec94c5b3c` · 锚点 **258 可解析／定位失败 0／24/11/10 rc=1＝对 431 终态逐项净中性** · **位移**：bare-member 名 **68→66**、语料 **7 个调用点**改绑（strftime 5／setdefault 2）＋ **2 处 `icmp slt`→`host_str_cmp`**、t467/p1 崩溃格 rc=139/0B → rc=0/13B；**主线 301 位移 0**（语料 stdout 两侧均 11B 堆地址；pre 6 跑 1 次崩 vs post 0 次，n=6 不足归因）。**两把尺子当场作废**：机器码 md5（430 已证）与语料 stderr 内容 diff（地址归一后仍 321 行里 154 行不同＝ASLR＋`rows` 抖动） | 代码批 `0cafbf52`＋锚点批 `a7904b48` 已提交；记录批随本行入库 |
| 433 | bootstrap | **字段写侧不再自己扫一遍布局，改问读侧要答案**（3.2 Lowering／读写的名→槽对称）：`MirStmt::StructFieldStore` 原先自跑 `struct_defs.values().find_map(…)` 取**第一个**提到该字段名的布局（不看接收者、不比宽度、失败 `unwrap_or(0)`、无上界夹回，HEAD `:5811-5834`）⇒ 2 字段接收者上写"也被字母序在前的类别声明在索引 2"的名字，落偏移 16 于 16 字节块：**写丢失 ＋ 越界堆写**，而读同名字段答 0。修法＝读侧整段搬进 `resolve_field_slot`（文档 `:6115`／签名 `:6126`／返回 `:6344`，夹回在返回前完成），写点 `:5826`、读点 `:6992` 共用一个答案；探针分 `read`／`write` 角色。新夹具 `t468` 改前 `z 10 20` → 改后 `z 99 20`（CPython 3.14.5 当场对数），`a`/`b` 读数不变。**边界**：构造期 `self.x=v` 下成 `StructNew` ⇒ 写在 `__init__` 内的夹具打不到这一路。代码批 `d5b95c26`（2 文件 **+291/−213**：`codegen.rs` 244/213＝净 +31、`t468` 新 47 行）＋锚点批 `86970a98`（24 行/43 数字，**未 `--bless`**） | **`GATE_RC=1`**（`/tmp/b433/gate433b.log` 155 行；非绿项组成与 432 **逐条相同**＝official link-only 3 ＋ `t231`/`t233` ＋ known-fail 6 ⇒ **零新增红**；锚点核对不是 `run_all.sh` 的一步，#37 仍 OPEN）· python_style **350/2/6/0**（349→350 的增量＝`t468` 落在 passed 侧）· 194/194 compile ＋ 191/194 link · 语料 40/40 · jit **176/379/555** segv=0（对 432 的 176/378/554：total +1＝新夹具，ok 未动）· diff 120/130 92.3% bad 0 · 各翼 rc=0（knob 23／swallow 6／import 22／empty_stmt 68／pysrc 42／cli_semantics 87／ignore_rules 19／mbvar 23／emit_stable 2／dyn_binding 4／comment_drift 0）· clean_checkout rc=0 rev=`65ca4430`（口径点名＝跑在**本批代码提交之前**）· `md5 zetac`=`d7d44a9a367825b535ea9a39d3520433` · **锚点归属实验**：同一命令只换 `codegen.rs` ⇒ 改前 258 可解析／0 定位失败／漂移 24，改后 **漂移 48**，新增 24 条**全部在 `codegen.rs:6011` 之后**＝本批 +31 行位移所致（非文档写错、非顺带修存量）；`--rebind` 后终态 **258／0／24/11/10、rc=1**，残差漂移集合与改前**逐字相同**（`diff` 判 IDENTICAL）⇒ 对 HEAD 净中性；抽查 `sed -n` 三条落点（`:6864` 存侧 bitcast 注释、`:6358` `StringLit`、`:7513` `note_return_slot_mismatch`）与文档措辞逐条对得上 · **位移**：套件 IR **0**（360 份 `--emit-llvm` 逐字节比，唯一差异 `t23`＝418 在册发射序噪声，同二进制 3 遍 2 md5）、38 条字段 `store` 偏移改动 **0**；**语料 3 条指令／2 条源语句**（`nautilus_backend.py:113` 16→32、`jq_wufu_local.py:336` 72→0＝56B 越界写）；**主线 301 位移 0**（cwd＝REasyQuant 根、n=6 两侧、主道逐字全同 rc=0/321 行 stderr/`回测完成: 1000000 ->`；`rc=134 numpy.vstack` 两侧都有、`rc=139`/119 行仅 post 1 次 ⇒ 记本侧方差不记位移） | 代码批 `d5b95c26`＋锚点批 `86970a98` 已提交；记录批随本行入库 |
| 434 | bootstrap | **只在构造之后出现的属性有了自己的槽，不再踩接收者声明的第一格**（3.2 Lowering／字段布局的**收集面**）：`struct_defs` 只收构造器名单 ⇒ 构造之后的属性名两条布局路都不认，落到替身 `("", 2)` 后索引夹到 0，写踩掉该类声明的第一格、读读回那一格（＝433 §九.3 登记的那一格）。修法＝出码前再扫一遍 `MirStmt::StructFieldStore` 记成追加槽 `struct_ext_layouts`（`codegen.rs:114`、收集遍 `:1546-1597`、探针 `FA ext+ :1587`），两处分配点按 `declared+追加数` 开块并零初始化（`:5952`、`:7097`、`struct_alloc_words :6271`），`resolve_field_slot`（`:6361`）夹回前先问 `ext_slot`（`:6295`、调用点 `:6577`/`:6587`）；两处保守判据：类别构造宽度须唯一、类别名须无歧义命中。**夹具 `t469` 三行改前全错**（`b1 5 8 5 5 / b2 100 8 100 / empty 4 4`）⇒ 改后＝CPython 3.14.5 实拍值（`b1 7 8 99 5 / b2 7 8 100 / empty 3 4`）。**语料位移 151 行**：5 处分配加宽（`_G` 328→384、`PositionLedger` 32→40、`ValueRecorder` 0→8、`_Impl`×2 0→16）、13 个追加格零初始化、**7 条写从偏移 0 挪到 328~376**（`jq_wufu__initialize` 里 7 个策略参数名改前互相踩）、12 处读宽度改 ⇒ 433 之后第二条在语料上真改到落点的批；**主线 301 三格未实测到移动**（崩族方向一致但 n 不足 ⇒ 未判定）。| **`GATE_RC=1`**（`/tmp/b434/gate434.log`，mtime 晚于二进制；非绿项组成与 432/433 **逐条相同** ⇒ **零新增红**）· python_style **351/2/6/0**（350→351＝t469 在 passed，failed 仍 `t231`/`t233`）· official 194/194＋191/194（link-only 3 存量）· 语料 40/40 · jit 176/380/**556**（+1 成员＝新夹具，ok 未动）· diff 120/130＝92.3% bad_case 0 · 各翼 rc=0（knob 23／swallow 6／import 22／empty_stmt 68／pysrc 42／cli_semantics 87／ignore_rules 19／mbvar 23／emit_stable 2／dyn_binding 4／comment_drift 0）· clean_checkout rc=0 但检的是 `1e641235`＝本批代码之前 · 诊断 111→112 文件／237→238 行＝夹具里 `class Empty: pass` 触发的 `PY-A` 前端告警（`src/frontend/**` 越出所有权矩阵，只登记）· **锚点终态 259 可解析／0 定位失败＋漂移 23／新 11／消失 10，rc=1**（漂移＝433 的 24 减 `codegen.rs:1392`，消失 10 与 433 同集 ⇒ 净中性且严格更好）；五处同形 mangle 引用手工搬家 2419/2642/2677/2693/2805 → 2517/2740/2775/2791/2903（`--bless-only` 逐项带全路径），另清掉 399 留下的过期共用行号；核对器两条缺陷（`--rebind` 撞键不报、`find_snippet_lines` 宽容假行号）进 #52 族；**未定位读数**：126 行替身读离开探针路径（columns 60／empty 30／index 19／values 10／loc 4／iloc 2／dt 1），具名命中只 +30（`data`@`DataFrame`22／`data`@`Series`8），余 96 行改后不再打任何 `FA read` ⇒ 不记作收益。| 代码批 `a5418a77`（299/10 ＋ 夹具 49 行）＋锚点批 `49a92075`（ABI.md 75/75、tsv 86/85）已提交；记录批随本行入库。（435 追加：该行"96 行改后不再打探针 ⇒ 不记作收益"这一支**作废** —— 同目录重测后两侧探针逐条相同，见 §1 的 435 结案条与本表 435 行。） |
| 435 | bootstrap | **那对"改前/改后"trace 其实差在二进制所在目录 —— 126 行替身读的位移是假的**（度量口径批／434 §六 的头名 ①；无代码改动）：`pylib` 库面基从二进制位置向上找 ⇒ 434 改前那趟在 `/tmp/b434/` 缺 `pandas.z` ⇒ `struct_defs` 少 3 条布局（`DataFrame_1`/`Series_1`/`GroupBy_2`）⇒ pandas 访问器名退成替身读（`columns 60/empty 30/index 19/values 10/loc 4/iloc 2/dt 1`＝126）；同目录重测 `FA read` 502／替身 125／`FA write` 126（替身 20）／`FA decls` 145（`declarers=0` 138、去重 48 名）**两侧逐条相同**（按 `(role,variant,field,field_count,base_ty)` 多重集差异集为空）⇒ 非收益、非丢读；污染侧同命令复现 271 替身行 ⇒ 位置决定非偶发；IR 侧 md5（`e5470dd9…`/`fb201a62…`）证明 434 §五 本来就干净。**主线 301 位移 0**（本批无代码；434 的"未判定"只撤销读侧那一支）。#165/#166 结案、#156 换现口径；新登记候选＝库面随二进制位置变化（#76/#351 的位置维度） | **`GATE_RC=1`**（`/tmp/b435/gate435.log` 155 行；非绿项组成与 419–434 **逐条相同**＝official link-only 3 ＋ `t231`/`t233` ＋ known-fail 6 ⇒ **零新增红**）· python_style **351/2/6/0**（与 434 逐字同）· official 194/194 与 191/194 · 语料 40/40 · jit 176/380/556 GREEN · diff 120/130=92.3% bad_case=0 · 诊断 official 2/194·6 行、python_style 112 文件/238 行（同 434）· 各翼 rc=0（knob 23／swallow 6／import 22／empty_stmt 68／pysrc 42／cli_semantics 87／ignore_rules 19／mbvar 23／emit_stable 2／dyn_binding 4／comment_drift 复述 0）· clean_checkout rc=0（rev `cc0feda1`＝本批之前，**本批那份头注不在其覆盖内**）· 无 `.rs` 改动 ⇒ **无锚点重绑** | 夹具批 `ab48e72f`（`t469` 头注 +13/−8 全在注释、expect 三行未改）已提交；记录批＝本提交 |
| 436 | bootstrap | **锚点核对器的快照键里少一个区间终点：撞键的 `--rebind` 改判拒收、"同键多义"进 rc**（G.5d 工具项／证据核对器，任务 #167＝434 §残口入册的两条；**非 pyramid 出码层**）：`collect()` 写 `snapshot[(rel, start)] = snippet` ⇒ `f:105` 与 `f:105-129` 是同一条槽位、后写顶掉先写。**现量**：`docs/ABI.md` **306 条引用塌成 259 个键（47 条从未进过比较）、8 个键有两种内容** ⇒ 每个这种键上至少一条引用带着**不是它自己**的核对结果，而打印那句是"锚点全部对上"；434 那条 `ABI.md:679 → codegen.rs:1392`撞键（434 靠人手工发现）就是它在改号路径上的表现，354 把"一个键只能有一处引用"当**夹具前提**手写守住、本批变成机器判据。修法两支＝`rebind()` 落点已被占用即 `[拒改]`＋原样保留（每搬成一条 `occupied.add`，防同批抢同一落点）、`key_conflicts()/print_conflicts()` 在只读核对/`--rebind`/`--bless` 三路径逐组打印每条引用各自的区间并把 rc 判红。代码批 `fb6030dc`（**仅 1 文件 +98/−6**）。**红→绿实拍**（`/tmp/b436/fixture.py`，行号当场用工具自己的 `normalize`+唯一性筛）：改前 E1（撞键搬家）`搬家 1/拒改 0`→rc=**0**、键 2→**1**、**文档被改**；E2（同行两长度）rc=**0** 且打印"全部对上" ⇒ 改后 E1 `搬家 0/拒改 1`、rc=1、键 **2**、文档**未改**；E2 rc=1 并逐条列出 `:2` 与 `:2-4`（`pre_fix.txt`/`post_fix2.txt`）。**#167 收一半**：`find_snippet_lines` 宽容假行号那条**未动**（**"漂移 0"≠"引用行号对"**）；真修要把终点写进键（连锁改基线格式与 `--bless-only` 语法）登记余项。**主线位移 0**（未动编译器一行）。OPEN 净增 0 | **`GATE_RC=1`**（`/tmp/b436/gate.txt` 154 行，唯一红源仍是存量 `py_fail=2`＝t231/t233，419–435 同格）⇒ **零新增红** · python_style **351/2/6/0 ＝ 与 435 入册值逐字相同**（`xpass=0`）· official 194/194 compile、191/194 link（3 条 link-only 名单未动）· 语料 40/40 · jit 176/380/556 segv=0 **GREEN** · diff 120/130 92.3% bad_case=0 · 真值面 truth 22/23、str 20/23、container 27/28、numeric 23/28、control 28/28 逐项同 435 · knob 23/swallow 6/import 22/empty_stmt 68/pysrc 42/cli_semantics 87/ignore_rules 19/mbvar 23/emit_stable 2/dyn_binding 4 全 0 违规 · comment_drift 0（本批新增文本未复述判据）· clean_checkout rc=0 rev=`522951e9`（**口径点名**＝跑在本批代码之前那颗，不含 `fb6030dc`）· 诊断 official 2/6 与 python_style 238 行/112 文件与 435 逐字相同 · **锚点：无 `.rs` 改动 ⇒ 无重绑**，核对器自身不在基线里（`grep -c check_abi_anchors tools/baselines/abi_anchors.tsv`＝0）也不在门禁里（`grep -n check_abi_anchors tools/run_all.sh` 零命中）⇒ 基线 md5 改前改后 `bfce918f…`、`git status -- docs/ABI.md tools/baselines/` 为空 · 真文档核对**净中性**：漂移 23/新 11/消失 10/基线 258/rc=1 逐项未变，只多一类 `同键多义 8 组 / 被顶掉的引用 47 条`；`--rebind --dry` 拒改 33（10 多命中＋3 零命中＋10 同键不同长度＋10 消失）且**新护栏触发 0 次**；`--bless` 仍 E1005 拒收（rc=2，33 条会被抹平）· 表头改口径点名：`锚点：259 个可解析（来自 306 条引用）` ⇒ 按 `锚点：[0-9]+ 个可解析 /` 匹配的老 grep 会落空 | 代码批已提交 `fb6030dc`；记录批随本行入库 |
| 437 | bootstrap | **度量批／主线 301：434 的写侧位移把函数在镜像里挪了 76 字节、把崩点留在原地 —— 三格 0 位移**（3.2 字段写侧的**运行期后果**；闭 434 §六 那句"未判定"，任务 #169）。判据开跑前定死三条：① 两颗二进制**同目录**（435 证：位置换库面＝假位移）② 读数取 `符号+偏移`＋rc，**不比行数**（423/430）③ 先定 n＝6/侧、两侧交替序，跑完对差值做**算出来的**显著性检验。**产物层**：`target/release/zetac` md5 `e3267a47…` 与 `zetac_pre434` md5 `d7d44a9a…` 均与 434/435 入册值逐字对上（复测）；各建一颗到 `/tmp/b437/`，compile_rc 0/0、**都 586,768 B、`cmp` 在 char 217 differ** ⇒ 写侧位移真进了链接产物。**运行期 n=6/侧**：主道 8 次（pre 5／post 3）读数八次全同＝`[PARITY]` 74、`TRADE` 0、`失败: 1` 74、`[晨间]` 37、`动态池更新失败` 37、stderr 321 行、末行 `[local] 回测完成: 1000000 -> 0 (-100.00%)`、stdout 1 行＝地址样整数（#117 未动）；崩 4 次（pre run2／post run1,4,6）都 rc=139／stdout 0 行／stderr 119 行、末行 `backend.market_data: 缓存命中 103 只；待拉取 0 只`。**崩点身份（本批真判据，取 macOS `.ips`）**：`str_trim+24 ← backend_datasrc_code_conv__normalize_to_jq+16 ← MarketDataFetcher::fetch_stocks+3080`，pre 那 1 次与 post 那 3 次**符号／`symbolLocation`／帧序三项逐字相同**，唯一差别是镜像内偏移（`str_trim` 250904→250980、`normalize_to_jq` 50624→50664）⇒ **位移搬了函数位置、没搬崩的指令**（#145 由此拿到改后二进制仍死在 `fetch_stocks` 的新正证据）。**1/6 vs 3/6 不算位移（Fisher 精确，2×2）**：单侧 p=0.9394、双侧 **p=0.5455** ⇒ n=6/侧分不出本侧方差，按 432 §十.3 措辞记为"真实但偶发、样本不足以归因"。**结论栏：主线位移 0，三层正证据**（三格读数同／崩点身份同／产物却不同 ⇒ 不是"没打到那一格"的空 0）；**"修好了"栏照旧空**。**门禁未重跑并点名口径**：`git status --short` 只有 ` M .ouroboros/work.md`（不是本批、不是我的）＋既有 `??` ⇒ 零跟踪文件改动，生效读数仍是 436 那颗 `GATE_RC=1`（唯一红源＝存量 t231/t233）、HEAD＝`97a8ea90`；无 `.rs` 改动⇒无锚点重绑。工具坑两条：① **lldb 打不出这个偶发崩**——`-b -o run -o "bt all"` 连跑 20 次（每侧 10）**20/20 `exited with status = 0`**、每份日志 329 行且 `[PARITY]` 74 与 `回测完成` 都在 ⇒ 走完全程 ⇒ "调试器下未复现"≠"崩不可达"，更不许当"改后不崩"；② `.ips` 的 `symbolOffset` 与 `address` 恒 0、镜像基址随 ASLR 逐次变（0x102ac4000/0x1005c4000/0x100074000/0x102388000）⇒ 比对键只能 `symbol`+`symbolLocation`+`imageOffset` 三件。**头名换格**（顺手从 435 那份同目录 trace 量到，`/tmp/b435/post435.err`）：145 条 `FA decls` 里 `declarers=0` **138 行／45 名**、`declarers>0` 只剩 **7 行**（`trading_dates`×2／`routines`×2／`bar_types`×3＝427 那对 `NautilusBackend`(11)↔`NautilusJqStrategy`(9) 分歧 ⇒ 按名反查判据到头）；**配对 `FA read` 里有 10 条接收者 declared type 已到手却仍回 `("", 2)`** —— `_backend` 6 行 `base_ty=Some(Named("backend_strategy_nautilus_backend__NautilusJqStrategy"))`＋1 行 `Named("Any")`、`history` 4 行 `base_ty=Some(Named("backend_engines_bt_helpers__ValueRecorder"))`（另 `positions` 7 行 `base_ty=Some(I64)`＝425 残留 ②）。已核到的结构事实＝`struct_field_decls`（`codegen.rs:6188`）**只扫 `struct_defs`**、而 `resolve_field_slot` 另问 434 的 `struct_ext_layouts`（`:6285`/`:6301`）⇒ 读侧按名反查与写侧追加表不对称；**"declared type 为何没用上"的因果未证**，先读接线点（登记任务 #170）。OPEN 净增 0（#169 闭、#170 开） | **无门禁读数**（本批零跟踪文件改动 ⇒ 读数不随 `/tmp` 产物变；生效读数＝436 那颗 `GATE_RC=1`，红源＝存量 `py_fail=2`＝t231/t233，419–436 同格）· python_style 351/2/6/0 · official 194/194 + 191/194 · 语料 40/40 · jit 176/380/556 segv=0 GREEN · diff 120/130 92.3% bad_case=0 · 各翼全 0 违规 · comment_drift 0 · 锚点无重绑（无 `.rs` 改动）· 产物留盘 `/tmp/b437/`（`run6.log` 12 行读数、`r_<side>_<i>.{out,rerr}` 24 份、`lb_*.log` 20 份）· 崩溃报告 4 份 `~/Library/Logs/DiagnosticReports/bin_{pre,post}434.n1.bin-2026-09-26-08*.ips` | 记录批＝代码批（本行随记录批入库，无独立代码提交）；随后推送 |
| 438 | bootstrap | **函数体里的 `class`：兄弟方法调用绑回自己的合成符号，无定义成员改由调用点抛异常**（3.2 Lowering／ImplBlock 收尾补一趟窗口改写）：机制＝`lower_closure` 在 hoisting 臂发布别名前 clone `closure_vars` ⇒ "先写后定义"的 `self._jq_bar_types()` 当趟永不绑。两处根因分开撞出：① 判据问了 `func_ret_types`（调用点证据表，未定义成员也在键里 ⇒ 幽灵臂永不触发，实拍 `ld: Undefined symbols: _Inner__notsdefined`）→ 改问 `generated_mirs` 定义名；② 只扫顶层漏 `If/For/While`（`Vec<MirStmt>` 只嵌这三处）→ `rebind_nested_calls` 递归。无定义 ⇒ `[dynamic]{ty}::{m}` 走 428 的按名抛（不去 `unavailable_stubs.c` 加名）；顶层 `impl` 一字未动。代码批 `5e69d584`（5 文件 +408/−89＝gen.rs +224/−13、t470 重写 +75、t471 新 +45、ABI 两侧 26/38 行重绑）。**头名换格**：本号原派 #170，被这格吃掉（硬链接失败＞静默降级），#170 原样留队头；新队头＝登记 ①（try 体幽灵调用＝整方法体静默丢） | 快子集（按 2026-09-26 节奏裁定）· official 194/194 compile（191/194 link，3 条 link-only 缺绑清单逐字同 423/424）· python_style **353/2/6/0**（failed 仍 t231/t233；352→353＝t471）· 诊断 official 2 文件/6 行、python_style **239 行/113 文件**（+1＝t471 点名）· comment_drift 0 · dyn_binding 4 条/不一致 0 · 锚点 `--rebind` rc=2（23/10/10）＝**HEAD 对照 rc=2（23/11/12）本就红** · **位移**：语料单文件未定义 **4→2**、按名点名 0→1（`[dynamic]_Impl::subscribe_bars`）、驱动 cerr 34,423→34,528 B／`无定义` 7→8（6/6 两侧一致）；**运行侧不判定**（两侧都 2/6 rc=139） | 代码批已提交 `5e69d584`；记录批随后（本行随记录批入库） |
| 438 收尾 | bootstrap | **合并旁路 440/460/461 ＋ 代录入册 ＋ 合并树快门禁复跑**（无编译器改动，纯批间窗口动作）：`git merge cleanup`＝`58f9c5db`（带入 `pattern.rs` +23、`tests/python_style/{run.sh,run_one.sh,t501*.z}`、`tools/jit_sweep.sh`、`tools/selfhost_compile.sh`；`worktree.md` 自动合并后两本台账逐段核实在位＝§4 我的 438 行在 107、§5 他们三行在 114–116）。旁路三条记录按时间归位誊进 roadmap.md 新节「旁路并入（2026-09-26，主线侧代录）」（排在 §优先级调整 之前，那一节仍为文件末节），**三条读数逐字标注为"旁路侧自取、主线未复测"**。**代录时当场复跑发现的抄录错**：上一行入册的 python_style `352→353`、`238→239` 两格是抄错的，真值＝**353 passed／239 行／113 文件**（AGENTS.md 那格已随行改）。**移交项登记**＝#176（`src/backend/codegen/codegen.rs:6759` 的 `exprs[&values[1]]` 索引 panic，SemiringFold 操作数未注册；坐标当场核过，那一行就是 `self.gen_expr(&exprs[&values[1]], exprs, None)`）⇒ gen/codegen 车道归主线。OPEN 净增 1（#176）；#170 仍队头未动 | **合并树快子集**（`/tmp/b439_gate.log`，二进制 md5 `bed3540ad05e5f019dbd990d247e911d`＝合并后 `cargo build --release` 现建，**不靠 run_all.sh 自建**＝绕开 #175 那格）：**`GATE_RC=1`、139s**（同一条 `--skip-*` 配方在 438 是 208s ⇒ 461 的并行化真进了主线树并生效）· official 194/194 compile、191/194 link（3 条 link-only 名单逐字同）· python_style **354/2/6/0**，红源仍存量 t231/t233 ⇒ **零新增红**；对 438 的 353 只 +1 过＝`t501`（`ls t*.z` 361→362，known-fail/xpass 未动＝不是判据放松）· 诊断 official 2/6、python_style 239 行/113 文件 逐字同 438 · comment_drift 0 · dyn_binding 4/不一致 0 · 无 `.rs` 改动 ⇒ 无锚点重绑。**未覆盖轴点名**：跳过的 13 步（含 `corpus`/`jit`/`truth+diff`）主线侧未在合并树复现 ⇒ 到下一次全量之前，全轴一致性只有"保留步逐项一致"一层证据。工具车道按 2026-09-26 用户裁定移交旁路侧，`tools/corpus_baseline.py` 那份改动留在工作树不代提交 | 合并 `58f9c5db` ＋ 代录 `b789b8f1`（`roadmap.md` +13、`AGENTS.md` +3/−1），已推 `agentic bootstrap`（`git rev-list --count agentic/bootstrap..HEAD`＝0） |
| 439 | bootstrap | **度量口径批／3.2 读侧字段布局：那把尺打在被解析状态的中途 —— 替身读 125→107，437 那条"10 条 declared type 已到手仍回 ("",2)"作废**（任务 #170 接手即结案）：根因＝`ZETA-DBG FA read\|write` 打在 `codegen.rs:6512`，而追加槽（434 的 `struct_ext_layouts`）那步在 `:6580` **之后** ⇒ 探针行上的 `variant=""` 是中间态。把那 10 条逐条对到收尾：`_backend` 6 条改后经 `variant="_Impl" -> ext slot 0 of 2` 解出、`history` 4 条两颗二进制都解出 ⇒ **没有一条是"类型到手没用上"**。改法＝收尾处（`:6615`）另打 `ZETA-DBG FA {role} final field= variant= field_count= idx=`（与探针行逐字一条对一条：502 读／126 写），旧行保留＋上方三行注释点名它是中间态。引用面先查过：四份夹具（t463/t464/t466/t469）只在头注文字里提到该探针，`tools/`、`docs/`、`src/middle` 唯一命中是 `gen.rs:1056` 一句注释 ⇒ 无工具/基线解析其格式，加行不碰判据。**顺带补记 438 未报的一处语料位移**：`-> ext slot` 行 **23→29**，多出的 6 行全是 `_backend`（438 只报了链接面）。**真口径现量（这颗二进制）**：读 502＝替身 **107**＋非替身 395、写 126＝替身 9＋非替身 117；107 行按接收者类型分＝`I64` **72**／`Named("Any")` 7／**具名类别 24**（Series 6 全 `index`、FinanceModule 5、ValueRecorder 4、DataFrame 4、MarketDataFetcher 2、OhlcvRepairReport 2、PyLogger 1）／Str 2、PyDynamic 2；按名头名＝`error_code` 11、`schema` 8、`metadata` 8、`positions` 7、`index` 6。**下一格形状换掉了**：437 的"类型到手没用上"是假问题，真问题是两类独立（72 行裸句柄只剩按名反查、24 行类别已知但名字不在声明格也不在追加表，后者与 #163 同形嫌疑、**成因未测**）。**新登记一条度量完整性形态**：同颗 `zetac_pre439`（md5 `bed3540a…`）、同份驱动，11:36 产物 **586,696 B**、11:50 起三趟全 **603,208 B**（三趟 md5 互异、尺寸稳定）⇒ **"产物尺寸变了没"不能当位移判据**，437 那格"两侧都 586,768 B"按此重读，成因未定位。**主线位移 0（正证据）**：两侧 `--emit-llvm` 出的 IR **逐字节相同**（4,398,143 B、md5 `bdbbbe86…`），改动整体在 `env_flag` 臂内；运行侧未取 A/B（同一份 IR 无对照可判）。代码批 `19ce411f`（4 文件 +58/−48） | **`GATE_RC=1`**（`/tmp/b439_gate.log` 134 行，跑在**改后**二进制 md5 `359a22d3…`）⇒ **零新增红** · official 194/194 compile、191/194 link（3 条 link-only 名单未动）· python_style **354/2/6/0**（红源仍是存量 t231/t233）· 诊断 official 2 文件/6 行、python_style 239 行/113 文件 · comment_drift 0 · dyn_binding 4 条/不一致 0 · 夹具头注改后单步复跑 `python_style` 逐字不变（354/2/6/0、239/113）· **锚点**：codegen.rs 净 +6 行 ⇒ `--rebind` 收 **23 条搬家**（`docs/ABI.md` 24 行/40 个数字、基线 23 行刷新）；**对照在隔离 worktree 取 HEAD 自基线**＝改前 漂移 28/新 10/消失 14/同键多义 8 组 ⇒ 改后 **27/10/12/7 组、rc 仍 1**＝净中性偏改（那 28 条起点不是本批：436 入册 23，旁路 460/461 的 `pattern.rs`、`run.sh` 行号搬家涨到 28）· **余量 39 条拒改**（同键多义／多命中／零命中／消失未配对），含 `tests/python_style/run.sh:16`、`:96`、`:136` 三条＝旁路侧无权重绑、按合并协议归主线手改，已入 backlog §4.1 · **未覆盖轴点名**：跳过 13 步未跑（`corpus`/`jit`/`truth+diff`），距下次全量还有 450 那格 | 代码批已提交 `19ce411f`；记录批随本行入库 |
| 441 | bootstrap | **函数体里写的 `def`／`class`：提升分支只克隆 `body`、不读 `ret_expr` ⇒ 每个嵌套函数与方法少最后一条语句**（3.2 Lowering／尾元素摘出后的消费面不对称，任务 #171 结案）：解析层把 `{ … }` 体的**尾元素**从 `body` 摘进 `FuncDef::ret_expr`（`src/frontend/parser/top_level.rs:297-331`；`ExprStmt` 脱壳，裸 `Block`/`If`/`Call`/`PathCall`/`Match`/`Loop` 整条摘走；批次 287 例外＝内层末项是 `Return` 的 `Block` 不摘；`Assign`/`Return`/`While`/`For` 从不摘），顶层消费点读它（`gen.rs:2420-2426`），而 `fn_depth > 1` 的提升臂只 clone `body` ⇒ 嵌套 `def`、以及函数体内 `class` 的每个方法，**静默少最后一条**（无 warning、编译 rc=0）。**语义裁决＝折回成一条语句**，不套顶层那层 `Return`：Python 丢弃表达式尾的值，提升副本的返回值仍由它自己的 `return` 决定 —— 绕开 `func_ret_types`（416/#142 那格）。**同时锁三种形状**（t472）：① 尾是调用语句 ⇒ 只丢那一条 ② 尾是裸 `if` ⇒ 整条 `if` 被摘走、方法体变空壳 ③ 尾是 `return` ⇒ 不摘、不受影响。**改前实拍**（`target/release/zetac_pre441`，与改后那颗同目录）：只有 `outer 1`/`cls a`/`def a`/`outer 2` 四行，三条尾行一行不出；改后 7 行逐字＝当场跑 `python3` 的对数。**438 登记②改判**：那格"try 体里有幽灵成员调用 ⇒ 整个方法体被丢"**因果错** —— 被丢的不是"含幽灵调用的 try"，是**任何**尾元素；try 在解析期脱糖成的 `Block` 末项是 `If`，正落在摘出清单里。旧文不回改，本行为对照。**#171 原读数当场复拍**（同目录两颗跑 438 那份 `probe438_shapes.z`）：改前 stdout 只 `fwd 4`，改后 `fwd 4`＋`caught`（stderr 一句 `[dynamic]Inner::nonesuch — raising`，被 except 接住 ⇒ run_rc=0）⇒ 决定吞不吞的是"在不在尾部"，不是 try。**批号 440→441**：440 已被旁路度量批用掉（`worktree.md` §1 已注明），改后重编 md5 未变 ⇒ 改名纯文案。**损害量**：**主线 301 位移 0**（A/B n=6/侧、逐轮两侧相同；第 5 轮两侧同 rc=139/rerr 119；stdout 那行是堆地址样整数＝不是尺）；**语料家族成员 0**（负结果：`class` 体内嵌 `def` 的写法在语料 40 文件里没有）；**用户项目 37 处**（19 文件；`jq_shim.py` 11／`nautilus_engine.py` 6／`data_prefetch.py` 3／`local_strategy_backtest_worker.py` 2＋15 文件各 1）＝CPython AST 当场数（口径点名：只数函数体内出现的 `FunctionDef`）。**新登记 #178**：方法名换成 `run` 时调用点绑到 `zeta_sieve_run`（std 例子遗留的裸名兜底）而非嵌套类自己的合成符号 —— 名字串线，与本批缺陷无关。**#172 收窄**：`%-format` 那支量到归属已登记的 **#47**，非新缺陷。代码批 `5e2c235a`（`src/middle/mir/gen.rs` **+14/−1**、`tests/python_style/t472_nested_def_tail_stmt.z` 新 **59** 行、`docs/ABI.md` 29/29、tsv 37/34）＋锚点批 `38bad1d7`（tsv 0/3，删手移后遗留的 `gen.rs` 9205/9927/11695 三条孤立键，基线 349 行） | **全量门禁（441 那格，两次独立跑、机器独占）**：第一趟 `GATE_RC=1`／388s（`/tmp/b440/gate.log`）· 第二趟（清干净后）**`GATE_RC=1`／211.25s**（`gate_clean.log`）⇒ **零新增红**：official 194/194 compile、191/194 link（link-only 3 名单逐字未动＝`integration_all_features`／`quantum_basic`／`selfhost`）· python_style **356/2/6/0**（364 夹具；比 439 的 354 **+2＝t472＋旁路 t502**，红源仍是存量 `t231`/`t233`）· 语料 40/40＝100%（63s）· jit **ok=177** trap=383 fail=1 timeout=0 segv=0 total=561（ok 176→177＝新夹具进 ok；第一趟 total 562／fail 2＝临时探针在目录里；**该步只有 ok/segv/total 能当尺**：同颗二进制单独跑得 trap 373/fail 11，分档随跑法变）· truth 22/23、str 20/23、container 27/28、numeric 23/28、control 28/28 逐项同 439 · diff 120/130＝92.3% bad_case=0 · knob 23／swallow 6／import 22／empty_stmt 68／pysrc 42／cli_semantics 87／ignore_rules 19／mbvar 24／emit_stable 2／dyn_binding 4 条不一致 0／comment_drift 复述 0 · clean_checkout rc=0（3s，第二趟 rev `5e2c235a`＝本批代码提交；第一趟 `4e0626ae`＝其前一颗，口径点名）· 诊断 official 2 文件/6 行、python_style 239 行/113 文件 · **锚点终态 vs 同文件集 HEAD 对照**（对照在隔离 worktree 取自基线）：漂移 29→**26**、消失 12→**12**、新 10→**11**、定位失败 3→**2**、rc 2→2、tsv 349→349 ⇒ 净中性偏改；那 ±1 是同一条既有坏引用（`ABI.md:56 → gen.rs:3730`）**故意不 bless**；剩 2 条定位失败＝旁路 461 的 `run.sh:96`/`:136` 越界引用，**主线本批仍未手改**（#167 余项，439 已代录欠账）。**三条度量完整性新形态**：① 隔离 worktree 缺未跟踪生成物 `runtime/aliases.inc.c` ⇒ 对照组虚报 5 定位失败/14 消失（拷进去才 3/12），差值是产物不是修复 ② 同一步内两套 glob（`python_style` 数 `t*.z`、jit 数 `*.z`）⇒ 第一趟把 `tests/python_style/_tmp_probe440.z` 算进 jit（total 562/fail 2），而 `python_style` 从没算过（356+2+6=364＝`ls t*.z` 逐字对上）——我先前那句"第一趟读数被污染"只对 jit 那一支成立 ③ 门禁后台重编时别并发取被测二进制读数 | 代码批 `5e2c235a` ＋ 锚点批 `38bad1d7` 已提交；记录批随本行入库，随后推送。**遗留移交**：`run.sh:96`/`:136`/`:16` 三条代做仍欠（439 入册、441 仍未清）；产物留 `/tmp/b440/`，`/tmp/b440/headwt2` 那份 detached worktree 收尾时移除 |
| 442 | bootstrap | **`("", 2)` 替身读的"接收者类别已知"那一格：pylib 的 `Series` 没有 `index`**（3.2 Lowering／4.3.b 库数据面，任务 #177 收窄 24→18）：`ZETA_DBG_FA` 在合并树二进制上重拍＝**502 行成对读探针、107 行 `variant="" field_count=2`、其中类别已知的 24 行**（与 #177 在册数逐字相同，这条同时是分组 awk 不漏项的正证据）。**24 行按名字归位后逐名找声明点，六类因**：`Series.index` 6（库缺成员——`DataFrame` 有 `def index(self)`（`pylib/pandas.z:168`）、`Series` 没有）· `jq_shim___FinanceModule.FUND_NET_VALUE` 5（`jq_shim.py:562` 的替身类是**零字段** `struct__FinanceModule_0`，`FUND_NET_VALUE` 全树从不赋值＝平台侧表名）· `…bt_helpers__ValueRecorder.strategy` 4（`bt_helpers.py:137` 继承 `bt.Analyzer`，`self.strategy` 由基类注入，体内只赋 `self.history` 且在 `start()`）· `DataFrame.attrs` 4（库缺成员，且语料往里装**异构值**：`market_panel.py:74` 装一个帧、`market_data_fetcher.py:108` 装 str）· `…MarketDataFetcher._calibrator_obj` 2（全树 grep 0 命中）· `OhlcvRepairReport.__dict__` 2（内建 dunder）· `PyLogger.level` 1（`pylib/registry.txt:169` 句柄型，成员表 :177–180 里没有 `level`）。七格的 `FA decls` 全部 `declarers=0`，且 24 行的 final 探针**全部 `idx=0`** ⇒ 机制＝类别布局落空→按名全局反查也落空→`recovered.unwrap_or(("", 2))`（`codegen.rs:6498`）按 2 字宽加载、`extractvalue 0` ⇒ **读回接收者自己的第一个词，零诊断**。**最小修＝`pylib/pandas.z` +6/0 补 `Series.index`**（把 `DataFrame` 在册那条"行下标 0..n_rows-1"的约定补到同族；**没有**顺手补 `attrs`——那张表要装异构值，shim 的 `lt(map, str, 固定值类型)` 表达不出来，硬补＝在静默错值上再叠假绿，缺的那格能力已写进 backlog）。**夹具 `t473_series_index.z`（18 行／4 条 expect＝当场跑 pandas 3.0.5 的 `3/0/2/y`）**：改前同一颗二进制打 `3 / 4365373395 / 4365373399 / y`（`idx[0]`、`idx[2]` 是**地址**；`len` 偶然对＝句柄本身就是那个 3 元素向量），改后逐字相符。**A/B 用的是旧/新 `pandas.z` 两颗同目录产物**（`target/release/b442_{pre,post}.bin`，二进制 md5 `0c899d98…` 两侧同一颗＝本批不动 Rust） | **快门禁**（AGENTS.md 配方逐字复制，13 个 `--skip-*`）：`GATE_RC=1`／`real 2:12.91` ⇒ official 194/194 compile、191/194 link（link-only 3 名单逐字未动）· python_style **357/2/10/0**（369 夹具；356→357＝`t473`，known-fail/xpass 未动，红源仍是存量 `t231`/`t233`＝**零新增红**）· 跳过步以 JSON 零分母作证（corpus total=0、jit total=0）· 诊断面 official 2/6、python_style **114 文件/242 行**（上一格 113/239；那 +1 文件＝`t473` 自己发 2 条 warning，**+3 行未逐项归因**＝新登记 #180）。**位移＝0**：n=6/侧两侧都只落在在册两态（`rc=139 out=0 rerr=119`／`rc=0 out=1 rerr=321`），pre 4×崩 2×过、post 2×崩 4×过 —— **这差别不报成位移**（同侧方差吃不下 n=2，坑 51；两个态两侧都出现过）；同态 stderr 逐字节比只相差 12 行"跳过 <代码>：上市日…"日志（418 族的迭代序），`out=1` 那行是堆地址＝不是尺。**探针分母 502→508**（新属性读走进方法体，多 6 条＝`self.data` 字段读与 `len`）⇒ 替身计数只在同分母口径内可比，24→18 是同一 awk 同一驱动的读数。**锚点零搬家**（`grep -c "pandas.z" docs/ABI.md`＝0；本批不动 `src/**`/`tools/**`） | 代码批 `6e38ff5f`（`pylib/pandas.z` 6/0 ＋ `tests/python_style/t473_series_index.z` 18/0）＋记录批随本行入库，随后推送。**遗留移交**：`run.sh:96`/`:136`/`:16` 三条代做**第四次登记仍未清**；`/tmp/b442/` 留 A/B 两颗产物与 `pandas_{old,new}.z` 快照，`target/release/b442_pre.bin`/`b442_post.bin`/`b442_drv.bin`/`zetac_pre442` 收尾时移除 |
| 443 | bootstrap | **`PathCall` 走完所有臂没命中 ⇒ 交回一个从没进过 `exprs` 的 id**（3.2 Lowering／abort 面在 4.x 发射层）：`is_upper` 判的是**方法名**首字母，落空即无语句无登记；`SemiringFold` 与结构体字面量字段裸索引 `exprs` ⇒ `no entry found for key` 中止（`codegen.rs:6715`/`:6735`/`:6765`/`:7143`）。两处：① 接线孤儿截获 `std::mem::size_of`/`align_of`（`codegen.rs:4323`，`array.z:50` 的 `malloc(0*count)` 到此收）；② `lower_expr` 改闭合出口——未登记的 id 就地 `IntLit(0)` + 一声 W1010（248 处递归全过这道门，`lower_expr_node` 只在门内被叫一次）。#41 第二半闭、#176 结案、#181 新登记（65 行/25 文件仍是静默错值）。selfhost 48/3 → 50/1/0 新失败；official 194/194·191/194 两侧逐项相同、诊断面 2/6 → 5/15（+9 行逐条对上 W1010）；主线位移 0（驱动 IR 逐字节相同）。锚点 `--rebind` 25 行/46 数字，漂移 59→28、rc 2。夹具 `t474_mem_intrinsic_operand.z`（改前 rc=101 panic／stdout 0 字节实拍）。 |
| 444 | bootstrap | **`host_str_*` 有两个宿主、各长一批、谁也不核对谁**（4.x 运行期／3.2 名表的下游）：名表把 `find`/`index`/`count`/`lstrip`/`rstrip` 下成 `host_str_*`（`gen.rs:15375-15385`），JIT 侧 `src/runtime/host.rs:252/:266/:285/:290` 早就有实现、AOT 侧 `runtime/tokio_runtime_stub.c` 那串别名从来没写过 ⇒ 编译一路绿灯、死在链接期（rc=1、`_host_str_find` 未定义）。补四支实现＋四支别名（`tokio_runtime_stub.c` **+36/−0 纯插入**；`tokio_runtime.o` 重编 163,748→165,660 B、全局定义符号 **648→656**、只增不减）＋新用例 `t475_str_host_bindings.z`（9 expect 逐条 `python3` 当场对数）。代码批 `1738c12a`（6 文件：C +36/−0、夹具 +50/−0、ABI 10/10 等量、tsv 11/11、diff 基线 500/16）。**A/B 只用一颗编译器**：同一个 `target/release/zetac`（md5 `0d8433af…` 改前改后同一颗，因为 `src/**` 零改动）、只把 `ZETA_RUNTIME_DIR` 指向 `git show HEAD:tokio_runtime.o` 那一颗 ⇒ 改前 rc=1／缺的正是这四个名字／无产物，改后 rc=0／9 行逐字相符。**主线 301 位移 0（三层正证据，不是空 0）**：两颗链接产物的未定义符号集合逐字节相同（144/144＝驱动从未引用新名字）、崩溃身份 8/8 份 `.ips` 相同（`str_trim+24 ← normalize_to_jq+16 ← fetch_stocks+2924`，imageOffset 251428/51404/19740 两侧相同）、三格读数 5/5 次成功运行相同；3/6 vs 4/6 的崩与不崩 Fisher 单边 **p=0.5000**／双边 1.0000＝统计上什么也不是。差分面 **223→224／251（89.2%）**、转绿那条＝`str_find`；顺手量出 `diff_consistency.json` 基线停在 130 例时代 ⇒ 追平并登记 **#186**；`index` 的 ValueError 分歧登记 **#185**（损害等级从"链不上"改成"静默错值"）、#183 扩成"argc 不匹配⇒裸名外发一族"（`s.repeat(n)` 同因，`gen_str_s314159_003` 改后仍红、判据词 `compile`）。 | **rc=1**（`/tmp/b444/gate_post.log`；唯一红源＝存量 `t231`/`t233`）· official **194/194**＋**191/194**（link-only 3 条不变）· python_style **359/2/10/0**（`ls t*.z` 371＝359+2+10）· 诊断面 official **5 文件/15 行**、python_style **115 文件/243 行**＝与 443 终态逐字相同（t475 一条 warning 都不产生）· dyn_binding 4 条断言不一致 **0** · diff 224/251 89.2%（`--bless` 后 `match_min=224`，两次同读数）· 锚点漂移 **28→（本批 +9）37→26**、新 11／消失 12／rc **2** 与 443 §六 逐项相同 · **改动面不含 `src/**`** ⇒ 语料/jit/truth 等步输入未变，未重跑（全量＝批次 450）· OPEN 净增 +2（⬜4＋🟡16＝20 → 22，≤30，无可配对关闭旧项） | MERGED（代码 `1738c12a` ＋ 记录批 `8b44f1ff`；合并 `01c19422` 并 481–483 后在合并树重取读数：五步逐字相同、diff 224→**225** ⇒ 基线重 bless，详见 roadmap 批次 444 收尾段） |
| 445 | bootstrap | **重复族的两条独立缺臂：`*` 只认 str 在左（`n*s` 打堆地址）、名表没有 `repeat` 一行（`s.repeat(n)` 兜底外发裸名 `_repeat`）**（3.2 Lowering／#183 收一格＋#47 收一半）：镜像臂 `gen.rs:4886-4907`（整数判据照抄 `:4908` list×int 臂的六个整型，调 `host_str_repeat(right, left)`）＋名表一行 `"repeat" => ("host_str_repeat", 2, "str")`（`:15405`）——**两支收往同一支已存在符号，0 个新运行期符号、`tokio_runtime.o` 一字未动**（对 444"两个宿主各长一批"那格，本批做法是**不去长**）。改前实拍（从 HEAD 重编那颗，md5 `9f4c902d…` 与 444 终态逐字节同一颗）：`3*"ab"` 打 `12913397472` 静默错值、`.repeat(3)` rc=1 `Undefined symbols: _repeat`。新夹具 `t476_str_repeat_operand_order.z`（58 行／9 expect，含零/负重复、链式、括号形），改前整文件 `FAIL (编译失败: Linking failed)`、改后 PASS／stderr 0 行。**先量成员再定形状**：`.repeat(`／两参 `.find(`／`<int>*<str>` 语料 40 文件各 **0** ⇒ 收益在套件/差分面（`str_repeat_left` 转绿），不押主线。代码批 `2e68b09c`（5 文件：`gen.rs` +25/−0、t476 新 58、ABI 23/23、tsv 31/31、diff 基线 7/7） | **快门禁**（`/tmp/b445/gate_final.log`）：`GATE_RC=1`、红源仍存量 `t231`/`t233` ⇒ **零新增红** · official 194/194 compile、191/194 link（link-only 3 名单逐字同 444）· python_style **360/2/10/0**（`ls t*.z` 372＝360+2+10 逐项对上；对 444 的 359 只 +1 过＝t476，known-fail/xpass 未动＝判据没放松）· 诊断 official 5 文件/15 行、python_style 115 文件/243 行＝与 444 终态逐字相同 · comment_drift 0 · dyn_binding 4/不一致 0 · diff（本批单跑）**226/251＝90.0%** bad_case 0、`--bless` 抬 `match_min=226`、`by_verdict.compile` 仍＝1（＝剩下的 `find(t,start)`）· **锚点**：`--rebind` 31 条搬家全在 `gen.rs`＋22 行（ABI.md 23/23、tsv 31/31，等量净 0 行），终态漂移 26/新 11/消失 12/rc 2＝与 444 同残余形状（4 条拒改存量：codegen.rs:1412/:6212、gen.rs:1049、run.sh:16）· **位移 0（正证据）**：acceptance 驱动 `--emit-llvm` 两侧 4,401,701 B/113,166 行、md5 `4c497421…` 逐字节相同；**同侧噪声底实测**：改前那颗自两跑 stderr 相差 145 行而 IR 同 md5 ⇒ 这条路上 stderr 多重集不能当判据（443"各差 1 行"是运气）；运行侧未取 A/B（同 IR 无对照可判）· **JIT 一侧如实登记**：三形（含改前就坏的 `"ab"*3`）JIT 一律 E4016 rc=1，`host_str_repeat` 无 JIT 宿主 ⇒ 折进 #183，未造新静默形状。**自错入册**：夹具一稿 expect `I` 行脑补成 `[abababab]`（真值 `[abab]`），快门禁实跑判 FAIL 才纠出。**OPEN 净增 0**（#183/#47 各收窄、无行可换）；**配对关闭＝无** | MERGED（代码 `2e68b09c` ＋ 记录批 `407e7ea2`；合并 `f9fe9939` 并 484–487 后在合并树重取读数：快门禁逐项零新增红、python_style known-fail 10→11＝旁路 `t507` 钉子、diff 270/306 ⇒ 基线重 bless `match_min=270`，新登记 #188/#189，详见 roadmap「旁路并入 484–487」节） |
| 446 | bootstrap | **#182 W1010 族"逐名裁决"第一格：`HashMap::new()` 下成空 map、`import` 短写的 quantum 接上路由，一条名字不少地报给对方**（3.2 Lowering／443 那道 closed 出口的下游）：机制＝`lower_expr` 的 W1010 出口只保证"不崩、出声"，那 65 行仍是静默 `IntLit(0)` ⇒ 本批按**修法代价**（不是 443 的名字种类）分堆裁第一格。两处根因：① `HashMap::new()` 走完所有臂落空 ⇒ 读 0（`MirStmt::MapNew` 早就存在于 `:5540` 字典面量与 `:8456` `dict(m)`，只差 `PathCall` 这一条臂；新臂放在**表路由之后** ⇒ 用户自定义 `HashMap` 仍然赢）；② quantum 两条臂的判据问的是**前缀拼写**（`path[0]=="std" && path[1]=="quantum"`）⇒ `import std::quantum;` 后写 `quantum::Circuit::new(2)` 不命中，改判"路径里有没有 `quantum` 这个模块段"（末段是类名 ⇒ `quantum::new()` 旧答案不动）。**故意没动**的两处逐个看过才留：`std::memory::capability::new`（`:13158` 仍前缀死路径，65 行名单里没有它的成员＝改了没有实测受益方）、`std::time::now`（同族因、宿主形状另一件事，随 #190 走）。**新登记一条量法坑（第 70 条候选）**：W1010 标签恒打 `{method}()`、**不带实参个数** ⇒ 按字面 `"Parser::new()"` grep 得 0 命中（真身在 `BufStream::new(3)` 那类调用点），按标签计数先剥括号。**A 堆为什么这批改不了（实拍，不是推断）**：`fs_read_to_string` 链接期 `Undefined symbols`、`grep -l` 全部 `runtime/*.c` 零命中 ⇒ "宿主早已存在"只有三档（Rust `#[no_mangle]` ≠ AOT 可链 ≠ 返回值布局能用），AOT 缺宿主时接线＝把静默 0 换成整程序链不上（#428 存在的理由）⇒ 23 行转 **#190**（跨界面，需 `runtime/*.c` 授权）。代码批 `5782dc6a`（5 文件：`gen.rs` **+34/−4**、t477 新 37 行／3 值断言、ABI 5/5、tsv 6/6） | **快门禁**（`/tmp/b446/gate_post.log`）：**`GATE_RC=1`**、红源仍存量 `t231`/`t233` ⇒ **零新增红** · official 194/194 compile、191/194 link（link-only 3 缺绑符号名逐字同 444/445）· python_style **361/2/11/0**（`361+2+11=374`＝`ls t*.z` **374** 逐项对上；passed 360→361 那一格＝本批 t477，known-fail/xpass 未动＝判据没放松）· 诊断 official **5 文件/15→14 行**（−1 逐条归因＝W1010 标签集差集只有 `quantum::Circuit::new()`×1 消失，其余 11 条逐字仍在 ⇒ 是修好的、不是丢诊断）、python_style 115 文件/243 行＝逐字同 444/445 · comment_drift 0 · dyn_binding 4/不一致 0 · **全仓 988 个 tracked `.z` 普查（`--no-link`）W1010 65→59**，消失标签**逐字只有 6 条**＝`HashMap::new()`×5＋`quantum::Circuit::new()`×1，`diff` 无 `>` ⇒ **0 条新告警** · selfhost 51／通过 50／已登记失败 1／**新失败 0**，两颗 rc=0 逐项相同 · **锚点**：改后先读到漂移 **26→32**，+6 全是 `gen.rs`（`:13289→13319` 等 6 条）＝+22 行的机械搬家，`--rebind` 后回到 445 终态**逐字相同**（`diff` 空、新 11/消失 12/rc 2 不变）· **位移 0（正证据）**：acceptance 驱动 `--emit-llvm` 4,401,701 B/113,166 行、md5 `4c497421…` 两侧 `cmp` 无输出，同一 md5 也是 444/445 在册值 ⇒ **连续三批不动主线**；运行侧未取 A/B（同一份 IR 无对照可判，口径照 441/443）· **pre/post 两颗**：改前那颗从 `HEAD~1` 重编（`e199254d…`→二进制 md5 `722316ea…`，与本批首次构造逐字节相同＝两次独立构造同哈希），改后 `2d9c6f7c…`，**提交后 `touch` 重编仍同一哈希** ⇒ 门禁跑的是已提交的码；t477 改前实拍 **FAIL**（`len=0`/`get=0`/`norm=0`＋两条 W1010，存 `/tmp/b446/red/wd_pre/verdict`）· 工具坑入册：`run_one.sh` **恒 rc=0**、判据只写在 `<wd>/verdict`，且 `git checkout <rev> -- path` 会顺手 staged（要跟 `git restore --staged`）。**OPEN 净增 +1（24→25，≤30）**：#182 不关、按 59 行改窄，拆出 #190 | 代码批已提交 `5782dc6a`；记录批随本行入库，随后推送。**下一批队头按已实测损害量交用户裁定**：#190 宿主批（23 行，跨界面需授权）vs #165/#177（读侧布局 14 条）vs #183 余格（`s.find(t,start)`）vs #48（等方言裁决）。下一次全量门禁＝**批次 450**；`run.sh:16/:96/:136` 三条代做**第五次登记仍未清** |
| 446 收尾 | bootstrap | **合并旁路 488–492 ＋ 代录入册 ＋ 合并树复跑**（无编译器改动，纯批间窗口动作）：`git merge cleanup`＝`8d311ff6`（59 文件 +1366/−2；`tests/diff/cases/**` 新 `.dcase` **55 个全是新增**＝`gen_control_s1122_*` 20／`gen_dmethod_s97531_*` 20／`gen_builtin_s24680_*` 15，另 t508/t509 两钉子、`gen_random_diff.py` +116/−2、`worktree.md` +5）。**并入面零 `src/`、零 `runtime/`、零 `pylib/`、零 `docs/ABI.md`、零 `tools/baselines/`** ⇒ 同一颗 `zetac`（md5 `2d9c6f7c…`）、无重编、无锚点重绑。旁路五条读数按时间归位誊进 roadmap.md 新节「旁路并入（2026-09-26）：批次 488–492」（排在 §优先级调整 之前，那一节仍是文件末节）；**488/489/490 原标"旁路侧自取"，其中 490 那条（316/361）本次在合并树逐字复现 ⇒ 升为"主线已复测"**；491 的普查用 80 例**临时样本不进库** ⇒ 主线不可复测，只作旁路证据引用（口径点名）。**代录时当场改判一条归因**：488 在册"round 舍入族 ×4＝银行家舍入差异"被旁路 492 推翻（真凶＝`/` 真除法缺失 `66/10→6`，`round` 本身与 CPython 一致）⇒ #188 收窄为两面（sign 旗标、负数进制），t508/t509 已钉。**#186 当场复发**：跑前基线 `total=306`/`match_min=270` 而实跑 `judged=361` ⇒ **55 例红绿都不算数、该步仍 rc=0** ⇒ 已 `--bless` 追平 `match_min=316`（json +231/−11）；"基线 ≠ 实跑要出声"的修法在 tools 车道＝旁路侧，主线只代录数据。OPEN 净增 0（25，≤30） | **合并树快门禁**（`/tmp/b446m/gate_merge.log`）：**`GATE_RC=1`**／123s · official 194/194 compile、191/194 link（link-only 3 名单未动）· python_style **361/2/13/0**（`361+2+13=376`＝`ls t*.z` **376** 逐项对上；红源仍存量 t231/t233＝**零新增红**；known-fail 11→13 全是旁路两钉子，passed 361 一格未动＝判据没放松）· 诊断 official 5 文件/14 行、python_style 115 文件/243 行＝逐字同 446 · comment_drift 0 · dyn_binding 4/不一致 0 · clean_checkout rc=0（8s，rev=`8d311ff6`＝本次合并，覆盖到了）· **diff 步单跑**（快门禁跳过项补齐，`/tmp/b446m/diff_pre.log`）：match **316**／judged **361**／**87.5%**／bad_case **0**／rc **0**（146s，同一颗二进制）；bless 后复跑同读数（`diff_confirm.log`）· 无 `.rs` 改动 ⇒ **无锚点重绑** | 合并 `8d311ff6`（代码批 `5782dc6a` ＋ 记录批 `8f5af796` 已在合并之前推送）＋ 代录批（含 `tools/baselines/diff_consistency.json` 的 bless）＝本提交，随后推送。**遗留移交**：`run.sh:16`/`:96`/`:136` 三条代做**第六次登记仍未清**；下一次全量门禁＝**批次 450** |
| 447 | bootstrap | **泛型 `impl` 的登记键带着 `<K, V>` 进名表，调用点写的是裸类别名**（2.2 名字绑定／#182 B 堆第一格）：根因一条＝`impl<K, V> Map<K, V>` 头部的类别名**连同尖括号参数**被拼成 `Map<K, V>::new` ⇒ `func_ret_types` 永不命中。后果两条分开量：方法名 `new` ⇒ 幽灵槽读 0 ＋ W1010 出声；方法名是别的 ⇒ 落"首字母大写即句柄"兜底、改写成 `zeta_platform_obj("<m>", …)` **一声不出**。修法＝`resolver.rs` 登记 4 处改用 `impl_key_base(ty)`（截到 `<` 前、无 `<` 原样），定义名与调用名同源一起改名；`self.impls` 与 `gen.rs` 的 3.2 判据一个字未动。代码批 `70ee016c`（4 文件 +110/−7＝resolver.rs +20/−4、t478 夹具 87 行/5 断言、ABI 1 行 2 数字、tsv 2 行）。**#191 结案、#182 再改窄、一格改判**：B 堆 28→23（−2 收掉、−3 是 `Mir::new` 仓内无定义 ⇒ 转 C 堆 8→11），A 23 不变 ⇒ 23+23+11=57 与普查闭合 | 快子集 13 步（GATE_RC=1，红源同 446）· official 194/194·191/194（3 条 link-only 缺绑名单逐字同 446）· official 诊断 5 文件/14 行（＝446 终态）· python_style **362/2/13/0**（362+2+13=377＝`ls t*.z` 逐项对上；红源仍 t231/t233；诊断 243 行/115 文件＝446 终态）· 新夹具 t478 pre **FAIL**（actual 空＋1 条 W1010 `` `Box2::new()` ``）→ post **PASS**（0 条 W1010）· selfhost 51/50/1/0/0 rc=0 · **普查 W1010 59 → 57 行、去重 29 → 27 种，多重集 diff 只有两条 `<`（`Container::new()`、`Map::new()`）＝0 条新告警** · **位移 0（正证据）**：acceptance IR 两侧逐字节相同 4,401,701 B/113,166 行/md5 `4c4974216c3737309ac806cd5141a55c`（连续四批同一值）· 锚点 `--rebind` 后回到 446 终态 26/11/12（+2 全是 resolver.rs 机械搬家 `:2011→:2015`、`:2822→:2826`）· **两条负结果**：静默句柄族在语料无额外成员（18 个声明 `impl<` 的文件 `@zeta_platform_obj(` 调用点 35→35、逐文件 diff 空）；"删 `is_self_compilation` 硬写"＝`main.z` W1010 2→4 行 ⇒ 负收益 · **普查里那条 `Segmentation fault: 11` 已排除本批**（`census.sh` 不带 `-o` ⇒ 会执行语料程序；全集逐文件 rc 两侧只差 2 个，均已归因） | 代码批已提交 `70ee016c`；记录批随后（本行随记录批入库）；`run.sh:16`/`:96`/`:136` 三条代做**第七次登记仍未清**；下一次全量门禁＝**批次 450** |
| 447 收尾 | bootstrap | **合并旁路 493–496 ＋ 代录入册 ＋ 合并树复跑**（`git merge cleanup`＝`af811fd9`，并 10 颗：`46 files +813/−2`）。并入面 **`src/`·`runtime/`·`docs/ABI.md`·`tools/baselines/` 零命中**（唯二代码面之外：`pylib/registry.txt` +1＝496 的 `W str find`、40 个新 `.dcase`、t510/t511 两颗用例、`tools/gen_random_diff.py`）⇒ 无编译器改动、无锚点重绑；但合并后 `cargo build --release` **真的重编**、md5 `5a517173…`→`f58b3da0…`，据 445 在册判读为"零源码改动下 md5 搬家"，**不用 md5 论证二进制同异** | 合并树复跑：official 194/194·191/194·link-only 3（名单逐字同 447）· official 诊断 5 文件/14 行 · python_style **363/2/14/0**（`363+2+14=379` 与 `ls t*.z` 逐项对上；passed 362→363＝t511 转绿、known-fail 13→14＝t510 钉子；红源仍 t231/t233 ⇒ **零新增红**）· 诊断面 243 行/115 文件（＝446/447 终态）· `check_registry_symbols.sh` **rc=0**（496 那条 `W` 行主线侧独立复测）· **diff 步 match=356 / judged=401 / 88.8% / bad_case 0**（judged 361→401＝494/495 那 40 例首次进闸且**全 match**，与旁路 495 在册读数逐字相同 ⇒ 升为主线已复测）· 快门禁 `GATE_RC=1`／3'43" · **#186 那格第二次当场复发**：基线停在 `judged=361/match_min=316` 而实跑 401 ⇒ 已 `--bless` 追平（json 1480→1640 行、+168/−8，删的 8 行全是计数字段），复跑判定 `CONFIRM_RC=0` | 代录进 `roadmap.md`「旁路并入：批次 493–496 ＋ 批次 447 收尾合并」（四节：合并面/复跑读数/三条与主线记账直接相关的/基线追平/OPEN 账务）；`41d5c1e6`＝447 记录批、`af811fd9`＝本次合并；**#190 的负边界代录**（493 定性＋495b 解析器脱糖两轮皆否 ⇒ 正解唯一在 `gen.rs` lowering，"解析器侧不要再试"）；**#188 余半移交主线**（两参 `s.find(t,start)` 缺三参 shim `host_str_find3` ⇒ `runtime` 车道需授权，与 #183 同路由）；主线侧 OPEN 净增 **0**；`run.sh:16`/`:96`/`:136` 三条代做**第八次登记仍未清**；下一次全量门禁＝**批次 450** |
| 448 | bootstrap | **字段在接收者布局里没有格子时出声：读侧与写侧共用一条 W1011**（3.2 Lowering／出码，任务 #177 结案、#163 半收、#192）：`resolve_field_slot`（`codegen.rs:6361`）三条布局路（跟构造表达式／跟接收者声明类型／按字段名全局反查）都放手时落到全文件**唯一**一处 `("", 2)`（:6498，grep 作证），读侧调用点 :7287 与写侧 `StructFieldStore` :5924 自 433 起共用同一副替身 ⇒ 访问一律打在 2 词的**第 0 词**：读回接收者自己声明的第一格、写把那一格踩掉，此前一声不出（427 的探针要 `ZETA_DBG_FA=1` 才响）。**先量后写**（442 授权文字逐字遵守："#163 与 #177 余量合并一批做"）：39 文件逐个 `--emit-llvm`（带 `-o`＝只读不执行）读 `FA {role} final` 且 `variant=""`（439 更正过的口径：`FA read` 那条早于追加槽，量到的是中间态）＝**替身读 3275／写 343，全部 idx=0，合计 3618**，与改后二进制上语料 W1011 行数**逐字相等**（告警覆盖面＝普查面）；驱动单文件 120、`tests/unit-tests` 7（全在 `quantum_basic.z`：`im`/`re`/`qubits` 打在 `receiver typed I64`）、`python_style` 0。**修法**＝一个标志位＋一个发射块（`used_stand_in && variant.is_empty()`，放在最终三元组**之后**）：一码一义（读写共用兜底 ⇒ 拆两码即破 356/361 的判据）、role 写进文本各说后果、**不去重**（`&self` 拿不到可变表，且重复行＝不同站点）、**不按接收者种类收窄**（只报具名类别会让 89% 损害继续静默）。**唯一一次返工实拍**：第一版只看 `used_stand_in` 在驱动文件多打 15 行（71→86）＝把 434 追加槽接管的站点也说成"落在第 0 词"，改后三文件逐字对上 71／262／174。**夹具两条**：`t479`（22 行，静默→出声；改前二进制 W1011=0、改后 1，stdout 与 IR 逐字相同；`ghost 7` 是**现状锁**，CPython 3.14 在这里抛 `AttributeError`）＋`t480`（21 行，追加槽接管形状必须不出声＝`expect-no-compile: [W1011]`，改前改后皆 0）；第三方形证 `t469` 在新二进制 0 行。**写侧夹具没做**并如实记录：`def put(o, v): o.tag = v` 探针实测不进替身（走动态 map 路），写侧证据只有 343 条普查。代码批 **`023ce4c7`**（6 文件：`codegen.rs` +29/−0、`error_codes.rs` +7/−0＝`FIELD_HAS_NO_SLOT_IN_LAYOUT` 沿用 W1010 的裸 `eprintln!` 先例、两夹具、`ABI.md` 25/25、tsv 25/25）。**两条口径核对入册**：① #177 的"18 条"按 (文件,字段,类别) 归位、本批按站点逐条，另三刀 236 探针行／71 站点／21 名字都不等于 18 ⇒ **口径不同不是矛盾**，也不许把 3618 说成 442 量错；② 数字字段名一族：语料替身站点 0 条、套件 `final` 探针 0 条 ⇒ 判据不加那一支。 | **快门禁**（AGENTS.md 13-skip 配方逐字复制）：`GATE_RC=1`／红源仍是存量 `t231`/`t233` ⇒ **零新增红** · official 194/194 compile、191/194 link（link-only 3 名单逐字未动）· official 诊断 **5 文件/21 行**（上一格 5/14；**+7 行/0 文件逐项归因＝全在 `quantum_basic.z`（6→13）**）· python_style **365/2/14/0**（363→365＝`t479`＋`t480`）· python_style 诊断 **116 文件/244 行**（115/243；**+1/+1＝`t479` 那一条 W1011**）· comment_drift 0 · dyn_binding 4 条不一致 0 · 跳过步以零分母作证。**位移＝0（正证据，非空 0）**：语料 39＋unit-tests 194＝**233 文件的 `--emit-llvm` 逐个逐字节相同**，驱动两侧 `.o` 逐字节相同（761,792 B）；stderr 面 `ADDED=3970/REMOVED=345` 那 345 行**不是被吞的告警**——剔掉 W1011 后**排序多重集 234 文件全等**，且同侧噪声底当场量（改前二进制自比三跑：quantum 差 4 行、jq_shim 差 10 行）⇒ 差的是 W1010/ABI/PY-A 的**发射序**（存量，与本批无关）；运行侧 n=4/侧两侧同为 1×rc=139(rerr 119)＋3×rc=0（stdout 11 字节＝堆地址样整数，不是尺）⇒ 分布相同、不报成位移。**锚点 `--rebind` 两趟才收完**（25 条引用；3 条 `:7700/:7709/:7779` 被 436 撞键守卫拦下，理由是落点已被占用，而那三把键正是本批刚搬走的 ⇒ 守卫问的是改号前的快照，第二趟放行＝#167 余项的直接代价）；只读核对 rc **2**＝HEAD 对照 rc 2，漂移 28→**26**（−2＝`pylib/registry.txt` 两条存量漂移被收，非本批造成）、消失 14→**12**（−2＝`runtime/aliases.inc.c:1/:12` **只在对照组出现**——该文件未跟踪、隔离 worktree 里没有它＝441 §门禁完整性 ① 同形**第二次复发**）、新 11→11、定位失败 4→2、同键多义 7 组未动；`ABI.md` 行数 1032→1032（等长）。 | 代码批 `023ce4c7` ＋ 记录批随本行入库，随后推送。**OPEN 净增 0**：#177 结案、#163 不关（`@property` 写侧 setter 真修要的是"写不存在属性⇒抛 AttributeError"的语义裁决，属 #48 方言族，不是出码判据）；`quantum_basic.z` 那 7 条并进 #163/#177 收口注记不另起号。**队头**：主线崩点仍卡 **#145**（需 `runtime/py_additions.c` 授权）；纯编译器侧按损害量排＝#182 余 34 行、#167 余项（键结构迁移）、#52。**下一次全量门禁＝批次 450**。`run.sh:16/:96/:136` 三条越界引用**第八次登记并改判移交旁路侧**（工具面 2026-09-26 归 zcode，主线不再占这笔账）。产物留 `/tmp/b448/`（`ir/` 233 对 IR、`per/` 普查、`shift/` 两侧驱动产物），`target/release/zetac_pre448` 收尾时移除。 |
| 448 收尾 | bootstrap | **合并旁路 497–506 ＋ 代录入册 ＋ 合并树复跑**（无编译器改动，纯批间窗口动作）：`git merge cleanup`＝`a3c6abff`，并 **19 颗**（`10 files +243/−0`）。**并入面比合并信息里写的宽**：标题只说"503–506"，实含 497–502（`git rev-list --count 2539171e..a3c6abff`＝19，首颗 `2325b715`＝497、末颗 `474fe2f2`＝506 台账行）——提交不回改，以 roadmap 代录段为准。带入：`.dcase` 新 **7**（499/500/501/502/504/505/506 各一）＋ `t512_bool_print_in_logic.z`（known-fail 钉简报 ④）＋ `docs/HANDOFF-CENSUS-FAMILIES-2026-09-26.md` **+124**（498 十一族移交简报）＋ 旁路台账 9 行。**并入面 `src/ runtime/ docs/ABI.md tools/baselines/` 零命中** ⇒ 与 448 同一颗 `zetac`（`cargo build --release` 当场回 `Finished in 0.42s`＝未重编；依 447 那条教训，不拿 md5 论证二进制同异）。收尾动作：`target/release/zetac_pre448` 已移除，产物留 `/tmp/b448/`（改批）与 `/tmp/b448c/`（合并树）。**OPEN 净增 0**；449 头名候选换成简报 ⑪（`Assign(Tuple, ·)` 解包不写类型标记＝三族一修，闸门用例已就位），仍交用户按损害量裁定 | **合并树快门禁**：`GATE_RC=1`／166s 墙钟，红源仍是存量 `t231`/`t233` ⇒ **零新增红**；official 194/194·191/194·link-only 3、诊断面 5 文件/21 行（逐字同 448）；python_style **365 passed / 2 failed / 15 known-fail / 0 xpass**（`365+2+15=382` 与 `ls t*.z` 382 对上，known-fail 14→15 的唯一增量＝t512）、诊断面 116 文件/244 行；`comment_drift` 0；`dyn_binding` 4 条断言/不一致 0；只读锚点 **rc=2**、漂移 26／新 11／消失 12、同键多义 7 组/被顶掉 47 条（复述 448 重绑后终态）。**diff 单跑（补 `--skip-diff`）：`match=359 / judged=408 / 88.0% / bad_case 0 / rc=0`** ⇒ 那 7 例首次进闸（3 match＋4 mismatch，mismatch 恰为 `container_cmp_order`/`list_cmp_order`/`container_dict_comp_filter`/`tuple_unpack_str_type`）；跑前基线停 `match_min=356`（401 例时代）＝**#186 那格第三次当场复发**，主线侧 `--bless` 追平：json **1640 → 1668 行**（+41/−13），`cases` **401→408** 且**删 0 条／新增恰为那 7 条／保留条目 verdict 改动 0 条**，新基线复跑判定 **`CONFIRM_RC=0`、"差分一致率无回归"**。下一次全量门禁＝**批次 450** | ✅ 完成 |
| 449 | bootstrap | **元组字面量把每个槽的类型标记硬写成 I64 ⇒ Str 成员被抹平**（3.2 Lowering／简报 ⑪、任务 #193；代码批 `8ecd0eaa` `gen.rs` **+10/−1** ＋ 夹具 `t481`/`t482`，基面批 `f52b91a6`）：**根因位置与旁路 506 的定性不同**——不在 `Assign(Tuple,·)` 的解包绑定，而在 `gen.rs:14394` `Type::Tuple(vec![Type::I64; size])`；决定性证据是同一条 `--dump-mir` 的 `type_map`：改前元素自己 `2: Str`、`3: Str` 明明在表里，而 `4: DynamicArray(Tuple([I64, I64]))` 一字未看它；改后 `Tuple([I64, Str])`。下游三跳（数组元素型 :13464/13489/13505 → `pair_tys` :2912 → `pty` :2956）与读侧 `codegen.rs:4072-4078`（键在 dest 型）逐跳无错可挑；仓内现成正确形状＝返回元组 :2275-2317。槽恒 8 字节裸字 ⇒ **纯标记改动、不动布局/ABI**。**三触发按实测只有两族同根**：for 解包 `4303384678/4303384680`→`a/b` 收、过滤推导值侧 `4300714116`→`b` 收、`a,b=b,a`（纯 int、无 str 成员）`2 2`→`2 2` **不收**⇒ 交换族独立成因在 :1900 求值序，留 #190/`t510` 红 ⇒ 旁路「一条修法治三族」「修法＝解包绑定写标记」两处定性按实测改判（原文不回改）；负向对照＝纯 int 元组两侧逐字相同（`1 10`/`2 20`）＝"只补标记不改语义"的正证据 | **快门禁**（13-skip 配方）`GATE_RC=1`、红源仍存量 `t231`/`t233` ⇒ **零新增红**；official 194/194·191/194·link-only 3；诊断 official **5 文件/21 行**、python_style **116 文件/244 行**（两项＝448 终态未涨）；python_style **367/2/15/0**、`xpass=0`（＝两条新夹具不是顺带绿）；comment_drift 0；dyn_binding 4 条不一致 0；**diff 359→361/408 = 88.5%**、bad_case 0、逐条比对 **0 回归/2 转绿/0 消失/0 新增**（`tuple_unpack_str_type`＋`container_dict_comp_filter`）⇒ bless 到 `match_min=361` 后 CONFIRM 复跑无回归；锚点**一趟 `--rebind`**（只搬 `gen.rs:14712→14721`，`ABI.md` 1/1、tsv 1/1），终态 **漂移 26/新 11/消失 12（rc=2）**＝448 终态逐项相同。**主线 301 位移≠0**：acceptance IR `894eb139…`(4,431,845 B/113,859 行)→`ca5dad81…`(4,431,671 B/113,854 行)，裸 diff 2,657 行 → 归一 **7 处/17 行**，**6 处逐处归因**（−2 段 `to_string_i64`＝f-string `{bs_code}`/`{fname}` 插值不再走整数转串、宿主 `market_data_refresh.py:29-110`；+1 段 `map_str_key`＝dict 键成真题 str；`zeta_identity`→`"DataFrame::groupby"`＝接收者标记恢复后命中真方法；`py_not` 外呼→`icmp ne`＝语义等价；alloca 挪位＝中间量随标记增减）⇒ **无未归因增量**。运行侧 26 次/侧（14+12 两批）：pre 19×0/6×139/1×134，post 17×0/7×139/1×134/1×138；按 stderr 字节尺只有三站点且**两侧都出现**（正常 28,801 B／"缓存命中 103 只"后早站崩 14,957 B／`numpy.vstack` 桩中止 16,302 B）⇒ 判"不构成本批回归"，但**不写成"分布完全相同"**：post 有一次本批未见过的 SIGBUS(138)、n=1 不能归因，按 #145 崩点/信号非确定存量族登记 | ✅ 完成。**OPEN 净增 0**（`gen.rs:6387` 同形状残留不另开号、损害量未测，挂 #193 余项）。**任务 #193 改题**为实测形状。**收尾**：`zetac_pre449` 移除、产物留 `/tmp/b449/`。**下一次全量门禁＝批次 450**；449 后候选＝简报 ① 真除法（11 例闸门）／#182 余 57 行／#167 余项 |
| 449 收尾 | bootstrap | **合并旁路 507–511 ＋ 代录入册 ＋ 合并树复跑**（`git merge cleanup`＝`5fa84b94`，**24 files +1050/−2**：`tests/diff/cases/` 22 颗新 `.dcase` ＋ `tools/gen_random_diff.py` +57/−2 ＋ 旁路台账 6 行；**并入面对 `src/`、`runtime/`、`docs/ABI.md`、`tools/baselines/` 零命中** ⇒ 合并树仍用 449 那颗 `d8fdbfe7…`，门禁读数可直接对照 449） | **合并树 diff 382/430 = 88.8%、bad_case 0**；跑前基线仍停 `total=408` ⇒ **#186 第四次当场复发**，主线代录 `--bless`：json 1668→1757 行、`cases` 408→430、`match_min` 361→382；逐条比对 **新增 22／消失 0／既有条目 verdict 改动 0**（追的是分母不是判定）；`by_verdict` 首次多第五桶 `{match 382, mismatch 46, compile 1, runtime 1}`；bless 后 CONFIRM `CONFIRM_RC=0`、读数逐字相同。**#194 归因用对照实测、不用时序**：511 的 `.dcase` 提交 `6a5cfcad` 已含 449（祖先关系可证）⇒ 旁路"449 邻域"不能靠时序排除；临时退回 449 那行重编一颗（`8a1d0428…`）与 449 那颗各跑三次＝**两侧 compile_rc=0 / run_rc=1、3/3 全崩、`first word=8530099604827435105` 两侧逐字相同**（唯一差别是 ASLR 句柄）⇒ **449 不是病因**；夹具 `--dump-mir` 只有两条 `Tuple([])`（单元返回型）无非空元组＝结构面佐证；源文件已 `git checkout` 复原、重编二进制与原那颗**逐字节相同** | ✅ 完成。**OPEN 净增 +1**：新登记 **#194**（类字段 dict × 方法内 for × `w[0]` × if/else 读改写的组合崩溃；四单因子最小化均不崩）。记录代录入 `roadmap.md`（`## 旁路并入（2026-09-26…）`，**roadmap diff 67/0、0 删除**，优先级调整仍为末节）。`zetac_pre449` 已移除。**下一次全量门禁＝批次 450** |
| 450 | bootstrap | **全量门禁批（4.x 门禁与度量面／不动编译器一行）**：树＝`b63f4293`（并入旁路 512＝2 文件 +39，`src/`/`runtime/`/`docs/ABI.md` 零命中）；门禁前先跑 diff 单步发现**基线又滞后 1 例**（`total=430` vs 实跑 431）⇒ **#186 第五次当场复发**，代录 `--bless`（json 1757→1761、`cases` 430→431、`match_min` 保持 382＝新例是红、不抬地板）后才跑全量 | **17 步逐项入册**（`/tmp/b450/gate_full.log`，`GATE_RC=1`、**325s**）：official 194/194·191/194·link-only 3；诊断 official **5 文件/21 行**；python_style **367/2/15/0**（红源仍 `t231`/`t233`＝零新增红）、诊断面 **244 行/116 文件**；语料 **40/40**；jit **ok=180**/trap 401/fail 0/segv 0；truth 42/43·str 66/79·container 140/154·numeric 65/86·**control 69/69**；diff **382/431 = 88.6%/bad_case 0**（对 507 的 359/408：+2＝449 转绿两例、+21＝并入 22 例中 21 match）；knob 23/0、swallow 6/0、import 22/0、empty_stmt 68/0、**clean_checkout rc=0（3s，rev `b63f4293`）**、pysrc 42/0、cli_semantics 87/0、ignore_rules 19/0、mbvar 24/0、comment_drift 0、emit_stable 2/0、dyn_binding 4/0；锚点只读复跑 **rc=2、漂移 26/新 11/消失 12**＝448/449 终态逐项相同（三次并入没搬锚点） | ✅ 完成。**耗时口径更正入册**：438 的 666s 是**含 `cargo build --release`** 的口径，本批 325s 是**不重编**的口径（二进制未变），两数并列、不覆盖原读数。**OPEN 净增 0（26 不动）**。**512 的"506 元组解包同根邻域"归因不采信**（夹具无任何元组）＝旁路连续第二条未对照就归到 449/506 邻域，留给批次 451 实测。**下一次全量门禁＝批次 460** |
| 451 | bootstrap | **py 类方法没有注解，可脱糖把 `ret` 硬写成 `i64` ⇒ 中间层"按体恢复返回类型"的两道 `()` 守卫永远进不去**（3.2 Lowering／返回标记，450 §五 队头 ①＝512 钉的 `class_method_str_accum`）：根因链四跳逐跳实拍——① `top_level.rs:953-965` 无注解方法只有**裸 StringLit** 返回才写 `"str"`，否则一律 `"i64"`（同文件顶层 `def` 无注解写的是 `"()"`）；② `unannotated_return_ty` 两道守卫（改前 `resolver.rs:3122` 问 AST `ret`、`:3339` 问签名表 Unit）只认空注解 ⇒ 方法**永远返回 None**；③ 解析器自带推理遍同样按 `ret=="()"` 开门，改前探针实拍方法 `ret="i64" infer_return=false`、同形函数 `ret="()" infer_return=true`；④ `gen.rs:11801-11815` 查不到就 `.unwrap_or(I64)` ⇒ 运行期按整数打出一个 str 指针（靶夹具 `4310769584`→`a,b,`，而 `len(b.parts)` 两侧都是 `2`＝**值本身是好 str**，与 #117"槽上没标记"同症状不同源：这里标记有、被表默认值顶掉）。**修法**＝两道守卫各认一条新判据，判别据取接收者参数的**类型拼写**：脱糖把 `self` 定成类别名（`top_level.rs:944`）、手写 `impl` 定成 `"Self"`（`:43/:53`），加 `class_ty` 有定义这一问 ⇒ zeta 模式的 `-> i64` 一个字不动；恢复本身仍守 300 的严格聚合（每个 `return` 都推得出且相同）。代码批 **`19c54e59`**（2 文件 +67/−2＝`resolver.rs` +23/−2 ＋ 新夹具 `t483` 44 行/4 值断言，期望值当场跑 CPython 3.14.5 对数），基面批 **`0ccc9fd7`**（差分基线 7/7 行）。**四形边界实测**（不是三形，多测了一形正对照）：手写 `impl` 两颗二进制 IR 逐字节相同（1,195/1,261 行）＝`Self` 排除项的正证据；`@staticmethod` 返回本地 str `4336414720`→`b` **顺带收**，但成因是脱糖给非 `cls` 方法前置的**幽灵 `&mut self`**（#291 那一族），不记在本批账上；`@classmethod` `4336414722`→`4339314690` **未收**（`is_classmethod` ⇒ `new_params` 为空、判据看不见接收者）⇒ 立 **#195**；py 方法**写出** `-> int` 而体返 str 两侧都打地址＝判据没被用户注解触发（有意不越权，声明/实现不一致归 #33），**为何不触发未取探针、此格标未测** | **快门禁**（13-skip 配方，`/tmp/b451/gate451.log`）`GATE_RC=1`、红源仍存量 `t231`/`t233` ⇒ **零新增红** · official **194/194**·**191/194**（link-only 3 名单逐字同 448/449/450）· 诊断 official **5 文件/21 行**＝450 终态 · python_style **368/2/15/0**（`368+2+15=385` 与 `ls t*.z` **385** 逐项对上；367→368 唯一增量＝本批 `t483`，`xpass=0`＝不是顺带绿）· python_style 诊断 **116 文件/244 行**＝450 终态 · comment_drift 0 · dyn_binding 4 条/不一致 0 · **diff 382→383／431（88.6%→88.9%）**、逐条比对 **0 回归／1 转绿／0 消失／0 新增**（转绿那条＝靶 `class_method_str_accum`）⇒ `--bless` 抬 `match_min` 382→**383**（json 1761 行不动、只改 7 行），复跑 **`CONFIRM_RC=0`／"差分一致率无回归"** · **锚点未搬号**：只读核对 rc **2**、漂移 26／新 11／消失 12、待归属 100 条/91 种、同键多义 7 组/47 条＝**450 终态逐项相同**（本批两处改动点 `:3120`/`:3337` 都在 resolver 四条锚点 622/636/2015/2826 之后，故不跑 `--rebind`）· **主线 301 位移 0（正证据）**：语料 **40 文件**逐模块 `--emit-llvm`（两颗同目录、仓根 cwd、串行、120s/文件）＝ **DIFF_FILES 0／DIFF_LINES 0／RC-DIFF 0／MISSING 0**，acceptance 驱动 IR 两侧逐字节相同（4,401,527 B／113,161 行／md5 `5d47a0cd…`）；IR 相同⇒出码相同，运行侧 A/B 不取（口径照 441/443/446）· 两颗同目录：pre `d8fdbfe7…`／post `af087b05…`，**提交后 `touch` 重编仍同一 md5**＝门禁跑的是已提交的码 · **新量法坑入册（第 71 条候选）**：驱动 IR 的**绝对尺寸不是跨批对照的尺**——本批 4,401,527 B vs 449 在册 4,431,671 B vs 446/447 在册 4,401,701 B 三值互异而各批内部皆"两侧逐字节相同"；只比"同树两颗是否相同"，不用尺寸连续性论证位移，**本批不据此对 449 读数提异议**（成因未定位：候选按可能性＝仓外驱动输入状态＞取数目录＞并入面，后者 450 已记为 2 文件 +39 全为 `.dcase`） | 代码批 `19c54e59` ＋ 基面批 `0ccc9fd7` ＋ 记录批（本行＋roadmap §批次 451＋backlog #195）随本行入库，随后推送。**OPEN 净增 +1（26→27，≤30）**：#195 新立、无可配对关闭；512 的缺口**不立号**（本来就用基线那条 mismatch 顶着，本批转绿即结）。**收尾**：`zetac_pre451` 移除、产物留 `/tmp/b451/`；下一次全量门禁＝**批次 460**；451 后队头＝#194 组合崩溃 → 简报 ① 真除法 → #182 余 57 行 → #167 余项 → #195（先量再动）。`run.sh:16/:96/:136` 三条越界引用仍在他侧车道，主线不记账 |
| 451 收尾 | bootstrap | **合并旁路 507b/513b/514/515 ＋ 代录入册 ＋ 差分基线追平**（`git merge cleanup`＝**`2feff1de`**，并 6 颗：`376e46bc` 简报 ⑫ 文档、`89a5df8a` 513b 台账、`770d7629` 514 两条 `.dcase`、`75e7d06e` 515 一条 `.dcase`、`eefd7071` 515 台账、`0ae7b4f1` 507b 台账。文件面 5 颗：`docs/HANDOFF-CENSUS-FAMILIES…` 28/0、三条新 `.dcase` 22/38/17、`worktree.md` 3/1）。**并入面 `src/`·`runtime/`·`docs/ABI.md`·`tools/baselines/` 零命中、也没有新 `t*.z`** ⇒ `python_style`/`official`/语料/jit 的输入未变，沿用 451 在册读数不重跑；`cargo build --release` 当场回 `Finished in 0.55s`＝未重编，`md5 target/release/zetac`＝`af087b05…`（与 451 那颗在册哈希逐字相同；按 447 教训只作观察、不用哈希论证二进制同异） | **§4 冲突一处，按"两侧都留"解**：HEAD 侧 449／449 收尾／450／451 四行全留，cleanup 侧 §5 的 515／507b 与 513b 指针改判全收。**成因入册并立 #196**：旁路纯文档提交 `89a5df8a` 从陈旧快照整文件重写，**把 §4 的 449／449 收尾／450 三行主线台账整段删掉**（`git show cleanup:worktree.md \| grep -c '^&#124; 450 &#124;'`＝0 vs bootstrap＝1）——451 那行恰好插在同一位置才撞出冲突；本批若无 §4 新增，这三行会"自动合并"一声不出地消失。**差分单步（唯一受并入面影响的步）**：跑前基线 `total=431` vs 实跑 **judged=434** ⇒ **#186 第六次当场复发**；读数 **match 383／judged 434／88.2%／bad_case 0／rc 0**，分步 container **141/157**（450 时代 140/154 ⇒ 分母 +3、match +0＝三条新例全红，514/515 的钉桩按预期红）、truth 42/43、str 66/79、numeric 65/86、control 69/69；主线代录 `--bless` **`BLESS_RC=0`**：json **1761→1773 行（+17/−5**，删的 5 行全是计数字段）、`cases` 431→434、`match_min` **保持 383**（新例是红、不抬地板），逐条核对 **新增恰为那 3 条／消失 0／既有条目 verdict 改动 0**，`by_verdict` mismatch 46→49；复跑判定 **`CONFIRM_RC=0`／"差分一致率无回归"**。**合并后 cleanup 又前进 2 颗**（`7750e116` 510 两条 `.dcase`＋`283473fa` 台账，`git merge-base --is-ancestor 7750e116 HEAD`＝否）⇒ "并 6 颗"只对本次成立，下次合并分母再动。**OPEN 净增 +1（27→28，≤30）**：#196（合并协议看不见台账行被顶掉，S）。收尾移除 `target/release/zetac_pre451`，产物 `/tmp/b451/`（改批）与 `/tmp/b451c/`（合并树）；下一次全量门禁＝**批次 460** | ✅ 完成 |
| （续） | | | | |

## 5. Agent-2 旁路台账（ZCode 维护；cleanup）

| 批次 | 分支 | 主题（族 / 靶） | 门禁读数 | 状态 |
|---|---|---|---|---|
| 440 | cleanup | 度量批：目标族 `static mut` 实测已被 326–437 间批次清零——truncation inventory 在 437 基线报 **0 W1002 / 238 文件**；另发现 `parse_bisect.py` 对无分号方言判据失配（切点要求 `;`，DG06 增补建议）。反向队列第一子队列（截断）随即转入 zeta_src 余量 | 0 hit / 238 files | ✅ 度量完成（纯文档，已含在 460 提交） |
| 460 | cleanup | **引用模式修复**：`Some(&value)` 的 `&` 模式不被识别 → 整 fn 连同其后文件被丢（zeta_src/runtime/array.z 的 array_get，#79 第 2 条钉住项）。修：pattern.rs 补 `&`/`&mut` 引用模式臂（词边界检查，按槽位模型剥 & 绑定内层，递归支持 `&&p`）。t501 三期望实拍 42/0/5 ✓；array.z 解析 W1002=0；selfhost 判据 48 过/3 登记/0 新失败。**移交主线一项**：array.z 解析修好后暴露下一层 —— codegen.rs:6759 `exprs[&values[1]]` 索引 panic（SemiringFold 操作数 id 未注册 exprs，MIR 实测 12 节点；M06 家族的索引变体），钉住行已更新原因，gen/codegen 车道待主线接 | selfhost 48/3/0；t501 ✓ | ✅ READY —— 全量门禁 rc=0（gate460.log）：official 194/194（191 link）· python_style **352 过**/2/6/0（t501 在内）· 语料 40/40 · jit ok=176/segv=0，全轴与 413 基线一致 |
| 461 | cleanup | **门禁并行化**（tools 车道，backlog #23 的并行半步 / 架构评审 P2#16）：run.sh 单用例逻辑抽到 run_one.sh（判定逐字保留），按 PY_JOBS 分片派发、按名排序聚合，输出格式字节不变，PY_JOBS=1 可回串行；jit_sweep.sh 同法（ZETA_JIT_JOBS）。实测（437 基线）：python_style 步 **174-242s → 119s**、jit sweep 步 **60s → 32s**（同树同基线读数逐项一致：352/2/6/0；176/381/0/0/557）。**更正本行初版的错误对比**：我曾写"1h20m → 7分10秒"，1h20m 是我轮询被会话打断造成的墙钟误读，不是门禁真实耗时——全量门禁基线是 **11'06"**（qwen 批次 438 逐项计时，见 AGENTS.md 门禁节奏）；7'10" 是非独占条件下的墙钟，只作参考。本批与主线侧的 `--skip-*` 快门禁配方（208s）互补：skip 砍掉无关步骤、本批把剩下的步骤本身提速。并行上限 8 → 24（本机 10 核，默认即 10 路） | 全轴与基线一致；rc=0 | ✅ READY |
| 462 | cleanup | **parse_bisect 补无分号方言回退**（反向队列 · tools 车道）：换行分隔写法没有 `;`，原切点判据在长函数体内部找不到切点 →"未定位"（array.z 排查实测，#79 家族）。新增 bisect_line_wise 兜底：逐行两测 + 向后排除多行语句假阳性。实测：合成用例精确命中病因行 4（`let x: = 3`）；健康文件正确报无截断。已知边界记入提交说明：被解析器静默吞掉的坏语句会让破坏显形在更晚的行 | 合成用例 rc=0；健康路径 rc=1 | ✅ 完成（纯工具，按门禁分层只跑工具自身） |
| 463 | cleanup | **cargo test 存量红灯清零**（反向队列 · #23 前置）：① monomorphize 测试初始化器补批次 437 新增的 Mir 两字段——lib 测试此前**编译不过**且无人发现（#23 盲区的现场证据）；② header_colon 测试改 `#` 尾注释——python 方言里代码后 `//` 是地板除（t138 真用法），旧测试用 `//` 自批次 334 前后红到现在；加方言钉子一条。实测：`cargo test --lib -- --test-threads=1` **142 过 / 0 败**（SIGABRT 并行问题维持单线程口径，#23 既有） | 142/0 | ✅ 完成（按门禁分层：cargo test 本身即判据） |
| 464 | cleanup | **GC 懒初始化竞态根除**（#23 最后一块）：并行 cargo test 的 SIGABRT 根因＝两个线程同时首次 `std_malloc` → 都跑 `GC_init` → 静态根区间重叠 → libgc abort（macOS 崩溃报告栈实证：`GC_exclude_static_roots_inner` ← `GC_init` ← `GC_malloc` ← `std_malloc` ← identity 测试）。修：`src/runtime/std.rs` 用 `std::sync::Once` 显式 `GC_init` 一次。实测：并行 `cargo test --lib` **142/0 × 3 连跑**。**移交主线一条**：#23 至此全清，cargo test 可进门禁——但加步骤要动 `run_all.sh`（ABI.md 裸行号引用 ：10/:103/:198/:628，锚点搬家须走 #52 流程），建议主线在下一个锚点重绑批顺带加第 18 步 | 并行 142/0 ×3 | ✅ 完成（越车道说明：`src/runtime/std.rs` 5 行，未在所有权矩阵内、主线未持有，已在此报备） |
| 465 | cleanup | **fn 原型声明**（#61 + #67 的 `fn…;` 成员，一石二鸟）：`fn f(...);` 无 extern 前缀、`;` 收尾 → peek 判定后与 `extern fn` 同路（ExternFunc：签名承诺、external linkage、缺实现链接期响亮失败）。修前此类声明截断整个文件（顶层与 def 体内同病）。语义实测：原型在前实现在后 ⇒ 调用解析到实现（print 7）；未调用原型仅声明、链接过。`pass;`/`del x;` 实测已可解析（#67 其余两成员已随历史批次消解）。门禁：快门禁 official 194/194 · py 355/2/6/0 · 语料 40/40 · cargo 142/0 | 全绿 | ✅ 完成 |
| 466 | cleanup | **LAST_PP 串号根除**（#65）：行表改栈（上限 64/同文去重）+ 查询按"remaining 是哪份 pp 的后缀"选表 + 三处消费点（W1002/W1003/W1004）合并为一步'选表+算行' + parse_zeta 入口不再全清。旧机制每次解析抹上一份表 ⇒ 语料路径主文件诊断查到被导入模块的行表（批次 340 实测 128 行报 :729）；无匹配时诚实缺省（W1004 不出行号，不再夹假行号）。新增 3 单元测试；W1002 单文件行号实测仍准（:4 命中坏行）。门禁：cargo 并行 145/0 · swallow/empty_stmt 全过 · 快门禁 194/194 · 355/2/6/0 · 语料 40/40 | 全绿 | ✅ 完成 |
| 467 | cleanup | **静默偏差钉 known-fail ×3**（反向队列 · BR-C/BR-L 提取收成落测试）：t503 大字面量溢出静默 0（L06/L07）· t504 {x:c} 按十进制（CT33）· t505 {x:,d} 无千分位（CT32）——三条实测稳定、按值判 KNOWN-FAIL（known-fail 6→9，修复即 XPASS 自动转正）。**新发现移交主线**：`concurrent.futures` Executor.submit 运行期 SIGTRAP rc=133（d.z 实测，done() 语义钉不了——runtime/pylib 车道，建议按 423 的"句柄判形再解引用"套路定价） | py 355/2/**9**/0 | ✅ 完成（纯 tests，切片=python_style 本身） |
| 468 | cleanup | **#84 锁步族落差分库**：移位三面——numeric_shift（基础+负数右移，match 121）· numeric_shift_overflow（1<<64 截断错值，已知 mismatch 记账）· t506 known-fail（known-fail 10）。负数 floordiv/mod 已在库 ⇒ #84 闭合（roadmap2 登记 7 转 ✅）；除零族 oracle 自报错不适配 harness 维持登记 | diff 121/132 无回归 · py 355/2/10/0 | ✅ 完成 |
| 469 | cleanup | **状态补录两处**（roadmap2 登记 8/9）：refactor.md G.5d ②(b) 刷新（399 已交付"无声明半"，剩两形指 backlog #33）+ 轴 A 表补前端死代码四件套与 memory 孤儿文件两行 | 纯文档 | ✅ 完成 |
| 469b | cleanup | **合并前全量门禁**（462–469 八个批次的保险，分支自跑）：official 194/194（191 link）· python_style **355/2/10/0**（四条新 known-fail 钉子全数入读）· 语料 40/40 · jit **179 ok**/0 segv（t50x 三例进 sweep 且通过）· diff 121/132 91.7% 无回归 · 断言族全 0 违规。**全量 rc=0，cleanup 分支可安全合并** | 全绿 | ✅ 门禁入册 |
| 470 | cleanup | **轴 A 真删**（refactor §1 判据 (a)/(b)，全仓 grep 复核后删三个零引用死模块）：proc_macro(845)+macro_expand_advanced(617)+identity_ownership(470+tests 170) = **-2,105 行**（含 mod.rs 三行声明）；borrow_enhanced 保留——tests/memory-management 在消费（(b) 实证）。验证：build 绿 · cargo test 141/0（-4=被删内部测试）· dc_audit --diff rc=0 基线净减 · py 356/2/10/0。深化评审 C5 的最大一块落地 | 全绿 | ✅ 完成 |
| 471 | cleanup | **W1003/W1004 带文件名**（批次 340 承接项；466 栈式行表的延伸）：W1004 在解析期间发射 ⇒ 路径必须随解析传入——新增 parse_zeta_tagged（线程局部传递），4 个有路径的调用点换用；诊断从 ":行号:" 升级 "路径:行号:"。跨文件场景主文件诊断仍带主文件自己的路径（栈式选表）。实测 "e2e.z:6:"；142/0；swallow/empty_stmt/356 全过 | 全绿 | ✅ 完成 |
| 472 | cleanup | **G.3 进阶档第一块**：随机算术表达式生成器 tools/gen_random_diff.py（固定种子可复现；文法收窄绕呈现层噪声；CPython 先验滤 bad_case）+ 首批 20 例入 diff 库。**首批即量到 6 条真缺口、两个族**：① i64 溢出静默回绕 ×3（t503 的运算期兄弟）② 比较结果经混合运算丢 Bool 性 ×3（打印 0/1 非 False/True，M11 改型链缺口）——随机采样读数首次成立：缺口率 numeric ~24%，移交主线（diff 基线自动盯修复）。另：#81 登记（borrow_enhanced (b) 处置任务，配对 466 闭合盈余） | diff 135/152 91.7% 无回归 | ✅ 完成 |
| 473 | cleanup | **随机采样扩容**（seed=77102，+20 例，累计 40）：2 条新 mismatch 全落已知族 ⇒ 族谱稳定、普查可信；总 147/172 无回归 | diff 147/172 | ✅ 完成 |
| 474 | cleanup | **生成器扩 str 模式**（拼接/重复/索引/比较/upper·lower·strip·find·count·startswith；刻意不含 split/sort 列表 repr）+ 首批 20 例：19 match + 1 **compile 能力缺口**——`s.find(t)` 两参形式缺运行时绑定（链接期缺符号 `find`，registry/派发车道移交主线）。str 类采样从 3 人工例扩到 20。总 166/192 86.5% 无回归 | diff 166/192 | ✅ 完成 |
| 475 | cleanup | **合并前全量门禁**（469b 后 +10 提交，纪律到点）：official 194/194 · py **357**/2/10/0 · 语料 40/40 · jit 179/0 · diff 166/192 86.5% 无回归 · 0 RED。rc=0，cleanup 可安全合并 | 全绿 | ✅ 门禁入册 |
| 476 | cleanup | **生成器第三模式：随机赋值链**（语句级采样——变量类型流经签名表，单表达式测不到的路径）：15 例，13 match + **2 条 in-range 错值**（新缺口形状：链含负数按位 OR / 大数 floordiv / 求和，同量级但错误——机制定位移交主线，diff 基线自动盯）。溢出主导程序已过滤（|尾部|>=2^62）。修生成器一处：python_eval 包 print 会灭掉多行程序，加 python_eval_program | diff 179/207 | ✅ 完成 |
| 477+478 | cleanup | **容器采样 + diff 并行化**：容器模式（list/dict：字面量·len·索引·append·覆盖写·成员测试）24 例**全 match**——容器层稳（423 判形 + 430 布局见效）。diff_test.py 并行化（judge 入线程池；run_ref/run_zeta 按用例子目录——zetac 中间产物落 cwd 共享会互踩，实测全表 Linking failed 后修）。累计随机 84 例（数值 40/字串 20/赋值链 15/容器 24） | diff 203/231 87.9% 无回归 | ✅ 完成 |
| 479 | cleanup | **cmp 模式 20 例全 match**（链式比较/not/浮点比较——Python 链式语义无缺口）+ **补漏**：生成器本体的 str/stmts/list/dict 模式此前没随用例入库（可复现性漏洞，479a 修复）。随机库累计 139 例六模式；diff 223/251 88.8% 无回归 | diff 223/251 | ✅ 完成 |
| 480 | cleanup | **大样本普查**（seed=888，200 例临时）：mismatch 24/200 ≈ **12%**——小样本 24% 系高估，G.3 读数首次有大 N 统计；族分布：溢出主导、Bool 性、1 条 in-range 微差亚型（258802802811051 vs 259928218567380，疑似中间取整）。临时样本不进库；committed 维持 251。顺带修 479 引入的生成器回归（numeric/str 求值路径恢复） | 399/451 大 N | ✅ 度量批 |
| 481 | cleanup | **位运算/移位优先级改 Python 语义**（用户裁定 python 风格第一优先；随机普查 480 的 in-range 微差定性后的修复）：旧阶梯 & \| ^ 绑得比加减紧（C 式），`1 & 2 - 3` = -3 而 CPython = 1。重排（松→紧）：比较 → \| → ^ → & → 移位 → 加减 → 乘除 → range → 一元；4 处 LHS + 4 处循环 RHS 对齐。五探针与 CPython 逐字节一致；全量门禁全绿且 **diff match 223→224**（普查案例翻 match = 首例普查驱动修复闭环）。语义变更已在提交说明备案 | diff 224/251 89.2% · 全绿 | ✅ 完成 |
| 482b | cleanup | **普查不可复现观测定性 + DIFF_JOBS 开关**：482 那条一次性 mismatch（s51502_009）定性 = zeta 值嵌 GC 堆地址（str_repeat_left 两跑实得 12913443744/13106611104 即指针实证）⇒ 普查口径修正：verdict 稳定可判、嵌地址用例的 detail 天然不稳（#117 家族次生效应）。A/B（并行 vs DIFF_JOBS=1 串行）verdict 层逐字节一致 ⇒ harness 无窜位；顺带修复 478 缩进往返弄破的 try 语法（部分路径静默）。生成器确定性/编译确定性各自 10 连验证 | A/B 一致 | ✅ 完成 |
| 483 | cleanup | **mismatch detail 的 GC 地址脱敏**：zeta 侧 10 位以上纯数字归一 <N>（堆地址每次编译不同）——harness 两次全量输出 diff 仅剩 str_percent_fmt 一条短垃圾值（非地址、按进程随机，无法按位宽归一；verdict 层稳定、门禁不受影响，随 #117 家族修复自然消失）。差分读数的可复现性由此完备 | 全稳定（除 1 条已记残余） | ✅ 完成 |
| 484 | cleanup | **普查收敛确认 + 基线 bless**：新种子（90210）50 例——6 mismatch 全溢出族，481 后无新族、in-range 族消失（被优先级修复吸收）；`find` 缺口仍在（444 修的是 print 家族，registry/派发车道待主线）。diff 基线 bless：**诚实更正**——我最初记的 259 被 40 个未提交临时用例抬高（rm 未生效），合并树重 bless 实测 **match_min=225**（251 例 committed，89.6%），与主线 444 收尾值一致；账目错误已在本行备案 | 225/251 89.6% | ✅ 完成 |
| 485 | cleanup | **循环积累模式 20 例全 match**（M08 家族首次采样：for-range/while 五形状——计数器槽、累加类型流）+ rebase 冲突与账目更正（484 的 259 系未提交临时用例抬高，真值 225 与主线一致）。随机库累计 159 例七模式 | diff 245/271 90.4% 无回归 | ✅ 完成 |
| 486 | cleanup | **f-string 规格采样 fmt 模式**：进制/符号/宽度/补零/对齐/精度随机组合 20 例——15 match + **5 mismatch 新族**：sign 旗标不渲染（{n:+d} 的 + 被吞，宽度/浮点两形）+ 负数进制型（{x:b} 位回绕）——py_format runtime 车道移交。基础 11 探针全对（规格实现主体扎实）。随机库累计 179 例八模式 | diff 254/291 87.3% 无回归 | ✅ 完成 |
| 487 | cleanup | **切片模式 15 例全 match + None 打印钉子**：正/负/省略/步进切片与切片后 len 全对（323 修的切片族语义扎实）；t507 known-fail 钉 `3 and None` 打 0 而非 None（L09 词法降级 + print 无 None 渲染，#117 近亲）。随机库累计 174 例九模式 | diff 269/306 87.9% 无回归 | ✅ 完成 |
| 488 | cleanup | **内建函数族采样 15 例**（sum/min/max/sorted/abs/round/len）：11 match + **round 舍入族 ×4**（Python 银行家 half-to-even vs zeta 四舍五入，registry/runtime 车道移交）。**排障记录**：期间误报 str_repeat_left 回归——根因 = rebase 后未重建二进制，重建后 3 * "ab" 正确（444 收尾已修左形式）⇒ 教训：rebase 后必须先重建再取读数。随机库累计 189 例十模式 | diff 276/321 无回归 | ✅ 完成 |
| 489 | cleanup | **control 模式 20 例全 match**：for/while 的 else（无 break 才执行）/break/continue 随机组合——Python 特有控制流首次系统采样（while+continue 死循环组合被 CPython 先验正确淘汰 10 条）。随机库累计 194 例十一模式。qwen 已落号我方移交（#188/#189）| diff 296/341 86.8% 无回归 | ✅ 完成 |
| 490 | cleanup | **字典方法链模式 20 例全 match**：小键池的 len/get 带缺省/成员/覆盖写后读随机链——g_keystr 键文本找回表（C12）与 map_get_default 缺省路首次系统采样。随机库累计 194 例十二模式 | diff 316/361 87.5% 无回归 | ✅ 完成 |
| 492 | cleanup | **#188 两面补钉**：t508 sign 旗标（+ 被吞）+ t509 负数进制（位回绕）——known-fail 11→13。定性澄清：builtin 案例的 round "错值"真凶是 / 真除法缺失（66/10 → 6），round 本身（含银行家舍入）与 CPython 一致，该族已有 numeric_truediv 在案 | py 360/2/13/0 | ✅ 完成 |
| 494 | cleanup | **嵌套容器链模式 20 例全 match**（dict[str,list] 读写 / list[list] 双层 / dict[str,dict] 三层——历史 SEGV 族的普查面首次铺开，446 布局裁决见效）。随机库累计 214 例十三模式 | diff 336/381 88.2% 无回归 | ✅ 完成 |
| 495 | cleanup | **容器方法链模式 20 例全 match**（list sort/reverse/insert/pop/index 随机序列、str split/join、dict keys——12 条错误形态被 CPython 先验淘汰）。随机库累计 234 例十四模式 | diff 356/401 88.8% 无回归 | ✅ 完成 |
| 498 | cleanup | **普查族移交简报**（docs/HANDOFF-CENSUS-FAMILIES-2026-09-26.md）：九族一屏——最小复现/CPython 期望/zeta 现状/疑似修复位置/闸门用例/优先级建议（①②③ 打印格式化邻域可并批）。主线修复从此机械可执行 | 纯文档 | ✅ 完成 |
| 499 | cleanup | **列表字典序比较族钉住**：`<`/`<=` 错（句柄比较）而 `>`/`==` 对——不对称形状边界实测四例；list_cmp_order.dcase 钉住，gen.rs/派发车道移交（#190 邻域）。探针同时证实 str 比较、while-else+break 正确 | diff 无回归 | ✅ 完成 |
| 500 | cleanup | **容器比较族扩展定性**：tuple `<` 同病 + **dict `==` 也错**（两个相同字典得 False）——族从 list 扩到 tuple/dict，形状边界（`>`/list `==` 对）已实测；简报 ⑩ 更新。十模式普查 + 移交简报就位 | diff 无回归 | ✅ 完成 |
| 513b | cleanup | **交接包落地**：zcode-handoff.md 已入 bootstrap（主树推送 1a73b57a），本文件 §6 指向它——下一个 ZCode 会话读 AGENTS.md + zcode-handoff.md 即可自包含接手 | 纯文档 | ✅ 完成 |
| 501 | cleanup | **边界探针全对 + 重复边界钉住**（零/负次重复 = 空串、int()/str() 转换、负索引求和——CPython 逐项一致）。探针证实 int 键字典、f-string 内表达式、enumerate 亦通 | 全对 | ✅ 完成 |
| 502 | cleanup | **enumerate/zip 钉住**：元组解包遍历 + 平行遍历全对（控制流 + 解包组合面） | 全对 | ✅ 完成 |
| 503 | cleanup | **新种子收敛普查**（864213，60 例临时）：mismatch 8/60 ≈ 13%——与 12.5% 稳态一致，无新族（cmp 20 例全 match）。三读数并存：小样本 24%→大样本 12%→本轮 13%，普查进入稳态 | 410/465 | ✅ 度量批 |
| 508 | cleanup | **类/字段/方法模式 20 例全 match**（ctor 多字段/方法读写/跨实例运算——446/448 W1010/W1011 修复的普查面首次随机背书）。随机库累计 254 例十五模式。工具侧负结果备案：插入代码的转义层错位弄破 gen_random_diff.py，git 恢复 + chr(10) 重写 | diff 379/428 88.6% 无回归 | ✅ 完成 |
| 515 | cleanup | **字典方法族扫描**：20+ 方法扫完——13 个全对（rstrip/lstrip/strip/replace-count/center/zfill/split-maxsplit/remove/extend/count/pop/len），**2 缺口钉住**：values() 哈希序非插入序（[2,1]）+ items() 元素打地址（元组解包同根）——简报 ⑬ 移交 gen.rs/派发车道 | diff 无回归 | ✅ 完成 |
| 511 | cleanup | **组合普查首例崩溃**（class×loop×dict 交互）：类字段 dict + 方法内 for self.words + w[0] + if/else 读改写 = map_get 非字典 rc=1（三连确定复现；单因子最小化均不崩 ⇒ 需完整组合，gen.rs 车道 449 邻域）。组合普查的价值实证：单模式各自全对 ≠ 组合正确 | runtime verdict 钉住 | ✅ 定性批 |
| 517 | cleanup | **OOP 组合普查三四例**：① 类变量自增崩溃（Counter.count += 1 把 int 0 当字典解引用 rc=1——类级属性读写未实现）② 类方法内 self.data.get(k, default) 全打地址（set ✓ get ✗——⑫ 家族扩展）。对照组：普通函数 dict.get ✓ | runtime verdict 钉住 | ✅ 定性批 |
| 518 | cleanup | **稳态确认批**：XPASS 0（#188/#189/#190 未修）、diff 无回归、__pycache__ 清理。qwen 451 已将我方组合崩溃落号 **#194 并列为 452 队头**（复现件 /tmp/b449/c511/），协作闭环运转中。cleanup 领先 16 提交，等主线合并 | 全绿 | ✅ 稳态批 |
| 516 | cleanup | **OOP 组合普查三例**：① self 链式返回 ✓ 对照组 ② **列表字段经方法 append = map_get 非字典 rc=1**（515 字段槽位族扩展：ctor 存的列表经方法读出非列表）③ **跨类实例引用方法 = 静默无输出**（rc=0 应打 200，#117 邻域）——均 gen.rs 车道 | ①✓②③钉住 | ✅ 定性批 |
| 512 | cleanup | **sorted(reverse) 字符串版排错钉住**：["b","a","c"] reverse → zeta ["c","a","b"] vs CPython ['c','b','a']——整数版正确 ⇒ 字符串降序路径缺口（registry/runtime 车道）。同批证实：while+continue（正确增量）✓、for-else+continue ✓、sorted(reverse) 整数版 ✓ | diff 无回归 | ✅ 完成 |
| 513 | cleanup | **int 键字典迭代序哈希序钉住**（values 顺序族扩展到 int 键——插入序 10,2,30 vs 哈希序 30,2,10）。同批证实：混合比较 1==1.0 ✓、负浮点 ✓ | diff 无回归 | ✅ 完成 |
| 514 | cleanup | **类方法字符串族两例入闸 + 区分定性**：字段存储 ✓（c.name 打 Kitty）但方法内 self.name + "..." 的拼接结果打地址 ⇒ 缺口收敛到"方法上下文的表达式结果类型标记"（⑫ 家族，非继承/继承两形都中）。类继承本身无罪 | diff 无回归 | ✅ 完成 |
| 515 | cleanup | **继承链实例字段跨类不可见钉住**：Dog(4) ctor 写 self.legs=4、继承方法读到 0（字段槽位按"哪个类的方法"分配而非按实例）——W1010/W1011 族的 OOP 面，gen.rs 车道。对照组：类级属性 ✓、默认参数 ✓、非继承字段 ✓ | diff 无回归 | ✅ 定性批 |
| 512 | cleanup | **类方法内字符串积累丢类型钉住**：类方法 loop 拼接 out 打地址，普通函数/模块级同形全对 ⇒ 缺口特定于类方法上下文的局部变量类型标记（gen.rs，506 元组解包同根邻域）。组合普查第二例（c2 方法返回 dict 链式访问 ✓ 对照组） | runtime verdict 钉住 | ✅ 定性批 |
| 509 | cleanup | **高频构造回归面钉住**：变量边界切片 / 字符串·列表增强拼接 / 函数多返回值解包——四发全对（qwen 448 收尾已并入 497–506） | 全对 | ✅ 完成 |
| 510 | cleanup | **str.format 方法缺绑定钉住**（无参/带参同病，registry+C shim 双缺，#188 家族）+ dmethod2 模式十例（setdefault/sorted-reverse ✓；enumerate-start 与 items 哈希序为已知族实例）+ 证子串 in 正确。t478 抖动监控：套件三轮全 PASS 未复现 | diff 无回归 | ✅ 完成 |
| 511 | cleanup | **% 格式化操作符整体缺失定性**（⑭，最高频缺口）：字符串左操作数的 % 没接格式化路径——五形态全错（%d 垃圾/%s 地址/%.2f 错值/%x 十进制/%% 字面量）。py_format 机器已在（486 基础探针全对）只缺 % 路由——gen.rs 车道。t513 钉子 + str_percent_format_family 闸门 | diff 无回归 · py 368/2/16/0 | ✅ 定性批 |
| 510b | cleanup | **任务完成度审计**（用户问询触发）：backlog OPEN 19；selfhost 钉住收敛 3→1（actor/map 已修，剩 actor/result + array.z 的 codegen panic 均 gen.rs 车道）；XPASS 0（#188/#189/#190 未修，主线 447/448 在 W1010/W1011 族）；我车道 actionable 清空——普查稳态、简报在主线手里。full-gate 尾数：365/2/15、diff 359/408 | 全绿 | ✅ 审计批 |
| 510 | cleanup | **深度普查**（十五模式全开，seed=777777，375 例临时——历史最大 N）：总 804 例 judged，match 727 = **90.4%**；分模式缺口：numeric 19/44、fmt 18/25、builtin 18/25（round 族经 truediv 放大）、stmts 8/33，其余十一模式 **0 缺口**（容器/控制流/切片/循环/类全净）。族分布与历史一致（溢出/sign/负进制/round·truediv/Bool），无新族。临时样本不进库 | 727/804 90.4% | ✅ 度量批 |
| 504 | cleanup | **字典推导式验证**（BR-L 漂移疑点④解除）：{k: k*2 for ...} 的 len/键读/键序全对——"是否真产出 dict 存疑"的旧问题实测无缺口，疑点关闭 | 全对 | ✅ 完成 |
| 507 | cleanup | **全量门禁**（30+ 提交纪律，含 481 优先级与 496 registry 两个编译器面改动）：official 194/194（191 link）· python_style **364+1 抖动**/15 known-fail · 语料 40/40 · jit **180**/0 · diff 359/408 88.0% · RED 0。**一次性抖动备案**：t478 在门禁中 FAIL、手动+三连套件全 PASS（365/2/15）——当时正并发另一 worktree 的全量构建（3m26s），判为资源争抢抖动；若静默门禁复现则升级为真问题。同批确认：#188/#189/#190 未修（主线 447/448 在 W1010 族），497–506 已并入主线 | 全绿（1 抖动备案） | ✅ 门禁入册 |
| 506 | cleanup | **元组解包字符串无类型——根因定性收束**（三触发同根）：for k,v 解包的字符串变量打地址（最小判据 tuple_unpack_str_type）⇒ 505 过滤 dict 推导、#190 交换全是同一条 Assign(Tuple,·) lowering 的不同触发。修法收敛为一条：gen.rs 解包绑定时写类型标记（一次修三族全绿）。简报 ⑪ 已更新 | 最小判据 mismatch 钉住 | ✅ 定性批 |
| 507b | cleanup | **陈旧二进制连环剧收场（重建后全量重验）**：505 过滤推导 ✓ b、506 for 解包 ✓ a/b——两缺口**已被 qwen 449 修复**（我当时二进制是 pre-449 陈旧产物，"match_min 违约/回归/不可复现"三连全是假象）；**#190 交换仍 2 2**（449 修槽位标记非顺序赋值，t510 known-fail 正确保持）。**教训二次入账且升级：rebase 拉主线后 cargo 增量检查可能漏重建——用 touch 或 clean 强制**（494b 一次、本次二次） | 重建后 88.2% 全量无回归 | ✅ 重验批 |
| 505 | cleanup | **带过滤字典推导式值侧地址**钉住：{k: v for ... if k > 1} 的 len 对、d[2] 打印堆地址——无过滤版本全对（504）⇒ 缺口特定于过滤哨兵路径的值类型（#117 新触发形状，gen.rs 车道移交）。同批证实：链式赋值 a = b = 5、带过滤列表推导式全对 | 全对（新缺口已钉） | ✅ 完成 |
| 496 | cleanup | **str.find 一参接入**（#188 一半，纯数据修复）：C shim host_str_find 早已在（stub:200），registry 缺 W 行 ⇒ 派发落空。补 `W str find host_str_find args=2`：s.find(子串) 实测 2/2/-1 与 CPython 一致；check_registry_symbols rc=0；t511 正向用例。**余半移交**：两参形式需三参 shim host_str_find3（runtime 车道，gen_str_s314159_003 钉着） | registry rc=0 · py 362/2/14/0 | ✅ 完成 |
| 493 | cleanup | **元组交换顺序腐蚀定性**（#190 新登记）：`a, b = b, a` → (2,2) 而 CPython (2,1)——Assign(Tuple,Tuple) 顺序赋值，右值引用左目标时腐蚀（字面量右值/元组解包正常 ⇒ 缺口特定）。解析期脱糖已试并**主动撤销**：单侧改解析器引发 t24 模块级全局收集漏认回归 ⇒ 正解在 gen.rs lowering 或连带 unwrap 两处消费端（修法草图已写进 #190）。t510 known-fail 钉住。python_style 回到 361/2/14/0 零回归 | 全绿（t510 known-fail 除外，有归属） | ✅ 完成（含一次主动撤销的负结果备案） |
| 494b | cleanup | **全量 mismatch 归族清点**（45 条逐一归类，零孤儿）：round/truediv 11（`/` 真除法缺失，gen_builtin 与 numeric_truediv 同根）· sign 旗标 4 + 负数进制 7（#188 两面）· Bool 打印 5（#117 近亲）· 溢出回绕 ~12 · 浮点 %.6f 呈现 3 · %s 格式化 1 · in-range 赋值链 2–3（负数位运算单算子**不复现**——归约证实是多算子链交互，案例钉库、机制定位属主线调试）。**45/45 全部有归属** | 分类完备 | ✅ 度量批 |
| 491 | cleanup | **普查收敛验证**（seed=60321，80 例临时不进库）：mismatch 10/80 = **12.5%**，与 480 大样本估计（12%）一致 ✓；族分布：numeric 8（溢出+Bool）、stmts 2、str 0——无新族，存量族各有归属（#188/#189 + 溢出族）。G.3 读数进入稳定期：每次门禁自动出数，主线每修一族比率自动上升 | 386/441 87.5%（含临时） | ✅ 度量批 |
| （续） | | | | |

## 6. 旁路侧启动清单（一次性 / 每会话）

```bash
# 一次性（已完成则跳过）
git worktree add ../zeta-bz -b cleanup bootstrap
cd ../zeta-bz && cargo build --release
mkdir -p /tmp/zeta-bz-tmp

# 每会话
cd ../zeta-bz && export TMPDIR=/tmp/zeta-bz-tmp
git log bootstrap --oneline -3     # 查主线进度
git rebase bootstrap               # 拿到主线新批次（提交前）
# 构建/重门禁加 nice；全量门禁与主线错峰
```

## 7. 合并记录（主线侧维护）

| 日期 | merge 点 | 带入族/批次 | 备注 |
|---|---|---|---|
| 2026-09-26 | `58f9c5db` | 旁路 440（度量）／460（`pattern.rs` 引用模式臂）／461（门禁两步并行化） | 自动合并无冲突；合并后 `git diff HEAD^1 HEAD --stat` 里 `src/middle/**`、`docs/ABI.md`、`tools/baselines/**` 三项为空＝主线禁手面未被带入改动；`worktree.md` 两本台账逐段核实在位后另建 `b789b8f1` 代录记录。**第一次在合并树上跑快子集**＝`GATE_RC=1`、139s、python_style 354/2/6/0（+1 过＝`t501`）⇒ 零新增红；被跳过的 13 步未复现，全轴一致性等下一次全量。 |
